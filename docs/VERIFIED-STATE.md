# Verified state

- **Unreachable servers, sixth step (26 September 2026, `docs/features/offline.md`):** root typecheck; web tests 427 (`retryDelayFor`, `retryLocked` with a running try); headless Chrome: the first automatic try 15 s after the failure, the next 30 s later, the button locked through the whole running try (27 s and 31.5 s) and free once it was through (37 s), locked after a click and free 5.5 s later. Not checked: the minute cadence after the fifth try on screen (only in the unit test), the Electron shell

- **Unreachable servers, fifth step (26 September 2026, `docs/features/offline.md`):** root typecheck; web tests 427 (`retryLocked`, icons in `reachableServers`); headless Chrome: centred "Verbinde mit …" against a silent server, the button locked while the automatic try ran (14 s) and free at 19 s, locked right after a click and free 5.5 s later, the reachable server as a row with initials left of the name. Not checked: a real directory icon in the row, the Electron shell

- **Unreachable listed servers and the members' column (26 September 2026, `docs/features/offline.md`):** root typecheck; web tests 426; the client without a home server in headless Chrome with three listed servers, two down, one without a session: both marked after the start, the notice without the join form, `no-members` on the notice and gone in the chat. Not checked: the real directory, the Electron shell

- **`pnpm dev:desktop` without `pnpm dev` (26 September 2026, `docs/features/desktop.md`):** `scripts/run.mjs` with `--dev-url=http://localhost:5173`, a fake directory, an own `--user-data-dir` and `ELECTRON_RUN_AS_NODE=1` in the environment, nothing on 5173: Vite started by the script, the client's login in the window after 3 s, Vite gone after `Browser.close` (exit 0). Not checked: Linux/macOS

- **The view for a rail server that does not answer (26 September 2026, `docs/features/offline.md`):** root typecheck; web tests 426 (`reachableServers`, `secondsUntil`); the client without a home server in headless Chrome with two rail servers (fake directory, one fake server down, one up): the centred notice with the countdown and "Zweiter Server" under "Gerade erreichbar:", the down server dimmed with a "!" in the rail, the button switched to the other server, the rail entry brought the notice back, the down server started and the client came up. Not checked: the Electron shell with this view, English texts on screen

- **Desktop shell, `--dev-url` without a dev server (26 September 2026, `docs/features/desktop.md`):** root typecheck; desktop tests 75 (`splashView` step `devServer`); the real unpackaged shell with `--dev-url=http://localhost:5177`, a fake directory and an own `--user-data-dir` over the DevTools protocol: start window with the dev server text for 20 s, main window hidden; Vite started on 5177, the app showed the client's login by itself; clean exit through `Browser.close`. Not checked: the packaged app, Linux/macOS

- **A signed-in server that does not answer (26 September 2026, `docs/features/offline.md`):** root typecheck; web tests 424 (`serverRetry.test.ts`); headless Chrome over the DevTools protocol against a Vite dev server (5199) whose `/api` proxy pointed at a fake server (3999, health, me, WebSocket welcome): notice instead of the login while down, 4 tries of `/api/me` in 15 s, the client up 11 s after the fake server started with no login in between, "verbinde neu …" when it went down again, "Abmelden" on the notice leads to the login. The client without a home server (`VITE_HOMELESS=1`, fake directory on 3998) against a silent fake server (accepts, never answers): "Verbinde mit …", the notice after the 10 s limit, retries; a stopped one: the notice at once with the rail usable; started: the client; no page exception in any state. Not checked: the real Electron shell with the real directory, a real server behind the bundled proxy, the English texts on screen

- **Reports, stages 1 and 2 (26 September 2026, `docs/features/reports.md`):** root typecheck; tests protocol 103 (the bit pinned), server 128, web 421, desktop 75; root build passed (new icon `flag`); `pnpm smoke` against a fresh `chat_smoke` on :3001 (migrations 0035 and 0036 applied at start) green, 230 checks, 17 of them new: the open count in the state (0 for a member, a number for the owner), one's own message and an unknown reason refused, a message report with text and an attachment, the same reporter and message once, a member report, the queue only with MANAGE_REPORTS, the snapshot with text, author, channel, attachment copy served with its signed link and a forged link 404, closing with delete (message gone, `message.delete` and `moderation.notice` without a reporter at the author, `deleted: 1`), a closed report refused again, the closed list with result, note and the surviving snapshot, deleteRecent with one `message.bulkDelete`, the log only with MANAGE_REPORTS and without message contents, the open count back. Not checked: the dialog, the queue, the badges and the notice on screen; two real clients; the retention and log sweeps with real dates.

- **Setup self-diagnosis (26 September 2026, `docs/features/doctor.md`):** root typecheck; tests server 128 (`classifyFailure`, `errorCode`, `requestCheck`), web 421 (`selectedPath`, `classifyMedia`, `isPrivateAddress`), protocol 103, desktop 75, link-preview 8; root build passed (new icon `triangle-alert`); `pnpm smoke` against a fresh `chat_smoke` on :3001 green, 213 checks, 7 of them new (403 without MANAGE_SERVER, the owner's report with the request view first and both texts on every check, self/websocket/rtc/livekit ok in dev, the loopback call without a session and without the request view, a forwarded request without a session 401, the media test token only with MANAGE_SERVER); the loopback report printed by hand; `bash -n` and shellcheck clean on the installer; directory: typecheck and tests (61) green, its smoke test with the probe against the fake chat server (see the directory's `docs/VERIFIED-STATE.md`). Not checked: a real installation behind a proxy with a real certificate and the public directory (T6), the browser's media test in a browser, `squorli doctor` on a Linux host, the admin panel's section on screen.

- **Security review group D (25 September 2026):** root typecheck; tests server 120 (`logRedact` with a real Fastify log), web 411, desktop 75 (`findControl` with the key); `pnpm smoke` green twice against fresh `chat_smoke` (before and after the token hashing); against a running instance: a second `hello` refused, signing out the current session closed its socket with 4011, a kicked member got 403 on `PATCH /api/me` and the avatar route while `GET /api/me` stayed 200, a ban made the session 401, `key` and `s` appeared as `***` in the log; a plaintext session row became valid after the migration's SQL (401 before, 200 after), no plaintext token left; with an unreachable directory 30 forged notify pushes started no registration and a forged leave push for the founder got `not_pending`. Not checked: the socket's expiry timer firing, the race between lookup and `hub.add`, the control links in the packaged app with a Stream Deck, the directory host check in the running client, the password change's sign-out in the UI.

- **Security review group C (25 September 2026):** root typecheck; tests server 118 (`ipKey`, `attempt`/`refund`), web 409 (`safeHref`); `pnpm smoke` against a fresh `chat_smoke` on :3001 green; against a fresh instance: six parallel first registrations gave one owner and five `invite_required`, 25 parallel wrong passwords for one handle 10 × 401 and 15 × 429 and then the right one refused; the built client in headless Chrome under the new policy: login screen and player window without a violation, `/api/health`, the directory, the same-origin WebSocket and LiveKit reachable, a `javascript:` link refused by the browser. Not checked: Firefox and Safari under the policy, a signed-in session with voice and embedded players in the browser, a real IPv6 client.

- **Security review group B (25 September 2026):** typecheck and tests in both repos (protocol 103, web 407, server 116; directory 61 and protocol 47), protocol copies identical; server `pnpm smoke` with `SMOKE_DATABASE_URL` green incl. the new claim block; directory `pnpm smoke` green incl. the membership proof checks; a real chat server with a real directory: a directory account signing in is listed for that server afterwards. Not checked: the claim and the backup rebinding in the running client.

- **Security review group A (25 September 2026):** root typecheck, root tests (web 407 with the `connectedHost` test), `pnpm smoke` against a separate instance green with a new check (an SVG attachment: download, CSP with `sandbox`). Not checked: a real relay attempt between two servers, the refusal message on screen.

- **Desktop keys in `safeStorage` (25 September 2026):** root typecheck, root tests (web 406 with 4 new in `identitySecrets.test.ts`; once a WebCrypto test failed with "Cipher job failed" during the parallel root run, three runs of the web tests afterwards were green, looks like a flake under load); the unpackaged app driven twice over the DevTools protocol with its own user data folder: new key in the store only, a key in localStorage moved at the next start and removed there, foreign keys refused, no plain text in `secrets.json`. Not checked: a packaged app, Linux.

- **Radio playlists against DNS rebinding (25 September 2026):** root typecheck and tests, `pnpm smoke` green (internal playlist addresses refused as before); live: SomaFM `.pls` and `.m3u` resolve to their streams, `127.0.0.1` and `localhost` give `forbidden_host`.

- **`VIEW_VIDEO` per channel (25 September 2026):** root typecheck, root tests (web 402 with 3 new for `channelVideoAccess`/`mayViewIn`, server 116); `pnpm smoke` against a separate instance green with a new check (a member deny of `VIEW_VIDEO` on the voice channel turns the seated member's `viewVideo` false in the next `voice.state`, clearing it true again). Not checked: two real clients with camera across such an overwrite.

- **Signed attachment links (25 September 2026):** root typecheck, root tests (server 116 with 3 new: valid for 7 to 8 days, the same link all day, another id / a changed expiry / a missing or foreign signature refused); `pnpm smoke` against a separate instance green with a new check (the upload's link carries an expiry of 7 to 8 days, unsigned, extended and forged links 404; the download with the signed link as before). Not checked: pictures in the running client, a desktop app kept open for more than 3 days, a foreign server's pictures in the rail.

- **Rate limits (25 September 2026, `docs/features/rate-limits.md`):** root typecheck, root tests (server 113 with 6 new, web 399, protocol 101, desktop 74, link-preview 8); `pnpm smoke` against a separate instance on :3001 green with `RATE_LIMIT_FACTOR=5` (at factor 1 the owner's setup burst hits the write limit), no limit reached in that run; 400 WebSocket messages answered 300 times, then closed with 4008. Not checked: behind a real proxy, many users behind one IP, the client texts on screen.

- **Server accounts `~name`, no temporary users (25 September 2026, user's requirement; `docs/features/local-accounts.md`):** root typecheck and tests green (protocol 101, server 107, web 399, desktop 74, link-preview 8), root build passed; smoke test without a directory 204 checks and with the directory (`:3101` instance) 221 checks, all green (fresh `chat_smoke` and `directory_smoke`); a member from before (account row removed by hand) checked by script: `registrationRequired`, REST 403, WS 4013, claim, then in. Headless Chrome against a server without a directory (register, owner, second profile signs in with name and password, claim view) and with one (prefix @ preselected, `~` switches, tabs); the unpackaged desktop app over the DevTools protocol (server accounts only, isolated server added and registered, then a directory account: the server account stays; restart keeps both). `bash -n deploy/install.sh`. Not checked: packaged app, Firefox/Safari, mobile layout of the forms, the install script end to end, `docker build`.

- **Unselected shares and deafen survive LiveKit's `startAudio` (24 September 2026, found while looking into the user's report; `docs/features/voice-video.md`):** root typecheck, root tests (web 397, server 107, desktop 74, protocol 94, link-preview 8) and root build passed; the new test in `videoAudio.test.ts` fails without the fix. Not checked: in the desktop app with a real share (nobody to test with).

- **The stage's tiles open the sidebar's voice member menu (24 September 2026, user's wish; `docs/features/channel-blocks.md`):** web typecheck, root tests (web 396, server 107, desktop 74, protocol 94, link-preview 8) and root build passed. Not checked: in a browser.

- **Voice activity on the server rail (24 September 2026, user's wish; `docs/features/mentions-unread.md`):** web typecheck, root tests (web 396, server 107, desktop 74, protocol 94, link-preview 8) and root build passed; the rail as a static page with the built CSS in headless Chrome, screenshot looked at. Not checked: with real servers and people joining and leaving.

- **Hotkeys follow the keyboard layout (24 September 2026, user's report; `docs/features/hotkeys.md`):** root typecheck, tests (web 393, server 107, desktop 74, protocol 94) and root build passed; a bare Electron app on the German Windows confirmed the old fault (Ctrl+Ö fires "Control+`", not ";") and that `keyCode` (192 for Ö, 89 for Y) and `getLayoutMap()` (ö, y, ß) deliver what the fix uses under a privileged scheme. Not checked: the settings tab with a physical key press, other layouts.

- **Small profile on a left click in the member list (24 September 2026, user's wish; `docs/features/directory.md`):** web typecheck, web tests 390 and root build passed; three states as a static page with the built CSS in headless Chrome, screenshots looked at (full-width menu buttons and a wrapping placeholder found and fixed). Not checked: the real component in a browser.

- **Roles and owner status in the member menu's "Server" section (24 September 2026, user's wish; `docs/features/channel-blocks.md`):** web typecheck, web tests 390 and root build passed. Not checked: in a browser.

- **Removing and blocking friends in the client (24 September 2026, user's wish; `docs/features/directory.md`):** web typecheck, web tests 390 and root build passed (icon `user-minus` added). Not checked: in a browser against a running directory.

- **"Ohne Sperre" in the removal's submenu (24 September 2026, user's wish; `docs/features/channel-blocks.md`):** web typecheck, web tests 390 and root build passed. Not checked: in a browser.

- **Voice entries in the channel list's member menu, stop streams greyed (24 September 2026, user's wishes; `docs/features/channel-blocks.md`):** root typecheck, web tests 390 and root build passed. Not checked: in a browser.

- **Removal notice instead of the PARTICIPANT_REMOVED error (24 September 2026, user's report; `docs/features/channel-blocks.md`):** root typecheck, tests (server 107 with `voice/removeLater.test.ts` 2, web 390) and root build passed; smoke test against `chat_smoke` on :3001 190 green. Not checked: a real removal in a browser.

- **Channel blocks and the member menu's moderation (24 September 2026, user's wishes; `docs/features/channel-blocks.md`):** root typecheck, tests (protocol 94, server 105 with `voice/channelBlocks.test.ts` 4, web 390, desktop 74) and root build passed. Smoke test against `chat_smoke` on :3001 (migration 0031 applied at start, no directory): 190 checks green, 8 of them the new section "Channel blocks" and one new check in "Vote kick" (its block listed and lifted). Migration applied to the dev database after a second restart of the user's dev server (`channel_blocks` exists, `/api/health` 200). Not checked: the menu, its submenu and the channel dialog's tab in a browser, a real voice connection being removed, a phone.

- **Roles in the owner's order, right-click menus, several roles from one menu (24 September 2026, user's wishes; `docs/features/ui-admin.md`):** root typecheck, web tests (390) and root build passed. Not checked: in a browser or on a phone.

- **Stage stays open on a voice channel switch (24 September 2026, user's report; `docs/features/voice-video.md`):** root typecheck, tests (web 388, server 102, desktop 74, protocol) and root build passed. Not checked: the switch in a real browser or the desktop app.

- **Installer refresh and configurable host ports (23 September 2026, user's wish; `deploy/AGENTS.md`):** shellcheck clean. `docker compose config` with a scratch `.env`: without port variables the published ports are 3000/7880/7881/7882 as before, with them 13000/17880/17881/17882 (local and remote overlay, `PROXY_BIND_IP` honoured) and `portainer.yml` the same. LiveKit 1.13.7 with `--rtc.tcp_port 17881 --udp-port 17882` listens on 17881/tcp and 17882/udp both with `livekit.yaml` and with `LIVEKIT_CONFIG` (read from `/proc/net` inside the container; LiveKit's `ports` command still prints 7881). Installer in a `docker:27-dind` host through a pseudo terminal (banner, colours, spacing seen in the output): fresh install with 3000/tcp, 7881/tcp and 7882/udp held by `nc` suggested 13000/17881/17882, `.env` got them, the app answered on 127.0.0.1:13000 and LiveKit listened on 17881/17882; a rerun with port 80 held steered away from Caddy, did not count its own ports as taken, refused `abc` and a doubled port and took 14000; an update checked 14000; the ASCII banner without a UTF-8 locale. Not checked: voice through changed media ports with a real client, the bundled Caddy on a real host.

- **Interactive installer `deploy/install.sh` (23 September 2026, user's wish; `deploy/AGENTS.md`):** shellcheck (stable) clean. End to end in a privileged `docker:27-dind` container (Alpine 3.21, bash 5.2, Docker 27.5.1, Compose 2.33.0, image `ghcr.io/danielklessa/squorli-server:latest`, deploy files from the working tree via `file://`), eight runs with piped answers: fresh install with a proxy on the host (server name with `$` and `&` reached `/api/health` literally, `.env` 600, `/rtc/validate` 401, `squorli status`, `squorli backup` wrote dump, files and `.env`); update (a locally changed Caddyfile kept as `.bak`); reconfigure to a proxy on another host (overlay and `PROXY_BIND_IP` switched, proxy IP in `TRUSTED_PROXIES`, `POSTGRES_PASSWORD` unchanged); directory switched on; bundled Caddy with an unresolvable domain (Caddy starts, certificate refused, the script warns and finishes); back to the host proxy (Caddy container removed); fresh install into an empty directory over the existing volumes (asked for the old password, same `serverKey`); the piped `cat install.sh | bash` path answering from a pseudo terminal. Not checked: a real host with a real domain and certificate, the Docker installation through get.docker.com, ufw/firewalld, Debian/Ubuntu/Fedora hosts, ARM.

- **Vote kick in voice channels (23 September 2026, user's wish and four decisions; `docs/features/votekick.md`):** root typecheck (five packages), tests (protocol 94 with `votekick.test.ts` 7, server 102 with `voice/votekick.test.ts` 11, web 385 with `voteKick.test.ts` 8 plus the i18n equality over 37 new keys, desktop 74) and root build (icons regenerated for `gavel`, `thumbs-up`, `thumbs-down`) passed. Smoke test against `chat_smoke` on :3001 (no directory, migration 0030 applied at start): the 168 checks from before unchanged and green, plus the 13 of the new section "Vote kick" = 181 green, run twice in a row (the first run failed three checks that waited on older `voice.state` events of the same channel; fixed with `waitNew` waiters created before the action, the trap already documented at `waitNew`). Checked there: a new channel allows vote kick, three guests without moderation get the offer, the starter's yes with the member it is about refused (403 `not_eligible`) and a second vote of one member refused (409), no offer and no second vote while one runs, the member leaving ends it as `cancelled`, a new vote about them right away 429 `cooldown`, everybody voting ends it at once with 2:0 of three passing, `403 votekicked` for that channel's token while another channel answers 200, no offer with two left and 409 `too_few_members`, no offer and 403 `moderator_present` while the owner sits in the channel, and the channel switch (403 `votekick_disabled`). The look in headless Chrome over the DevTools protocol against the built CSS: first the floating box at 1440, 900, 390 and 320 px (no horizontal overflow at any width, the box 272 px wide with its two buttons side by side down to 320 px), then, after the user's two corrections of the same day (the box belongs at the top **of** the member list, not over it; yes and no as a bar; the member the vote is about drawn like members everywhere else), the box inside a 15 rem member column next to real member rows, both result variants and the modal: bar with the quorum mark, avatar/initials with the presence dot, the name in the role's colour and a long name truncated like the list does it; screenshots looked at. Not checked: two real clients voting against each other, the desktop app, a real phone, LiveKit's actual `removeParticipant` (no participant in the smoke test), and a vote that really runs into its minute (the smoke test always ends it early or by a cancel).

- Viewport-aware modal width (23 September 2026, user follow-up): settings may grow to 64rem, administration and channel dialogs to 76rem, capped by the available backdrop area. Explicit minmax grid tracks and max-height:100% keep them inside the window, accounting for outer padding and the desktop title bar. Mobile remains full available size. Root typecheck, tests and build passed. Chrome geometry fixture using the real CSS: 36 containment checks across six viewport sizes (320x240 to 1920x1080), all three dialogs, with 0/32px title bar, passed. Native desktop and real mobile hardware not tested.

- **Modal layout review (23 September 2026, user request: prevent overlapping text in server, user settings and channel dialogs):** Unified the responsive rules of all three categorized modals while keeping categories on the left. Navigation and content scroll independently; navigation labels wrap, close buttons keep their space, and scrolling flex columns preserve the natural height of their children. Wider desktop dialogs, section dividers and explicit minimum widths improve readability. Channel permission inheritance hints sit below their labels, with controls in a separate column (below on very narrow screens); the target list moves above the editor at <=1100 px. Existing brand tokens and DE/EN copy are unchanged. Verified: root typecheck, root tests and root build; real React components with stub data in Chrome at 320, 390, 700, 900 and 1440 px in German and English (audio settings, server settings including key mode, advanced text-channel permissions): no measured horizontal overflow; six German desktop/mobile screenshots visually reviewed. Not checked: real phone, desktop shell, every remaining tab or persistence (CSS-only change). Further suggestions, not implemented: settings search, collapsible advanced sections, and consistent visible save feedback.

Part of the project description (entry point: root `AGENTS.md`, section 0; until 17 September 2026 this was section 8 of that file). After every complete test run (typecheck, test, smoke, build) add an entry at the top: date, what ran, the result, and what was not checked.

Base check of the complete workflow: 14 September 2026, Windows 11, Node 25.2.1, pnpm 10.15.0, Docker 29, LiveKit 1.13.6.

- **Channel and category permissions, private channels, sticky voice channels (23 September 2026):** root typecheck (five packages), tests (link-preview 8, protocol 87 with the two new bits and the channel groups, server 85 with `channelPermissions.test.ts` 8, `voice/confine.test.ts` 4, the eviction in `livekit/sync.test.ts`, held members in `afk.test.ts`, `mapOverwrites` and a private category in `import/discord.test.ts`; web 374 with `channelPerms.test.ts` 12 and `pickerEntries.test.ts` 3 plus the i18n equality over 150 new keys; desktop 74) and root build (icons regenerated for `ellipsis-vertical`, `lock-keyhole`, `megaphone`, `timer`) passed. Smoke test against a fresh `chat_smoke` on :3001 (no directory, migrations 0027/0028 applied at start): the 148 checks from before unchanged and green, plus the 20 of the new section "Channel permissions" (a private channel out of the guest's structure, state, read state, status API and socket; 404 not 403 everywhere; a member allow with the history back; the write rules; read-only and slowmode; the user limit; the sticky hold with 403 `confined`, the lock, `stickyHideVoice`, the hold across a reconnect, the welcome's lock, only `MOVE_MEMBERS` moves and frees; the AFK guard; a move into an invisible channel; a placed member through a permission change and the eviction on one's own seat; `BYPASS_STICKY`; a category deny with a channel allow) = 168 green. The built client in headless Chrome over the DevTools protocol against that server (the smoke owner's key in localStorage, "Ohne Konto fortfahren"): the channel menu (mute, edit, delete), the dialog's Übersicht, Berechtigungen in the simple and the advanced view with the inherit hints, "Privater Kanal" ticked (the everyone deny and the owner's own allow appear, "Gast darf hier nichts."), the picker above the modal (it sat behind it at first: z-index 9 under the modal's 10, now 20), the Sprache tab, the category menu and its dialog, the phone width 390 px (categories stay left, the editor in one column; the sidebar's ellipsis buttons); twelve screenshots looked at. The smoke test ran three times in a row green (a race in the new section, waiting on old events, was fixed by waiting for fresh ones). Not checked: two real clients side by side and the desktop app, a real phone, LiveKit's actual `removeParticipant` (no LiveKit participant in the smoke test), the Discord import against a real template with private channels.
- **Status API lists only the members in a voice channel (23 September 2026):** root typecheck (five packages), tests (protocol 86 with the case that a member without a seat is refused, server 71, web 360) passed. Smoke test against a fresh `chat_smoke` on :3001 (no directory): 148 green, two of them new (an empty member list while nobody sits in a voice channel, the seated member without an `online` field, the owner outside every voice channel not listed, the seat with its mute, camera and screen state as before). Website: `pnpm check`, build (20 pages) and `verify-site.mjs` passed with the changed guide, example and widget. Not checked: the admin panel's changed texts in a browser (strings only), the website's screenshot fingerprint. Details: `docs/features/status-api.md`.
- **Camera and screen share visible for the whole server (23 September 2026):** root typecheck (five packages), tests (link-preview 8, protocol 86, server 71 with the presence case extended, web 360, desktop 74) and root build passed. Smoke test against a test server on :3001 (`chat_smoke`, no directory): 146 green; `voice.status` with `screenOn` reaches the other member's `voice.state`, and the status API's seat carries `micMuted`, `deafened`, `cameraOn: false`, `screenOn: true`. The built client in headless Chrome over the DevTools protocol against that server: a second member seated over a bare WebSocket with `micMuted`, then `voice.status` with all four on, showed in the owner's sidebar (the owner in no voice channel) the titles "Mikrofon stumm", "Ton aus", "Kamera an", "teilt Bildschirm". Website: `pnpm check`, build and `verify-site.mjs` (19 routes) passed with the guide's example response and widget extended (camera and screen badges). Not checked: a real camera or share from a browser client (the effect sends `voice.cameraOn`/`voice.screenOn`, which the stage already uses), the website's screenshot fingerprint. Details: `docs/features/status-api.md`.
- **Invite links as app links, copy buttons that answer (23 September 2026):** web typecheck, web tests (360) and root build passed. The built client in headless Chrome over the DevTools protocol against a test server on :3001 (`chat_smoke`, smoke owner's key): Verwaltung > Einladungen after "Einladung erstellen" shows the web link as code and the buttons "Link kopieren" (title = the web link), "App-Link kopieren" (title `squorli://invite/localhost:3001/<code>`) and "Widerrufen"; a scripted `.click()` on an unfocused headless document showed "Kopieren nicht möglich" (the browser refused the clipboard: the failure path answers too); a real mouse event on the page brought to front showed "Kopiert" at once, the clipboard held the squorli:// link, and the label was back to "App-Link kopieren" 2 s later; screenshot looked at. Not checked: the status API's copy buttons after the switch to `CopyButton` (same component), Firefox/Safari clipboard behaviour, the invite revoked at the end (the script clicked it, not checked). Details: `docs/features/ui-admin.md`.
- **Status API, mute state for the whole server, squorli:// links in the chat (23 September 2026):** root typecheck (five packages), tests (link-preview 8, protocol 86 with the new schema cases, server 71 with the presence's mute case, web 360 with the squorli link cases, desktop 74) and root build passed after the consent was taken out again (migration 0026). Smoke test against a test server on :3001 (`chat_smoke`, no directory, `RADIO_IDLE_STOP_MS=2000`): 146 green, 15 of them new (mode off by default and 404, mode and key need MANAGE_SERVER, a key made on the switch to "key", 401 without or with a wrong key and 200 by header and by query, the structure, members without key or roles, regenerate invalidates the old key, public without a key and `?online=1`, off again 404 even with the key; `voice.join` with `micMuted` in everybody's `voice.state`, `voice.status` broadcast, the seat with its mute state in the status API). The built client in headless Chrome over the DevTools protocol against that server (smoke owner's key in localStorage, "Ohne Konto fortfahren"): Verwaltung > Server shows the section with the three modes, "Aus" ticked; the switch to "Nur mit Schlüssel" shows the address, a 43-character key with copy buttons and "Neu erzeugen", and GET /api/status answered 401 meanwhile; a second member signed in over the API and seated in Lobby over a bare WebSocket with `micMuted` showed the microphone icon in the owner's sidebar (the owner in no voice channel), and after `voice.status` with `deafened` both icons ("Mikrofon stumm", "Ton aus"); screenshots looked at. Migration 0026 ran on `chat_smoke` and, through the second restart of the user's dev server, on `chat` (27 rows, the column gone); the user had already switched their dev server's API to public. Not checked: the click on a squorli:// link in the running desktop app and in a browser (the parser is tested, the click path was read), a mute change from a real voice client in the browser (the effect sends what the smoke test sent by hand), the AFK channel's reported state, a phone, the website's screenshot fingerprint. Details: `docs/features/status-api.md`, `docs/features/markdown-input.md`.
- **Context menu of the desktop app, files pasted into the chat (23 September 2026):** web typecheck and tests (359 with `pasteFiles.test.ts`, 5 new), desktop typecheck and tests (74 with `contextMenuItems.test.ts`, 13 new) and root build passed. The assumptions behind the menu checked in Electron 44 with a page of its own (a scratch main script, the app untouched): a PNG written to the clipboard with the promise-based `clipboard.write([new ClipboardItem(...)])` and then `webContents.paste()` (what the menu's "Einfügen" role does) fired the page's `paste` event with one item of kind file and a File "image.png" (image/png, 107 bytes); a text paste arrived in the textarea; right-clicks sent as input events on a textarea, selected text, a link and a picture arrived as `context-menu` params with `isEditable`, the `editFlags` (canPaste true only in the field), `selectionText`, `linkURL`, `mediaType: "image"` with `srcURL`, and `frame.parent === null`; `Menu.buildFromTemplate` took labels, roles and enabled flags as `contextMenu.ts` builds them. The pending-file chips with the built stylesheet on a static page in headless Chrome (a picture chip with its preview, a plain file chip, the composer below; screenshot looked at). Not checked: the menu on screen in the running app (a native menu is not in a page screenshot), spelling suggestions from Windows' dictionary, "Bild speichern unter…" with a real attachment, a paste in the running client (the DOM event path was checked, the React handler only by its test of `pastedFiles`), Linux, a packaged app. Details: `docs/features/desktop.md`, `docs/features/markdown-input.md`.
- **Global hotkeys, push-to-talk across the system, control from outside (22 September 2026):** root typecheck and tests (link-preview 8, protocol 85, server 70, web 354 with `platform/hotkeys.test.ts`, desktop 61 with the key and control cases) passed; both native helpers rebuilt. The helper alone with `SendKeys` from a Node script: keys reported only while named, Space and F13 with press and release. The unpackaged app through the real bridge over the DevTools protocol: F13, Ctrl+Shift+M and the watched Space arrive as hotkey and push-to-talk events, nothing while suspended, `invalid` for Ctrl+Pause, a bare letter and an unknown push-to-talk key, `squorli://control/mic-toggle` and `--control=deafen-on` from second starts arrive as external commands, `squorli://control/quit` does nothing. The tab rendered with the built stylesheet in headless Chrome (screenshot looked at). Root build passed at the end (a first run failed on someone else's concurrent import work in this checkout, whose catalog keys were still missing; the app test used a `vite build` of the client meanwhile). Not checked: a real Stream Deck or G Hub, the tone by ear, a shortcut while really in a voice channel, a packaged app, Linux. Details: `docs/features/hotkeys.md`.
- **Import of a Discord server template (22 September 2026):** root typecheck (five packages), tests (link-preview 8, protocol 85 with `import.test.ts`, server 70 with `import/discord.test.ts`, web 354, desktop 61) and root build passed twice (`pnpm icons` took the `import` icon into the font; the second build carried the fix for names squeezed by their details). The smoke test against a built server on :3001 (`chat_smoke`, no directory) ran all green with the new checks: `importSources` in the state, 400 `bad_code`, 404 `unknown_template`, the preview of the real public template `discord.new/JfppTU3CNvuD` (15 categories, 58 channels, 29 roles), the import of its first category with 7 channels and two roles (names, kinds, topics, bitrates, colors and permissions as planned; the smoke role moved from position 1 to 3, the new roles sit at 2 and 1 in the template's order), the second preview marks them as existing, the same import again creates nothing, cleanup, 403 for a guest. The real built client in headless Chrome over the DevTools protocol (smoke owner's key in localStorage, Verwaltung > Import): the intro, the plan of that template (fieldsets per category, overwrite counts, role colors and permission labels, "101 Einträge übernehmen"), the import itself ("Übernommen: 15 Kategorien, 58 Kanäle, 28 Rollen", the role "Admin" existed), the sidebar showing the imported categories and channels, then the 101 entries deleted again through the API; the plan at 400 px phone width (rows truncate cleanly). Not checked: an import while somebody sits in a voice channel (nothing existing is touched), Discord answering 429 (the smoke checks would skip with a notice), the website's screenshot fingerprint (`pnpm screenshots` there after this client change).
- **Voice notices and errors are a modal (22 September 2026):** root typecheck, web tests (343) and root build passed. The two-client run of the entry below, repeated with the built client: after the same-room takeover the old tab shows the modal "Fehler" with the duplicate-identity text and no line in the dock, its "Schließen" button closes it and the client's error is gone; after the other-room takeover the old tab shows "Hinweis" with the notice, a mousedown on the backdrop closes it; after the phone's takeover the old tab's "Hinweis" closes on Escape. Screenshot of the notice modal looked at. Not run: a failed join on the phone (the sheet closing and the modal with the error), a camera error while in voice. Details: `docs/features/voice-video.md`.
- **One voice connection per account and server (22 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 63 with the new `othersOfUser` case, web 343 with `voice/elsewhere.test.ts`, desktop) and root build passed. Two desktop-width tabs and one phone-width tab of the same account (smoke owner) in a headless Chrome with fake media against a built server on :3001 (smoke database, dev LiveKit on :7880): A joins Lobby; B's click on Lobby shows "Andere Sprachverbindung beenden?" naming "Lobby", Abbrechen leaves B out and A connected; the second click plus "Hier beitreten" connects B, A's dock shows the disconnect text of the duplicate identity and the sidebar lists the account once; A's click on a second voice channel asks, confirmed it connects A there and B hangs up with the notice in its dock (server log: LiveKit's removal found no participant, B had already left on the event); the phone tab's "Verbinden" closes the sheet and shows the dialog naming "Zweiter", Abbrechen brings the sheet back, confirmed it connects and A hangs up with the notice. Screenshots looked at. Not run: an old client that ignores the event (its media ends through LiveKit's removal, the call is the SDK's `removeParticipant`), a real phone. Details: `docs/features/voice-video.md`.
- **Member list on a phone (22 September 2026):** root typecheck, web tests (342) and root build passed. A headless Chrome with touch and 400x800 phone metrics against a built server on :3001 (`chat_smoke`, smoke owner identity, real client): the server head ends with the `users` button (44x44, at the header's right edge); a tap slides the list in from the right (0.12 s in: translateX 77 px, 0.5 s in: 0, visibility visible, rail and navigation hidden), the header reads "Mitglieder · 14", a tap on a member opens the context menu inside the panel, the X (44x44) slides it out again (translateX 400, hidden, rail and navigation visible), a width change to 1200 px removes the header and the button and leaves the desktop column static in the grid. Screenshots reviewed (closed, open, menu, desktop). Not run: a real phone. Details: `docs/features/ui-admin.md`.
- **A refused sign-in no longer lists the server for the directory account (22 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 342, desktop 57) and build passed. The directory's side ran its smoke test with the new checks against an instance on :3101 (187 ok, its `docs/VERIFIED-STATE.md`). Not run: this server's sign-in against that instance; `lookup()` adds one query parameter, read in the source. The user's dev server and dev directory run under `tsx watch` and pick the change up by themselves; the wrong entries heal at the client's next sign-in attempt on those servers. Details: `docs/features/directory.md`.
- **Hang-up on a phone's stage returns to the channel list (22 September 2026):** web typecheck, web tests (342) and root build passed. The built client against a test server on :3001 (smoke database, dev LiveKit, `PUBLIC_DOMAIN=localhost`, so the page must be opened as localhost) in headless Chrome at 400x800 with touch emulation and fake media: sign in with the smoke owner's key, tap Lobby, Verbinden, the stage, hang up: the channel list is visible after 0.4 s and after 3 s, the main area slid out. The first version of the change (a `useCallback` below the login screen's early returns) crashed the client after signing in with React's error 310; the check found it, the function is now a plain one. Not checked on a real phone. Details: `docs/features/voice-video.md`.
- **Push-to-talk hold button on phones and tablets, on the stage and in the dock (22 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 342, desktop 57) and build passed twice (the second run took the `audio-lines` icon into the font). The real `PushToTalkButton` in both variants bundled with esbuild into a harness page with the real `styles.css`, headless Chrome at 400x800 with touch emulation over the DevTools protocol: touch down and up, a finger sliding off, a 1.2 s press, Space from the keyboard, disabling while held and a tap while disabled all gave the expected `setPttHeld` calls and labels; screenshots of the idle, held and disabled states looked at. Not checked: a real phone, the settings dialog's text on a device. Details: `docs/features/voice-video.md`.
- **Avatar crop editor (22 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 342, desktop 57) and build passed (`pnpm icons` took `zoom-in`, `zoom-out` and `check` into the font). The real `AvatarEditor` in a harness page (esbuild bundle of the client sources with the real `styles.css`, headless Chrome over the DevTools protocol): editor opens with a four-colour test image, frame 256 px with dimmed corners, wheel zooms without scrolling the settings body, slider, mouse drag, plus key, pinch via touch events, apply renders a 256 x 256 WebP of the dragged corner (pixels sampled), note, cancel, 400 px view without overflow; screenshots at 1280 and 400 px reviewed. The account page's twin checked end to end in the directory repo. Not checked: the logged-in client against a directory, the desktop app, Safari, a real touch screen. Details: `docs/features/directory.md` (entry "Avatar crop editor").
- **The viewer's statistics of a share (22 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 338, desktop 57) and build passed (`pnpm icons` inside the build took the new `activity` icon). The overlay's markup and `.tile-stats` were rendered with the built stylesheet on a static page in headless Chrome (a large and a small tile: six rows, the diagnosis line in amber or mint, rows cut with an ellipsis on the small tile). Not checked: the numbers of a real received share (`videoReceiveSample` against a live receiver), the pop-out window, fullscreen, Firefox.
- **Typing line above the message input (21 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 325, desktop 57) and build passed. A static page with the real `styles.css` and the markup of dock and composer in headless Chrome: the input's and the dock's name row's top and bottom edges on the same pixels, screenshot looked at. Not checked: the running client, a phone. Details: `docs/features/markdown-input.md`.
- **A game's share: motion hint, H.265 on the graphics unit (21 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 325, desktop 57) and build passed. Encoders measured per codec in the app's Electron (loopback): H.264 constrained baseline with the hint "detail" 13-14 frames per second in OpenH264, with "motion" 28-30; H.265 and the other H.264 profiles on the NVIDIA encoder; AV1 in software. The real `livekit-client` with the real `screenSharePublish` in Electron against the dev LiveKit: H.265 negotiated, hardware encoded at 1080p30 and 6 Mbit/s, decoded by a second participant; a participant without H.265 (`--disable-gpu`) makes the share fall back to VP8 for everyone. Client only, no server change, smoke test not run. Not checked: a real game in the running app, the dialog's text on screen, Firefox/Safari viewers, AMD/Intel graphics units, a packaged app. Details: `docs/features/voice-video.md`.
- **Share dialog of the desktop app: widgets left out, tabs, codec choice, Quick Share (21 September 2026):** root typecheck, tests (link-preview 8, protocol 81, server 62, web 321, desktop 57) and build passed; native helpers rebuilt. The helper's new `windows` question against the real windows of the user's computer (14 Rainmeter skins, all tool windows). The unpackaged app over the DevTools protocol: source list without Rainmeter, `gameId` on the windows of a program added as a game, no audio for the own window. The real `ScreenPicker` in headless Chrome with that list (tabs, audio tick, codec defaults, the pick, screenshot). Later the same day the windows' order (games, full screen, the rest by name; web tests 322): the helper's full screen field checked in the unpackaged app with a game running in full screen and the app's own window in full screen. The real `VoiceClient` in the app against the dev LiveKit: Quick Share sends H.264 with the window's audio, a plain window VP8. Measured: LiveKit negotiates H.264 constrained baseline only, which Chromium encodes in software (OpenH264) although the RTX 4080 encodes the other profiles; the dialog's text was corrected to claim no hardware encoding. No server or protocol change, smoke test not run. Not checked: the dock's button with a real game, a second client (Firefox, Safari) watching an H.264 share, a packaged app, Linux. Details: `docs/features/voice-video.md`.
- **Local icons in Einstellungen > Spiele, and the game line without "Spielt" (21 September 2026):** root typecheck, tests (protocol 81, server 62, web 318, desktop 55) and build passed. The real scan and the new icon reader bundled with esbuild and run in Electron on the user's computer: icons for all 33 installed games (Steam 26 from its cache, Epic 2 from the executable, Xbox 5 from the logo pictures) and an added program, none for a missing file, 110 ms; the result looked at as a headless Chrome screenshot, and the list's markup with the built CSS too. Not checked: GOG (not installed), the tab in the running app, a packaged app, the game line in a browser.
- **Sealed settings and the hide list in the account (21 September 2026):** protocol `directory.ts` extended and copied to the directory repo (byte-identical). Root typecheck (five packages), tests (link-preview 8, protocol 81, server 62, web 318, desktop 51) and build passed. The real `Store` and `GameDetection` driven by a Node script (tsx, stubbed `localStorage`/`window`/desktop bridge, one process per device) against a directory smoke instance on :3101: plaintext settings of an older client adopted, sealed at once and gone from the account; second device takes them over and joins its hide list with the account's; a tick set again reaches the account and the other device; a browser's change leaves the hide list alone; no reload. Directory side: its notes (smoke test 185 green). Not checked: the chat repo's smoke test (the chat server did not change), `docker build`, the two new texts in a browser or the app, two real devices.
- **Link previews in direct messages, the lookup as a package (21 September 2026):** `packages/link-preview` split out of the server (also used by the desktop app, copied to the directory repo, `diff -r` clean), protocol `dm.ts`/`directory.ts` extended and copied (byte-identical). Root typecheck (five packages), tests (link-preview 8, protocol 74, server 62, web 314, desktop 51), build, `docker build --target app` green. Chat smoke test 133 again after the move; directory: typecheck, tests, build, `docker build`, smoke 181. Two headless browsers with the real client and the unpackaged desktop app's `lookUpLink`: details and what was not checked in `docs/features/link-previews.md`.
- **Link previews, YouTube videos in the chat, their sound on the screen share device (21 September 2026):** root typecheck, tests (protocol 72, server 70, web 309, desktop 50), build and `docker build --target app` passed; migration 0024 via `pnpm db:generate`. Smoke test against :3001 (`chat_smoke`, no directory, `LINK_PREVIEW_TEST_ORIGIN=http://127.0.0.1:3198`): 133 green, 12 new (122 without the variable). The fetcher against real sites, the built client in headless Chrome against :3001 (no request to a foreign host before play, then only `www.youtube-nocookie.com`; the x removes page and video previews; none on another member's message; 1400 and 420 px), and the unpackaged desktop app with a harness: the chat player on the chosen output device, the radio-style player on the default one. Not checked: the picture sweep, limits under load, the packaged app, Firefox/Safari, a second real person. Details: `docs/features/link-previews.md`.
- **Game display: "Spielt X" in member list, friends list and conversation head (21 September 2026):** root typecheck, web tests 306 (3 new) and build passed; a harness page with the real `MemberList` and `HomeSidebar` by headless Chrome against the public directory's game library (name and icon from the directory, nothing for what it does not call a game, reported name with a gamepad otherwise, the member's menu by DOM dump). Client only: no server change, smoke test not run again. Not checked: two real clients end to end. Details: `docs/features/games.md`.
- **Game display: reporting the game to chat servers and the directory (21 September 2026):** root typecheck, tests (protocol 66, server 62, web 303, desktop 49) and build passed; smoke test 121 checks against :3001 (fresh scratch database, no directory; 4 new); the shared protocol files copied to `../squorli-directory` (byte-identical), its typecheck, tests (44) and smoke test against :3101 green (170, and 171 with its online check: a program the library does not call a game is not passed on to friends). **Not checked: the whole way from a running game in the desktop app to another client (nothing displays a game yet), two real people, old and new sides together.** Details: `docs/features/games.md`.

- **Protocol part of the directory's game library (21 September 2026):** root typecheck and protocol tests (61, 2 new in `directory.test.ts`) passed; `directory.ts` and its test copied to `../squorli-directory` (all shared files byte-identical), where the service was built and checked against the real catalogs (its `docs/VERIFIED-STATE.md`). No client or server code uses the new schemas yet, so no build, no smoke test.

- **Game detection in the desktop app, local only (21 September 2026):** root typecheck, tests (protocol 59, server 59, web 295 with `gameDetection.test.ts`, desktop 49 with `games/launchers.test.ts`, `games/match.test.ts` and the game lines) and build passed; both helpers compile without warnings. The helper alone: a watched executable or folder in front is reported within a second, an empty list ends it, other programs are never reported. The real scan on the user's computer: 33 games in 30 ms (Steam, Epic, Xbox on two drives; GOG not installed). Unpackaged app with a scratch profile over the DevTools protocol: no list while off, an added program in front is reported, a malformed entry dropped, no paths reach the client, switching off ends the game. Einstellungen > Spiele by headless Chrome screenshots of a harness page. After the user's first test ("nothing is shown", development mode) two fixes: the client subscribes in an effect (`GameDetection.start()`; web tests 296), the helper takes the topmost window of a watched program instead of the window in front; checked against the user's running Aviassembly (found while another window was in front) and Wallpaper Engine (not counted), and the built React client registers by itself. The user's second test in the running app was successful throughout: games from Steam, the Xbox app and Epic are detected (the Steam and the Xbox title multiplayer games with anti-cheat), an added program too. **Not checked: GOG for real, a packaged app; Linux is not built.** No smoke test: no server or protocol change.

- **AFK: a playing video keeps the user present, at most 4 hours (21 September 2026):** root typecheck, tests (protocol 59, server 59, web 290 with 3 new in `activity.test.ts` and the video case in `systemActivity.test.ts`, desktop 31) and build passed. Before it the user tested the helper of the entry below with real hardware, all successful: controller and joystick input without focus, nothing from untouched devices, the display state per browser, on pause and in a voice channel. **Not checked: the rule in the running app** (ten minutes hands off with a video, four hours for the cap), a packaged app. No smoke test: no server or protocol change.

- **AFK: controller input through a native helper of the desktop app (21 September 2026):** root typecheck, tests (protocol 59, server 59, web 286 with `systemActivity.test.ts`, desktop 31 with `systemWatchLines.test.ts`) and build passed; both native helpers compile without warnings. The helper alone: `ready`, `display 0`, ends 5 ms after stdin closes. Unpackaged app with a scratch profile over the DevTools protocol: one helper process while the app runs, none after it, `info.systemWatch` true, a subscriber gets the display state. The first try of the day before (Chromium's Gamepad API; `app://squorli` is a secure context and the API answers) failed the user's test: no input without focus; removed. **Not checked, needs the user's hardware:** controller and joystick input while a game has the focus, an untouched controller, the display state with a video per browser and in a voice channel, a packaged app. No smoke test: no server or protocol change.

- **Desktop app: start window with the update check, quieter client start (20 September 2026):** root typecheck, tests (protocol 59, server 59, web 284, desktop 28 with the start window's pure part) and build passed. Unpackaged app over the DevTools protocol: the start window's steps with `--fake-update`, the main window only after the client's start screen, "install later", a normal start (main window at 2.6 s); see-through window: start card, login card and the focus outline photographed. **Not checked: a real update at the start** (feed, silent installer, restart into the new version, AppImage), Linux, a start without network.

- **A radio with nothing left to play turns itself off (20 September 2026):** root typecheck, tests (protocol 59, server 59, web 284, desktop 25) and build passed; smoke test 116 checks measured against :3001 (`chat_smoke`, no directory; 3 new; the count with a directory, 129, is derived, not run). Real client in headless Chrome in a voice channel on :3001: a single YouTube video that ran out turned the radio off (server state and player), an offline Twitch channel did after the 60 s grace, a live one stayed on for 90 s. Not checked: a Twitch stream ending while watched, a YouTube live stream's end.

- **Message cue, task bar mark, start with the system (20 September 2026):** root typecheck, tests (protocol 59, server 58, web 282 with `attention.test.ts` and new cases in `sounds.test.ts`/`accountSettings.test.ts`, desktop 24) and build passed; server smoke test 114 checks against :3001 (`chat_smoke`, no directory); squorli-directory typecheck, tests and smoke test (one new check) against an own instance on :3101. Real client against :3001 with a second member posting over the API: cue for a mention in another channel and in the open channel without focus, none for a plain message, for the open channel with focus, or with the cue switched off in the settings dialog. Unpackaged desktop app: overlay "3", "9+", none on the real task bar; autostart value written and removed under the `Run` key; `--autostart` starts minimized, with `closeToTray` hidden, with the user's choice "open the window" (`autostartBackground: false`) open; desktop tests 25. Not checked: direct messages end to end, by ear, a packaged app at a real system start, Linux, the tray dot by eye.

- **YouTube player without captions at every start (20 September 2026):** root typecheck, web tests (278, 1 new in `embedControl.test.ts`) and build passed. Headless Chrome, fresh profile, real player: a muted start turns captions on by itself (measured), `setOption("captions","track",{})` at `onApiChange` keeps them off; the real `YoutubeControl` against the real player: no caption text in any 100 ms sample from the start through a `loadVideoById` to a second captioned video, and a real click on CC turns them on and they stay. Not checked: Firefox/Safari, forced captions, ads, the pop-out window (same control).

- **Desktop app: the embedded players' sound on the radio's output device (20 September 2026):** root typecheck, tests (web 277, 2 new in `playerWindow.test.ts`; desktop 20, 2 new; protocol 59, server 58) and build passed. Unpackaged Electron app with a scratch user data folder and fake audio devices over the DevTools protocol, harness page with the real `EmbedPlayer` from `app://squorli`: YouTube and a live Twitch channel move to the chosen device within 3 s and back to the default (`sinkId` read inside the frame), an unknown label changes nothing, the frame sees no device labels without a grant; after the user's report of silence after a queue's video change: reproduced with `isCurrentlyAudible()`, and audible through three videos once the permission check stays open while a device is chosen. Not checked: real devices by ear, the pop-out window, a packaged app, Linux.

- **YouTube playlists as a server-kept queue (20 September 2026):** root typecheck, tests (protocol 59, 2 new; server 58, 3 new in `radio/queue.test.ts`; web 275, 7 new in `youtube.test.ts` and `embedControl.test.ts`; desktop 18), build, migration 0023 applied on the dev database and on a freshly created `chat_smoke`, smoke test on :3001 without a directory: 114 checks green twice (4 new). Headless Chrome: the playlist probe against YouTube (9 and 183 entries, unknown list), the real `EmbedPlayer` in a Vite harness (next video in the same iframe, the player's end reported once, a shared place behind the end no longer restarts the video), and the real built client against the test server as the smoke owner in a voice channel: playlist typed into the radio menu, skip, natural end moves the queue on. Not checked: two real people, a station that is a playlist, the pop-out window across a video change, the desktop app, Firefox/Safari.

- **Tile for a video source while the radio is off for me (20 September 2026):** root typecheck, tests (web 268; protocol 57, server 55, desktop 18; no new ones, the change is a component) and build passed. Headless Chrome over the DevTools protocol with the real `VoiceStage` in a temporary Vite harness page: the tile with 2, 3 and 9 participants, in the speaker view's strip, at phone width, German and English; the slider applies on release only, the button keeps the default volume unstored, arrow keys plus Enter work, the x dismisses the tile (first in the speaker view's strip). Not checked: the rule in `App.tsx` that brings a dismissed tile back with the next video, the running app with a real Twitch/YouTube source, touch on a real phone, Firefox/Safari.

- **Stereo capture on phones, one-sided input (19 September 2026):** root typecheck, tests (web 268, 5 new in `oneSided.test.ts`; protocol 57, server 55, desktop 18) and build passed. Real `MicPipeline` in headless Chrome with a synthetic two-channel capture: left only becomes both sides after half a second, real stereo returns to stereo. Not checked: the iPhone (echo on the loudspeaker, both ears), a real audio interface.

- **Interrupted AudioContext on iOS (19 September 2026):** root typecheck, tests (web 263, 1 new in `gate.test.ts`; protocol 57, server 55, desktop 18) and build passed. Not checked: the iPhone; no desktop browser has the state "interrupted", so nothing but the unit test covers it.

- **Refused microphone explained (19 September 2026):** root typecheck, tests (web 262, 3 new in `micPermission.test.ts`; protocol 57, server 55, desktop 18) and build passed. Headless Chrome without the fake permission UI against the dev LiveKit: permission state "denied", the join fails with the explained text. Not checked: the iPhone, where the refusal was reported and whose cause is still open.

- **Home screen icon and name (19 September 2026):** root typecheck, tests (protocol 57, server 55 with 2 new, web 259, desktop 18) and build passed; client with `app-icons/` copied to `apps/server/public`. Dev server: `/api/manifest.webmanifest` answers `application/manifest+json`, `no-cache`, "Squorli - Test Server", also through Vite; icons served; the four PNGs inspected. Not checked: a real iPhone or Android phone, the smoke test.

- **Microphone prompt inside the joining tap (19 September 2026):** root typecheck, tests (protocol 57, server 53, web 259, desktop 18) and build passed; client copied to `apps/server/public`. Real `VoiceClient` against the dev LiveKit in headless Chrome: the prepared capture is taken over (one `getUserMedia` for a join), released when the join does not happen or wants another mode. Not checked: the iPhone (Safari and home screen app), which is where the fault was reported.

- **Camera choice and mobile devices (19 September 2026):** root typecheck, tests (protocol 57, server 53, web 259, desktop 18) and build passed; client copied to `apps/server/public`. The real `VoiceClient` against the dev LiveKit in headless Chrome with two fake cameras: the camera picked after switching off is opened (new publication), the same camera keeps its publication, resolution and device change on a running camera take effect, no error. Not checked: any real phone (whether the iPhone now reaches the voice activation threshold is open; the debug view's "Eingang" gives the raw level), Firefox with the republish path, smoke test (no server or protocol change).

- **Mobile dialog opening focus (19 September 2026):** root typecheck, tests and build passed. Settings input autofocus disabled for narrow/coarse-pointer devices; both dialogs focus Close instead. Real-device keyboard behavior not tested.

- **Mobile administration (19 September 2026):** root typecheck, tests and build passed. Real AdminPanel in an isolated Chrome harness with populated example data: all six categories fit 320x640, 390x844 and 700x400 without horizontal content overflow; full-viewport bounds, no page errors. Channel screenshot reviewed. Real phone keyboard and administrative writes not tested.

- **Mobile settings (19 September 2026):** root typecheck, tests and build passed. Isolated headless Chrome rendered the real dialog: all browser categories fit 320x640, 390x844 and 700x400 without horizontal body overflow; modal fills viewport. Voice/audio screenshot reviewed. No real-phone keyboard, Safari or media acceptance performed.

- **Mobile zoom (19 September 2026):** root typecheck, tests and build passed; client refreshed in apps/server/public. Real mobile-browser pinch/focus zoom not checked.

- **Mobile navigation and voice preview (19 September 2026):** root typecheck, tests (protocol 57, server 53, web 244, desktop 18) and build passed; current client copied to apps/server/public. Initial sandbox test/build attempts hit filesystem permissions; rerun outside the sandbox passed. Isolated headless Chrome fixture using the real stylesheet: full-height rail and full-width chat at 320, 390 and 700 px; desktop column geometry identical to HEAD at 1440x900. Real MobileVoicePreview component: participant rendering, no automatic join, explicit connect, inline failure and retry checked; screenshot reviewed. Not checked: authenticated end-to-end navigation, live microphone/camera/voice continuity on a real phone, mobile Safari or operating-system background audio.

- **Roles of owners, role menu, header control height (19 September 2026):** all four packages typechecked; server tests 53 (2 new in `authz.test.ts`), web tests 244 (4 new in `memberRank.test.ts`), root build green. Smoke test against :3001 (`chat_smoke`, no directory): 110 green, 1 new. Built client on :3002 (fresh database, dropped afterwards) in headless Chrome: role menu as first owner, further owner and moderator (see `docs/features/ui-admin.md`), and the stage header's controls measured at 35 px each with centered icons. The user's dev containers stayed running. Not checked: desktop app, the pop-out stage window's header, narrow screens.

- **Avatar in every view (19 September 2026, user's requirement: the image replaces the initials everywhere):** every `<Avatar>` in `apps/web/src` passes `src` (checked with grep: none without). Built client on :3001 (fresh database, local LiveKit, directory instance on :3101) driven in headless Chrome with fake media devices; the avatar was stored at the directory BEFORE the first sign-in, so the server picked it up at sign-in without a push. Image loaded in: dock, member list, chat message, mention suggestion, voice channel row in the sidebar, stage tile without camera, mini profile; screenshots reviewed. Earlier the same day: settings preview, friends' `friends.update`. Not checked in a browser: friends list and direct messages (they use the same component with `friendAvatar`), the pop-out stage window, the desktop app.

- **Avatar upload from the client (19 September 2026):** all four packages typechecked, web tests 240 (`avatarImage.test.ts` 2 new), root build green. Built client on :3001 (fresh `chat_avatar` database, dropped afterwards) with a directory instance on :3101, driven in headless Chrome over the DevTools protocol: sign-in with a planted key that has a handle, settings, upload through the file input, preview/dock/member list show the image, removal brings the initials back; screenshot reviewed. Not checked: desktop app, Safari, client without a home server.

- **Avatars from the directory account (19 September 2026):** all four packages typechecked; tests green (protocol 57 with 4 new avatar tests in `directory.test.ts`, server 51, web 238, desktop 18); migration 0022 via `pnpm db:generate`. Smoke test against a server on :3001 (`chat_smoke`, `DIRECTORY_URL=http://localhost:3101`, a directory smoke instance of `../squorli-directory` with the new routes): 127 green, 5 new for the avatar (null before, push after an upload, the address serves the image, `/api/me`, null after removal). Directory side: see its `docs/VERIFIED-STATE.md`. Not checked: the logged-in web client in a browser with a real avatar, the desktop app.

- **Password eye placement (19 September 2026):** CSS places the visibility toggle inside the right edge of both server-login password fields, reserving text space and a 44 px target. All four packages typechecked, 360 tests passed and root build copied the updated client to `apps/server/public`. Visual browser verification remains unavailable (no connected browser).

- **Login handle prefix (19 September 2026):** all four packages typechecked; all 360 tests and the full root build passed. Current client copied to `apps/server/public`. Sign-in now uses a fixed @ prefix and strips leading @ characters from typed/pasted values; API calls still use the bare handle. Browser interaction remains unchecked (no connected browser).

- **Login language buttons and Directory link explanation (19 September 2026):** all four packages typechecked; all 360 tests passed; full root build passed and copied the current web bundle to `apps/server/public`. Language buttons use the detected locale and `store.setLocale`; the registration link is on its own row and names the configured Directory host in German/English. Browser rendering remains unchecked (no connected browser); no backend or protocol changes.

- **Login icons and external registration (19 September 2026):** all four packages typechecked; 360 tests passed (53 protocol, 51 server, 238 web, 18 desktop). Removed the obsolete inline-registration view test, retaining five view-policy cases. Full root build regenerated icons and copied the updated client to `apps/server/public`. Sign-in and existing-account backup fields now use eye toggles; creation links directly to the configured Directory URL. No API/auth changes or smoke run. Browser interaction/rendering remains unchecked (no connected browser).

- **Server login clarity (19 September 2026):** all four packages passed `pnpm.cmd typecheck`; `pnpm.cmd test` passed (53 protocol, 51 server, 239 web including six new login-view tests, 18 desktop). Tests needed a rerun outside the sandbox because esbuild could not read ancestor directories while loading Vite configuration. Root `pnpm.cmd build` passed and copied the current client to `apps/server/public`; Vite reported its existing mixed-import/large-chunk advisories. Only client UI/view policy and documentation changed; no backend auth, protocol or shared-brand changes, no API smoke run. Browser screenshots, keyboard interaction and live login/registration flows were not checked (no connected browser). Notes: `docs/features/directory.md`.

- **Login: invite field always shown on invite-only servers (19 September 2026):** `pnpm typecheck` 4/4, tests green (web 238), `pnpm smoke` 109 green against a test server on :3001 with `chat_smoke`, no directory (new check `health inviteRequired follows openJoin`), `pnpm build`. Real login of that server in headless Chrome: field shown from the start, an invite link's code in the field, "In der App öffnen" = `squorli://invite/<host>/<code>` while a code stands there. Not checked: the link opening the installed app, servers older than this change (docs/features/directory.md).

- **Confirmed by the user with real devices (19 September 2026):** microphone test: hears himself on the chosen output device; in a voice channel the second person hears nothing during the test and hears him again afterwards. Video: "Ansehen" brings picture and sound; a pop-out closes when its feed is turned off; the hide button appears only while a video runs and all tiles are back after the last video. No answer yet on two points: whether the offer tile appears at once when a share starts while the viewer is already in the channel, and whether turning off by the eye button works and a restarted share is off again.

- **Watching video per feed, hide participants without video (19 September 2026):** `pnpm typecheck` 4/4, tests protocol 53, server 51, web 233 (`videoWatch.test.ts` 6 new), desktop 18, `pnpm build` green (client only, no smoke run). End to end against the dev LiveKit in headless Chrome with the real `VoiceClient` as the viewer: camera received by itself, share only after turning it on, both can be turned off (unsubscribed), a restarted share is off again; stage screenshots of the offer tile and of the tile view with and without participants without video. Not checked: two real people, share audio, pop-outs, Firefox/Safari, desktop shell (docs/features/voice-video.md).

- **Settings: "Sprache und Audio" in one tab, microphone test (19 September 2026):** `pnpm typecheck` 4/4, tests protocol 53, server 51, web 227, desktop 18, `pnpm build` green (no server or protocol change, no smoke run). Headless Chrome with the fake microphone, harness bundles of the real sources: test in no channel (monitor RMS 0.33, survives a device change, capture ended afterwards), test over a running pipeline with a stand-in for the published track (`mute` -> `monitor-on` -> `monitor-off` -> `unmute`; stays muted when muted by hand before; published track 0 with the gate closed while the monitor carries signal), the dialog with the built CSS (screenshots). Not checked: a real microphone, a real LiveKit room, Firefox/Safari, the desktop shell (docs/features/settings.md).

- **Microphone boost in stereo channels (18 September 2026, the cause of the report):** `pnpm typecheck` 4/4, web tests 227, `pnpm build` green. Same browser setup as the entry below, voice channel set to stereo by REST: boost off -> level 0.017, gate open 0 % (report reproduced); automatic -> x6, level 0.066, gate open 88 %; file RMS 0.003 -> x11.9, level 0.030, gate open 85 %; second join starts at the remembered gain. Not checked: real microphones, Firefox, desktop shell, listening to the result, room noise in a stereo channel.

- **Microphone boost (18 September 2026):** `pnpm typecheck` 4/4, tests protocol 53, server 51, web 227 (`micBoost.test.ts` 10 new), desktop 18, `pnpm build` green. Real client in headless Chrome against a test server on :3001 and the dev LiveKit, a generated WAV file as the microphone, debug view read for 12 s per run: boost off and automatic at file levels RMS 0.003 / 0.012 / 0.1 all arrive at about 0.085 (Chromium's own gain control), the automatic ends at x1.0; by hand x3 -> 0.24, peaks 0.44 (docs/features/voice-video.md). Not checked: real microphones, Firefox/Safari, stereo channels, the desktop shell, listening to the result; `pnpm smoke` (no server change since the run above).

- **LiveKit grants follow a permission change (18 September 2026):** `pnpm typecheck` 4/4, server tests 51 (`livekit/sync.test.ts` 4 new), `pnpm smoke` 108 green against a test server on :3001 with a fresh `chat_smoke` (no directory); end to end against the dev LiveKit with a real client in headless Chrome (fake camera): guest -> "Mitglied" by REST while in voice -> LiveKit lists camera and screen sources, the camera publishes; role removed -> microphone only, camera track gone (docs/features/voice-video.md). Not checked: role edit/deletion and owner status end to end, `pnpm build` (no client change). The smoke test's remembered owner key (`apps/server/scripts/.smoke-owner.json`) was removed together with the old `chat_smoke`; the database was left fresh, the next run creates both again.

- **Directory: e-mail address required for new handles (18 September 2026):** `pnpm typecheck` 4/4, tests protocol 53 (`directory.test.ts` 4 new), server 47, web 217, desktop 18, `pnpm build` green; protocol copies in `../squorli-directory` synchronized (`diff -r`), its typecheck, tests and smoke test green (147 / 148 with `REQUIRE_EMAIL`). The login's handle registration driven in headless Chrome against a test server on :3001 and a `REQUIRE_EMAIL` directory on :3101 (docs/features/directory.md). Not checked: `pnpm smoke` here (server unchanged), desktop shell, Firefox.

- **Language change while in voice (18 September 2026):** `pnpm typecheck` 4/4, web tests 217, `pnpm build` green; checked in headless Chrome against a test server on :3001 (docs/features/settings.md). Not checked: desktop shell, the account's language arriving while in voice.

- **Voice presence after a server restart (18 September 2026):** `pnpm typecheck` 4/4, server tests 47 (`presence.test.ts` 5), web 217, `pnpm build` green; restart of a test server on :3001 under a client in voice, today's and a simulated old client (docs/features/voice-video.md). Not checked: several members, production.

- **Dead connections (18 September 2026):** `pnpm typecheck` 4/4, server tests 46 (`ws/liveness.test.ts` 3 new, `hub.test.ts` 6), `pnpm build` green; behaviour checked against a test server on :3001 (docs/features/afk.md). Not checked: real sleep/network change, behind a proxy.

- **Desktop app wording (18 September 2026):** `pnpm typecheck` 4/4, web tests 217 (`i18n/index.test.ts` 2 new), `pnpm build` green; texts looked at in the dev shell.

- **Desktop app: AFK detection without a switch (18 September 2026):** `pnpm typecheck` 4/4, web tests 215, `pnpm build` green; dev shell with a fresh profile: detector started at load, settings section without a checkbox.

- **Stage in a window of its own (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 215 (`usePushToTalk.test.ts`, `voice/screenAudioHint.test.ts` new; the second one with the desktop hint text that followed) / desktop 18, `pnpm build` green. Behaviour in headless Chrome and in the dev desktop shell against an own server on :3001 with LiveKit (list in `docs/features/voice-video.md`). **Not checked:** Firefox, a second participant, push-to-talk in that window by hand. No smoke test (no protocol or server change).

- **Desktop app P4 (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 211 / desktop 18 (`updateMode` new), `pnpm build` green; release workflow green on GitHub for a test build and for the tag `desktop-v0.1.0` (six files, published by the user). Local update test with a packaged 0.1.0 test app against a local manifest with absolute addresses: checking -> downloading -> ready, restart offered; nothing installed. Website: `pnpm check`, `pnpm build` (12 pages), `pnpm test` (11 routes incl. feed consistency, 8 recaptured screenshots), `pnpm brand:check` green. **Not checked:** installing an update, the deployed feed, AppImage/deb.

- **Tray, scrollbars, opt-in share audio (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 211 (`videoAudio.test.ts` 1 new, 1 adapted) / desktop 17, `pnpm build` green. App over the DevTools protocol: tray reported, `closeToTray` stored and validated, close button hides the window while the page lives on; rail rounded with mica (through the settings dialog); scrollbar thumb style computed. **Not checked:** the tray icon's look, share audio with a real second participant, the dock message in a real disconnect.

- **Share audio through the native helper (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 210 / desktop 17, `pnpm build` green; `node apps/desktop/native/build.mjs` builds the helper with VS 2022; `pnpm --filter @squorli/desktop dist` packs it as `resources/native/`. Helper alone: include captures a tone of another process (rms 0.035), exclude does not (0.0001), ends on stdin close. In the app (voice channel on an own server :3001, fake microphone, the client's share button): one helper process while sharing, worklet output peak 0.0600 = the tone's amplitude, audio publication present, helper gone after the share and after the app. **Not checked:** window mode end to end, a listener, long runs, the GitHub runner build, Windows 10.

- **Own title bar, background modes (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 210 / desktop 16, `pnpm build` green. App over the DevTools protocol: title bar (32 px, drag region, three buttons), maximize/restore with the button following, in the opaque and the see-through window; mica live; see-through after a restart (body transparent), restart pending reported both ways. **Not checked:** the look of the see-through mode, dragging and edge-resizing by hand, Linux.

- **Window place, icon, layout (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 210 (`layout.test.ts` 3 new) / desktop 15 (`restoreWindowState` 3 new), `pnpm build` green. App over the DevTools protocol: opens at stored bounds, stores at close, reopens there, an unreachable place falls back to the default. Built web client on an own server (:3001) in Chrome with real mouse events: 6 px gaps, member list as a panel, both column handles drag, widths stored and applied after a reload. **Not checked:** the task bar icon (not readable by script), moving the window by hand (the protocol cannot move an Electron window), touch.

- **Desktop app P5/P6 and settings without a server (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 / server 42 / web 207 / desktop 12 (deep link arguments, appearance new), `pnpm build` green. The unpackaged app over the DevTools protocol: deep link at cold start (join view, invite prefilled, no session), from a second start (shown in the running window, second process exit 0), invalid links ignored; window background set through the bridge and through the real settings dialog, normalized, stored, applied after a restart (computed styles); settings dialog without a server (seven tabs, key on the account tab). **Not checked:** the look of mica/acrylic, a link clicked in a browser, Linux. The website's screenshot check is stale after today's client changes.

- **Desktop app P3, packaging (18 September 2026):** `pnpm --filter @squorli/desktop dist` on Windows 11 builds `Squorli-Setup-0.1.0.exe`, blockmap and `latest.yml`; the packaged `win-unpacked/Squorli.exe` shows the client from `app://squorli/` with the app's user agent and ignores development arguments (DevTools protocol). `pnpm typecheck` 4/4, `pnpm test` (protocol 49 / server 42 / web 207 / desktop 8), `pnpm build` green; workflow and CI YAML parse. **Not checked:** the installer run, the GitHub release workflow, Linux targets.

- **Desktop app P2b, user agent label (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 49 (2 new) / server 42 / web 207 / desktop 8; `../squorli-directory`: typecheck green, tests protocol 25 / directory 21, the six shared protocol files byte-identical. `pnpm smoke` "alles ok" against an own server on :3001 (`chat_smoke` recreated, `DIRECTORY_URL=`, `RADIO_IDLE_STOP_MS=2000`). **Not checked:** directory smoke test (the change there is the copied label only).

- **Desktop app P2, Electron shell (18 September 2026):** `pnpm typecheck` 4/4, `pnpm test` protocol 47 / server 42 / web 207 / desktop 8 (new), `pnpm build` green, `docker build --target app` green (full workspace, frozen lockfile, no Electron binary). The unpackaged app driven over the DevTools protocol on Windows 11, Electron 44.4.2 / Chrome 152: origin, secure context, storage, user agent, bridge, CSP (eval blocked), traversal guard, public directory reachable; join by address, voice connected (fake microphone), screen picker (cancel = AbortError, pick = video + loopback audio track), window rules, restart reopens the server; Twitch and YouTube embeds with and without the shell's fix. No console errors besides expected 404 lookups. **Not checked:** listener's side of system audio and echo, camera/screen towards a second participant, pop-outs with a real track, YouTube in step, Linux, `pnpm dev:desktop` against Vite.

- **Desktop app P1, client without a home server (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 / web 207 (`clientHome.test.ts` 10, `railServers.test.ts` 3 new), `pnpm build` green. Client only, no smoke test. Headless Chrome over the DevTools protocol (scratchpad scripts), client with `VITE_HOMELESS=1` on :5199, test chat server :3001 (fresh database, once without and once with the test directory :3101, `PUBLIC_DOMAIN=localhost:3001` with `DIRECTORY_PROOF_URL`): device key, no-servers view, refused address, join view before any session, join, reload and browser restart reopen the server, invite-required message, account login on a second profile (wrong password, then key replaced and the account's server opened by itself), sign-out; the built web client on :3001 still logs in and keeps its session. No console errors. **Not checked:** authenticator and e-mail code in the desktop login, friends/DMs without a home server, a real domain. The website's screenshot check is stale after these client changes (`pnpm screenshots` there).

- **Desktop app P0, platform interface (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 / web 194 (`platform/deepLink.test.ts` 7 new), `pnpm build` green. Client only, no smoke test. **Not checked:** the desktop adapter (no shell before P2); the unchanged browser behaviour was not clicked through by hand.

- **Own tab in the screen share (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 / web 187, `pnpm build` green. Client only, no smoke test. Headless Chrome 153 reports `restrictOwnAudio` in `getSupportedConstraints()`. **Not checked:** a real share of the own tab with a second listener (picker and two people needed).

- **"Starting videoinput failed" on Firefox (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 / web 187 (cameraRetry.test.ts 3 new), `pnpm build` green. Client only, no smoke test. **Not checked:** a real Firefox with a camera (none available here).

- **Pop-out froze on Firefox, black tile after camera off (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 / web 184 (videoDisplay.test.ts 2 new), `pnpm build` green. Client only, no smoke test. Firefox nightly 158 (zip in the scratchpad, headless) and headless Chrome 152 with a real `RemoteVideoTrack` in a pop-out: before, Firefox reported the view invisible after 100 ms (LiveKit pauses the track), after the fix it stays visible; Chrome fine both ways (`docs/features/voice-video.md`). **Not checked:** a real LiveKit session in Firefox; the black tile only by reading LiveKit's `setTrackEnabled` (mute, not unpublish).

- **Microphone gone: fallback to the default device, own error message (18 September 2026):** `pnpm typecheck` 3/3, `pnpm test` server 42 / web 182 (micPipeline.test.ts 4 new), `pnpm build` green. Client only, no smoke test (server, protocol and auth untouched). Against the reporting server: `https://community.k9-kommando.de/rtc/validate` -> 401 from LiveKit, `/api/health` ok (`proxyMode: external`), so the proxy was never the cause. **Not checked:** a real browser with an unplugged or renamed microphone.

- **AFK: fixed 10 minutes, no time per server (17 September 2026):** `pnpm db:generate` (0021), `pnpm typecheck` 3/3, `pnpm test` protocol 47 / server 42 (hub.test.ts 5) / web 178, `pnpm build` green. `pnpm smoke` on :3001 against a freshly created `chat_smoke` without a directory (both new migrations from scratch): 108 checks, all ok, twice. `../squorli-directory`: `friends.ts` copied (byte-identical), typecheck 2/2, tests 23 + 21, build green; its smoke test not run again (only the constant in the copied file changed, the service does not use it). The user's dev server took 0021 by itself (22 migrations, `afk_move_minutes` gone, `/api/health` ok). Website: check, build, route verification green. Not checked: the real client in a browser, `docker build`.

- **AFK detection, AFK channel, AFK in the friends list (17 September 2026):** `pnpm db:generate` (0020), `pnpm typecheck` 3/3, `pnpm test` protocol 47 (afk.test.ts 4 new) / server 41 (hub.test.ts 3, voice/afk.test.ts 5 new) / web 178 (activity.test.ts 5 new), `pnpm icons` (moon), `pnpm build` green. `pnpm smoke` on :3001 against a freshly created `chat_smoke` without a directory (`directoryUrl: null` checked first) with `RADIO_IDLE_STOP_MS=2000`: 108 checks, all ok, four runs in a row (5 new; the first runs failed one new check now and then, the cause was the script's `waitFor` helper, whose expired timer removed another waiter with `splice(-1)`; fixed, the server had moved the member every time). `../squorli-directory`: protocol copies byte-identical (`cmp`), typecheck 2/2, tests 23 + 21, build green, its smoke test on :3101 144 checks green (6 new). The user's dev server on :3000 picked up migration 0020 by itself (21 migrations, `/api/health` ok). Looks: headless Chrome screenshot of a static page with the built CSS (crescent on the presence dot, moon icons, the dock's notice with the way back). Not checked: the real client in a browser with audio (move, way back, locked buttons), the Idle Detection permission prompt, LiveKit's reaction to `silence`, `docker build`.

- **Web radio: YouTube in step, pop-out window of our own, idle stop (17 September 2026):** `pnpm db:generate` (0019), `pnpm typecheck` 3/3, `pnpm test` protocol 43 / server 33 / web 171, `pnpm build` green (two pages: `index.html`, `player-window.html`). `pnpm smoke` on :3001 against `chat_smoke` without a directory (`directoryUrl: null` checked first) with `RADIO_IDLE_STOP_MS=2000`: 103 checks, all ok, three runs (5 new: YouTube source with title and offset, playback permission/validation/event/state, no playback for audio, the setting, the idle stop's timing incl. "not while somebody is inside" and "not with the setting off"). **Headless Chrome over the DevTools protocol with Chrome's default autoplay policy, real clicks and keys inside the players, against a harness page with a fake server (BroadcastChannel) in two windows, controller and follower:** both start at the shared place at our volume; the controller's pause, jump while paused, play, jump back while playing reach the follower (within about 0.5 s of each other); exactly the six actions were published; the follower's pause and jump are undone with one notice and nothing published; the follower stays in step in a minimized window; the pop-out window plays in step at our volume and coming back published nothing; a live stream (`live: true`) advances 10.1 s in 10 s untouched; a video with embedding disabled gives one notice. Twitch: plays at volume 0.05, is paused by Twitch when the window is minimized, **plays again by itself when it is back**, plays in the pop-out window while the page is minimized, and comes back into the page (there after the one more click the notice asks for). Screenshots of both players with our button, the radio menu for a YouTube source and the admin switch. The user's dev database took migration 0019 by itself. The harness and the DevTools scripts were deleted again. Not checked: the running app with two real people, Firefox/Safari, YouTube ads, a controller on a slow connection.
- **Web radio: Twitch with the official player (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 28 / web 147 (twitch.test.ts 5 new), `pnpm build` green, `pnpm smoke` on :3001 without a directory: 98 checks, all ok (1 new). Real player driven in headless Chrome over the DevTools protocol (`docs/features/radio.md`). No migration.

- **Web radio: typed address (17 September 2026):** `pnpm db:generate` (0018), `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 28 / web 142, `pnpm build` green, `pnpm smoke` on :3001 without a directory: 97 checks, all ok (1 new). The field in the menu was not looked at in a browser.

- **Web radio: 5 % start volume, own output device (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` web 142 (radioPlayer.test.ts +1, accountSettings.test.ts extended: the radio's device id stays on the device), `pnpm build` green; `setSinkId` on the playing stream checked in headless Chrome (`docs/features/radio.md`). No server or protocol change, `pnpm smoke` not run again.

- **Web radio, now playing (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 28 (icy.test.ts 5 new) / web 141 (radioLabel.test.ts 4 new), `pnpm build` green, `pnpm smoke` on :3001 without a directory 96 checks all ok (no new ones: the feature needs the internet), plus the live, end-to-end and screenshot checks named in `docs/features/radio.md`. Not checked: a real browser session with sound, Firefox/Safari.

- **Web radio (17 September 2026):** `pnpm db:generate` (0017), `pnpm icons` (new: radio, play, square), `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 23 (playlist.test.ts 10 new) / web 137 (radioPlayer.test.ts 3 new), `pnpm build` green. `pnpm smoke` on :3001 against `chat_smoke` without a directory (`directoryUrl: null` checked first): 96 checks, all ok (6 new for the radio). Headless Chrome: screenshot of the stage button, the menu, the sidebar mark and the admin tab against the built CSS; playback probe of the example stream and of addresses resolved from a real `.m3u` and `.pls`. The user's dev server applied migration 0017 by itself (18 migrations, `radio_stations` present). Not checked: two real people in a voice channel, Firefox/Safari, output-device routing.

- **Mention suggestions in the edit field (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 13 / web 134 (no new tests: the pure logic was already covered, the rest is wiring), `pnpm build` green; headless Chrome screenshot of the list under the edit field. Not checked: typing, keys and accepting in the running app.

- **No false alarms for mentions (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 37 / server 13 / web 134 (new: scanner, unique names, two members with one name, scanner against parser), `pnpm build` green. `pnpm smoke` on :3001 against a fresh `chat_smoke` **without a directory**: 90 green, twice (new check: tokens in a code span, in a fence and behind a backslash do not count, a real mention counts, an edited-away mention stops counting). The first attempt ran against the production directory by mistake (`docs/features/mentions-unread.md`); the new guard in `smoke.mjs` was checked against the dev server (stops after two health requests). Not checked: `recountMarks` in the running app (edit/delete while another channel is open), the fallback path against a server without read states.

- **Rail marks for all servers, mutes (17 September 2026):** `pnpm db:generate` (0016), `pnpm icons` (new: bell-off), `pnpm typecheck` 3/3, `pnpm test` protocol 36 / server 13 / web 131, `pnpm build` green. `pnpm smoke` on :3001 against a fresh `chat_smoke` without a directory: 89 green, twice (4 new checks for mutes). The user's dev database took migration 0016 through `tsx watch` (restart triggered by touching a source file after generating, `apps/server/AGENTS.md` pitfalls). Headless Chrome screenshot of the rail and the channel list with all states and both menus. Not checked: background connections against two real servers with a directory, muting from a second device, the rail menu on the own server.

- **Read states on the server (17 September 2026):** `pnpm db:generate` (0014) plus custom 0015, `pnpm typecheck` 3/3, `pnpm test` protocol 36 (1 new) / server 13 / web 131, `pnpm build` green. `pnpm smoke` on :3001 against a fresh `chat_smoke` without a directory: 85 green, twice (6 new checks). The user's dev database took both migrations through `tsx watch` (4 seeded rows = 2 members x 2 channels with messages) and :3000 answers `/api/read-state` with 401 without a token. Not checked: the client side in a browser (sync after the welcome, acknowledgements, `read.update` between two devices, the fallback against an older foreign server). `docker build` not repeated (no Dockerfile change; the migrations are plain files under `apps/server/drizzle`).

- **Unread marks across sessions, mention highlight (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 131 (readState.test.ts 5 new), `pnpm build` green; headless Chrome screenshot of the channel list (read, unread, mentioned, open). Not checked: the catch-up after connecting against a running server with real channels (request burst, channels without read access), a second device. `pnpm smoke` not run (client only).

- **Mentions; emoji font fix (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 126 (mentions.test.ts 7 new, MessageText.test.tsx +1), `pnpm build` green. Headless Chrome screenshots against the built CSS: emoji render (text, large, message input, picker grid, flags, skin tones, ZWJ, keycaps), the smiley button matches "Senden", mention chips, highlighted message, suggestion list and both badges look right. `pnpm smoke` not run (no server or protocol code touched); `docker build` not repeated after the font moved from `node_modules` into `apps/web/src/emoji/font/` (the Dockerfile is unchanged). Not checked: Firefox and Safari, the mention and picker interaction in the running app.

- **Emoji, more Markdown, focus after sending, licenses tab (17 September 2026):** `pnpm emoji`, `pnpm licenses` (28 packages, 22 license texts), `pnpm icons` (new: smile, search, clock, hand, leaf, utensils, plane, trophy, lightbulb, heart, flag, scale), `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 118 (emoji/convert.test.ts 9, emoji/picker.test.ts 8, markdown.test.ts +3, MessageText.test.tsx +2), `pnpm build` green (font slices, `data.de`/`data.en` and `thirdParty` as separate files), `docker build --target app` green (10 font slices in `/app/public/assets`, `COPY LICENSE NOTICE THIRD-PARTY-NOTICES.md`). `pnpm smoke` not run (no server or protocol code touched). Not checked (no browser connected): how the font renders in Chromium, Firefox and Safari, the picker (placement above the button, search, keyboard, skin tones, narrow screens), emoji size in messages, the task list boxes, the licenses tab, the focus after sending.

- **Markdown tables and copy button on code blocks (17 September 2026):** `pnpm icons` (new: copy, check), `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 96 (markdown.test.ts 4 new for tables, MessageText.test.tsx 1 new), `pnpm build` green. `pnpm smoke` not run (client-only change). Not checked (no browser connected): the look of tables, sideways scrolling of wide tables, the copy button (hover, touch, clipboard in the browser, the http fallback).

- **Growing message input (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 91 (autoGrow.test.ts 4 new), `pnpm build` green. `pnpm smoke` not run (client-only change). Not checked (no browser connected): the growth itself, the limit at a third, the history staying at the bottom, the edit field, narrow screens.

- **Markdown in the text chat (17 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 35 / server 13 / web 87 (markdown.test.ts 21 new, MessageText.test.tsx 4 new), `pnpm build` green. `pnpm smoke` not run (client-only change, no server or protocol code touched). Not checked (no browser connected): the look of headings, code blocks, quotes and lists in the channel chat and in direct messages, long code lines on a narrow screen.

- **Settings dialog, mini profile, settings in the account (17 September 2026):** `pnpm icons` (new: cloud, languages), `pnpm typecheck` 3/3, `pnpm test` protocol 34 (directory.test.ts 5 new) / server 13 / web 52 (accountSettings.test.ts 4 new, menuPosition.test.ts 2 new for the upward mini profile), `pnpm build` green. `pnpm smoke` on :3001 (fresh `chat_smoke`, `DIRECTORY_URL=http://localhost:3101`) against the directory smoke instance: 91 green twice (server code unchanged; regression run for the protocol change). Directory repo: typecheck 2/2, tests 23 + 21, build, smoke 138 green twice. Not checked (no browser connected): the settings dialog and its eight categories, the mini profile's keyboard handling, adopting settings on a second device/server, the language reload, the speaker view with the option off.

- **Cue settings in the directory account (16 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 29 / server 13 / web 33, `pnpm --filter @squorli/web build` green. Not run: `pnpm smoke` (no Docker/Postgres in the editing environment) and the browser flow (settings tab text, account takeover on a second device); the directory side is covered by its smoke test additions once a service runs.

- **Account deletion through the directory (15 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 29 / server 13 / web 8, `pnpm icons` (external-link, user-x), `pnpm build` green. `pnpm smoke` on :3001 (`chat_smoke`, `DIRECTORY_URL=http://localhost:3101`) against the directory smoke instance: 91 green (handle for a member, signed `server-leave`: founder 409, member deleted with WS close 4012 and dead token, gone from the account's server list, re-join creates a new user). Directory repo: typecheck 2/2, tests 18 + 16, build, smoke 107 green. Not checked by hand: the rail context menu and the login note after a deletion in the browser.

- **Multi-server client (14 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 29 / server 13 / web 4, `pnpm build` green. `pnpm smoke` on :3001 against a fresh `chat_smoke` without a directory: 78 green; `curl` with a foreign `Origin` gets `access-control-allow-origin` on GET and on the preflight (methods, `authorization`). Not checked by hand (needs two deployed servers with different hosts plus the directory): switching servers in the rail, login at the foreign server, voice staying up across the switch, joining voice on the second server disconnecting the first, attachments and icons of the foreign server, `ServerStatus` for an unreachable server.

- **M7 friends and direct messages (14 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 29 (dm 5 new), web 4, server 13, `pnpm build` green (server 114 kB), `docker build` green (protocol now depends on @noble/curves). `pnpm smoke` on :3001 against the directory smoke instance on :3101: 86 green (unchanged; the chat server takes no part in DMs). Directory repo: typecheck 2/2, tests 18 + 14, build, docker build, smoke 100 green. Not checked by hand: home view, DM view, rail badge, member context menu, two real accounts messaging in the browser.

- **Server registration at the directory (14 September 2026):** `pnpm typecheck` 3/3, `pnpm test` 24 + 13 + 4, `pnpm build` (web 1.05 MB, server 99 kB). `pnpm smoke` on :3001 (`PUBLIC_DOMAIN=localhost`, proof `http://localhost:3001/api/health`) against the directory smoke instance on :3101: 77 green (server key generated and registered at start, `serverKey` in `/api/health`, names from the directory applied at login). Directory repo: 61 smoke checks green. Chat smoke 78: a name changed at the directory reaches the server via push within a second, without a new login. The 5-minute sync itself is not covered (interval). Not yet checked by hand: registration of a real https deployment (the directory must reach `DIRECTORY_PROOF_URL`).

- **Display names from the directory (14 September 2026):** `pnpm typecheck` 3/3, `pnpm test` protocol 24, server 13, web 4, `pnpm build` (web 1.05 MB, server 93 kB). `pnpm smoke` against `chat_smoke` on :3001 with `DIRECTORY_URL=http://localhost:3101` (directory smoke instance): 76 green, twice in a row (global name applied at login, per-server name wins, cleared names leave the local null so the handle shows). Directory repo: 49 smoke checks green. Not yet checked by hand: the two name fields in the profile dialog and the name refresh via `GET /api/me`.

- **M6d server directory (14 September 2026):** chat typecheck 3/3, tests 24/13/4, build green; `pnpm smoke` on :3001 against a fresh `chat_smoke` with the directory smoke instance on :3101: 86 green (new: patch listed + description, server appears in `GET /api/servers` of the directory with description and member count, unlisted disappears, description > 200 = 400; 4 registrations, no 429 thanks to the debounce). Directory: typecheck 2/2, tests 13 + 14, build, `pnpm smoke` against :3101 with `DIRECTORY_SECRET_KEY` 65 green (listing with description/open join/member count/icon, icon served as PNG with identical bytes, unknown host 404, status carries `iconUpdatedAt`, unlisted re-registration keeps the icon version). Dev DBs migrated (chat 0012, directory 0006). Not checked by hand: rail icons from the directory, "Server entdecken" modal, account page icons.
- **Only with account + login splash (14 September 2026):** typecheck 3/3, tests 24/13/4, `pnpm build` green. `pnpm smoke` against a fresh `chat_smoke` on :3001: with `DIRECTORY_URL=http://localhost:3100` 81 checks green (twice), without a directory 75 green (new: patch `requireAccount`, in `/api/state` and `/api/health`, `version` present, key without handle 403 `account_required` with directory / 200 without, owner exempt, member without handle locked out). Dev DB migrated to 0011 (12 migrations). `REQUIRE_ACCOUNT=true` on :3001: `/api/health` reports `requireAccount: true` although the stored setting is false, owner login 200, `PATCH requireAccount` 409 `locked_by_config`, `PATCH name` still 200, state shows `requireAccountLocked: true`, fresh key 403 `account_required`; smoke without the variable 82 green. Not checked by hand: splash and footer in the browser, checkbox in the admin panel.
- **Directory moved out (14 September 2026):** after removing `apps/directory` and the Compose/Caddy/proxy/Dockerfile parts: `pnpm typecheck` 3/3, `pnpm test` protocol 24 / server 13 / web 4, lockfile without the directory importer, `docker build --target app` green. In `../squorli-directory`: typecheck 2/2, tests 13 + 14, build, `docker build`, `pnpm smoke` 38 green against its own Postgres (:5433). `ChallengeRequest`/`ChallengeResponse` moved into `directory.ts`, no protocol version bump (same shapes). Not run again: the chat smoke test with `DIRECTORY_URL` (unchanged code path).
- **Owner/icon/title (14 September 2026):** `pnpm smoke` against `chat_smoke` without a directory: 71 green, twice (appointing an owner only by an owner, permissions immediately ADMINISTRATOR, first owner and oneself cannot be demoted, demotion restores role permissions; icon upload as PNG, served with type, `iconUrl`/`serverName` in `/api/health`, SVG and non-admin rejected, removal). Typecheck 4/4, 55 tests, build green. Still open by hand: favicon change in the browser, icon upload in the admin panel ("Verwaltung"), owner group in the member list.
- **M6c (14 September 2026):** `pnpm typecheck` 4/4 packages, `pnpm test` protocol 24 (new: useragent 9), directory 14 (new: totp 11 with RFC 6238/4226 vectors, secrets 3), server 13, web 4. `pnpm smoke:directory` against its own instance (:3101, `directory_smoke`, with `DIRECTORY_SECRET_KEY`): 38 green (status, setup without backup 409, without account 404, enable with wrong/correct code, `totp_required` only after the correct password, TOTP fetch, `totp_reused`, recovery code single-use and normalized, change password without/with code, regenerating codes invalidates the old ones, disabling, then fetch without code); without the key 22 green (503 `totp_unavailable`, block skipped). `pnpm smoke` against `chat_smoke` with `DIRECTORY_URL`: 67 green (twice in a row; migration 0008 on existing sessions), against a completely fresh `chat_smoke` without a directory 65 green (session list with exactly one `current`, label "Firefox auf Windows" (Firefox on Windows) from the user agent, remote logout closes the WS with 4011 and the token returns 401, foreign/unknown id 404, "others" leaves one's own session in place, "current" ends one's own). `pnpm build` (web 1.04 MB, server 85 kB, service 45 kB + account page 105 kB incl. QR code), `docker build --target directory` and `--target app` run through.
- **Not yet checked by hand (M6c):** account page in the browser (scan the authenticator with a real app, codes, fetch list), code field in "Mit Konto anmelden" (sign in with account), Profile > Devices in two browsers, "Passwort festlegen" (set password) from the chat client (CORS for PUT now allowed).
- `pnpm typecheck`: 3/3 packages without errors.
- `pnpm test`: protocol 11, server 13, web 4 tests. All green (permission bits 1152/7616 pinned).
- `pnpm smoke` (protocol v4, against test DB `chat_smoke`, twice in a row): 55 checks green, including moderation (move without permission 403, `voice.moved`/`voice.stop` at the target, block removes STREAM_VIDEO from `me` and token, unblocking restores it, removing = `voice.moved null`). Before that (v3): 49 checks green (owner, roles "Gast"/"Mitglied" (guest/member), guest may not write but may use voice, invites incl. limit, permission hierarchy, messages with history/cursor/edit/delete, attachment upload and download, cleanup on delete, typing, voice presence, RTC token with/without camera grant, kick with `removed` event, ban/unban).
- Migrations 0003/0004 on the existing dev DB: the default role is now called "Gast" (1152), new role "Mitglied" (7616) assigned to all existing members.
- **M3 load test (13 September 2026, local, Docker Desktop):** `pnpm bots --video 15 --audio 0 --subscribers 1` (lk preset "high", simulcast, 4x4 layout): the listener receives 15/15 tracks at a combined 4.1 Mbit/s (236-309 kbit/s per track), packet loss 0.03 %; LiveKit ~9 % CPU, ~135 MB RAM. Matches the estimate in PLAN 4.3 (client download ~4 Mbit/s); values entered there.
- **M6b (14 September 2026):** `pnpm smoke:directory` 20 checks green (additionally: backup without account 404, wrong signature 401, storing, `hasBackup`, params without iv, wrong password 401, fetch + decryption yields the seed, unknown handle 404, replacing = old password rejected). Chat smoke test with directory still 61 green; against a completely fresh `chat_smoke` (new installation) 59 green with all three roles. `pnpm build` produces `apps/directory/public/` (main.js 77 kB), account page served via https://community-id.datlicht.eu/ from the user's dev stack (200, `features.backup: true`). Protocol tests 15 (4 new for backup.ts). **Confirmed by hand (14 September 2026, Daniel):** account @datlicht created on the account page, signed in on the PC (owner), then "Mit Konto anmelden" (sign in with account) on the phone: same key, second session of the same user. Not yet checked: "Passwort festlegen" (set password) for an existing browser key, password change on the account page.
- **M6a (14 September 2026):** `pnpm smoke:directory` 11 checks green (registration, double registration, taken handle, wrong signature, handle rules, resolution in both directions, replay). Chat smoke test with the directory attached: the owner gets their handle at login (61 checks), without a directory 59. Docker image `--target directory` builds.
- **Not checked automatically:** the UI in the browser (login with invite, chat, admin panel, member list, stage with camera and screen share, notice for a share without audio). Must be done by hand in two browser profiles; the screen-share test matrix (PLAN 3.6) with `/test/screenshare.html` per browser/operating system.
- **Done for the M3 acceptance (15 September 2026):** webcam and screen share transmission with real devices.
- **Open for the M3 acceptance:** 15 real cameras (or video bots against a target server on the internet instead of locally), tab audio from Chrome at all receivers, matrix 3.6 filled in.
- LiveKit accepts the app server's tokens (`/rtc/validate` -> 200).
- Load test `pnpm bots --subscribers 1`: 15 audio bots + 1 listener, all 15 tracks subscribed, 0 % packet loss, ~284 kbit/s at the listener, LiveKit 10-15 % CPU / ~90 MB RAM, app server and Postgres unimpressed. The LiveKit log shows "error reading data channel ... User Initiated Abort" during teardown: harmless.
- **Confirmed by hand (13 September 2026, Daniel):** voice chat from an iPhone (Safari, external) to the dev stack behind router NAT, access via Nginx Proxy Manager on another host, domain community-test.datlicht.eu. Required: `PUBLIC_DOMAIN` = domain, `LIVEKIT_PUBLIC_URL` without port (or omitted), `LIVEKIT_DEV_NODE_IP` = public IP, 7882/udp + 7881/tcp forwarded at the router to the chat host.
- **Testing restrictive networks:** `docs/DEVELOPMENT.md` section "Simulating restrictive networks" (remove the UDP forwarding -> TCP fallback; `?ice=relay` -> TURN only). The debug view shows the active ICE path from `candidate-pair` statistics (`room.engine.pcManager`, internal API, check after SDK updates).
- **Deferred (decision of 13 September 2026): remaining M1 acceptance later, M2 begins.** Open points see the next line; procedure in the `docs/DEVELOPMENT.md` section "Simulating restrictive networks".
- **Still open for the M1 acceptance:** a peer in a restrictive network (corporate Wi-Fi, UDP blocked) and thus TURN; device switching and PTT key only checked superficially; the 15-bot test only ran locally.
- `pnpm docker:dev`: Postgres and LiveKit start, migrations create `users` and `sessions`.
- `pnpm dev` (wrapper): containers up, server on :3000, Vite on :5173, proxy `/api/health` works. Ctrl+C (a real console event) and a crash of the apps both stop the containers and leave no processes behind on :3000/:5173.
- `pnpm build`: web bundle ~209 kB, server bundle ~10 kB. `node dist/index.js` with `STATIC_DIR` serves the SPA incl. fallback.
- `pnpm --filter @squorli/server smoke`: challenge/verify 200, replay 401, wrong signature 401, RTC token 200, WS welcome/pong, version 99 rejected. Exit 0. Green alike against `tsx watch`, against `node dist/index.js` and against the container.
- `docker build`: image ~291 MB, starts against the dev database, serves the web client and passes the smoke test.

Bugs fixed along the way (M0 check): the server did not read `apps/server/.env` (loader added); the smoke test aborted on exit under Windows (wait for close); `.dockerignore` was missing; `pnpm deploy` needed `--legacy`; the server bundle imported `@squorli/protocol` as TypeScript source (now bundled in).
Fixed in M1: the RTC URL ended in `/rtc` (the SDK appends it itself); the LiveKit dev secret did not match the app server; LiveKit in the container offered an unreachable ICE address (`--node-ip`).

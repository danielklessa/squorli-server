# Feature notes: Directory service integration (M6 handles, backup, second factor, e-mail; account page)

Part of the project description (entry point: root `AGENTS.md`, section 0). Dated entries: what was built, the user's wishes and decisions, consequences, and what was and was not checked. Standing rules live in the `AGENTS.md` of the area. Whoever changes the feature adds or updates its entry here in the same step.

Code: server `apps/server/src/directory.ts`; client `apps/web/src/api.ts` (directory functions), `LoginScreen.tsx`, `store.ts`; protocol `directory.ts`, `backup.ts`; the service itself in `../squorli-directory`. Standing rules: `apps/server/AGENTS.md` (directory service, device management, account deletion), `packages/protocol/AGENTS.md` (key backup), root `AGENTS.md` section 2a (synchronized copies). Friends and direct messages (M7): `../squorli-directory/docs/features/friends-dm.md`.

## 19 September 2026: separate server login tasks

**A small profile on a left click in the member list (24 September 2026, user's wish):** "Wenn ich einen Linksklick auf einen Benutzer in der Benutzerliste mache, mit dem ich bereits befreundet bin soll ein kleines Miniprofil mit einer Eingabezeile zum Chatten erscheinen (auch Emojis) schicke ich den Text ab wechsle ich in die Freunde-Ansicht mit diesem Freund geöffnet. Bin ich noch nicht befreundet soll stattdessen ein Button sein um eine Freundschaftsanfrage zu senden."
- *Built:* `MemberProfile.tsx`, opened by a left click on a member row (`MemberList.tsx`; the right click keeps the context menu; on a phone a tap still opens the menu, which has "Nachricht schreiben"). It shows avatar, name, handle and the game, then: for a friend a growing line (`AutoGrowTextarea`, placeholder "Nachricht schreiben …") with the emoji button and a send button; Enter sends through `Store.sendDm`, then the friends view opens with that friend (`friends.onSend` in App.tsx: `selectPeer`); for somebody with a handle who is no friend "Freundschaftsanfrage senden"; an incoming request "Freundschaftsanfrage annehmen"; a sent one, a block or a member without a directory account as a line of text; for oneself only the identity. Without a directory socket only the identity.
- *Around it:* `ContextMenu` counts the emoji picker (its own portal) as inside, so choosing an emoji no longer closes the menu it was opened from; the profile's line keeps arrows, Home/End and Tab (the menu would take them for its item order and close on Tab) and gets the focus after the menu's own.
- *Decisions made by Claude, not asked:* the phone keeps the tap for the menu (confirmed by the user on 25 September 2026); the request button closes the profile; the profile opens to the left edge of the row.
- *Checked:* web typecheck, web tests 390, root build; the three states (friend with the line, request button, no directory account) as a static page with the built CSS in headless Chrome, screenshot looked at (it caught the menu's full-width buttons squeezing the line and a wrapping placeholder, both fixed). Not checked: the real component in a browser (focus, emoji picker over the profile, sending and the switch to the friends view).

**Removing and blocking friends in the client (24 September 2026, user's wish):** "Es fehlt generell die Möglichkeit Freunde zu entfernen, wie können wir das einbauen?" The directory already knew `friends.remove` (ends the friendship for both, withdraws the presence) and `friends.block`, and `Store` had both, but the client offered them nowhere (only "withdraw" for a sent request and "unblock"). Proposed and confirmed by the user ("ja"), blocking included:
- *Where:* right-click on a friend in the home view's list (on a phone a tap on the dots, `platform.mobile`) opens "Nachricht schreiben", "Freund entfernen", "Blockieren"; the conversation's head has a dots button with the last two; a server's member list offers "Freund entfernen" next to "Nachricht schreiben" when the member is a friend (`friends.onRemove` from App.tsx).
- *Confirmation:* `friendActions.ts` (`askRemoveFriend`, `askBlockFriend`) asks first and says what follows: no messages until a new request is accepted, the history is kept; blocking also keeps their requests out and can be lifted under "Blockiert".
- *Afterwards:* `Store.removeFriend`/`blockFriend` close an open conversation with that person (`currentPeer` null), the home view shows its start page. The history stays at the directory (not deleted: that would take it from the other side too) and shows again after a new friendship.
- *Not changed:* the directory and the chat server. Checked: web typecheck, web tests 390, root build. Not checked: in a browser, against a running directory.

**Invite field on the login (19 September 2026, user's wish):** "wenn der Server eine Einladung verlangt sollte das Feld Einladungscode immer sichtbar sein und wenn man mit einem invite Link gekommen ist auch den Code in dem Feld stehen haben. der Link 'In der App öffnen' sollte dann auch ein app link direkt mit invite code sein wenn in dem Feld ein Invite Code steht". The second and third part already worked (`inviteFromUrl()` fills the field, `appLink` in `LoginScreen.tsx` follows the field); what was missing: before a failed sign-in the login could not know that the server takes members by invite only, so the field hid behind "Ich habe eine Einladung". `/api/health` now reports `inviteRequired` (`!openJoin`), `ServerConnState.inviteRequired` carries it, and the login shows the field from the start with a hint that existing members may leave it empty (`login.inviteRequiredHint`); the field no longer takes the focus unless the user or a refused sign-in asked for it. Checked: typecheck 4/4, tests, smoke 109 green on :3001 (new check: `inviteRequired` follows `openJoin`), the real login of that test server in headless Chrome: plain address = field shown and empty, app link `squorli://server/localhost`; `/invite/AbCdEf123456` = code in the field, app link `squorli://invite/localhost/AbCdEf123456`; typing a code changes the link (screenshot). Not checked: an open server in the browser (field hidden as before, covered by the smoke check of the flag only), the link actually opening the desktop app, a server older than this change.

**Password-icon placement:** following the user's request, the eye button is positioned inside the input's right edge, with reserved text padding and a 44 px click target. The input retains its own border and focus state; the control no longer occupies a separate column.

**Handle-prefix follow-up:** the account sign-in field now has a fixed decorative @ prefix, matching Directory registration. Typed/pasted leading @ characters are removed from the editable value so the prefix appears once; input allows 33 characters to accept a pasted @ plus a 32-character handle. Existing sign-in/e-mail-code calls continue sending the bare handle. Autocapitalization and spellcheck are disabled.

**Language/link follow-up:** visible Deutsch/English buttons replace the server login dropdown; the detected locale is selected, with existing browser-language/English fallback and remembered preference behavior. Changes use `store.setLocale()` to respect voice-safe reloads. "Create account" is on its own row below the login choices, with bilingual text naming the configured Directory host and explaining that it opens in a new tab and users return here to sign in.

**Follow-up correction:** the user requested eye icons on password fields and account creation at the configured Directory service. The current UI therefore has only account/local views; "Create account" opens `state.directoryUrl` in a new tab, preserving the server login/invite. The inline handle/e-mail registration state and handlers were removed from `LoginScreen.tsx`. Existing accounts without a backup retain password setup. Both password inputs use `PasswordInput.tsx` with eye/eye-off icons, translated tooltips, `aria-controls`/`aria-pressed`, 44 px targets and independent visibility that resets on clearing or unmounting. This supersedes the inline registration design below.

The user reported missing spacing and confusing registration controls after choosing "Without account". Previously, a toggle added a second panel containing both local access and optional directory registration. The server login now selects exactly one view: account sign-in, local access (or a saved verified account), or explicit registration of the current key. Registration retains required e-mail verification, pending input, retries and the password-backup step; it is also available on account-only servers, while local access remains unavailable there. No authentication API, store or protocol changes.

The active choice is indicated visually and with `aria-pressed`. Fields have persistent labels and vertical spacing, handle help distinguishes username from display name/e-mail, the invite field precedes the active form, and the primary action belongs to its panel. The public key and discard action live in collapsed details. All new copy is German/English, including desktop-specific wording where browser storage is mentioned. Existing brand assets and tokens are reused.

Verification: all four packages typechecked; 361 tests passed (53 protocol, 51 server, 239 web including six login-view regression tests, 18 desktop). Full root build passed and copied the current web bundle into `apps/server/public`. Browser rendering and real interactive login/registration were not checked because no browser is connected. No API smoke run: auth implementation and backend are unchanged.

**M6 brought forward (user's decision, 14 September 2026):** directory service so that registration works properly. **Since 14 September 2026 it lives in its own, unpublished repo `../squorli-directory`** (user's decision: the directory is not published with the chat; until then `apps/directory` in this monorepo). Operated on a **dedicated host with its own subdomain**; this repo only knows `DIRECTORY_URL`. Sync rules for the shared protocol part and the brand package: root `AGENTS.md` section 2a. The second factor is a TOTP authenticator; e-mail via SMTP (the M6c remainder) was added on 17 September 2026: confirmed address per account, notice on every key retrieval, 8-digit code by e-mail as a fallback second factor (button "Code per E-Mail senden" in the login after `totp_required` with `email: true`, `api.directoryEmailCode`). Phases: **M6a (done)** register and resolve handles, chat server shows verified handles; **M6b (done 14 September 2026)** password-encrypted key backup: create an account (handle + password) directly on the service's account page (user's wish), log in on any chat server with handle + password, change the password on the account page, backup for existing browser keys in the login; **M6c (done 14 September 2026; e-mail part 17 September 2026)** TOTP authenticator and recovery codes as second factor for key retrieval and account changes (user's decisions of 14 September 2026: factor on retrieval + password change + disabling + regenerating codes, never on chat login by key; setup only on the account page; dependency `qrcode-generator` allowed), device management = sessions per chat server with remote logout (Einstellungen > Sitzungen) plus a read-only display of key retrievals on the account page (the service has no sessions; whoever has the key has it); **M6d (done 14 September 2026)** server registration with host proof (1/2) and server directory (2/2): server rail with the own handle's servers, public listing with description/icon/member count/open join, icons served by the directory, join via link.

- **Account page of the service (`../squorli-directory/apps/directory/web/`):** deliberately without a framework (one script file, tsup), same colors as the client, German texts with umlauts, no browser dialogs. "Konto anlegen" (create account) first registers the handle and then stores the backup; if only the backup fails, the key stays in page memory and the next click repeats only the backup step; afterwards you are directly in the account area. "Anmelden" (sign in; handle + password, code if needed) fetches the seed into page memory only (never localStorage) and signs the account actions with it; "Abmelden" (sign out) nulls it. The chat client links the page in the login ("Noch kein Konto?", "Konto verwalten" with `?handle=`) and in Einstellungen > Konto and signs in with "Mit Konto anmelden": `api.directoryRestore()` fetches and decrypts, `store.loginWithHandle()` replaces the device key (with a confirmation via `askConfirm` if the previous one has a handle) and then logs in normally.
- **Second factor (M6c):** TOTP per RFC 6238 (own implementation `../squorli-directory/apps/directory/src/totp.ts`, no library) or recovery code; the service recognizes the form (6 digits = TOTP). Required, if `totp_enabled`, for `POST /api/backup/fetch`, `PUT /api/backup`, `totp-disable` and `recovery-regenerate`; **never at chat login by key** (the key is the factor). `totp_required` (401) comes only after a correct password or valid signature, so that nobody learns without the password whether an account has an authenticator; public responses (`/api/handles`, `/api/keys`) reveal nothing. Each TOTP step is valid once (`totp_last_step`, otherwise 401 `totp_reused`), window +-1 step; recovery codes are consumed atomically (`used_at`). Only failed attempts count against `BACKUP_RATE_LIMIT` per account (`RateLimiter.blocked()/hit()`), likewise only wrong passwords per IP/handle. The secret is stored AES-256-GCM-encrypted in the DB (`secrets.ts`, key `DIRECTORY_SECRET_KEY`, AAD = account ID); without the key `features.totp=false` and 503 `totp_unavailable`, recovery codes (SHA-256 only) keep working. Setup: `totp-setup` generates the secret (only with a backup, otherwise 409 `no_backup`), it becomes active only with `totp-enable` + code, which at the same time returns the 10 recovery codes (shown once). Signed account actions: `directoryActionMessage(host, action, nonce, code)`, body `SignedActionRequest`/`CodeActionRequest`; the code is part of the message so that it cannot be swapped.

**E-mail address required for new handles (18 September 2026; user's request in the directory repo, notes and decisions in `../squorli-directory/docs/features/email.md`):** a directory started with `REQUIRE_EMAIL=true` reports `features.emailRequired`; an address belongs to at most one handle there, with or without the switch. Protocol (source here, copied over): `directoryRegisterMessage(host, handle, nonce, email?)` covers the address when there is one, `DirectoryRegisterRequest` takes optional `email` + `emailCode`, 202 `DirectoryRegisterPending` = the code was mailed, `EmailAddress`/`EmailCode` moved above the registration (they are used there now). No protocol version bump: all additions are optional, older directories report `emailRequired: false` by default. Client: `store.refreshDirectory()` asks the directory's health while the key has no handle (`state.directoryEmailRequired`); the login's "Schlüssel dieses Browsers" box then shows an address field and "Code senden", after that the code field, "Handle registrieren" and "Andere Adresse" (`store.registerHandle(handle, email?, emailCode?)` returns `"done"`, `"failed"` or `{ sentTo }`). Because a taken address gets the same answer as a free one (its holder gets a notice instead), the note says "if no code arrives, the address already belongs to another handle". New error texts `dir.email_required`, `dir.email_code_invalid`, `dir.email_taken`. The chat server does not take part. This repo's smoke test registers handles without an address: run it against a directory without `REQUIRE_EMAIL`. Checked: typecheck, tests (protocol `directory.test.ts` 4 new), `pnpm build`, the login driven in headless Chrome against a test server on :3001 and a `REQUIRE_EMAIL` directory on :3101 (code mailed, wrong code inline, handle registered). Not checked: the desktop app (same build; `DesktopLogin.tsx` has no handle registration), Firefox, this repo's smoke test (server unchanged).

**Avatars (19 September 2026; user's request in the directory repo, notes and decisions in `../squorli-directory/docs/features/avatars.md`):** users upload one avatar image per handle on the directory's account page; the client shows it instead of the initials. The image is public at the directory (`GET /api/avatars/<key>?v=<version>`) and the browsers load it from there; the chat server only caches its address. Server: `users.avatar_url` (migration 0022) is filled by `DirectoryClient.refresh`/`syncAll`/`syncOne` from `DirectoryAccount.avatarUpdatedAt` via `directoryAvatarUrl(DIRECTORY_URL, ...)`; a changed address counts as a change like a new name, so the directory's push after an upload broadcasts `members`. `Member.avatarUrl` and `Me.avatarUrl` are new protocol fields with the default null (older servers and clients keep working, no `PROTOCOL_VERSION` bump). `DIRECTORY_URL` is the address the browsers use as well (it already is for registration), so the cached address works in the client. Client: `Avatar.tsx` had a reserved `src`; it is now passed everywhere an avatar is drawn: messages (by author from the member list; former members keep initials), member list and its context menu, mention suggestions, voice rows in the sidebar (voice rows carry no avatar themselves, it comes from the member list), stage tiles without video, the dock, the mini profile, the settings dialog's preview, the friends list and direct messages (`friendAvatar` in `Home.tsx` builds the address from `Friend.avatarUpdatedAt`, the own side from the directory account). An image that fails to load falls back to the initials (`onError` in `Avatar.tsx`), e.g. when the directory's avatar folder was not moved along. The desktop app's CSP already allows `img-src https:`; the server sets no CSP for the web client. Not built: one avatar per server (user: maybe later), avatars for users without a directory account. Checked: typecheck 4/4, all tests, the smoke test against a server on :3001 with a directory instance on :3101 (127 green; new: no avatar = null, an upload at the directory is pushed and the member carries the address, the address serves the image, `/api/me` carries it, removal = null again). Not checked at that point: the rendering in the logged-in client (done with the next entry).

**Avatar upload from the client (19 September 2026, user's wish, same day: "the upload should be possible from the client too"):** the settings dialog's profile tab gets a part "Profilbild" under the preview: "Bild auswählen" (hidden file input, PNG/JPEG/WebP/GIF), "Bild entfernen" while an image is stored (no confirmation, an upload undoes it), a status note and the hint that the image is stored at the directory, applies on all servers and is public like the handle. `avatarImage.ts` crops the centered square, scales to 256 px and encodes WebP/PNG/JPEG exactly like the directory's account page (each file names the other; change both together; `centeredSquare` and `toBase64` tested). `api.directorySetAvatar` signs `avatar-set` over type and SHA-256 and answers a directory without `features.avatars` with `dir.avatars_unsupported` instead of a bare 404; `store.setAvatar` translates the errors and writes the new `avatarUpdatedAt` into `state.directoryAccount` at once. The part only shows with a directory account and `state.directoryAvatars` (read with the signed status in `refreshAccountServers`, which now also refreshes the account's `avatarUpdatedAt`, so an image changed on the account page shows here). Own avatar (`myAvatarUrl` in `App.tsx`): with a directory account always from the account (it knows an upload first; the member list follows with the directory's push), otherwise from the open server. Checked: typecheck 4/4, web tests 240 (2 new), build; the built client on :3001 (fresh database, directory instance on :3101) driven in headless Chrome over the DevTools protocol: key with a handle planted as the device identity, sign-in, settings, a 720 x 700 PNG through the file input -> "Profilbild gespeichert.", preview, dock and member list show the 256 x 256 image (the member list through the directory's push), the directory serves 6 KB WebP, "Bild entfernen" brings the initials back and the address answers 404; screenshot reviewed. Not checked: the desktop app, Safari, a client without a home server.

**Avatar crop editor (22 September 2026, user's wish: "crop and zoom the picture, a simple online edit before it is uploaded"):** choosing a file no longer uploads at once. The profile tab shows `AvatarEditor.tsx` in place of the two buttons: a square frame (256 px, smaller on narrow screens) with the image and a circle showing what others see (the outside dimmed), a zoom slider between two icons, a hint and the buttons "Zuschneiden und speichern" / "Abbrechen". Drag with the mouse or a finger (pointer capture), zoom with the slider, the wheel (around the cursor; a non-passive listener, so the settings body does not scroll) or two fingers (pinch around the midpoint), or the keyboard on the focused frame (arrows move, Shift = larger steps, plus and minus zoom). The geometry is pure and tested in `avatarCrop.ts`: zoom 1 = the short side fills the frame (the centered square of before is the start), zoom up to 4, never a crop below 32 px, the crop always inside the image; `cropRect` turns the view into the square in image pixels. `avatarImage.ts` is split into `loadAvatarSource` (decode) and `renderAvatar` (cut the square out, scale to 256, encode as before); the dialog keeps the decoded bitmap while the editor is open and closes it when replaced, cancelled, saved or the dialog goes. Large photos are drawn from a copy scaled to at most 1024 px, the final image from the full bitmap. `store.setAvatar` now takes the finished image instead of a file (the dialog translates the errors). The same editor exists on the directory's account page (`avatarEditor.ts` there, a plain-DOM twin; `avatarCrop.ts` is byte-identical apart from the header; change both together; notes in `../squorli-directory/docs/features/avatars.md`). New i18n keys `profile.avatarFrame`, `profile.avatarZoom`, `profile.avatarEditHint`, `profile.avatarApply`; `profile.avatarHint` now says the crop is chosen after the file. New icons `zoom-in`, `zoom-out`, `check` (`pnpm icons` ran). Checked: web typecheck and tests (342, `avatarCrop.test.ts` 5 new, the centered-square test moved there), `pnpm build`; the real component in a harness page (esbuild bundle of the client sources with the real `styles.css`, headless Chrome over the DevTools protocol): four-colour test image, frame 256 px, four quadrants and dimmed corners in the frame, wheel zooms without scrolling the body, slider, mouse drag moves the crop as intended (pixels sampled), plus key, pinch via touch events, apply renders a 256 x 256 WebP whose pixels are the dragged corner, note "Profilbild gespeichert.", cancel, 400 px view without overflow; screenshots at 1280 and 400 px reviewed. Not checked: the logged-in client against a directory (the wiring to `store.setAvatar` and the directory route is unchanged apart from the argument), the desktop app, Safari, a real touch screen.

**A refused sign-in listed the server for the account (22 September 2026, user's report):** "ich sehe dort noch die Server auf denen mein 'normaler' Account angemeldet ist in der Serverliste", and on the directory's account page the server showed for an account that was never a member. Cause: `POST /api/auth/verify` looked the key up at the directory (with the server token, which the directory records as a sign-in on this server) before the membership decision, so a sign-in refused for want of an invite put the server on the account's list; the client then showed it in the rail and kept trying to sign in there, refreshing the entry. Fix: the membership is read first and `directory.refresh(user, member)` sends `?member=0` for a non-member (the directory then records nothing and drops an existing entry, its change of the same date in `../squorli-directory/docs/features/servers.md`); a new member gets a second lookup with `member = true` after joining; `GET /api/me` passes the membership too (a kicked user's session). Older directories ignore the parameter. Checked: typecheck, tests, build; the directory's smoke test with its new checks against a directory instance on :3101 (see the directory's `docs/VERIFIED-STATE.md`); not run: a chat server against that directory (the parameter is one string, read in `lookup()`).

**Server accounts (25 September 2026):** accounts that exist on one server only (`~name`), with the same client-side key backup as the directory, and no access any more for a key without an account (a directory handle or a server account); they also have an avatar of their own, kept by their server (the "Not built" of the avatars entry above). Friends and direct messages stay with directory accounts. Everything else: `docs/features/local-accounts.md`.

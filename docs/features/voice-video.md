# Feature notes: Voice, camera, screen share, stage

Part of the project description (entry point: root `AGENTS.md`, section 0). Dated entries: what was built, the user's wishes and decisions, consequences, and what was and was not checked. Standing rules live in the `AGENTS.md` of the area. Whoever changes the feature adds or updates its entry here in the same step.

Code: `apps/web/src/voice/`, `VoiceStage.tsx`, `VoiceDock.tsx`, `VideoWindows.tsx`, `StageWindow.tsx`, `usePushToTalk.ts`, `PushToTalkButton.tsx`, `VideoAudioControls.tsx`, `videoDisplay.ts`, `CameraPicker.tsx`, `UserVolumeControl.tsx`; server `apps/server/src/livekit/`, `apps/server/src/voice/`. Standing rules: `apps/web/src/voice/AGENTS.md`.

**Joining a second voice channel opens its stage too (24 September 2026, user's report):** "Wenn ich einem Sprachkanal beitrete (auf den Sprachkanal klicke) soll auch immer der Sprachkanal im main Bereich zu sein, das funktioniert im Moment nur beim ersten mal, danach wird bei jedem Kanalwechsel immer auf den obersten Eintrag der Kanalliste gewechselt".
- *Cause:* `VoiceClient.join()` leaves the old room first, and `leave()` set the status to "disconnected" for a moment. The effect on `voice.status` in `App.tsx` took that for an unexpected end of voice: it closed the stage just opened by `joinVoice`, sent `voice.leave` and cleared `voiceHost`, so the main area fell back to the first text channel. A join on another server went through `leaveVoice()` and hit the same effect.
- *Built:* `leave()` reports "connecting" instead of "disconnected" while `switchingRoom`; `joinVoice` no longer calls `leaveVoice()` for a channel on another server, it fetches the token, withdraws the presence at the old server (`voice.leave`) and lets `client.join()` switch the room. A token refused by the new server now leaves the old connection standing.
- *Checked:* root typecheck, tests and build. *NOT checked:* a real channel switch in a browser or the desktop app (same server and across servers).

**Push-to-talk on a phone or tablet: a hold button instead of a key (22 September 2026, user's wish):** "wenn auf Mobilgeräten Push-to-talk ausgewählt ist, ist es sinnlos eine Taste zuzuweisen. Stattdessen soll über der stage-bar ein Button angezeigt werden der das Mikrofon öffnet solange er gedrückt ist", "der Mikrofon Muten Button in der stage-bar kann in dem Fall auch ausgeblendet werden", and "auch im minimierten Sprachchat soll in dem Fall der Mikrofon Muten Button durch den Sprechen Button ersetzt werden. vielleicht finden wir noch ein andere Symbol dafür, so dass es keine Verwirrung gibt".
- *Built:* `PushToTalkButton.tsx`, shown by `VoiceStage.tsx` in a row of its own (`.stage-ptt`) above the bar when `platform.mobile` (the device, not the narrow layout: `platform/mobile.ts`) and the mode is push-to-talk. It holds the gate through `client.setPttHeld()` with pointer capture, so a finger that slides off still releases; a lost capture, the window losing focus, the page going hidden and unmounting release too. `touch-action: none`, `user-select: none` and a swallowed contextmenu keep a press from scrolling, selecting or opening the long-press menu. Enter and Space hold it from a keyboard when the button has focus. It is disabled while muted or in the AFK channel. The bar's mute button is left out in that case, except while muted: on a phone the dock is out of sight while the stage shows, so the stage keeps the red unmute button as the way back. The dock (the minimised voice chat, `VoiceDock.tsx`) puts the same button in the place of its mute button (`compact`: two columns of the controls grid, the short labels "Sprechen" and "Offen"), again with the mute button back while muted. Its icon is Lucide's `audio-lines` (sound waves) in both places and states, not a microphone, so nobody takes it for the mute button. Einstellungen > Sprache und Audio shows no key on such a device, only a text that names the button; `usePushToTalk` keeps listening for the stored key, so an iPad with a keyboard still has it (the text says so).
- *Decisions made by Claude, not confirmed by the user:* muted = the button disabled and the unmute button back in the bar and the dock, rather than a tap on the hold button unmuting; the sound waves as the icon (the user asked for another symbol, not this one); the dock's button two columns wide.
- *Checked:* root typecheck, tests (web 342) and build. The real component bundled with esbuild into a harness page with the real `styles.css`, headless Chrome at 400x800 with touch emulation over the DevTools protocol: touch down opens (`setPttHeld(true)`, "Mikrofon offen" on mint), touch up closes; a finger that slides 300 px off the button keeps it open and does not scroll, lifting there closes; a 1.2 s press stays open (no context menu took it); Space on the focused button opens and closes; disabling while held closes; a tap while disabled opens nothing. Screenshots of the idle, held and disabled states looked at. The dock's compact variant ran the same sequence in the same harness (dock and stage row on one page), screenshot looked at.
- *NOT checked:* a real iPhone or Android phone (Safari's long-press callout and the settings dialog's text were not seen on a device), the stage in a window of its own (no phone can open one).

**The viewer's statistics of a share, and the plan for a share that adapts by itself (22 September 2026, user's report and wish):** a member shares a game with H.265 and the user, watching, sees a lagging picture on every share of that member. Wish: "erstmal eine Möglichkeit einbauen, dass man als Zuschauer Statistiken zu einer Bildschirmübertragung ansehen kann und einen Plan erstellen, wie man die Auflösung automatisch anpassen kann und andere Optimierungen on demand automatisieren kann."
- *Built:* a button (activity icon) in the actions of a received share's tile on the stage and in its pop-out window shows a box in the top left corner (`VideoStatsOverlay.tsx`, `.tile-stats`, takes no clicks so the tile still enlarges; it stays in fullscreen since it lives inside the tile): codec and decoder with hardware/software, picture size, fps decoded and received, bitrate, loss in percent, round trip, jitter buffer, decode time per frame, spread of the frame spacing, freezes with their duration, dropped frames, keyframes, and one diagnosis line. Read every second from the receiver's raw `getStats()` report (`VoiceClient.videoReceiveSample(tileId)`), because LiveKit's `getReceiverStats()` leaves out freezes, the jitter buffer, decode time, the round trip and `powerEfficientDecoder`. `voice/videoStats.ts` (pure, tested): `readInboundVideo` (the inbound-rtp report, its codec, the transport's selected candidate pair or else the nominated succeeded one), `deriveVideoStats` (the interval's rates; counters that went backwards start over), `diagnose`.
- *The diagnosis is my first guess, not measured against real cases:* network for loss of 1 % and more, a jitter buffer of 200 ms and more, or a freeze with retransmits; decoder for over 25 ms per frame or a tenth of the frames dropped; sender for an H.264/H.265 share (moving pictures by the desktop app's choice) arriving under 20 fps while the path is clean; a VP8 share is never blamed on the sender, a still desktop legitimately sends few frames. Network comes before decoder before sender. Only received screen shares get the button (the own share has no inbound side; cameras were not asked for).
- *Plan:* `docs/PLAN-share-adaptation.md` (what adapts today, where a lag can come from, stages: sender-side governor over `setParameters` without a republish, simulcast or quality reports from the viewers, one-click optimisations on demand, an automatic codec switch only if the data asks for it; open decisions for the user). Open decision 8 in `docs/PLAN.md`.
- *Checked:* typecheck, tests (web 338), build; the overlay's look on a static page with the built stylesheet in headless Chrome. *Not checked:* the numbers against a real lagging share (the user is to open the box on the reported share), the pop-out window and fullscreen in a browser, Firefox's report (it lacks some of the fields; those show "?").

**A game's share: smooth motion, and H.265 on the graphics unit (21 September 2026, evening; user's report and wish):** "die Übertragung auf H.264 über den Schnellbutton ist im Moment sehr laggy und hat Artefaktbildung", then: can it be encoded on the GPU to spare resources, and would H.265 be better.
- *Cause, measured* (a test window in the app's Electron 44.4.2 / Chrome 152, RTX 4080, 1080p30 of a moving test picture over a loopback connection; `encoderImplementation` only shows in `getStats` while a `getUserMedia` capture runs): every share was captured with `contentHint: "detail"` and LiveKit's "maintain-resolution". With that hint Chromium runs OpenH264 in its mode for screen content: 13-14 frames per second at 16-17 ms per frame; with "motion" 28-30 at 1.7 ms. The 5 Mbit/s limit did the rest with fast pictures.
- *Which encoders the graphics unit gets* (same test): H.264 baseline, main and high and H.265 go to `MediaFoundationVideoEncodeAccelerator` (NVIDIA MFT, about 3.4 ms per frame); H.264 constrained baseline (all LiveKit negotiates, entry below) is OpenH264; AV1 is libaom in software, also with the feature `MediaFoundationAV1Encoding`; VP8/VP9 are libvpx. So the way to the graphics unit through LiveKit is H.265.
- *Built:* `voice/screenShareOptions.ts` (`screenSharePublish`, pure, tested) makes the publish options and the content hint from the picked codec. VP8 as before (detail, no simulcast, 5 Mbit/s). H.264 and H.265 are the user's choice for moving pictures: hint "motion" (set on the captured track before publishing; the capture still asks for "detail" because the codec is only known after the picker), `degradationPreference: "maintain-framerate"`, limit 8 Mbit/s (H.264) or 6 Mbit/s (H.265), VP8 as LiveKit's backup codec with its default policy: once a viewer cannot decode the codec, the whole share regresses to VP8 (one encoder at a time, the resource-friendly choice; whether it returns to H.265 when that viewer leaves was not checked). **The limits and the regression policy are my choice (not confirmed by the user beyond "about 8 Mbit/s").**
- *Found on the way:* for a track of the source screen share LiveKit reads the limit from `screenShareEncoding` only; the `videoEncoding` the share passed until now was ignored (the room's `publishDefaults.screenShareEncoding` applied, the same 5 Mbit/s, so nothing was wrong before). The options now carry `screenShareEncoding`; a test pins that.
- *Which codec:* `ScreenCodec` is `"vp8" | "h264" | "h265"`. `movingCodec(h265)` (`screenPick.ts`): H.265 where `VoiceClient.supportsH265()` says yes (LiveKit's `supportsH265()`: the sender's capabilities; Chromium ships no software H.265 encoder, so the codec is on offer only with one in the graphics unit), else H.264. "Quick Share" and the dialog's preselection for a game's window use it; the dialog's second radio button is "H.265" or "H.264" by computer, each with its hint (H.265: encoded by this computer's graphics unit, less bandwidth, falls back to VP8 for everyone when a viewer cannot play it). The dock's tooltip no longer names a codec. The shell is unchanged (`codec` stays in the client).
- *Seeing it (user's question the same evening):* the debug view's rows for sent video name the codec really sent and its encoder, read from the raw reports of every sender of the track, the backup codec's included (LiveKit's `getSenderStats()` knows neither); a share that fell back shows the idle H.265 row next to the VP8 one. Typecheck, tests and build ran; the rows were not looked at in the running app.
- *Checked:* typecheck, tests, build. A harness with the real `livekit-client` and the real `screenSharePublish` in Electron against the dev LiveKit (`livekit-server:latest`): H.265 is negotiated, sent by the NVIDIA HEVC encoder at 1080p30 and 6 Mbit/s (`qualityLimitationReason: none`), hint "motion" and "maintain-framerate" in the sender's parameters, a second participant decodes it in hardware; a third one started with `--disable-gpu` (no H.265 among its codecs, as in Firefox) gets VP8, the H.265 sender goes inactive and the first viewer moves to VP8 too. H.264: OpenH264 at 30 frames per second, where the (noise-like, worst case) test picture is scaled down to stay within 8 Mbit/s, as "maintain-framerate" asks. *Not checked:* a real game in the running app, the dialog's new text on screen, a real Firefox or Safari viewer, AMD and Intel graphics units (which generations encode H.265 is from the vendors' lists, not measured: NVIDIA from GTX 900, AMD from RX 400, Intel from the 6th Core generation), a packaged app.

**The desktop app's share dialog: no desktop widgets, two tabs, a window's audio ticked, codec choice, "Quick Share" (21 September 2026, user's wishes):** "beim Teilen von Fenstern werden unnötige RainMeter Widgets mit angeboten", the dialog in two tabs (windows first, then the entire screen), a single window's audio ticked from the start unless it is Squorli itself (whose audio can never go along), a "Quick Share" button in the voice channel's quick controls while a game is detected that always sends with H.264, and in the dialog the choice between the current codec (preselected) and H.264 with the chosen one's advantage written next to it, H.264 preselected for games.
- *Widgets:* `desktopCapturer` offered every Rainmeter skin. The shell cannot ask Windows about a window, so the system watch helper got a question (`windows<TAB><request><TAB><hwnd>...` on stdin, answered with one `window` line per window: tool window or not, window class, the program's path, then `windows<TAB><request>`; `SystemWatch.describeWindows`, 700 ms, no answer = the list as before). `isDesktopWidget` (`captureSource.ts`, tested) leaves out tool windows (what the task bar and Alt+Tab leave out) and, by class and program, Rainmeter. Measured on the user's computer: all 14 Rainmeter skins are tool windows of class `RainmeterMeterWindow`; besides them only Explorer's `Progman` is one. **The tool window rule is my choice (not confirmed by the user)**; the user asked about Rainmeter only.
- *Games in the list:* the same answer's path goes through the game detection (`GameLookup.gameOfProgram`, null while detection is off), `ScreenSource.gameId` marks a detected game's windows (a gamepad next to the name).
- *Dialog* (`ScreenPicker.tsx`, rules in `screenPick.ts`, tested): tabs "Fenster" and "Gesamter Bildschirm"; the audio tick and the codec follow the chosen source until the user touches them (`defaultAudio`: a window with audio yes, a screen no, as before; `defaultCodec`: a game's window H.264, everything else VP8); changing the tab resets the audio tick, not the codec. The app's own windows had no audio before (`audioFor` in `displayMedia.ts`); their tick is off and disabled.
- *Order of the windows (user's wish, same day: "Spiele > Vollbild Anwendung > alles andere Alphabetisch"):* `sortWindows` (`screenPick.ts`, tested): detected games, then windows in full screen, then the rest, by name within each (the dialog's language, case and accents ignored). Full screen comes from the helper's answer (a seventh field; `WindowInfo.fullscreen`, `ScreenSource.fullscreen`): the window covers its monitor and is not maximized (a maximized window covers it too while the task bar hides itself). The shell's list keeps the z-order, which "Quick Share" uses. Wording of the audio ticks the same day (user's wish): "Ton dieses Fensters teilen", and by my choice also "Systemton teilen". Checked in the unpackaged app: a game running in full screen and the test app's own window after `requestFullscreen()` are marked, maximized windows are not.
- *Codec:* `ScreenPick.codec` stays in the client: `desktop.ts` remembers it and answers `screenSharePublishOverrides()` with `{ videoCodec: "h264" }` or null. The picker runs inside the capture, so `VoiceClient.setScreenShareEnabled(true)` no longer calls LiveKit's method of that name (which takes the publish options before the capture) but `createScreenTracks()` and then `publishTrack()` per track, asking the platform in between; `screenStarting` keeps a second click from opening a second picker. Stopping is LiveKit's `setScreenShareEnabled(false)` as before. A browser has no such dialog and stays on VP8.
- *Quick Share* (`VoiceDock.tsx`, gamepad button between camera and hang up; shown with `STREAM_VIDEO`, connected, no share running, and `GameDetection.state.running`, hidden from others or not): App.tsx notes the game's id, starts the share and answers the shell's request with `quickSharePick` (the game's topmost window, its audio, always H.264) instead of opening the dialog; when that window is not on offer (minimized windows are not listed) the dialog opens.
- **Measured, and it limits what H.264 is good for:** LiveKit's client prefers H.264's constrained baseline profile (`profile-level-id=42e01f`) for compatibility and the LiveKit server (1.13 dev container) negotiates nothing else, also when baseline, main or high are offered first. Chromium (the unpackaged app, RTX 4080) encodes exactly that profile in software (`OpenH264`); baseline, main and high go to the graphics card (`MediaFoundationVideoEncodeAccelerator (NVIDIA H.264 Encoder MFT)`), measured per profile over a loopback connection; the feature `MediaFoundationH264CbpEncoding` changes nothing. So a share with H.264 does NOT encode on the graphics card today, and the dialog's text does not claim it: it names what holds (viewers decode H.264 in hardware nearly everywhere; VP8 plays everywhere without extra modules). Open: whether the server can be made to accept another profile, and whether viewers' browsers (Firefox) then still decode it.
- *Checked:* typecheck, tests, build; the helper against the real windows; the unpackaged app over the DevTools protocol: the source list without Rainmeter, `gameId` on the windows of a program added as a game, no audio for the own window; the real dialog in headless Chrome with that list (tabs, ticks, codec defaults, the pick; screenshot); the real `VoiceClient` in the app against the dev LiveKit: Quick Share sends `video/H264` plus the window's audio track, a plain window `video/VP8` without. *Not checked:* the button in the running app with a real game, a second client watching an H.264 share (Firefox, Safari), a packaged app, Linux (no helper: nothing is filtered, no game marks).

**No channel could be joined from the iPhone's home screen app (19 September 2026, user's report):** "wenn ich die app über iPhone Safari auf den Homebildschirm lege (als PWA), kann ich keinem Kanal beitreten", error "Mikrofon: The request is not allowed by the user agent or the platform in the current context", and "ich wurde nie nach einer Mikrofonfreigabe gefragt".
- *Cause (from WebKit's rule and the missing prompt, not reproduced on a device):* WebKit shows the microphone prompt only while the tap counts; `join()` asked after the token and the LiveKit connection. Safari had the site's permission stored and needed no prompt, the home screen app has its own, empty storage and has to ask.
- *Fix:* `VoiceClient.prepareMic()` asks inside the tap (`App.joinVoice()`, before the first `await`, not for the AFK channel); `join()` takes that capture over or releases it; a failed token or connection releases it. Consequence (decided by Claude): on every browser the permission prompt now comes with the click and no longer after a successful connection, so a connection error can cost a prompt.
- *Checked:* typecheck, tests, build. The real `VoiceClient` against the dev LiveKit in headless Chrome with a counted `getUserMedia`: prepared and joined 1.5 s later = one request, the capture is published and live, ended after leaving; prepared and released = track ended; prepared for stereo and joined in mono = released, the join opens its own, no error.
- *Second report the same day, after the fix:* "neue Fehlermeldung: The request is not allowed by the user agent or the platform in current context, possibly because the user denied permissions", now without the "Mikrofon:" prefix: the mobile join sheet showed the bare error and the dock the explained one, so it is the same refusal, read in another place. Whether the home screen app ran the new client (iOS keeps a web app's old page in memory) and whether a prompt appeared is not known. Done: the microphone request is now the very first thing in the tap, before the audio context and the silent sound; a refusal is explained from the browser's permission state (`voice/micPermission.ts`: "denied" = stored refusal with the way to the iPhone's setting, "prompt" = refused without asking, otherwise the system keeps it back), so the next report names the case; the sheet shows that text. Checked in headless Chrome without the fake permission UI: state "denied", the sheet's text is the explained one. The cause on the iPhone is still open.
- *Third report the same day:* the permission prompt now appears in the home screen app and the others are heard, but "ich sehe beim iPhone Mikrofon jetzt gar keinen Pegel mehr". Cause as read from the code (not reproduced): iOS puts the AudioContext into the state "interrupted" when its prompt shows and the capture starts, and the client resumed only "suspended" (pipeline, `prepareAudio()`, the gesture fallback, the cues), so the microphone ran through a standing context. In Safari with a stored permission no prompt appeared, which is why it had worked there. Fixed with `contextNeedsResume()` (`gate.ts`) in all those places plus a resume from the context's `onstatechange` while a microphone runs. If the level stays at 0: the debug view's "Audio" line names the context's state, and "Eingang" says whether the capture itself delivers anything (a capture muted by iOS would be the next suspect).
- *Fourth and fifth report the same day, stereo channel:* "ein Benutzer sagt, wenn ich mit meinem iPhone im Kanal (Stereo Kanal) bin, hört er mich nur auf einem Ohr" and "außerdem hören sich die Benutzer doppelt, wenn ich mein iPhone mit Lautsprecher verwende". One cause: a stereo channel captures two raw channels without echo cancellation. Fixed twice. (1) Phones and tablets never capture raw stereo (`VoiceClient.capturesStereo()`): they get the browser's echo cancellation, noise suppression and gain control in every channel, the channel's Opus settings stay; decided by Claude, not confirmed by the user (a phone has one microphone next to its loudspeaker, raw stereo gains nothing there). Side effect: the quiet raw level on the iPhone in stereo channels is gone as well, because the system's gain control works again. (2) For computers, where an audio interface with the microphone on input 1 gives the same one-ear effect: `voice/oneSided.ts` finds a stereo capture with the signal on one channel only and `micPipeline.ts` puts that channel on both sides, and takes it back when the second channel comes alive. Checked in headless Chrome with the real `MicPipeline` and a synthetic two-channel capture: tone left only = output left only at first, on both sides at equal level after half a second (verdict "left"), tone on both = verdict "stereo" again. The echo on the iPhone in a MONO channel was not reported and not checked.
- *NOT checked:* the iPhone itself, in Safari or from the home screen. The client has no web app manifest and no Apple home screen tags; iOS runs it as a web app anyway, with storage of its own (another identity than in Safari unless signed in through the directory account).

**Camera picked in the dialog was ignored; phones and tablets recognised (19 September 2026, user's report and wish):** "auf meinem iPhone erreiche ich den Pegel nie, generell sollten wir mobilgeräte besser erkennen und dann auch die dazugehörigen Einstellungen anpassen (Video, Audio). Generell gibt es einen Bug bei der Kamera Auswahl. Wenn ich eine Standardkamera ausgewählt habe wird immer diese genommen, auch wenn ich in dem Dialog eine andere Kamera wähle."
- *Camera bug, cause:* a camera switched off is a muted track that stays published, and LiveKit's `setCameraEnabled(true, options)` (livekit-client 2.22.3, read in its source: `if (track) await track.unmute()`) then ignores the options and reopens the track with the constraints it was created with. So the first camera of a connection stuck until the user left the channel; the preselected "Standardkamera" is simply the one that gets opened first. A changed send resolution while the camera ran was ignored the same way.
- *Camera bug, fix:* `voice/cameraSwitch.ts` decides from what the published track was opened with: same camera and resolution = unmute as before (same publication, so the receivers' "camera off" choice stays); another one while off = unpublish and open fresh (one camera open, which matters on Firefox; the receivers see a new feed, a camera they had turned off shows again); another one while running = `restartTrack()` on the same publication.
- *Mobile devices:* `platform/mobile.ts` tells a phone or tablet by the device (not by the narrow layout), `Platform.mobile` / `PlatformMedia.mobile`. Audio: the boost gets the limits x40 and speech from a raw level of 0.0006 there (a computer: x12 and 0.003). The assumed cause of the iPhone report: iOS delivers its voice-processed capture so low that speech stayed under the 0.003 the automatic takes for the quietest speech, so it never started, and x12 would not have reached the threshold of 0.04 anyway. The hand slider goes to 4000 % there. Video: 360p as the send resolution, applied once per device also to stored settings (`withDeviceDefaults`, `VoiceSettings.mobileDefaults`; the settings say so, and 720p chosen afterwards stays); the own rear camera is not mirrored in the picker's preview, the stage and pop-outs; no screen share button where the browser has no `getDisplayMedia`. Debug view: the raw level in front of the boost ("Eingang").
- *Decisions made by Claude, not confirmed by the user:* the phone limits (x40, 0.0006) and 360p as the phone's default; the voice activation threshold and push-to-talk are unchanged on phones (a phone has no key for it, but an iPad with a keyboard does).
- *Checked:* typecheck, web tests 259 (`cameraSwitch.test.ts` 5, `mobile.test.ts` 5, 3 new in `micBoost.test.ts`, 1 each in `settings.test.ts` and `videoDisplay.test.ts`), build. The real `VoiceClient` against the dev LiveKit in headless Chrome with two fake cameras (`--use-fake-device-for-media-stream=device-count=2`, own room, token minted in the script): on with A, off, on with B opens B on a new publication (the bug), off and on with B keeps the publication, 360p while running gives a 360 px track on the same publication, A while running switches the device on the same publication, no error.
- *NOT checked:* anything on a real iPhone or Android phone. Whether the boost now reaches the threshold there is open; if not, the debug view's "Eingang" while speaking is the number needed (below 0.0006 the automatic still does not start, and then the capture itself is the problem, not the limits). Firefox with the republish path (its late camera release is covered by `retryCameraBusy` as before, but was not run).

**Watching video is each user's choice; hide participants without video (19 September 2026, user's requirement):** "wenn ein Desktop Share gestartet ist sollen nicht automatisch alle Clients ihn anzeigen, man soll ihn für sich aktivieren können. genauso soll man auch das Ansehen des Streams für sich ausschalten können. man soll generell die Möglichkeit haben Videoübertragungen einzeln für sich abschalten zu können, Webcam soll aber automatisch aktiviert werden. Es soll die Möglichkeit geben in der Kachelansicht Benutzer die kein Video übertragen ausblenden zu können, das aber nur wenn mindestens eine Videoübertragung aktiv ist. Wenn keine Videoübertragung aktiv ist werden immer alle Kacheln angezeigt. Die Einstellung soll nicht gespeichert werden sondern muss wenn dann immer neu gemacht werden."
- *Core:* `voice/videoWatch.ts` (`VideoWatch`: camera watched by default, screen not; choice per feed id, forgotten with the feed, the participant, the connection) and `VoiceClient.setVideoWatching()`; `applyVideoSubscriptions()` subscribes a feed only with `VIEW_VIDEO` and when watched, the share's audio with its picture; `refreshTiles()` drops an unwatched feed's tile at once. LiveKit still auto-subscribes a new publication for a moment before `TrackPublished` takes it back (same as for `VIEW_VIDEO`); not chosen: `autoSubscribe: false`, which would make the microphones our job too.
- *Stage (`VoiceStage.tsx`):* an unwatched share is an item without a tile (`off`): "<Name> teilt den Bildschirm" with "Ansehen", the whole tile is the switch, it never takes the focus of the speaker view and has no audio (no tile, so nobody can select it). Watched remote tiles get an eye-off button in the hover actions (leaves fullscreen first); a camera turned off shows the avatar, an eye-off mark in the label and an eye button; the right-click menu of a member offers the same for both feeds. A pop-out of a feed closes when it is turned off (its tile is gone, `useVideoWindows`).
- *Hide participants without video:* head button next to "Sprecher | Kacheln", only in the tile view and only while `videoActive()` (any camera or screen in the channel, or the radio's player tile); it hides camera items of participants whose camera is off (their share tile, watched or not, stays, and so does a camera the user turned off for themselves: that person does send video). Component state of the stage: reset when the last video ends, gone with the stage (switching to a text channel, moving the stage to its own window); never stored, by the user's word. Decision made here: the speaker view is not filtered (the wish names the tile view).
- *Fixed the same day (user's report: the share must get a tile of its own without showing the video yet, as cameras do):* the offer tile hangs on `VoiceParticipant.screenOn`, and the participant list was not refreshed on `TrackPublished` / `TrackUnpublished` (before, subscribing produced the tile); a share started while the viewer was already in the channel got its tile only with the next unrelated event (somebody speaking). Both events now call `refreshParticipants()`. End to end: a share started and ended during the connection is seen within 52 ms / at once, still unsubscribed. The first check had only covered a share that ran before the viewer joined.
- *Confirmed by the user with real devices (19 September 2026):* "Ansehen" brings picture and sound, a pop-out closes when its feed is turned off, the hide button appears only while a video runs, all tiles are back after the last video. Still without an answer: the offer tile appearing at once for a share started while the viewer is in the channel, and eye button off / restarted share off again.
- *Checked:* typecheck 4/4, web tests 233 (`videoWatch.test.ts` 6 new), build. End to end against the dev LiveKit in headless Chrome (own room, tokens minted in the script; sender = plain LiveKit room with the fake camera and a canvas as the share, viewer = the real `VoiceClient`): after joining camera tile and subscribed, share neither desired nor subscribed while `screenOn` is true; turned on -> tile and subscribed; turned off -> gone and unsubscribed; camera off/on likewise; share turned on, ended and started again -> off again. The stage rendered with the built CSS (screenshots: offer tile, eye-off mark, all tiles vs. only video). Not checked: real screens and cameras with two people, the share's audio with a watched share (path unchanged), pop-out closing on turn-off, Firefox/Safari, the desktop shell, the stage in its own window.

**Channel list after a server restart (18 September 2026, user's report):** "wenn der Server neu gestartet wird und noch Leute im Sprachkanal sind (livekit wird nicht neu gestartet) werden die Clients in der Kanalliste nicht mehr angezeigt. im Kanal sieht man aber die verbundenen Clients korrekt." The list is the server's `VoicePresence` (kept per WebSocket, in memory), the stage is LiveKit: voice outlives the chat server's process and every reconnect of the chat socket, the presence did not come back. Two cures, because desktop apps up to 0.1.2 carry the old client: (1) the client says its channel again after every reconnect (`ServerConnection.voiceChannelId`, see `docs/features/afk.md`, same day); (2) the server asks LiveKit at every hello (`LivekitAdmin.roomsByIdentity`, one listing shared for 3 s because after a restart everybody returns at once; identity = user id, room = channel id) and puts a member who still sits in a room back into the list, marked `restored`, unless a connection of theirs already has an entry. A restored entry is a guess about which of the user's connections is in voice: it gives way when any of their connections says `voice.join` or `voice.leave` (`dropRestored`), and the heartbeat (30 s) drops it once LiveKit no longer lists the participant (a page reloaded while in voice is listed for a few more seconds). LiveKit unreachable = nothing is restored and nothing dropped. **No sound on that (user, same day: "Im Falle, dass die Clients schon im livekit waren sollen sie auch keinen Beitritts-Sound triggern"):** nothing to change, the cues come from LiveKit's `ParticipantConnected`/`ParticipantDisconnected` and the own join/leave (`voiceClient.ts`), never from `voice.state`; keep it that way. Checked with a test server on :3001 restarted under a headless Chrome sitting in a voice channel: today's client back in the list after the restart (second `voice.join` sent); a client whose voice events were swallowed (= an old one) back in the list through LiveKit; that client hanging up without its `voice.leave` arriving: entry gone after 11 s; cue tones started: 2 at the join, none at either restart, 2 at the hang-up; no console errors. Not checked: several members, two connections of one user, the production server behind its proxy.

**The whole stage in a window of its own (18 September 2026, user's wish: "man soll auch den gesamten Bühnenbereich auspoppen können"):** a button in the stage's head (`stage.popoutStage`, next to "Sprecher | Kacheln") moves the stage into its own window, e.g. onto a second monitor, while the main window keeps showing the text channel; the same button there (`stage.restoreStage`) or closing the window brings it back. Same technique as the video pop-outs: `openPopoutWindow()` (`VideoWindows.tsx`, now shared) opens an empty window with the client's styles and `StageWindow.tsx` (`useStageWindow`, at App level) renders the SAME `<VoiceStage>` into it through a portal, so there is one client and one voice connection. `App.tsx` builds the stage from the VOICE connection's server (`voiceServer`, `voiceApi`), not from the server on screen: the window survives switching channels, servers and the home view. While it is open `showStage` is false, the dock's "show the stage" button focuses the window instead, and leaving the voice channel or closing the main window closes it. What rendering into another document needed:
- *Menus:* `ContextMenu.tsx` lives in the window of its trigger (`anchor.trigger.ownerDocument`): portal target, size, listeners, `ResizeObserver`. Used by the tile's volume menu and the radio menu.
- *Observers:* `useFittedGrid` (VoiceStage) and `useFits` (RadioControl) take the `ResizeObserver` of the element's own window, like `PopoutElementInfo` already did.
- *Windows opened from that window:* a tile's pop-out passes the window of the click (`onPopout(tile, opener)`); a browser lets only the window with the user's gesture open one.
- *Dialogs:* the camera picker and the desktop app's screen picker are shown in the stage's window when it had the focus at the moment they were asked for (`App.tsx` `pickWindow`, `stageWindow.focusedWindow()`); both take a `win` for their key listener. The window going away cancels an open dialog.
- *Push-to-talk and AFK input:* `usePushToTalk(win, ...)` (taken out of `VoiceDock.tsx`) listens in the main window and in the stage's window; `isTypingTarget` is duck-typed because an element of another window is no `HTMLElement` of this one (tested). `watchActivity` counts input there.
- *Web radio player (Twitch/YouTube):* it is ONE iframe of the main window and moving it would reload it, so it stays there (floating or in its own window); the player tile of the detached stage only says so (`stage.playerInMain`) and registers no `EmbedSlot`.
**Checked (headless Chrome 1400x900 against a test server on :3001 with LiveKit, real mouse events over the DevTools protocol):** the window opens with title "<channel> | Squorli", the camera tile plays there (1280x720, running), the main window shows the text channel; resizing the window refits the grid (643 px -> 494 px); mute from the window reaches the dock; a tile's pop-out opens from the stage's window (third window); a screen share starts from a click in the stage's window; "back" closes the window and the main stage returns; leaving the channel closes the window; no console errors in either window. **Desktop shell (Electron, dev start):** the window opens (window-open handler allows `about:blank`), 1264x761, opaque background also with a window material (the pop-out has no `data-material`), the screen picker comes up IN the stage's window and the share starts from there; back works. **Not checked:** Firefox, push-to-talk in the stage's window by hand, the camera picker in the stage's window with several cameras, a second participant.

**A share's audio plays only for who watches that share (18 September 2026, user's requirement after trying window audio in the desktop app: "er sollte aber am besten nur hörbar sein wenn man das Video selektiert oder auspoppt, es soll nicht jeder im Kanal gezwungen sein den Ton mit zu hören"):** until now everybody in the channel heard a share's audio as soon as it was published. Now `VoiceClient.setScreenAudioListening(tileId, reason, on)` decides: the audio element of a `ScreenShareAudio` track is muted unless somebody listens, and the publication is disabled at LiveKit meanwhile (`setEnabled(false)`, no bandwidth). Who listens: the stage while the user has clicked that share large (`pinned`, not a share that merely moved into focus by itself; leaving the stage ends it) and a pop-out window of that share (`VideoWindows.tsx`). Deafen still wins. The tile's label shows a crossed speaker with "zum Anhören anklicken oder auspoppen" while silent. Applies to browser and app. Tests: `videoAudio.test.ts` (the rule with two listeners and deafen; the pop-out test now listens like the window does). **Fullscreen counts as selected too (user's decision, 18 September 2026):** a share in fullscreen plays its audio, also when it was a small tile (`Tile` in `VoiceStage.tsx` listens to `fullscreenchange`, reason "fullscreen"). Not checked with a real second participant.

**Screen share codec: AV1 to be tested later, VP8 stays the default (18 September 2026; since 21 September 2026 the desktop app's dialog offers H.264 next to it, entry above; user's decision: "Vorerst für die Web App nicht umstellen"):** the user asked what speaks against AV1 with VP9 as the backup, assuming modern browsers. Nothing was changed; camera and screen share stay on VP8 (`videoCodec: "vp8"` in the room's `publishDefaults`). **To do at a later time:** try AV1 for the screen share only (`videoCodec` in the publish options of `setScreenShareEnabled`, camera stays VP8 with simulcast) and compare with VP8 in the debug view (`?debug`: resolution, fps, kbit/s, `limitation`), once with text and scrolling, once with video or a game, on a weak and a strong sender; keep it (possibly as an option) only if 1080p holds at a reasonable CPU load. Known from reading `livekit-client` 2.22.3, not measured: (1) the backup codec can only be VP8 or H.264 (`backupVideoCodecs`), so "AV1 with VP9 backup" does not exist; the realistic pair is AV1 (or VP9) with a VP8 backup. (2) A backup is needed: Safari decodes AV1 only with a hardware decoder (newer Apple devices). (3) For SVC codecs (AV1, VP9) the SDK publishes a screen share with `scalabilityMode: "L1T3"` and overwrites our `contentHint: "detail"` with `"motion"` (its workaround for Chrome's 5 fps limit and encoder bugs on the screen share path), so under tight bandwidth the resolution may drop instead of the frame rate, which is what we avoided for readable text. (4) AV1 is encoded in software almost everywhere; as soon as one viewer cannot decode it the sender encodes and uploads VP8 in addition. In favour: clearly sharper text at the same bitrate, or roughly 30-50 % less bitrate, which helps senders with a weak upload. The installed Chrome 153 reports AV1 and VP9 for sending and receiving. Tracked as open decision 7 in `docs/PLAN.md`.

**The own tab can be shared (18 September 2026, user's wish: "das eigene Fenster soll auch übertragbar sein"):** the screen picker hid the Squorli tab (`selfBrowserSurface: "exclude"`); it is `"include"` now, in `voiceClient.setScreenShareEnabled()` and on the test page `/test/screenshare.html`. Consequence handled in the same step: the share always asks for audio, and the own tab's audio is everybody else's voice (and the radio), which would come back to them as screen audio. The audio constraint `restrictOwnAudio: true` removes the capturing tab's playback from the captured audio; it also covers "entire screen" with system audio, where the same echo existed before. Chromium 141+ knows it, other browsers ignore it (they deliver no display audio anyway); LiveKit passes the `audio` object to `getDisplayMedia` unchanged (`AudioCaptureOptions` lacks the key, hence the cast). Known and accepted: sharing the tab that shows its own share gives the picture-in-picture tunnel. Checked: the installed Chrome (153, headless) reports `restrictOwnAudio` in `getSupportedConstraints()`; typecheck, tests, build. Not checked: a real share of the own tab with a second listener (needs the picker and two people).

**"Camera: Starting videoinput failed" on Firefox (18 September 2026, user's report, same Firefox user as the pop-out freeze):** that is Firefox's `AbortError` from `getUserMedia` when the device cannot be started. Firefox releases a stopped camera asynchronously and, unlike Chrome, cannot share it with another process; the client ran into that twice: the camera picker's preview track was stopped only in the effect cleanup when React unmounted the dialog, after `onPick` had already asked LiveKit for the same camera (the comment claimed the reverse; `setTrack(null)` released nothing); and off -> on is a mute (stops the track) followed by `restartTrack()` (opens it again). Now `CameraPicker.pick()` releases the preview synchronously (`releasePreview()`) before `onPick`, and `voice/cameraRetry.ts` (`retryCameraBusy`, tested) retries one opening after 600 ms when the error is `AbortError`/`NotReadableError`, used by `setCameraEnabled(true)` and the picker preview. A camera held by another program fails the retry too; `setCameraEnabled` then shows `voice.errCameraBusy` (close other programs with camera access) instead of the bare message. `AbortError` no longer counts as a user cancel for the camera (there is no browser picker to cancel; that reading swallowed device failures silently); `NotAllowedError` still does. Not checked: a real Firefox with a camera (none on the dev machine; the mechanism is documented Firefox behaviour, the sequencing bug is visible in the code).

**Camera froze in a pop-out on Firefox (18 September 2026, user's report: "wenn auf Firefox die Cam-Übertragung ausgepoppt wird, friert die Kamera in dem neuen Fenster ein"):** LiveKit's adaptiveStream watches every attached `<video>` with ONE `IntersectionObserver` created in the main window (`root: null`, `HTMLElementInfo`). Firefox never reports an element of another window as intersecting (Gecko's implicit root is the observer's own top-level document; Chrome measures against the element's document), so LiveKit took the pop-out's view for invisible, paused the track 100 ms after attaching, and the window kept the first frames. Fix: `PopoutElementInfo` in `videoDisplay.ts` implements LiveKit's exported `ElementInfo` for a view in another window (`visible` always true, the window shows nothing else; size from the element; a `ResizeObserver` of the pop-out's own window so the simulcast layer follows the window's size) and `attachVideoView()` registers it through `track.observeElementInfo()` before `attach()` (LiveKit then skips its own info for the element; `detach()` stops it). Remote tracks only; local tracks have no adaptiveStream. Verified with a Firefox nightly zip (158, unpacked into the scratchpad, headless, popups allowed through `user.js`) and headless Chrome, first with a plain probe (Firefox: main-window observer `isIntersecting: false`, the pop-out's own observer `true`; a main-realm `MediaStream` plays fine in the pop-out), then with a real `RemoteVideoTrack` (canvas stream, `adaptiveStream` on) bundled with esbuild: `track.attach()` -> visibility `true, false` in Firefox (paused), `attachVideoView()` -> stays `true`; Chrome `true` both ways. Not checked: a real LiveKit session in Firefox, release Firefox (the mechanism is old Gecko behaviour, not a nightly detail).

**Tile stayed black after somebody turned their camera off (18 September 2026, user's report: "wenn ein Benutzer seine Cam-Übertragung beendet, passiert es häufig, dass die Kachel schwarz bleibt"):** LiveKit's `setCameraEnabled(false)` does not unpublish the camera, it MUTES the track (it stays published; only screen shares are unpublished). Receivers get `TrackMuted`, and the client answered that only with `refreshParticipants()`, never `refreshTiles()`, although `refreshTiles()` already skipped muted cameras (`!cam.isMuted`). So the tile stayed, showing black, until any other event rebuilt the tiles (a stream-state change, someone joining), which is why it happened "often" and not always. Now `TrackMuted`/`TrackUnmuted` also call `refreshTiles()`; turning the camera back on shows the tile again for the same reason. Own tile was fine (`setCameraEnabled` refreshes explicitly). Checked: typecheck, tests, build; not checked in a real session.

**Joining failed with "Constraints could not be satisfied" and a proxy hint (18 September 2026, user's report from community.k9-kommando.de):** the message is the browser's `OverconstrainedError` from `getUserMedia`, not a connection error: the microphone stored under Einstellungen > Audiogeräte is requested with `deviceId: { exact }`, and the device was gone (unplugged, or the browser handed out new device ids). The join caught every error in one place and wrapped it in `voice.connErrGeneric` ("check /rtc/validate ... the proxy does not forward /rtc"), which sent the operator looking at a proxy that was fine (`/rtc/validate` returned 401 when checked). Now: `openMic()` in `micPipeline.ts` retries without the device when the chosen one is gone (`OverconstrainedError`/`NotFoundError`, recognised by `name` because Chromium's is no `Error` instance), so the default microphone takes over; the stored choice is kept, so the device is used again once it is back, and the settings show the device actually in use (`inputDeviceId` = `activeDeviceId()`); the debug log notes the fallback. This covers the join, the switch in the settings and the republish after a profile change. A microphone that still fails (no permission, no device at all) is reported as `voice.errMic` without the proxy hint; `setInputDevice()` reports it the same way instead of an unhandled rejection; `errorText()` falls back to the error's name when it has no message. The join still ends when the microphone fails (listening without a microphone was not asked for). Checked: `micPipeline.test.ts` (4, fake `getUserMedia`), typecheck, tests, build. Not checked: a real browser with an unplugged device.

**Screen share froze in the pop-out or in fullscreen (17 September 2026, user's report: "manchmal bleibt ein Screenshare hängen, wenn er ausgepoppt oder im Vollscreen ist"):** LiveKit's adaptiveStream pauses EVERY received video five seconds after the MAIN page is hidden (`pauseVideoInBackground`, default on; it only knows `document.visibilityState` of the page that loaded it, a video element in a pop-out window does not count, only Picture-in-Picture does). Hidden is also a main window that is minimized or, on Windows, completely covered by other windows, which is exactly how a pop-out on a second monitor gets used. Now `adaptiveStream: { pauseVideoInBackground: false }` (`voiceClient.ts`) and **each video view decides for its own window**: `TrackVideo` (`VideoWindows.tsx`) detaches its element once its `ownerDocument` has been hidden for 5 s and attaches it again when visible (`videoDisplay.ts` `watchDocumentHidden`, tested); a track without an attached element is paused by adaptiveStream as before, so a hidden tab without pop-outs still receives no video. Verified in headless Chrome over the DevTools protocol with a real `RemoteVideoTrack` (canvas stream) shown in the page and in a pop-out, main window minimized: before, both tracks turned invisible after about 6 s; now only the page's view does and comes back with the window, the pop-out's stays visible throughout. Not checked: a real LiveKit session, Windows' occlusion of a covered (not minimized) window.

**Playback volume per person, 0-200 % (17 September 2026, user's wish):** right-click on a member in a voice channel (sidebar), on their tile in the stage or the member list's menu shows "Lautstärke für mich" with a slider, the percentage and a reset to 100 % (`UserVolumeControl.tsx`); not for yourself. Local playback only, nothing is sent. Stored per device in `chat.userVolumes.v1`, keyed by the person's **public key** so it holds on every server (`voice/userVolumes.ts`, tested; only real adjustments are stored); deliberately not in the directory account, because it depends on the headset in front of this browser and is a map without bound. `VoiceClient.getUserVolume/setUserVolume(publicKey)` also work without a connection; `setPeerKeys` / `JoinOptions.peerKeys` map LiveKit identities to keys (`App.tsx`), bots fall back to `id:<identity>`. Up to 100 % the `<audio>` element's volume does it; above, a `GainNode` on the speaker meter's `MediaStreamAudioSourceNode` plays into the shared `AudioContext` while the element stays attached at volume 0 (Chromium feeds a remote stream into Web Audio only while an element plays it). Consequences: deafening sets that gain to 0 (`setDeafened` -> `applyUserVolumes`), a context that is not running falls back to 100 % instead of silence (re-applied on `statechange`), and the boosted voice follows the voice output device only where `AudioContext.setSinkId` exists (Chromium; elsewhere the system default). The volume control of a camera pop-out is the same per-person value (slider to 200 % there, screen audio stays 0-100 %). `ContextMenu` arrow navigation includes `[data-menu-item]` (the slider). Typecheck, all tests and build green; not checked in a browser with real audio.

**Permission to watch camera and screen (17 September 2026, user's requirement):** new role permission `VIEW_VIDEO` ("Kamera- und Bildschirmübertragungen sehen" / "Watch camera and screen shares", Admin > Roles). A member whose roles do not carry it hears the voice channel but receives no camera, no screen share and no screen audio; the stage shows avatars and a notice while somebody is sharing. **Guests do not have it (user's decision):** the default role "Gast" comes without it on fresh servers (`DEFAULT_EVERYONE_PERMISSIONS` unchanged = 1152), "Mitglied" has it (`DEFAULT_MEMBER_PERMISSIONS` = 24000); on existing servers migration 0013 grants it to every non-default role and leaves the default role alone, so pure guests stop seeing shares after the update. Enforced by LiveKit through the senders' track subscription permissions, see `apps/web/src/voice/AGENTS.md` "Watching camera/screen". Typecheck, all tests and the smoke test green; not checked in a browser with real cameras.

**Join and leave cues (16 September 2026):** Four different short sounds mark voice-room changes: you joining, you leaving, someone else joining your room, someone else leaving it. `apps/web/src/voice/sounds.ts` synthesizes them with Web Audio (no audio assets): your own pair is a sine C5/G5, the pair for other people a shorter, quieter triangle E5/A5; rising means arriving, falling means leaving. `voiceClient.ts` triggers them on a completed join, on leaving, and on LiveKit's participant events; switching between rooms plays only the join cue, a failed connection attempt plays nothing, participants already present within 1.5 s of connecting are not announced, and deafening silences all cues. Settings tab "Töne"/"Sounds" in `VoiceDock.tsx` has one switch per cue with a preview button plus a shared volume, stored per device in `chat.voice.v1` (`sounds`). `sounds.test.ts` covers the cue shapes, the stored-settings merge and the play decision.

**Video pop-outs and fullscreen (15 September 2026):** Camera and screen tiles expose separate-window and fullscreen buttons. `VideoWindows.tsx` manages one window per track at App level, retaining it across text/stage view changes, moving video display into the pop-out with a restore placeholder in the main stage, and closing windows when their track disappears or the main page closes. Pop-outs have no app frame or app header/footer; browser chrome (including the address bar) remains browser-controlled. Only the initial pop-out size follows the displayed video dimensions or track settings (16:9 fallback), limited to available screen space. After opening, window size is entirely user-controlled; metadata, format changes, manual resizing and fullscreen transitions never trigger automatic resizing; controls reuse the main-stage styles: close and fullscreen buttons at the top right, with per-feed playback controls at the bottom right. `VideoAudioControls.tsx` shares the volume slider, percentage and local mute toggle between received screen shares in the stage and pop-outs. Stage window/audio controls appear on hover or control keyboard focus (always visible on touch); pop-out controls follow the hover/focus rules below. Muting sets only the selected remote track volume to zero and restores its previous level on unmute, including across pop-out/restore; global deafen still takes precedence. Sender labels remain at the bottom left and audio controls at the bottom right on the same row. The control width is capped at 60% of the tile and names truncate within the remaining space to prevent overlap. Percentage outputs use intrinsic width with a four-character minimum so 100% fits. Controls hide when the window loses focus or the pointer leaves (keyboard-focused controls remain accessible; touch controls stay visible while the window is focused). A bottom-left label shows the sender using the voice server member name, including live renames; double-click or F/Enter toggles fullscreen. Existing audio elements move between documents: microphone audio follows camera pop-outs, screen audio follows screen pop-outs. This preserves one playback instance, volume, output-device selection and global deafen. Closing/restoring a pop-out returns its video and audio to the main window. Autoplay failures show a temporary unblock button. Fullscreen uses the target element's document, with translated blocked/unsupported/failure messages. Shared view detach and fullscreen behavior have unit coverage in `videoDisplay.test.ts`. Real multi-window/media browser acceptance is pending; no browser is connected in the editing environment.

**Voice connection card (15 September 2026):** `VoiceDock.tsx` now separates connection status, channel/server names, the stage shortcut, audio-unblock notices, microphone meter/muted label and equal-size controls. Toggle buttons expose pressed states, the connection label is a live status, and the mobile navigation scrolls to keep the taller voice dock reachable. Existing version 2 brand tokens and voice behavior are preserved; German/English stage labels updated together.

**Voice channel moderation (13 September 2026, user's requirement):** permission `MODERATE_VOICE`: move members to other voice channels or remove them, end their camera/screen share, block camera/screen per member (`members.stream_blocked`, overrides roles). LiveKit enforces muting and publish permissions server-side (`livekit/admin.ts`), the client follows via WS event. Also: camera selection with multiple cameras, background blur (`@livekit/track-processors`), separate output device for screen audio.

**Camera right after a role change: "failed to publish track, insufficient permissions" (18 September 2026, user's report):** the publish grants of a LiveKit token are fixed when a member joins the voice channel, and the server only adjusted them at runtime for the stream block and the AFK channel. A member who got "Mitglied" (or owner status, or whose role gained `STREAM_VIDEO`) while sitting in a channel saw the camera and screen buttons at once, because the client gets its new permissions with the structure broadcast, but LiveKit still only allowed the microphone until they left and joined again. The other direction was open too: a camera stayed on after the permission was taken away. Now `livekit/sync.ts` (`syncStreamGrantsOf`) brings LiveKit in line after `PUT /api/members/:id/roles`, `PUT /api/members/:id/owner`, `PATCH /api/roles/:id` (when the permissions changed) and `DELETE /api/roles/:id`: grants via `setCanStream`, and when the permission is gone also `stopStreams`; members in the AFK channel stay silenced. No client change, no protocol change. Checked: unit tests (`livekit/sync.test.ts` 4), typecheck, server tests 51, smoke test 108 green against a fresh `chat_smoke`; end to end against the dev LiveKit with a real client (headless Chrome with a fake camera, test server on :3001): as "Gast" LiveKit lists only MICROPHONE, after the owner assigned "Mitglied" by REST it lists CAMERA, SCREEN_SHARE and SCREEN_SHARE_AUDIO within a second and the client published its camera without an error, after the role was removed the grants were back to MICROPHONE and the camera track was gone. Not checked: role edit and role deletion end to end (same function, covered by the unit tests only), owner status end to end, whether the client's camera button shows "off" right after the permission was taken away while the camera ran.

**Microphone boost (18 September 2026; user's report: "der Mikrofonpegel ist bei vielen Leuten extrem niedrig, woran kann das liegen und können wir da Abhilfe schaffen?"):** the client had no gain of its own; it relied on the browser's automatic gain control (`autoGainControl`, off in stereo channels) and the voice activation opens at RMS 0.04. Built: `voice/micBoost.ts` (pure, tested) and its place in `micPipeline.ts`: source -> gain -> clipping guard (WaveShaper with a soft knee at 0.8, identity below it; deliberately no DynamicsCompressorNode, which adds make-up gain to every signal) -> level meter, gate and published track, so the threshold means what the others hear. `AutoGain` learns from the raw input while someone speaks (speech is told from a noise floor, independent of the gate, which sits behind the boost), only ever boosts (1..6, target RMS 0.1), comes down faster than it goes up and remembers its gain per microphone (`chat.micBoost.v1`). Einstellungen > Sprechen: "Leises Mikrofon automatisch anheben" (default on) or a slider 100-600 %, the gain applied right now is shown there and in the debug view ("Pegel / Tor · x..."). Per device and NOT in the directory account (decision by Claude: how quiet a microphone is belongs to the computer, like the device selection). Stereo channels stay unboosted.
**What the measurement says (same day, real client in headless Chrome with a WAV file as the microphone, debug view read for 12 s):** Chromium's own control brings speech of RMS 0.003, 0.012 and 0.1 in the file all to about 0.085 behind the pipeline, gate open ~90 % of the time, and the automatic ends at x1.0 in all three; by hand x3 gives 0.24 with peaks of 0.44 (chain and guard work). So in Chromium (and the desktop app) a quiet capture is NOT the cause of the report, and the automatic changes nothing there. Open causes, not checked: Firefox/Safari (weaker control; could not be measured, their fake microphone has a fixed level), real microphones with noise (the control lifts noise less willingly than a clean file), stereo channels (no control at all, raw level), and perception: about 0.085 RMS (-21 dBFS) is the usual WebRTC level and quieter than Discord, music or games, and the level bar (level x 400) sits at about a third while speaking normally, which looks low. The user was asked what exactly people see or hear; raising everyone's level (a higher target) or a master volume for voices are the candidates if it is perception.

**Cause found: stereo channel (18 September 2026, user's answer: "Pegelanzeige ist niedrig und Schwellwert muss nach ganz links gedreht werden. Die anderen hören sie auch sehr leise. Sowohl in der App als auch im Browser (Firefox), der Kanal ist Stereo"):** a stereo channel captures without the browser's processing (it would mix down to mono), so also without its gain control, and the microphone arrived at its raw level. The entry above still excluded stereo from the boost; that is reversed: **the boost runs in every channel** (gain and clipping guard keep both channels), the largest gain is now x12 (+21.6 dB; a raw microphone set low in the system needs it, slider 100-1200 %), and the learned gain is remembered per microphone AND capture mode (`<device>|stereo`), because the raw level of the same microphone differs a lot between the two. Measured with the same setup as above, channel switched to stereo by REST: boost off, file RMS 0.012 -> level 0.017 and the gate open 0 % of the time (the report, reproduced); automatic -> gain 6, level 0.066, gate open 88 %; file RMS 0.003 -> gain 11.9 (the maximum), level 0.030, gate open 85 %; the second join starts at the remembered gain (5.9 instead of 1). The first join with a quiet microphone needs about three to five seconds of speech until the gate opens; after that the remembered gain applies from the first word. Advice for operators: a channel people TALK in belongs on mono (the default): only there the browser's echo cancellation, noise suppression and gain control work and silence costs no bandwidth (DTX); stereo is for music. Not checked: real microphones, Firefox, the desktop shell, listening to the result, noise in a stereo channel (no noise suppression there: the boost raises the room's noise with the voice, while the gate is open).

**Mobile voice flow (19 September 2026, user request):** tapping a voice channel opens MobileVoicePreview from the bottom with live server presence and an explicit Connect button; opening it requests neither a token nor microphone access. Connect opens the stage across the viewport. Minimize returns to navigation without leaving VoiceClient; the existing dock restores the stage, including across servers. Existing stage actions remain at the bottom with safe-area padding. Join errors stay in the preview and permit retry. Desktop channel clicks still connect directly. Hanging up on the stage (22 September 2026, user's report: "momentan sehe ich dann den ersten Kanal des Servers") slides back to the channel list before the voice client leaves (`hangUp` in App.tsx, only at the narrow width; a plain function, since it sits below the early returns of the login screen, where a hook broke the client with React's error 310 after signing in, found by the check below); the dock's hang-up and the desktop are unchanged. Checked: the built client against a test server on :3001 (smoke database, dev LiveKit) in headless Chrome at 400x800 with touch emulation and fake media: sign in, tap Lobby, Verbinden in the sheet, the stage across the viewport, tap the hang-up button: 0.4 s later the channel list is visible and the main area slid out, still so after 3 s; screenshot looked at. Not checked on a real phone. Native dialog supplies focus trapping/Escape; reduced-motion preferences apply. Real phone microphone, camera and background audio acceptance remains pending.

**One voice connection per account and server (22 September 2026, user's wish):** "wenn ich mit meinem Handle auf einem Client einen Sprachkanal auf dem selben Server betrete auf dem ich schon mit einem anderen Client in einem Sprachkanal bin, dann soll sich ein Modal öffnen, dass mich darauf hinweist, dass ich damit meine andere Sprachverbindung beende. Das muss der Benutzer dann bestätigen oder abbrechen."
- *Before:* the same account (same user id on the server) could sit in two voice channels from two clients; only the same channel ended the older one, through LiveKit's duplicate-identity rule, with the bare text "dieselbe Identität ist von einem anderen Gerät/Tab beigetreten".
- *Server:* at `voice.join` the handler (`ws/handler.ts`) takes the user's other connections in voice (`VoicePresence.othersOfUser`, restored guesses included), sends each `voice.moved` with `channelId: null`, `by` = server name and the new `reason: "elsewhere"` (protocol, no version bump: a client from before drops the event and LiveKit ends its media anyway), leaves their presence, and removes the participant from every other room (`LivekitAdmin.removeParticipant`); in the room just joined LiveKit has already replaced the old participant, a removal there would hit the new one.
- *Old client:* `onVoiceMoved` with reason "elsewhere" hangs up (`leaveVoice`) and shows "Du bist von einem anderen Gerät oder Tab aus einem Sprachkanal beigetreten. Diese Verbindung wurde beendet." as the voice notice modal (entry below).
- *New client:* before a join by hand (channel click, the phone's sheet) `elsewhereName()` in App.tsx looks whether the server's voice presence lists my user id in some channel while this client has no voice connection to that server; then the confirm dialog "Andere Sprachverbindung beenden?" names that channel, "Hier beitreten" joins, cancel does nothing. Automatic joins (AFK move, moderator move, AFK profile change, the return from AFK) never ask. Without anything to ask the join starts synchronously inside the click, as WebKit's microphone prompt needs; after a confirm the dialog's click is the gesture. On a phone the sheet (a native modal dialog in the top layer, above which the confirm could not show) closes first and comes back on cancel, or with the error text when the join fails after the confirm (`MobileVoicePreview`'s `error` prop).
- *Same room:* LiveKit replaces the old participant before the server's `voice.join` arrives, so the old client sees LiveKit's duplicate-identity disconnect first and has already reset its voice host when `voice.moved` comes; that text (`voice.disc.duplicate`) now reads the same way: "Getrennt: du bist von einem anderen Gerät oder Tab aus diesem Sprachkanal beigetreten. Diese Verbindung wurde beendet."
- *Checked:* see `docs/VERIFIED-STATE.md` of the same date.

**Voice notices and errors are a modal (22 September 2026, user's wish):** "Meldungen die im dock angezeigt werden bitte aus dem Dock entfernen und stattdessen auch als Modal anzeigen, diese müssen nur einen Schließen Button haben und sich schließen wenn man woanders in das Fenster klickt."
- *Before:* `voice.notice` (a moderator moved, removed or stopped you, the account joined elsewhere, a failed pop-out) and `voice.error` (a lost connection, a failed join, camera, profile) were lines in the dock and on the stage; a notice that ended the connection was not even readable after the hang-up, because the dock showed it only inside its voice block.
- *Built:* `showNotice()` in `dialogs.tsx` (kind "notice" in the same queue as `askConfirm`/`askInput`: title, text, one "Schließen" button, focus on it; the backdrop's click and Escape close it, `Frame`). Two effects in App.tsx show `voice.notice` as "Hinweis" and `voice.error` as "Fehler" whenever the text changes and clear the client's text when the modal closes, if it is still the one shown (`client.setNotice(null)`, new `client.clearError()`); a text that changed meanwhile gets its own modal after it. A join that fails before the voice client has a text (token, server) shows the bare error the same way (`reportJoinError`). The dock and the stage lost their error and notice lines; the AFK box in the dock (with its "Zurück" button), the audio and microphone unblock buttons and the stage's hints about the AFK channel and a share stay, they carry actions or state, no message. The phone's sheet closes on a failed join and the modal shows the error (the sheet sits in the top layer, above the modal; its `error` prop of the same day went again).
- *Checked:* see `docs/VERIFIED-STATE.md` of the same date.

**Channel permissions (23 September 2026, `docs/features/channel-permissions.md`):** moving members is its own right `MOVE_MEMBERS` (migration 0028 gives it to every role with `MODERATE_VOICE`; `MODERATE_VOICE` keeps stopping streams and the stream block); both count server-wide or in the channel the target sits in. A move may put somebody into a channel they may neither see nor enter (the mover needs `MOVE_MEMBERS` resolved in the destination), the target's channel list gets the destination first and a grant lets their join through; such a seat is `placed` and survives permission changes. Whoever loses `VIEW_CHANNELS` or `CONNECT_VOICE` on a seat of their own is removed from the room and gets `voice.moved` null (`syncVoiceAccessOf`). Sticky voice channels hold their members (`members.confined_channel_id`): no token for another channel, the hang-up button disabled with the reason, `BYPASS_STICKY` exempt; per channel whether the hold outlives the connection and whether the other voice channels are hidden meanwhile. A channel's `allowVideo` false takes camera and screen out of the token and the stage; `userLimit` is best effort (409 `channel_full`, a move ignores it).

**Unselected shares and deafen survived LiveKit's `startAudio` (24 September 2026, found while looking into the user's report "voices get quieter when I watch a screen share"):**
- *Fault:* LiveKit's `room.startAudio()` sets `muted = false` on every attached audio element. It ran on every click on a tile's volume control (`VideoAudioControls` calls `client.startAudio()`), on "Ton freigeben", and by LiveKit itself whenever a local audio track is published (`ParticipantEvent.AudioStreamAcquired`: the microphone, the own share's audio). Afterwards the audio of shares the user had not selected played, and while deafened the voices played again.
- *Fix:* `VoiceClient.startAudio()` applies `applyAudioMuted()` in the same tick as LiveKit's synchronous unmute; a listener on `AudioStreamAcquired` does the same in a microtask after LiveKit's own listener. Test in `videoAudio.test.ts`.
- *Open:* the user's report itself (desktop app, watching a share, voice and screen audio on different devices; the voices get quieter) is not explained by this and not reproduced yet: the routing in `applyScreenSink()` looks right. Next with a second person: the debug view's "Bildschirm-Ton Ausgabe" (device, tracks, error) and the Windows volume mixer while it happens.
- *Checked:* see `docs/VERIFIED-STATE.md` of the same date.

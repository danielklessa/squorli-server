# Milestone log

Part of the project description (entry point: root `AGENTS.md`, section 0; until 17 September 2026 this was section 9 of that file). One row per change, newest first. Section numbers inside older rows refer to the single `AGENTS.md` of that time.

| Date | Change |
|---|---|
| 2026-09-26 | Desktop app version 0.7.1 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.7.1.md`; patch by the user's choice): the offline notice with retries, screen audio on a separate device and a watched share's audio at once, reporting a direct message (needs the directory of 26 September 2026); nothing of it ships with the server image alone. Not tagged yet. |
| 2026-09-26 | Server version 0.4.1 (`apps/server/package.json`, release notes `apps/server/release-notes/0.4.1.md`; patch by the user's choice): only the web client changed (the offline notice, the share audio fixes, the direct message report); no migration, no variable. Needs desktop app 0.7.1 for the app's users. Not tagged yet. |
| 2026-09-26 | A watched share plays its audio at once (`docs/features/voice-video.md`, user's report): the stage listens to every share the user turned on with "Ansehen", pinned or not, instead of only the pinned one. |
| 2026-09-26 | Reporting a direct message (`docs/features/reports.md`, stage 4 in part): the flag on a friend's message opens the report dialog addressed to the directory's operator, the reported message and up to 20 preceding ones travel in plain text inside the signed action `report` (`dmReports.ts`, `directoryDmReportPayload`), the dialog offers blocking afterwards; `ReportReason` moved to `primitives.ts`, `features.reports`; the directory's side in its own repository. Website: the guides describe reports (`/docs/admin/#reports`, `/docs/use/#report`). |
| 2026-09-26 | Screen audio on a separate output device no longer moves every voice there (`docs/features/voice-video.md`, user's report): Chromium plays all remote WebRTC tracks of a page through ONE output sink, so the share now runs through its own `AudioContext` with `setSinkId` (`applyShareAudio` in `voiceClient.ts`, LiveKit's element at volume 0; the share's volume in `shareVolumes`); rule in `apps/web/src/voice/AGENTS.md`; the debug view names the path. Typecheck, tests (436 web, 7 new), headless Chrome check of the route, build. |
| 2026-09-26 | Unreachable servers, seventh step (`docs/features/offline.md`, the user's rule): ten automatic tries in all (1x15 s, 4x30 s, 5x60 s), then the automatic tries pause until "Erneut versuchen" is pressed, then five more a minute apart (`RetryPlan` in `serverRetry.ts`, tested; `ServerConnState.retryPaused`, `retryByUser`); the notices say so in place of the countdown. Checked in headless Chrome over 9.5 minutes against a stopped fake server. |
| 2026-09-26 | Unreachable servers, sixth step (`docs/features/offline.md`, the user's cadence): retries 15 s after the failure, 30 s before the 2nd to 5th try, a minute each after that (`retryDelayFor`, tested); "Erneut versuchen" locked for 5 s at least and until the running try is through (`retryLocked` with `tryRunning`). Checked in headless Chrome against a silent and a stopped fake server. |
| 2026-09-26 | Unreachable servers, fifth step (`docs/features/offline.md`): "Verbinde mit …" centred like the notice, "Erneut versuchen" rests 5 s after every try that starts (`retryLocked`, tested), the reachable servers as rows with the directory's icon or initials left of the name. Checked in headless Chrome with a silent and a stopped fake server. |
| 2026-09-26 | Unreachable servers, fourth step (`docs/features/offline.md`): a server on the account's list without a session on this device gets the notice too (`Store.isMine`, health probe at start so the rail marks it, no sign-in without a click), and the main area takes the members' column while no server state is on screen (`.app.no-members`). Checked in headless Chrome with three listed servers. |
| 2026-09-26 | `pnpm dev:desktop` works without `pnpm dev` (`apps/desktop/scripts/run.mjs`, `docs/features/desktop.md`): a local `--dev-url` that does not answer gets the web client's Vite server started by the script itself and stopped with the app, so the client can be looked at while the chat server is down on purpose. Checked with the real shell over the DevTools protocol. |
| 2026-09-26 | The view for a rail server that does not answer, as the user asked (`docs/features/offline.md`): the notice centred in the main area, a countdown to the next automatic try above "Erneut versuchen" (`RetryCountdown.tsx`, `ServerConnState.retryAt`), up to five reachable servers below it as buttons (`otherServers.ts`, tested), the server marked in the rail (dimmed, red "!"); the own server's notice gets the countdown too. Checked in headless Chrome with two rail servers, one down. |
| 2026-09-26 | Desktop shell: `pnpm dev:desktop` without `pnpm dev` (the Vite dev server of `--dev-url` not running) showed an empty main window without a title bar after the reveal timer, which the user took for the selected server being down (`docs/features/desktop.md`). Now the start window names the dev server and waits (`splashPage.ts` step `devServer`), the shell asks it every 2 s and loads the page by itself once it answers; `--user-data-dir` keeps `--dev-url` from forcing `Squorli-dev`. Checked with the real unpackaged shell over the DevTools protocol. |
| 2026-09-26 | The desktop app's rail server that does not answer (`docs/features/offline.md`, after the user's report of a blank app, which could not be reproduced): `/api/health` and `/api/me` give up after 10 s / 15 s (`ServerApi.request` `timeoutMs`), so a silent machine counts as unreachable in seconds; a retry keeps the "unreachable" notice on screen; `ServerStatus` leaves out the invite field and account forms for a known server that does not answer and names it from the directory's list. Checked in headless Chrome with the client without a home server against a fake directory and a silent, a stopped and a returning fake server. |
| 2026-09-26 | A signed-in server that does not answer (`docs/features/offline.md`, user's wish): the stored session is dropped only when the server refuses it (`serverRetry.ts` `sessionRejected`, tested); otherwise the client shows a notice with "Erneut versuchen" and "Abmelden" instead of the login (`ServerOffline.tsx` for the own server, `status.offline` in `ServerStatus` for a known foreign one) and the connection tries again by itself (2/4/8/15 s, at once when online again or the tab is shown; `ServerConnection.retryLater`, `ServerConnState.waiting`, kept until the welcome so the login never flashes by). The home server's name is remembered for the notice (`chat.homeName.v1`). Checked with headless Chrome against a Vite dev server and a fake server that went down and came back. |
| 2026-09-26 | Desktop app version 0.7.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.7.0.md`; minor by the rule, the user's choice over 0.6.3): the client's part of the reports (the flag on messages, "Melden" in the member menu, Verwaltung > Meldungen with the moderation log, the rail badge, the notice, the ban's delete choice) and the connection check in Verwaltung > Server with the browser's media test. The Electron shell is unchanged; everything needs server 0.4.0, which goes out first, and hides on older servers. Not tagged yet. |
| 2026-09-26 | Server version 0.4.0 (`apps/server/package.json`, release notes `apps/server/release-notes/0.4.0.md`): the setup self-diagnosis (`squorli doctor`, Verwaltung > Server > "Verbindung prüfen", the directory's probe from outside) and reports with the moderation log and delete on ban. Migrations 0035 (`reports`, `mod_log`) and 0036 (`MANAGE_REPORTS` for roles with `MANAGE_MESSAGES`); new optional variables `LIVEKIT_TCP_PORT`/`LIVEKIT_UDP_PORT` for the app (defaults 7881/7882, compose.yml passes them). `squorli doctor` needs the installer run again (the wrapper is rewritten). The check from outside needs the directory of 26 September 2026. A desktop app release follows (the client's report menus, the queue, the connection check). Not tagged yet. |
| 2026-09-26 | Reports, stages 1 and 2 of `docs/PLAN-reports.md` (`docs/features/reports.md`): permission `MANAGE_REPORTS` (bit 18; migration 0036 grants it to roles with `MANAGE_MESSAGES`), tables `reports` and `mod_log` (migration 0035), `POST /api/reports` with a snapshot incl. attachment copies, the queue `GET /api/reports` and `POST /api/reports/:id/close` (delete, delete recent, kick, ban, dismiss), signed links for the copies, `reports.count` to moderators, `moderation.notice` to the reported person, the retention sweep; the moderation log (`GET /api/mod-log`, 180 days) fed by foreign deletes, kick, ban, unban, channel blocks and closed reports; `BanRequest.deleteMessagesHours` with `message.bulkDelete`; client: the flag on messages and members, `ReportDialog`, Verwaltung > Meldungen with the log, the rail's amber badge, `askSelect`. Server image (migrations 0035/0036) and a desktop app release. |
| 2026-09-26 | Setup self-diagnosis (`docs/features/doctor.md`; PLAN 2.1, the user's decision of 25 September 2026): `GET /api/doctor` (MANAGE_SERVER or loopback) with checks of the own public address, the WebSocket upgrade, `/rtc`, LiveKit's key, the TCP media port, the directory registration and the directory's probe from outside (`POST /api/servers/probe`, new in the directory), texts in both languages; `POST /api/doctor/rtc-token` and the browser's media test (UDP, TCP only, relay, private address) in Verwaltung > Server > "Verbindung prüfen"; `squorli doctor` in the installer's wrapper; `LIVEKIT_TCP_PORT`/`LIVEKIT_UDP_PORT` known to the app; `ipDoctor` limit; website: installation, proxy and administration guides. Server image and a desktop app release (the client's section). |
| 2026-09-25 | Desktop app version 0.6.2 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.6.2.md`; the number was the user's choice over 0.7.0): the app's part of security review groups C and D (control links with the installation's key: older links for mic-on, mic-toggle, deafen-toggle, deafen-off must be copied again; `safeHref`; directory signatures bound to the connected host; other sessions revoked after a password change) and the microphone boost off by default. Server 0.3.0 carries the server's part and goes out first; the app needs no newer server. Not tagged yet. |
| 2026-09-25 | Server version 0.3.0 (`apps/server/package.json`, release notes `apps/server/release-notes/0.3.0.md`; the number was the user's choice over 0.2.1): security review groups C and D and the microphone boost off by default. Migration 0034 (session tokens hashed in place; a downgrade afterwards signs everybody out). No new variable. The web client's pages carry a CSP and `frame-ancestors 'none'`. A desktop app release follows (link guard, control link key, directory host binding). Not tagged yet. |
| 2026-09-25 | Security review group D: session tokens stored as SHA-256 (migration 0034, `tokenHash`); sockets closed at sign-out, at the session's end and after a kick between lookup and `hub.add`, one `hello` per socket; a ban deletes the sessions, profile and avatar need a membership; request log without query secrets (`logRedact.ts`); directory answers checked against the key asked, registration retries paused after a failure, the founder not revealed by leave pushes; client: directory signatures bound to the connected host (`signingHealth`), other devices signed out after a password change; desktop app: control links with the installation's key (`controlKey`, `KEYLESS_CONTROL_ACTIONS`). Server image and desktop app release. |
| 2026-09-25 | Security review group C: password attempts counted before the check (`RateLimiter.attempt`/`refund`) and per account for password change and deletion; IP limits count IPv6 by its /64 (`ipKey`); the owner claimed with one conditional update before the membership (two first sign-ins no longer both become owner); the web client's pages with a CSP, `frame-ancestors`, `Referrer-Policy` (`webHeaders.ts`); link targets from servers and peers through `safeHref`. Server image and a desktop app release (link guard only). |
| 2026-09-25 | Desktop app version 0.6.1 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.6.1.md`; the number was the user's choice over 0.7.0): the keys in `safeStorage` (no downgrade afterwards), `VIEW_VIDEO` per channel on the sender's side, the claim on a fresh key and bound server-account backups, no connection by itself to unknown servers of the list, the sign-in only for the connected host, readable rate-limit refusals, histories reloaded for fresh attachment links. The claim and the channel video permission need server 0.2.0, which goes out first. Not tagged yet. |
| 2026-09-25 | Server version 0.2.0 (`apps/server/package.json`, release notes `apps/server/release-notes/0.2.0.md`; the number was the user's choice): the security fixes of the day (rate limits, signed attachment links, `VIEW_VIDEO` per channel, radio playlists against DNS rebinding, security review groups A and B). Migration 0033 (`server_settings.link_secret`), new optional variable `RATE_LIMIT_FACTOR`; `PUBLIC_DOMAIN` must match the address clients connect to. Needs the next desktop app (0.6.1, released after the server) for the client parts; the membership proof takes effect with the directory of the same day. Not tagged yet. |
| 2026-09-25 | Security review group B: the membership proof passed to the directory and no connection by itself to unknown servers of the list; the claim of a member from before on a fresh key (`localClaimMessage`, two signatures, `users.public_key` moved, `localClaimRekey`); server-account backups bound to the connected host (`BackupParams.bound`, rebinding at the next password sign-in). Server image and desktop app release. |
| 2026-09-25 | Security review, group A: a foreign server's sign-in signs the host actually connected to and is refused when the server names another domain (login relay, critical); attachments inline only for safe types, everything else a download, a sandboxing CSP on every answer (SVG script on the server's origin). Server image and desktop app release. The review's other findings: `docs/PLAN.md` 2.1. |
| 2026-09-25 | Desktop app: the identity key and the server accounts' keys and tokens encrypted with `safeStorage` (`apps/desktop/src/main/secrets.ts`, `DesktopBridge.secrets`, `Platform.secretStore`, `setSecretStore` in `identity.ts`), moved over from localStorage at the first read. Desktop app release; no downgrade afterwards. |
| 2026-09-25 | Radio playlists fetched through `safeGet` of `packages/link-preview` (the connected address is checked, also at redirects): closes the DNS rebinding gap of `docs/features/radio.md`. Server image only. |
| 2026-09-25 | `VIEW_VIDEO` per channel: the server resolves it per seated member through the overwrites (`VoiceMember.viewVideo`, optional, no version bump) and resends every occupied `voice.state` after permission changes; senders restrict their camera and screen by it (`channelVideoAccess`, `mayViewIn`). Closes the known gap of `docs/features/channel-permissions.md`. Server image and desktop app release. |
| 2026-09-25 | Signed attachment links (user's decision, 7 days): `?e=&s=` with an HMAC under `server_settings.link_secret` (migration 0033), 404 without a valid one; the client forgets every history after a reconnect and reloads those older than 3 days. Closes the gap "attachments reachable by id" (`docs/features/channel-permissions.md`). Server image and desktop app release. |
| 2026-09-25 | Rate limits on every public path (`docs/features/rate-limits.md`): one `onRequest` hook per IP and session token, WebSocket events per connection (close 4008), `RATE_LIMIT_FACTOR`; the client explains a refusal at sign-in and when sending. Server image; the new texts need a desktop app release (not urgent). |
| 2026-09-25 | Server version 0.1.1 (`apps/server/package.json`, release notes `apps/server/release-notes/0.1.1.md`; the number was the user's choice): the image differs from 0.1.0 only in comments; for operators it carries `squorli restore` in the installer's wrapper and LiveKit pinned to `v1.13.7` in the Compose files, both through the deploy files (running the install script again), not through `squorli update`. No migration, no new variable, no desktop app release needed. Not tagged yet. |
| 2026-09-25 | Plan cleanup with the user: `docs/PLAN.md` rewritten as guidelines plus open work ranked by relevance and a test campaign (built and documented content removed; the screen share audio matrix, bandwidth figures and architecture reasons moved to feature notes and area `AGENTS.md`); the decisions of `docs/PLAN-reports.md` and `docs/PLAN-mobile.md` made; confirmed decisions marked in the feature notes. Built: `squorli restore` in the installer's wrapper (dind-tested), LiveKit pinned to `v1.13.7` in all Compose files. Server image only, no desktop app release. |
| 2026-09-25 | Plan for reporting, blocking and deleting with evidence: `docs/PLAN-reports.md`, open decision 10 in `docs/PLAN.md` (the directory's side in its own repository). Documentation only; nothing built. |
| 2026-09-25 | Plan for a native client (Android, later iOS): `docs/PLAN-mobile.md`, open decision 9 in `docs/PLAN.md`. Documentation only; nothing built. |
| 2026-09-25 | Desktop app version 0.6.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.6.0.md`; the number was the user's choice): carries the server accounts in the client (one sign-in form where `@`/`~` decides, creating a server account, directory account and server accounts side by side, account settings of a server account, claim of members from before). Needs Server 0.1.0 (image `:v0.1.0`), which goes out first. Not tagged yet. |
| 2026-09-25 | Server version 0.1.0 (`apps/server/package.json`, release notes `apps/server/release-notes/0.1.0.md`; the number was the user's choice), the first numbered server release: everything `:latest` had (as of a5d1c70) plus the server accounts (migration 0032, `LOCAL_ACCOUNTS`, `REQUIRE_ACCOUNT` gone, every sign-in needs an account). Needs the next desktop app release for servers without a directory; the server goes out first. Not tagged yet. |
| 2026-09-25 | Server releases: skill `server-release` (version proposal, operator notes `apps/server/release-notes/<version>.md`, `apps/server/package.json`) and `.github/workflows/server-release.yml` (draft GitHub release for a tag `v*`, checks the version); the image jobs for `v*` were already there. No server tag exists yet. |
| 2026-09-25 | Server accounts (`~name`, docs/features/local-accounts.md): a server manages accounts of its own next to the directory's `@name`; the client encrypts the key with the password, the server keeps the ciphertext (table `local_accounts`, migration 0032, `src/auth/local.ts`). No temporary users: every sign-in needs an account (403 `registration_required`), the first owner too; members from before register afterwards (session with `registrationRequired`, WS close 4013). Setting per server (`server_settings.local_accounts`, `LOCAL_ACCOUNTS`; always on without a directory), `REQUIRE_ACCOUNT` deprecated. Avatar of a server account kept by the server. Client: one key per server account next to the directory account (`chat.serverAccounts.v1`), login with one form where the prefix decides, "Konto erstellen" with tabs, join view with the forms, claim view, password change and deletion in the settings, no friends/DMs for server accounts. Smoke 204 / 221 (with directory). Needs a desktop app release. |
| 2026-09-24 | Desktop app version 0.5.2 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.5.2.md`; the number was the user's choice): carries only the fix that keeps unselected shares silent and deafen in force after LiveKit's `startAudio`; client only, the shell is unchanged, no server image needed. The user's report of quieter voices while watching a share is not part of it (still open). Not tagged yet. |
| 2026-09-24 | LiveKit's `room.startAudio()` unmuted every audio element (a click on a tile's volume control, "Ton freigeben", every local audio publish): unselected shares played and deafen was lifted. `VoiceClient.startAudio()` and an `AudioStreamAcquired` listener put the mute rule back at once; test added. The user's report of quieter voices while watching a share is still open. No server or protocol change. Desktop app release needed (apps/web). |
| 2026-09-24 | Desktop app version 0.5.1 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.5.1.md`; the number was the user's choice): carries the channel blocks in the member and voice menus (remove, block for a time or for good, lift; channel dialog > Sperren), one menu for voice members in the sidebar and on the stage's tiles, the small profile on a left click, removing and blocking friends from the client, voice activity on the server rail, the tidied member menu, right-click-only menus, roles in rank order, the hotkeys that follow the keyboard layout, the fix for voice channel clicks after the first join and the removal notice instead of PARTICIPANT_REMOVED; client and protocol only, the shell is unchanged. The channel blocks and the removal notice need the server image of the same date (migration 0031, `/api/channel-blocks`, `voice.moved` reason `blocked`, the delayed LiveKit removal), which goes out first. Not checked before the release: most of these menus in a browser or the desktop app itself. Not tagged yet. |
| 2026-09-24 | The right-click menu on a stage tile is now the same as the one on a voice member in the sidebar (`VoiceMemberMenu.tsx`): the tiles gain move, remove with block, end streams, block streaming, lift blocks; the sidebar gains camera/screen off or on for myself. No server or protocol change. Desktop app release needed (apps/web). |
| 2026-09-24 | Server rail shows voice activity: a green speaker at the bottom left of each entry whose server has people in its voice channels (AFK channel left out; the number only in the tooltip, not on the server of my own voice connection; user's decisions), from the rosters the background connections already hold; no server or protocol change. Desktop app release needed (apps/web). |
| 2026-09-24 | Fix: global shortcuts follow the keyboard layout. The label shows the key as the layout writes it (Strg+Ö instead of Strg+Semicolon, also the push-to-talk key; `keyboardLayout.ts`, `keyName`), and on Windows a letter, digit or punctuation key is registered by the virtual key the layout gave it (`HotkeyBinding.vk`): before, Ctrl+Ö registered Ctrl+Ü and Ctrl+Y registered Ctrl+Z on a German keyboard. Desktop app release needed (apps/web). |
| 2026-09-24 | Left click on a member: small profile (`MemberProfile.tsx`) with a line to write to a friend (emoji, send opens the friends view) or a friend request button; the emoji picker no longer closes a context menu. Desktop app release needed (apps/web). |
| 2026-09-24 | Member list menu: "Rollen" and the owner entry moved into the "Server" section with kick and ban. Desktop app release needed (apps/web). |
| 2026-09-24 | Remove and block friends from the client: right-click on a friend in the home view, dots button in a conversation's head, "Freund entfernen" in the member list's menu; each confirms first (`friendActions.ts`), an open conversation closes. Directory unchanged. Desktop app release needed (apps/web). |
| 2026-09-24 | "Ohne Sperre" as the first choice of the submenu of "Aus dem Sprachkanal entfernen". Desktop app release needed (apps/web). |
| 2026-09-24 | The voice entries of the member list's menu (move, remove/block, stop streams, streaming block, channel blocks, vote kick) also in the sidebar's voice member menu (`VoiceMemberActions.tsx`), rights per channel; "Kamera/Bildschirm beenden" disabled while neither is on. Desktop app release needed (apps/web). |
| 2026-09-24 | Fix: a member removed or blocked from a voice channel saw "Fehler: Getrennt durch den Server (PARTICIPANT_REMOVED)"; now the removal notice with the block's duration (server removes from LiveKit 2 s after `voice.moved`, `voice/removeLater.ts`; `PARTICIPANT_REMOVED` is a notice in the voice client; a late `voice.moved` still explains). Server image and desktop app release needed. |
| 2026-09-24 | Channel blocks (user's wishes): "Aus dem Sprachkanal entfernen" as its own menu entry with a submenu that also blocks the channel for 5/15/30/60 minutes or for good; moderators lift blocks in the member menu and in the channel dialog's "Sperren"; the vote kick's block is one too; table `channel_blocks` (migration 0031), routes `GET /api/channel-blocks`, `PUT/DELETE /api/channels/:id/blocks`, `voice.moved` reason `blocked`; kick/ban under "Server", no volume slider in the member list's menu. Server image and desktop app release needed. |
| 2026-09-24 | Roles listed in the owner's order everywhere (`rolesByRank`), member and category menus open on a right-click only (a tap on phones), the member menu stays open while roles are ticked. Desktop app release needed (apps/web). |
| 2026-09-24 | Fix: after the first join, every further voice channel click showed the first text channel instead of the stage. `VoiceClient.leave()` reports "connecting" during a room switch, `joinVoice` switches servers without `leaveVoice()`. Desktop app release needed (apps/web). |
| 2026-09-23 | Installer refresh and configurable host ports (user's wish): `deploy/install.sh` opens with the small Squorli icon as block characters, spaces its questions and gets a step "Ports" that finds taken ports and suggests free ones; `compose.yml`, `proxies/nginx.ports.yml`, `proxies/remote-proxy.ports.yml` and `portainer.yml` read `LIVEKIT_TCP_PORT`, `LIVEKIT_UDP_PORT` (same number inside and outside, passed to LiveKit as `--rtc.tcp_port`/`--udp-port`), `APP_PORT`, `LIVEKIT_HTTP_PORT`; `.env.example`, README, `deploy/proxies/README.md`, `deploy/AGENTS.md`. Server image and desktop app unchanged. |
| 2026-09-23 | Interactive installer `deploy/install.sh` (user's wish): German/English questions for domain, server name, HTTPS setup (bundled Caddy, proxy on the host, proxy on another host), directory, owner key, LiveKit IP and image; Docker via get.docker.com and ufw/firewalld only after a yes; writes `.env` from `.env.example` with fresh secrets, starts and checks the stack; reruns update or reconfigure and keep secrets and data; the Compose wrapper `squorli` (update, status, logs, backup). README section "Interactive installer", `deploy/AGENTS.md`. Server image and desktop app unchanged. |
| 2026-09-23 | Desktop app version 0.5.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.5.0.md`; the number was the user's choice, minor because the app gets whole new areas: the channel dialog and the vote kick): carries the channel and category permissions with the channel dialog (private, read-only, sticky, user limit, slowmode, notification suggestion, radio/video per channel), `MOVE_MEMBERS` as its own right, the vote kick with its box at the top of the member list, the refusal sentences for joins and moves, Verwaltung > Kanäle as an overview with the pencil, the wider and readable categorized modals, and the status API texts of the same day; client and protocol only, the shell is unchanged. Everything except the dialogs' layout needs the server image of the same date (its routes, `myChannelPermissions`, `voice.state.voteKick`, migrations 0027-0030), which goes out first; older servers offer no channel dialog, no menu entries and no vote kick. Not checked before the release: the channel dialog and the vote kick inside the desktop app itself (both only in headless Chrome against a test server) and a real vote between two clients. Not tagged yet. |
| 2026-09-23 | Vote kick in voice channels (`docs/features/votekick.md`, user's wish and decisions): where at least three members sit and nobody present may remove anybody (`MOVE_MEMBERS` in the channel, `KICK_MEMBERS`), "Vote-Kick starten" in the member, sidebar and stage menus starts a one-minute vote; the starter counts as a yes, the member it is about does not vote, everybody else gets a modal, a tie leaves them in and at least half of the channel has to vote; a passed vote removes them from the channel and locks them out of that one channel for 15 minutes (`403 votekicked` at the token and at `voice.join`, a moderator's move wins). New: `packages/protocol/src/votekick.ts`, `apps/server/src/voice/votekick.ts` + `voiceState.ts` + `routes/votekick.ts`, `apps/web/src/VoteKickPanel.tsx` + `voteKick.ts`, the box at the top right over the member list, the per-channel switch `allowVoteKick` (migration 0030) in the channel dialog. Running votes, blocks and cooldowns live in memory (single node). No `PROTOCOL_VERSION` bump. Needs a new server image **and** a new desktop app release (protocol, server and client changed). |
| 2026-09-23 | Wider categorized modals, limited to the available window area including title bar and padding; 36 Chrome viewport containment checks, root typecheck/tests/build passed. |
| 2026-09-23 | Modal readability: shared responsive layout for settings, administration and channels; prevent flex content compression and navigation overflow, widen desktop dialogs, separate permission labels/inheritance/controls, stack the permission target list on smaller windows. Existing brand and bilingual copy preserved. Root typecheck/tests/build passed; DE/EN React fixtures checked at five widths. |
| 2026-09-23 | Channel and category permissions (`docs/features/channel-permissions.md`, user's wish and decisions): Discord-style overwrites for roles and members per channel and category (tables `channel_overwrites`/`category_overwrites`, migration 0027), live inheritance, the pure resolution (`channelPermissions.ts`), hard visibility (per-user `structure`, `hub.broadcastToChannel`, 404 never 403, read state and status API filtered), `MOVE_MEMBERS` and `BYPASS_STICKY` (migration 0028), sticky voice channels with persistence and hiding, a move into an invisible channel with grants and placements, eviction on lost access (`syncVoiceAccessOf`), user limit, slowmode, read-only, notification suggestion, `allowRadio`/`allowVideo`; the channel dialog with the overwrite editor, the sidebar's channel and category menus, Verwaltung > Kanäle as an overview; the Discord import takes overwrites over. No `PROTOCOL_VERSION` bump. Needs a new desktop app release (client and protocol changed). |
| 2026-09-23 | Desktop app version 0.4.1 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.4.1.md`; the number was the user's choice): carries the mute, sound, camera and screen icons for every voice channel, the status API section in Verwaltung > Server, clickable `squorli://` links in messages, invitations as app links with copy feedback, files pasted into the chat and the window's context menu. The status API and the icons outside the own channel need the server image of the same date, which goes out first; older servers show the icons only inside the own channel and no status API section. Not tagged yet. |
| 2026-09-23 | Status API lists only the members in a voice channel (user's decision): `members` = the seated members with a `voice` seat that is never null, the `online` field and `?online=1` are gone, admin texts and the website's guide (example, widget) changed. Server, protocol and client texts changed: needs a new server image and a new desktop app release (texts only; an old app still works). Details: `docs/features/status-api.md`. |
| 2026-09-23 | Camera and screen share join the mute state that goes over the chat server (user's wish: see from outside the channel who muted, deafened, shares camera or screen): `VoiceStatus`/`VoiceMember`/`voice.join` carry `cameraOn`/`screenOn` with defaults, the sidebar shows all four icons for members of other rooms, the status API's `voice` seat has them too. Server and client changed: needs a new server image and a new desktop app release; no version bump. Details: `docs/features/status-api.md`. |
| 2026-09-23 | Verwaltung > Einladungen: each invitation also as a `squorli://invite/<host>/<code>` app link ("App-Link kopieren"); `CopyButton.tsx` answers every copy with "Kopiert" or "Kopieren nicht möglich" (invites, status API address and key). Client only: needs a new desktop app release, no server change. Details: `docs/features/ui-admin.md`. |
| 2026-09-23 | Status API (user's wish): `GET /api/status` = server name, icon, categories and channels in order, members with online/AFK/owner, avatar and their voice seat with mute state; off by default, with the server's key or public (Verwaltung > Server, key shown and replaceable there); protocol `StatusApiMode`, `ServerStatus`, `VoiceStatus`; migration 0025 (+ 0026 dropping the avatar consent again, user's decision: the avatar is public anyway). Mute and sound off for the whole server: `voice.join` carries them, `voice.status` follows changes, `VoiceMember.micMuted`/`deafened`, sidebar icons outside the own room. `squorli://server/...` and `squorli://invite/...` links are clickable in messages (never a `control` command). Server image and protocol changed, client changed: needs a new server image and a new desktop app release (an old app still works: no version bump). Details: `docs/features/status-api.md`, `docs/features/markdown-input.md`. |
| 2026-09-23 | Context menu of the desktop app's window (user's wish): cut/copy/paste/undo/select all, spelling suggestions and the dictionary, copy or open a link, copy or save a picture, German or English following the client (`IPC.language`, the tray's menu too); shell `contextMenu.ts` + `contextMenuItems.ts` (pure, tested). Files pasted into the channel chat, both platforms: Ctrl+V or the menu's "Einfügen" puts clipboard pictures and copied files into the composer's files, picture chips with a preview (`pasteFiles.ts`, pure, tested). Shell and client changed: needs a new desktop app release; no server or protocol change. Details: `docs/features/desktop.md`, `docs/features/markdown-input.md`. |
| 2026-09-22 | Desktop app version 0.4.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.4.0.md`; the version was the user's choice, minor because the app gets a new settings category and new bridge members): carries the global hotkeys, the push-to-talk key across the system and the control from outside (shell, native helper and client), the server rail in the user's order (drag and drop, kept in the sealed account settings; client) and Verwaltung > Import of a Discord server template (client tab). The import needs the server image of the same date (its routes and `importSources`), which goes out first; older servers simply show no Import tab. Not tagged yet. |
| 2026-09-22 | Import of a Discord server's structure from a server template (user's question, decisions: "Gast" stays, only adding, bitrates taken over): protocol `import.ts` + `ServerState.importSources`, server `import/discord.ts` (pure plan, tested), `import/fetch.ts` (Discord's public template endpoint, no token), `routes/import.ts` (preview + import in a transaction, MANAGE_CHANNELS and MANAGE_ROLES), client `ImportTab.tsx` as Verwaltung > Import, smoke checks with a real template, `docs/features/import.md`. |
| 2026-09-22 | Global hotkeys, push-to-talk across the system and control from outside in the desktop app (user's wish, also for a Stream Deck or G Hub): Einstellungen > Tastenkürzel with shortcuts for mute and deafen (`globalShortcut`, per device), the push-to-talk key watched across the system by the system watch helper (Raw Input, only the named key, never taken from other programs; Windows), `squorli://control/<action>` links and `--control=<action>` for a second start, a short tone as feedback. Shell, helper and client changed: needs a new desktop app release. Details: `docs/features/hotkeys.md`. |
| 2026-09-22 | Desktop app version 0.3.8 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.8.md`; the version was the user's choice): carries one voice connection per account and server (the confirm dialog before a join that ends the account's voice elsewhere, the hang-up with a notice on the other client) and the voice notices and errors as a modal; client only, the shell is unchanged. The ending of the other connection needs the server image of the same date (`voice.moved` reason `elsewhere`), which goes out first; the phone changes of the day (push-to-talk button, member list panel, hang-up to the channel list) never show in the app. Not tagged yet. |
| 2026-09-22 | Voice notices and errors are a modal with one close button, closed by a click beside it or Escape (user's wish): `showNotice()` in dialogs.tsx, effects in App.tsx; the dock and the stage lost their message lines; a failed join on the phone closes the sheet and shows the modal. Details: `docs/features/voice-video.md`. |
| 2026-09-22 | One voice connection per account and server (user's wish): a join from a second device or tab ends the account's voice elsewhere on that server (`voice.moved` reason `elsewhere`, LiveKit removal in another room), the old client hangs up with a notice, the new client asks with a confirm dialog first (phone sheet closes for it). Details: `docs/features/voice-video.md`. |
| 2026-09-22 | Member list on a phone (user request): a `users` button at the right end of the server head slides the member list in from the right over rail and navigation, with a header and close button of its own; closes by itself when content opens, home toggles or the server changes. Details: `docs/features/ui-admin.md`. |
| 2026-09-22 | A sign-in refused for want of an invite no longer lists the server for the directory account (user's report): the membership is read before the directory lookup, `directory.refresh(user, member)` sends `?member=0` for non-members (login and `/api/me`), a new member is looked up again after joining. Directory counterpart of the same date. Details: `docs/features/directory.md`. |
| 2026-09-22 | Hanging up on a phone's stage shows the channel list again instead of the server's first text channel (user's report; `hangUp` in App.tsx). Details: `docs/features/voice-video.md`, mobile voice flow. |
| 2026-09-22 | Push-to-talk on a phone or tablet (user's wish): no key to assign there; the stage shows a hold button above its bar (`PushToTalkButton.tsx`, `.stage-ptt`) that opens the microphone while it is pressed, and the dock (minimised voice chat) the same button in the place of its mute button; the mute buttons are left out meanwhile (back while muted), the icon is the sound waves so it is not taken for mute, and the settings show a text instead of the key. Checked with touch events in headless Chrome. Details: `docs/features/voice-video.md`. |
| 2026-09-22 | Desktop app version 0.3.7 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.7.md`; the version was the user's choice): carries the gap between the home view's conversation and the window's right edge; client only, the shell is unchanged. It also goes out with the server image for browsers; nothing needs a newer server. Not tagged yet. |
| 2026-09-22 | Home view (friends and direct messages): the main area kept no gap to the window's right edge, because that gap belongs to the member list's margin and the home view has no member list (user's report from the website's screenshot). `.app.home .main { margin-right: var(--gap) }` in `styles.css`; the narrow layouts already had their own rule. Client only: needs a desktop app release. |
| 2026-09-22 | Desktop app version 0.3.6 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.6.md`; the version was the user's choice): carries the crop editor for the profile picture; client only, the shell is unchanged. It also goes out with the server image for browsers; the directory's route is unchanged, so no newer directory is needed. Not checked before the release: the editor inside the desktop app itself and a real touch screen. Not tagged yet. |
| 2026-09-22 | Avatar crop editor (user's wish): a chosen profile picture opens an editor in the settings' profile tab (`AvatarEditor.tsx`, geometry `avatarCrop.ts`): drag, zoom by slider, wheel or pinch, keyboard, a circle showing the visible part; "Zuschneiden und speichern" cuts the chosen square out and uploads it as before. `avatarImage.ts` split into decode and render; `store.setAvatar` takes the finished image. The same editor on the directory's account page. New icons `zoom-in`, `zoom-out`. Notes: `docs/features/directory.md`. |
| 2026-09-22 | Desktop app version 0.3.5 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.5.md`; the version was the user's choice): carries the viewer's statistics of a screen share and the typing line above the message input; client only, the shell is unchanged. Both also go out with the server image for browsers; nothing needs a newer server or directory. Not checked before the release: the statistics against a real received share, the pop-out window and fullscreen. Not tagged yet. |
| 2026-09-22 | The viewer's statistics of a screen share: a button in the tile's actions (stage and pop-out window) shows codec and decoder, picture size, fps, bitrate, loss, round trip, jitter buffer, decode time, frame spacing, freezes, dropped frames, keyframes and a diagnosis line, read every second from the receiver's raw report (`VideoStatsOverlay.tsx`, `voice/videoStats.ts` pure and tested, `VoiceClient.videoReceiveSample`). Plan for a share that adapts by itself: `docs/PLAN-share-adaptation.md`, open decision 8 in `docs/PLAN.md`. Client only; needs a desktop app release. |
| 2026-09-21 | The typing line ("... schreibt") sits above the message input instead of below it, so that the input and the dock with the mini profile stand on one base line, and the empty input is as high as the dock's name row (`--base-row-h`; user's wishes; `ChatView.tsx`, `DmView.tsx`, `.composer` in `styles.css`; measured in headless Chrome). Client only: goes out with the server image (browser) and with the desktop app version after 0.3.4 (whose tag was already pushed). |
| 2026-09-21 | Desktop app version 0.3.4 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.4.md`; the version was the user's choice): carries the game share tuned for motion (hint "motion", frames before resolution, 8 or 6 Mbit/s) and H.265 on the graphics unit for "Quick Share" and the share dialog, which fixes the stuttering H.264 share of 0.3.3; client only, the shell is unchanged. Nothing ships with the server image instead and nothing needs a newer server. Not tagged yet. |
| 2026-09-21 | A game's share was laggy with artifacts (user's report): the share of moving pictures (H.264/H.265) now goes out with the hint "motion", "maintain-framerate", 8 or 6 Mbit/s and VP8 as the backup codec (`voice/screenShareOptions.ts`); measured cause: with "detail" OpenH264 made 13-14 frames per second. H.265 where the computer can send it (its graphics unit encodes it, measured on the RTX 4080; `movingCodec`, `VoiceClient.supportsH265()`), else H.264, for "Quick Share" and the dialog's preselection for games. Fixed on the way: a screen share's limit must be `screenShareEncoding`, LiveKit ignores `videoEncoding` there. Client only; needs a desktop app release, no server change. |
| 2026-09-21 | Desktop app version 0.3.3 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.3.md`; the version was the user's choice): carries the reworked share dialog (desktop widgets left out, two tabs with windows first, games and full screen windows on top, a window's audio ticked, codec choice VP8 or H.264) and "Quick Share" in the dock; shell, native system watch helper and client changed. Nothing ships with the server image instead and nothing needs a newer server or directory; browsers keep their own share dialog. The notes promise no hardware encoding for H.264 (measured: software with LiveKit). Still untested: Quick Share's button with a real game, a second client (Firefox, Safari) watching an H.264 share, the packaged app, Linux. Not tagged yet. |
| 2026-09-21 | Desktop app's share dialog (user's wishes): desktop widgets (Rainmeter, tool windows) left out of the list (system watch helper answers a `windows` question; `captureSource.ts` `isDesktopWidget`), two tabs with windows first (games, then windows in full screen, then the rest by name; `sortWindows`), a window's audio ticked from the start (never the app's own), codec choice VP8 or H.264 with the chosen one's advantage (H.264 preselected for a detected game's window, `ScreenSource.gameId`), "Quick Share" in the dock while a game is detected (always H.264, no dialog; `screenPick.ts`). `VoiceClient` captures and publishes a share in two steps so the picker's codec applies. Found by measuring: with LiveKit H.264 is encoded in software. Notes: `docs/features/voice-video.md`. Needs a desktop app release (shell, native helper and client). |
| 2026-09-21 | Desktop app version 0.3.2 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.2.md`; the version was the user's choice): carries the sealed settings of the directory account with the game display's hide list inside (web client the app shows; `gameDetection.ts`, `store.ts`), the local game icons in Einstellungen > Spiele (shell: `games/icons.ts`, `DetectedGame.icon`), the game line without "Spielt" and the sharper hints of the games tab. The sealed settings need a directory with `features.settingsSealed`, which goes out before the app; browsers get everything but the icons with the server image alone. Still untested in a running app: the settings' new line and the games tab with its icons (the icon reader ran in Electron on the user's computer, the store against a directory test instance), two real devices, GOG, the packaged app. Not tagged yet. |
| 2026-09-21 | Einstellungen > Spiele shows each game's icon from the computer itself (user's wish; never from the directory, which would learn what is installed): Steam's local cache, the Xbox app's logo pictures, the executable for Epic, GOG and added programs (`apps/desktop/src/main/games/icons.ts`, `InstalledGame.icons`, `DetectedGame.icon`). The game line shows the name without "Spielt" (user's wish; key `game.playing` removed). Notes: `docs/features/games.md`. Needs a desktop app release. |
| 2026-09-21 | Sealed settings (user's wish: only the user can read what the directory account stores about them): the web client encrypts the account's settings with a key from the identity's seed (`deriveSettingsKey`, `sealSettings`, `openSettings` in the protocol's `directory.ts`; action `settings-sealed`, `features.settingsSealed`), seals plaintext settings it finds at once, and writes nothing in the open any more; the game display's hide list follows the account inside the blob (launcher ids only, merged so that a hidden game is never suddenly shown). Texts in Einstellungen say so. Notes: `docs/features/settings.md`, `docs/features/games.md`. Needs the directory's deployment first, then a server image and a desktop app release. |
| 2026-09-21 | Desktop app version 0.3.1 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.1.md`; the user chose the patch step over the recommended 0.4.0): carries the link previews in channels and direct messages and YouTube videos in the chat (all in the web client the app shows), the app's own link lookup for previews in direct messages (bridge `lookUpLink`, so the directory is not asked) and the sound of chat videos on the screen share output device (bridge `setChatPlayerOutput`). Previews in channels need the server image too (`Message.previews`, migration 0024), previews in direct messages a directory with `features.dmPreviews`; both go out before the app. Browsers get everything but the two bridge features with the server image alone. Still untested in a running app: a direct message with a preview sent from the app end to end (its lookup and the client's sending were checked separately), the packaged app, Linux. Not tagged yet. |
| 2026-09-21 | Link previews in direct messages (user's wish): the sender's client makes the preview and sends it inside the encrypted message (`DmPreview` in `dm.ts`); the desktop app looks the link up itself (bridge `lookUpLink`), a browser asks the directory; the picture is drawn small, encrypted by the client and stored as a blob at the directory; the x sends an encrypted instruction (`DmControl`) that removes the preview on both sides. The guarded lookup moved into the new package `packages/link-preview` (server, desktop app, copy in the directory repo). Needs the directory update (`features.dmPreviews`) and, for the app's own lookup, a new desktop app release. |
| 2026-09-21 | Link previews (user's wish): links in channel messages get a preview made by the server (title, description, picture; public hosts only, the picture is served from `DATA_DIR/previews`, so readers never contact the linked host), YouTube links a video that plays in the chat once the reader presses play; the author removes each with its x (`POST /api/messages/:id/previews/remove`), `<https://...>` writes a link without a preview. `Message.previews` optional (no protocol bump), `messages.previews` (migration 0024), `LINK_PREVIEWS`. In the desktop app a chat video's sound follows the screen share output device (bridge `setChatPlayerOutput`, frames marked `#squorli-chat`): needs a new app release. Shared scanner `scan.ts` for mentions and links; `readBareUrl` moved into the protocol package. Notes `docs/features/link-previews.md`. |
| 2026-09-21 | Desktop app version 0.3.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.3.0.md`; the user chose the minor step over 0.2.1): carries the AFK signals of the system watch helper (controller input, a playing video for at most 4 hours; Windows), the game detection with Einstellungen > Spiele (Windows) and the game display (reporting the running game, "Spielt X" with the directory's icon). The game display needs the server image too (`Member.game`, the hub's game per connection) and a directory with the game library, which the public one has; browsers get the display with the server image alone. Still untested in a running app: the video rule, the way between two real clients, GOG on a real installation, a packaged app with the new helper. Not tagged yet. |
| 2026-09-21 | Game display, stage 3 step 3: every client shows "Spielt X" with the game's icon in the member list (in place of the handle line, and in the member's menu), the friends list and the head of a conversation. Name and icon of a launcher's game come from the directory's library, never from the playing client; what the directory does not call a game shows nothing; a game by name alone shows the reported name with a gamepad (`shownGameOf`, `GameLine.tsx`). Client only. Needs a new desktop app release to reach app users; browsers get it with the server image. |
| 2026-09-21 | Game display, stage 3 step 2: the desktop app reports the running game with the `activity` state, to the directory (friends) and, unless the user keeps it to friends, to the chat servers (`GamePresence`, `Member.game`, `Friend.game`; no version bump). Only a launcher's id ever leaves the computer, never an added program's path; the directory's library decides what is a game. The two switches moved into the account's settings (`AccountSettings.games`), the hide list stays per device. Server: game per connection in the hub, changes at most every 5 s. Nothing displays a game yet (step 3). Needs the server image, the directory and a new app release together to have any effect. |
| 2026-09-21 | Protocol: the directory's game library (`LibraryGameId`, `DirectoryGame`, `directoryGameUrl`, `directoryGameIconUrl`, `features.gameLibrary` in `directory.ts`, copied to the directory repo). The directory now answers launcher game ids with name, icon and whether it is a game; nothing here uses it yet. No release needed for this alone. |
| 2026-09-21 | Game detection in the desktop app on Windows, stage 2 of the game display: local only, nothing is sent. The shell reads the installed games of Steam, Epic, GOG and the Xbox app (`apps/desktop/src/main/games/`), the system watch helper reports when a program from one of those folders, or one the user added, is in front (and never another program), Einstellungen > Spiele has the switch (off by default), "Gerade erkannt" and the list with a tick per game (`gameDetection.ts`, `GamesTab.tsx`, per device). The helpers' build moves a running helper aside. New feature notes `docs/features/games.md`. After the user's first test two fixes on the same day: the client's subscription survives React's development mode, and the helper finds a game by its topmost window (also one that ran before), checked against a real Steam game on the user's computer. The user then tested successfully with games from Steam, the Xbox app and Epic (two of them with anti-cheat) and an added program. Linux, icons and more launchers are open. Needs a new app release. |
| 2026-09-21 | AFK: a video playing in another program keeps the user present for at most 4 hours since their last input (`ActivityTracker.setVideoPlaying`, 2 minutes of grace between two videos; the signal is the helper's "display required" state, ignored while Squorli shows video itself because it then holds that state; never sent to anybody). The user's tests of the helper (controllers without focus, the display state per browser and in voice) were all successful. The rule in the running app is NOT tested yet. Desktop app on Windows only; needs a new app release. |
| 2026-09-21 | AFK: planned that controller input and running videos (at most 4 hours without input) count as activity, no fullscreen signal; a game display is a later, separate feature (decisions in `docs/features/afk.md`, plan in `../squorli-directory/docs/PLAN-activity-games.md`). Built: controller input counts as activity in the desktop app on Windows. Chromium's Gamepad API only delivers while the window has the focus (the user's test), so a second native helper (`apps/desktop/native/system-watch`, XInput plus Raw Input for every HID joystick and gamepad, only real steps count) reports it through the shell (`systemWatch.ts`, bridge `onSystemActivity`, `platform.systemActivity`, `apps/web/src/systemActivity.ts`). The helper also reports whether a program keeps the display on (a playing video); that is shown in the debug view only, the video rule is NOT built yet. Input with the user's controllers while a game is in front is NOT tested yet. Needs a new app release (with the helper built by the release workflow); the server image alone changes nothing. |
| 2026-09-20 | Desktop app version 0.2.0 (`apps/desktop/package.json`, release notes `apps/desktop/release-notes/0.2.0.md`; the user chose the minor step over 0.1.9): carries the start window with the update at the start, the client's start screen instead of a flashing "no server yet", the see-through window's start and login cards and its focus outline, and the radio that turns itself off. The radio part needs the server image too (`POST /api/channels/:id/radio/offline`, `ended` for a single video). First release whose notes the workflow takes from the repository. Not tagged yet; a real update at the start is still untested. |
| 2026-09-20 | Desktop app: a start window (`splash.ts`) shows the update check, downloads and installs an update before the client opens ("install later" leaves it to the background; a version is tried at a start only once), and stays until the client knows its first screen; the client shows a start screen instead of a flashing "no server yet" (`Store.starting`). See-through window: start card and login card look like a hovered chat message, and the window shows a 1 px outline in the title bar's colour while focused. A real update at the start is NOT tested yet. Needs a new app release. |
| 2026-09-20 | A radio with nothing left to play turns itself off: a single YouTube video or a queue's last video that ended (`radio/advance` with `ended`; a skip by hand still goes around), and a Twitch stream that is over or a channel that is not live, 60 s after the player said so (`POST /api/channels/:id/radio/offline`). Replaces the queue going around at its end. Needs the server image and the client (browser: reload; app: next version). |
| 2026-09-20 | Desktop releases carry release notes: `apps/desktop/release-notes/<version>.md` goes into the GitHub draft release (`desktop-release.yml`), written by the new skill `desktop-release`, which also proposes and sets the version. First file: 0.1.8 (its tag was already pushed: paste by hand). |
| 2026-09-20 | Desktop app version 0.1.8 (`apps/desktop/package.json`; user's request): carries the message cue with the task bar and tray mark, "start with the system" with the choice of a background start, the embedded players' sound on the radio's output device, YouTube playlists as a queue, the tile for a video source while the radio is off, the x on the player, YouTube without captions. The queue needs the server image too (migration 0023, `POST /api/channels/:id/radio/advance`). Not tagged yet. |
| 2026-09-20 | A cue for new direct messages and mentions in every client (settings > Töne, follows the account through the optional `SoundSettings.message`); desktop app: a count on the task bar icon and a dot on the tray icon for them, and "start with the system" under Ansicht (Windows login item, Linux autostart entry; whether such a start stays in the background or opens the window is a second switch). Needs a new app release and, for the setting to follow the account, a directory deployment. |
| 2026-09-20 | The embedded YouTube player starts every video without captions (a muted start turned them on by itself; `onApiChange` is answered with an empty caption track, `youtube.ts`, `embedControl.ts`); the viewer can still turn them on with the player's CC button. Twitch has them off by default. |
| 2026-09-20 | Embedded Twitch/YouTube player: an x next to the pop-out button turns the radio off for oneself (same as the radio menu's switch); the iframe itself is rounded by a clip-path (video stood out over the tile's corners, confirmed fixed by the user); desktop app: the players keep their sound after a queue's video change (the audio permission check stays open while a device is chosen). |
| 2026-09-20 | Desktop app: the sound of the Twitch/YouTube players follows the web radio's output device (the shell sets it inside the player's frame by the device's label, `apps/desktop/src/main/playerAudio.ts`; in a browser the players stay on the system's default device, no page can route a foreign iframe). Needs a new app release. |
| 2026-09-20 | YouTube playlists as a radio source: the starting member's client reads the video ids from YouTube's player and hands them to the server, which keeps the queue (`channels.radio_queue`, migration 0023), moves on when the listeners' players report the end, wraps around, and lets CONTROL_RADIO skip (`POST /api/channels/:id/radio/advance`); one player for the whole queue. Fixed on the way: a shared place behind a video's end restarted the video every few seconds. Protocol additive, no version bump. |
| 2026-09-20 | Radio turned off for me while the channel's radio is a Twitch or YouTube source: the stage shows a tile that says a video runs and turns the radio back on (button, or volume slider that applies on release; `PlayerOffTile` in `VoiceStage.tsx`). The tile can be dismissed and returns with the next video. Until then nothing told such a user that a video was running. Client only. |
| 2026-09-19 | Stereo channels: phones and tablets capture with the browser's echo cancellation instead of raw stereo (the others heard themselves back from an iPhone on its loudspeaker, and heard the iPhone in one ear); on computers a stereo capture with the signal on one channel only is put on both sides (`voice/oneSided.ts`). Not tested on an iPhone. |
| 2026-09-19 | An AudioContext that iOS left "interrupted" (microphone prompt, start of the capture) is resumed like a suspended one (`contextNeedsResume()` in `voice/gate.ts`; pipeline, gestures, cues, `onstatechange`): the iPhone showed no microphone level after the prompt. Not tested on an iPhone. |
| 2026-09-19 | A refused microphone says why (`voice/micPermission.ts`: stored refusal, refused without asking, kept back by the system) instead of WebKit's one text; the microphone request is the first thing in the joining tap; the mobile join sheet shows the explained text. The iPhone home screen refusal itself is still open. |
| 2026-09-19 | The web client on a phone's home screen: Squorli icons (`tools/web-app-icons.mjs`, `apps/web/public/app-icons/`), manifest as the server route `/api/manifest.webmanifest` with the name "Squorli - <server name>", Apple tags in `index.html`. Not tested on a phone. Notes: `docs/features/home-screen.md`. |
| 2026-09-19 | The microphone is asked for inside the tap that joins (`VoiceClient.prepareMic()` in `App.joinVoice()`), and `join()` takes that capture over: WebKit refuses a prompt seconds after the tap, so an iPhone's home screen app could join no channel. Not tested on an iPhone. Notes: `docs/features/voice-video.md`. |
| 2026-09-19 | Camera picked in the dialog is really opened (LiveKit ignored the choice once a switched-off camera track existed: `voice/cameraSwitch.ts`); a changed send resolution takes effect on a running camera. Phones and tablets recognised by the device (`platform/mobile.ts`, `Platform.mobile`): wider microphone boost limits (x40, the iPhone never reached the threshold), 360p as the default applied once, rear camera not mirrored, no screen share button without `getDisplayMedia`, raw microphone level in the debug view. Not tested on a real phone. Notes: `docs/features/voice-video.md`. |
| 2026-09-19 | Settings and server administration focus Close on mobile opening; desktop profile autofocus retained. |
| 2026-09-19 | Server administration adopts the mobile settings layout: full viewport, categories on the left, independent scrolling, stacked role/channel forms and larger touch controls. |
| 2026-09-19 | Mobile settings use the full viewport, compact categories on the left, independent scrolling, larger touch controls and wrapping session/license content. Desktop unchanged. |
| 2026-09-19 | Disable mobile page zoom through viewport and touch-action; mobile form controls at least 16 px to avoid focus zoom. |
| 2026-09-19 | Mobile-only navigation: full-height rail beside channels/friends, chats slide in across the viewport, back arrow, voice preview from below with explicit Connect, minimizable full-screen stage and bottom actions. Desktop behavior preserved. Notes: ui-admin.md and voice-video.md. |
| 2026-09-19 | Desktop app version 0.1.7 (`apps/desktop/package.json`; user's request): carries the role menu that only offers what works and the header controls of one height. The server part (first owner sets other owners' roles) ships with the server image, not with the app. Not tagged yet. |
| 2026-09-19 | Roles of owners (user's decision): only the first owner may change another owner's roles (`canSetRolesOf`), and the member menu offers "Rollen" and single roles only where the server would accept them (`memberRank.ts`). Header controls of a voice lobby share the layout switch's height, icons centered, icon buttons keep no ground (`--head-control-h`). Notes: `docs/features/ui-admin.md`. |
| 2026-09-19 | Desktop app version 0.1.6 (`apps/desktop/package.json`; user's request): carries the avatars of the directory account and their upload from the settings. Not tagged yet: the release follows `apps/desktop/AGENTS.md`, "Packaging and releases". |
| 2026-09-19 | Avatar upload from the client (user's wish): the settings' profile tab chooses, uploads and removes the avatar of the directory account (`avatarImage.ts`, `api.directorySetAvatar`, `store.setAvatar`, `state.directoryAvatars`); own avatar comes from the directory account, so it changes at once. Notes: `docs/features/directory.md`. |
| 2026-09-19 | Avatars from the directory account (user's request): protocol `avatar-set`, `AvatarUpdateRequest`, `directoryAvatarUrl`, `DirectoryAccount.avatarUpdatedAt`, `Friend.avatarUpdatedAt`, `Member.avatarUrl`, `Me.avatarUrl` (defaults, no version bump; copied to the directory repo); server caches the address in `users.avatar_url` (migration 0022) at sign-in, on the 5-minute sync and on the directory's push; the web client shows the image instead of the initials everywhere (`Avatar.tsx` `src`). Upload, storage and the public image route live in `../squorli-directory`. Notes: `docs/features/directory.md`. |
| 2026-09-19 | Login (user's wish): on a server that takes new members by invite only the invite code field is shown from the start (`inviteRequired` in `/api/health`, no protocol bump); an invite link's code stands in the field and "In der App öffnen" carries it (both already worked, now checked). Desktop app version 0.1.5. |
| 2026-09-19 | Web login password eye buttons moved inside the input edge, with text padding and preserved 44 px targets. |
| 2026-09-19 | Web sign-in handle field now shows a fixed @ prefix and removes typed/pasted leading @ characters to avoid duplicates. |
| 2026-09-19 | Server login: visible language buttons instead of dropdown; account-creation link on its own row with a bilingual explanation of the configured Directory destination. |
| 2026-09-19 | Login follow-up: account creation now links to the configured Directory, removing the local registration form; eye/eye-off controls added to sign-in and existing-account password-backup fields. |
| 2026-09-19 | Server login reorganized into exclusive account/local/registration views. Local access no longer contains handle or e-mail registration; labelled vertical fields, clearer copy, scoped spacing and collapsed key details. Account-only policy and registration/backup flow retained; six view regression tests. |
| 2026-09-19 | Video is each viewer's choice (user's requirement): a screen share is no longer shown to everyone by itself, its tile offers "Ansehen" and it is only received after that; cameras still show by themselves; every remote feed can be turned off for yourself (eye button on the tile, context menu), not watching = not subscribed; choices live in memory and end with the feed (`voice/videoWatch.ts`, `VoiceClient.setVideoWatching`). Tile view: button to hide participants without video, only while any video is sent, never stored. Website guide (de/en) got a section on it. |
| 2026-09-19 | Settings (user's wish): "Sprechen" and "Audiogeräte" are one tab "Sprache und Audio"; new microphone test there (you hear yourself behind the boost, before the gate; `MicPipeline.setMonitor`, `VoiceClient.startMicTest`). While it runs in a voice channel the published microphone is muted, so nothing of the test is sent. Fixed on the way: the level meter was squeezed to a line in the longer tab (`.meter` `flex-shrink: 0`). Website guide (de/en) names the new tab. |
| 2026-09-18 | Microphone boost (user's report of very low microphone levels): gain with a clipping guard in front of meter, gate and track, automatic by default or 100-600 % by hand (Einstellungen > Sprechen, per device). Cause of the report found with the user's answer: STEREO channels capture without the browser's gain control, the microphone arrived at its raw level and the gate never opened; the boost now runs there too (up to x12, remembered per microphone and capture mode). In mono channels Chromium already normalizes a quiet capture (`docs/features/voice-video.md`). |
| 2026-09-18 | "Kamera: failed to publish track, insufficient permissions" (user's report): LiveKit's publish grants of members sitting in a voice channel now follow a change of their roles, of a role's permissions, a role's deletion and the owner status (`livekit/sync.ts`); before, the camera button appeared but LiveKit refused until the member rejoined. |
| 2026-09-18 | Directory with `REQUIRE_EMAIL` (user's request in the directory repo): protocol takes `email` + `emailCode` at registration and `features.emailRequired`; the login registers a handle in two steps (address, then the mailed code). No protocol version bump, server unchanged. |
| 2026-09-18 | A language change no longer ends voice: the reload waits until the voice connection has ended (`store.reloadForLocale`, hint in Einstellungen > Ansicht). User's requirement. |
| 2026-09-18 | Voice channel list survives a server restart: at the hello the server restores a member's presence from LiveKit's participant list (for clients that do not announce their channel again, desktop app up to 0.1.2), checked again in the heartbeat; no join sound on it (cues only follow LiveKit). User's report. |
| 2026-09-18 | AFK and voice presence survive dead connections: server heartbeat (`ws/liveness.ts`: ping, terminate after 90 s, silent clients count as idle), client says its voice channel again after a reconnect and replaces a deaf socket (chat and directory connection; the directory service got the same heartbeat). User's report: not moved after 20 minutes, a member in two channels. |
| 2026-09-18 | Desktop app: texts no longer speak of "this browser": overlay catalogs `i18n/app.de.ts`/`app.en.ts` (21 keys), taken first by `t()` in the app; `lang.autoSystem` folded into them. |
| 2026-09-18 | Desktop app: system-wide AFK detection always runs (the shell grants the permission by itself), the settings show an explanation instead of the browser's switch and permission text (`platform.systemIdle`). |
| 2026-09-18 | Desktop app: the "shared without audio" hint no longer talks about a browser dialog; it names the app's picker, or that share audio is Windows only. |
| 2026-09-18 | The whole voice stage can be moved into a window of its own (user's wish): `StageWindow.tsx`, shared `openPopoutWindow()`, stage built from the voice connection's server; menus, observers, dialogs, push-to-talk (`usePushToTalk.ts`) and further pop-outs work from that window; the radio's player stays in the main window. Web and desktop app. |
| 2026-09-18 | Desktop app P4 after release `desktop-v0.1.0` (first workflow run failed at `pnpm licenses`, pnpm's own command; fixed with `pnpm run`, test builds without a tag added): auto-update with electron-updater against the feed on squorli.com (settings category "App", restart button in the title bar, deb = notice with the download page), app version 0.1.1; website: `pnpm desktop:release`, update feed, download pages DE/EN, product wording, new screenshots; web login offers "In der App öffnen" and "App herunterladen". |
| 2026-09-18 | After the user's second look: tray icon with open/quit and the setting "keep running in the tray when the window is closed" (off by default); the server rail is a rounded area with mica too; client (browser and app): own scrollbars, a share's audio plays only for who selected that share in the stage or popped it out (muted and disabled at LiveKit otherwise), the dock's disconnect message keeps off the edge. |
| 2026-09-18 | Desktop app: audio of a single window and system audio without the app (user's wish). Native helper `squorli-window-audio.exe` (C++, WASAPI process loopback, built by `apps/desktop/native/build.mjs`, packaged as a resource, built on the release workflow's Windows runner); the shell streams its PCM to the client, an AudioWorklet turns it into the share's audio track, the voice client publishes and ends it. The picker's audio choice follows the chosen source. Also ends the echo of the app's own playback in a share. |
| 2026-09-18 | Desktop app: own slim title bar (frameless window in every mode, `TitleBar.tsx`, overlays start below it) and new background modes after the user's decision: opaque, mica, or a really see-through window whose gaps show the desktop (`transparent: true`, applies after a restart; acrylic dropped because Windows turns materials solid on focus loss and cannot limit them to elements). Decided as well: audio of a single window through a native module, before P4. |
| 2026-09-18 | After the user's first look at the desktop app: the window reopens where it was closed (`windowState.ts`), the unpackaged app shows Squorli's icon (window icon + AppUserModelID); client (browser and app): gaps between the areas halved to 6 px, the member list is a panel of its own, channel list and member list resizable by drag handles (`layout.ts`, `ColumnHandle.tsx`, per device). Open decisions recorded in `docs/features/desktop.md`: see-through window vs. Windows materials together with the custom title bar; audio of a single window (needs a native module). |
| 2026-09-18 | Desktop app P5 + P6: `squorli://server/<host>` and `squorli://invite/<host>/<code>` reach the app at start and from a second start (single-instance lock) and go through `store.openLink()`/`addServer()` (join view for unknown servers, link wins over the server viewed last); window background on Windows 11 (mica/acrylic + opacity of the client's surfaces, Einstellungen > Ansicht, per device in `desktop-config.json`, no restart). The settings dialog opens without a shown server (no Profil/Sitzungen then). Open: P4 (auto-update, website feed and download page, "open in the app" row). |
| 2026-09-18 | Desktop app P3: packaging with electron-builder (NSIS per user, AppImage, deb; renderer as a resource; fuses; protocol `squorli`; feed address squorli.com), app icon from the brand mark (`tools/desktop-icon.mjs`), release workflow on GitHub for tags `desktop-v<version>` into a draft release, server image jobs restricted to tags `v*` on both pipelines. Windows installer built and the packaged app checked locally; the workflow has not run yet. The deb package's maintainer address was named by the user afterwards (desktop@squorli.com). |
| 2026-09-18 | Desktop app P2b: `labelFromUserAgent` knows the desktop app ("Squorli Desktop auf Windows"), checked before the browsers; copied to `../squorli-directory`. |
| 2026-09-18 | Desktop app P2: `apps/desktop` (`@squorli/desktop` 0.1.0, Electron 44): a shell without renderer code that serves the web client's build from the privileged scheme `app://squorli` (CSP header, traversal guard), sandboxed preload bridge, permission/navigation/window rules, own user agent, screen share through the client's new `ScreenPicker.tsx` with optional system audio on Windows, `pnpm dev:desktop`. Origin decided by a spike: Twitch and YouTube embeds work under `app://` once the shell vouches for the client (two header fixes for exactly these hosts). Dockerfile and both CI test jobs skip Electron's binary. Runs unpackaged; installers follow in P3. |
| 2026-09-18 | Desktop app P1: the client runs without a home server (`platform.home === null`): own login (`DesktopLogin.tsx`: directory account, or the device's key), the account's servers plus servers added by address, invite link or `squorli://` link (`clientHome.ts`; a server the key has never been on shows a join view and waits for a click), the server viewed last per device (`chat.client.v1`), no-servers view, sign-out of all servers; `railServers.ts`. Browser behaviour unchanged. Tried in Chrome with `VITE_HOMELESS=1` against a test server and a test directory. Fixed on the way: the web login never offered "Code per E-Mail" (the store dropped the directory's error body). |
| 2026-09-18 | Desktop app (M4) planned and started. Decisions (user): `apps/desktop` in this repo, Electron, `apps/web` stays the shared client (one build, two shells, no registry), installers in GitHub Releases with the feed on squorli.com, Windows + Linux first, directory account as the default login plus servers by address, last viewed server per device, acrylic/mica window background; all in the new `docs/features/desktop.md`. P0: platform interface `apps/web/src/platform/` without a change in behaviour (`Store` and `VoiceClient` take the platform's facts as constructor arguments; `squorli://` parser with tests). Fixed on the way: the admin panel's invite links named the page's host instead of the server the panel acts on. Also decided: the screen share codec stays VP8, AV1 to be tested later (PLAN 9, item 7). |
| 2026-09-18 | Screen share: the own tab can be picked (user's wish; `selfBrowserSurface: "include"` in `voiceClient.ts` and `/test/screenshare.html`). The audio constraint `restrictOwnAudio` keeps this tab's playback out of the captured audio, so the others do not hear themselves back (also for "entire screen" with system audio). Typecheck, tests, build green; a real share with a listener not checked. |
| 2026-09-18 | Firefox: "Camera: Starting videoinput failed" (user's report). The camera picker stopped its preview only on unmount, after LiveKit had been asked for the same camera, and Firefox releases a stopped camera late and cannot share it. `CameraPicker.pick()` releases the preview first; `voice/cameraRetry.ts` (tested) retries one camera start after 600 ms on `AbortError`/`NotReadableError` (used by `setCameraEnabled` and the preview); a camera held elsewhere is reported as `voice.errCameraBusy`; `AbortError` is no longer swallowed as a user cancel for the camera. `docs/features/voice-video.md`. |
| 2026-09-18 | Firefox: a camera popped out into its own window froze (user's report). LiveKit's adaptiveStream observes attached elements with an `IntersectionObserver` of the main window, which in Firefox never sees an element of another window, so the track was paused. `PopoutElementInfo` (`videoDisplay.ts`, tested) reports a pop-out view from its own window; verified with a headless Firefox nightly and a real `RemoteVideoTrack`. Also: a tile stayed black after somebody turned their camera off, because `setCameraEnabled(false)` mutes instead of unpublishing and `TrackMuted` did not rebuild the tiles; now it does. `docs/features/voice-video.md`. |
| 2026-09-18 | Joining a voice channel failed with "Constraints could not be satisfied" plus the hint to check the proxy (user's report from community.k9-kommando.de; the proxy was fine). Cause: the stored microphone was gone and was requested with `deviceId: { exact }`. `openMic()` (`micPipeline.ts`, tested) falls back to the default microphone and keeps the stored choice; microphone errors get their own message `voice.errMic` instead of `voice.connErrGeneric`; `setInputDevice()` reports errors instead of rejecting unhandled. `docs/features/voice-video.md`. |
| 2026-09-17 | AFK: one fixed time of 10 minutes (user's decision: a time per server makes no sense when the directory shows the same state to friends). `AFK_AFTER_MS` = 10 minutes; `afkMoveMinutes`, `AFK_MOVE_MINUTES` and the select in the admin area removed, migration 0021 drops `server_settings.afk_move_minutes`; the server moves a member to the AFK channel the moment they turn absent. The client's feature flag is now `settings.afkChannelId`. Hub: after the connection in use closed, the remaining idle ones make the user AFK only `AFK_AFTER_MS` later. Texts, README, website guides adjusted. |
| 2026-09-17 | AFK detection (user's requirement and decisions, `docs/features/afk.md`): absent after 5 minutes without input in a Squorli window, without speaking and, in Chromium with the user's permission, without input anywhere in the system (Idle Detection API, switch in Einstellungen > Ansicht, per browser). The clients report `activity` per connection; the chat server's hub and the directory's hub call a user AFK once all connections are idle: `Member.afk` (amber crescent, moon in the sidebar) and `Friend.afk` / `friends.presence.afk` for friends. AFK channel as a server setting (`afkChannelId`, `afkMoveMinutes` 5/10/15/30/60, migration 0020, admin area > Kategorien/Kanäle): absent members of a voice channel are moved there by the server (`voice.moved` with `reason: "afk"`, not out of a channel showing a Twitch or YouTube video), its LiveKit token has no publish and no subscribe grant, no radio; the client locks the controls there and the dock offers "Zurück zu <Kanal>" (never automatically). No `PROTOCOL_VERSION` or `DIRECTORY_WS_VERSION` bump: feature flags `settings.afkMoveMinutes` and `features.afk`. Smoke test 108 checks; its `waitFor` helper no longer removes a foreign waiter when a served one times out. |
| 2026-09-17 | Documentation split by area (user's wish: the single `AGENTS.md` of 500 lines filled the context): root `AGENTS.md` keeps what every task needs plus the map (section 0); new `AGENTS.md` + importing `CLAUDE.md` in `apps/server/`, `apps/web/`, `apps/web/src/voice/`, `packages/protocol/`, `deploy/`, `tools/`; feature notes in `docs/features/`; former sections 8 and 9 are `docs/VERIFIED-STATE.md` and this file. Text moved verbatim, only section references re-pointed. No code change. `../squorli-website/tools/screenshot-source.mjs` now leaves Markdown out of the screenshot fingerprint, because `apps/web/src/voice/AGENTS.md` lies inside a fingerprinted folder; the stale pointer "see AGENTS.md section 1" in the comment of `apps/web/src/emoji/picker.ts` (now `docs/features/emoji.md`) was left alone on purpose: any edit under `apps/web/src` makes the website's screenshots count as stale, fix it with the next real client change. |
| 2026-09-17 | The edit field of a channel message has the `@` suggestion list too (second `useMentionSuggest` in `ChatView`, option `below`, `.mention-suggest.below`). |
| 2026-09-17 | No false alarms for mentions: typed names only when exactly one member carries them; the server checks mention candidates with `mentionedUserIds` (new `packages/protocol/src/mentions.ts`, same rules as the client's parser for code and escapes); marks are counted again after edits and deletions (`recountMarks`). `smoke.mjs` refuses directories that are not on localhost (after a run that registered two test accounts at the production directory). Smoke 90 green. |
| 2026-09-17 | The rail marks every server: all servers of the account are connected in the background (`store.connectAccountServers`). Muting per member on the chat server: `members.muted`, table `channel_mutes` (migration 0016), `PUT /api/channels/:id/mute`, `PUT /api/me/mute`, event `mute.update`, fields in `GET /api/read-state`; right-click menus in the sidebar and on the rail. Mentions still show for muted channels and servers. No protocol bump. Smoke 89 green. |
| 2026-09-17 | Read states on the server: table `read_states` (migrations 0014, 0015 seed), `GET /api/read-state`, `POST /api/channels/:id/read`, server event `read.update` to the member's own connections (no `PROTOCOL_VERSION` bump: clients ignore events they cannot parse). The client syncs marks and mention counters from the server and falls back to the per-device state against older servers. Smoke 85 green. |
| 2026-09-17 | Sidebar: unread channels marked (bold, bar at the edge), channels that mention me highlighted in amber with a counter. Read state per device (`readState.ts`) and a catch-up of all text channels after every welcome, so marks and counters survive a reload and cover messages from an absence. Client only. |
| 2026-09-17 | Mentions in the channel chat: `@` suggestions in the composer, stored as `<@userId>`, shown as a chip with the current name, messages that mention you highlighted, counters per channel and per server (`mentions.ts`, `MentionSuggest.tsx`, `MentionContext`, `ServerConnState.mentions`). Client only; no `@everyone`, roles, sounds or offline tracking yet. |
| 2026-09-17 | Einstellungen > Lizenzen with generated third-party notices (`tools/licenses.mjs`, `LicensesTab.tsx`, `THIRD-PARTY-NOTICES.md`, `NOTICE`, image carries LICENSE/NOTICE/notices). |
| 2026-09-17 | Bug fix: emoji were invisible (blank glyphs) because the slices of `@fontsource/noto-color-emoji` do not render in Chromium; now the COLRv1 slices from Google Fonts, downloaded by `tools/emoji.mjs` into `apps/web/src/emoji/font/` and committed, guarded by `@supports (font-tech(color-COLRv1))` for Safari; the package is removed. The smiley button is as high as "Senden" and has no background (user's wish). Checked with headless Chrome screenshots. |
| 2026-09-17 | Emoji: self-hosted Noto Color Emoji (OFL, unicode-range slices), conversion of `:shortcodes:` and emoticons at display time, emoji picker in both composers (`EmojiPicker.tsx`, `emoji/`, `tools/emoji.mjs`; new dev package `emojibase-data`). Markdown: task lists, `==highlight==`, `~sub~`, `^sup^`. The message input keeps the focus after sending. Browser look not checked. |
| 2026-09-17 | Chat Markdown: GFM tables (alignment, escaped pipes, sideways scrolling wrapper `.md-table`) and a copy button on every code block (`CodeBlock` in `MessageText.tsx`, Clipboard API with fallback, texts `chat.copyCode`/`chat.codeCopied`/`chat.codeCopyFailed`, icons copy/check). Browser look not checked. |
| 2026-09-17 | Message input grows with its content up to a third of the chat area, then scrolls (`AutoGrowTextarea.tsx`, `autoGrow.ts`; channel chat, direct messages, message editing); the history stays at the bottom while it grows. Browser look not checked. |
| 2026-09-17 | Markdown in the text chat, channels and direct messages alike: own parser `apps/web/src/markdown.ts` (no dependency) and shared `MessageText.tsx` replace the two `renderText` copies. Links only http/https/mailto, misleading masked links refused, images never embedded. Messages stay plain text in storage and protocol. 25 new tests; browser look not checked. |
| 2026-09-17 | Bug fix: with the stage open, opening the home view (friends) hid the dock's "Kanalansicht" button although the stage was no longer visible. `showStage` in `App.tsx` is now false while `homeOpen`; the button returns to the stage via `store.openServer()` (closes the home view). Typecheck and tests green, not checked in a browser. |
| 2026-09-17 | Bug fix: the channel's Opus bitrate never reached LiveKit (`audioBitrate` is no SDK option, all channels ran at 48 kbit/s). Now `audioPreset.maxBitrate`, return type `TrackPublishOptions`, test `voice/micOptions.test.ts`. Real rate not measured in a browser. |
| 2026-09-17 | Playback volume per person 0-200 % from the context menu of voice members, stage tiles and the member list (`UserVolumeControl.tsx`, `voice/userVolumes.ts`, `VoiceClient.setUserVolume/getUserVolume/setPeerKeys`, boost above 100 % via a GainNode on the meter source; camera pop-out volume = the same value). Per device, keyed by public key. Client only, no server or protocol change. New icon `rotate-ccw`. Not checked in a browser. |
| 2026-09-17 | Role editor: permissions sorted by meaning and shown in four framed groups (`PERMISSION_GROUPS` in the protocol package, `permGroup.*` texts, `fieldset.perm-group`); the list of active permissions follows the same order. Standing rule for every future permission in section 5. Test: every permission in exactly one group. Not checked in a browser. |
| 2026-09-17 | Permission `VIEW_VIDEO` (bit 14, "Kamera- und Bildschirmübertragungen sehen"): members without it receive no camera, screen or screen audio. Guests do not get it (user's decision): migration 0013 (custom) gives it to every non-default role and leaves the default role alone; `DEFAULT_MEMBER_PERMISSIONS` includes it (24000), `DEFAULT_EVERYONE_PERMISSIONS` stays 1152. Enforcement: `voice/videoAccess.ts` (tested) + `VoiceClient.setVideoAccess()`/`applyVideoAccess()` (senders restrict their tracks at LiveKit) and `applyVideoSubscriptions()` (receiver), `App.tsx` feeds both from the server state, notice `stage.noViewPermission` in `VoiceStage.tsx`. App server untouched apart from the bit, `PROTOCOL_VERSION` stays 4, one new smoke check. Not checked in a browser with real media. |
| 2026-09-17 | All user and profile settings merged behind the gear (`SettingsDialog.tsx`; `ProfileDialog.tsx` and the modal inside `VoiceDock.tsx` removed), mini profile on the own name (`MiniProfile.tsx`, only the display name on the server shown, `store.setServerDisplayName`, `ServerConnection.updateDisplayName`), all settings except the device selection stored in the directory account (protocol `AccountSettings`/`AccountSettingsUpdateRequest`/`parseAccountSettings`, action `settings`, `AccountStatus.settings`, `features.settings`, copied to `../squorli-directory`; `api.directorySetSettings`, `accountSettings.ts`, store adopt/push with `State.settingsSyncError`, `store.setLocale`, i18n `storeLocalePreference`/`accountLocalePreference`), option "Mich selbst als Sprecher hervorheben" (`featureSelfInSpeakerView`, `VoiceStage.tsx`), App-level effects hand mode/threshold/hold time to the voice client. Chat server untouched, `PROTOCOL_VERSION` stays 4. Directory: migration 0011, `POST /api/settings`. |
| 2026-09-17 | E-mail in the directory (M6c remainder, implemented in `../squorli-directory`): protocol `EmailCode`/`EmailAddress`/`EmailUpdateRequest`/`EmailVerifyRequest`/`EmailCodeRequest`/`EmailCodeResponse`, actions `email-set`/`email-verify`/`email-code`, `AccountStatus.email`+`emailPending`, `KeyFetch.factor` "email" (source here, copied there); web client: `api.directoryEmailCode(dirUrl, handle, password)`, login screen shows "Code per E-Mail senden" when the 401 `totp_required` body says `email: true`, messages `dir.email_unavailable`/`no_email`/`mail_failed`/`totp_disabled`, `login.emailCode*`. Typecheck 3/3, tests 29 + 13 + 38, web build green. |
| 2026-09-16 | Voice cue settings follow the directory account (user's wish): protocol `SoundSettings`, `directorySoundSettingsPayload`, `SoundSettingsUpdateRequest`, action `sound-settings`, `AccountStatus.soundSettings` (copied to `../squorli-directory`); web client `api.directorySetSoundSettings`, store adopt/push logic with `State.soundSyncError`, `voice/settings.ts` as external store + `useVoiceSettings`, sounds tab shows account/device and errors; per-device copy stays the fallback for servers without a directory. Web tests: sounds.test.ts +1, settings.test.ts new (3). |
| 2026-09-15 | Account deletion on a chat server through the directory (user's wish: from the server list on the account page and via right-click on the server in the client): protocol `server-leave` + request/response schemas and `AccountServer.leaveRequestedAt` (copied to the directory repo); server `POST /api/directory/leave` -> `users/deleteUser.ts` (founder refused, confirm at the directory with the token, delete the user with cascade + attachment files, WS close 4012), `DirectoryClient.confirmLeave/pendingLeaves`, `hub.closeUser`; web client: rail context menu with "Konto auf diesem Server löschen" (`askConfirm`, `store.leaveServer`, `api.directoryLeaveServer`, 4012 -> login with "Dein Konto auf diesem Server wurde gelöscht"), icons external-link/user-x. Directory: migration 0008, routes `/api/servers/leave`, `/leave/confirm`, `/leaves`, account page button. Smoke 91 / 107. |
| 2026-09-14 | Multi-server client (user's requirement): the rail switches between servers inside the client, the voice connection stays on its server and is only replaced when joining a voice channel elsewhere. `ServerApi` class with base URL, `ServerConnection` per server (`serverConnection.ts`), store with `servers`/`activeHost`/`openServer()`, sessions per host (`chat.sessions.v2`), `voiceHost` in App, rail entries as buttons with voice/unread badges, `ServerStatus` for foreign servers; chat server CORS open for all origins (Bearer auth only). Smoke 78 without directory. |
| 2026-09-14 | M7 friends and end-to-end encrypted direct messages via the directory (user's wish; plan `../squorli-directory/docs/PLAN-friends-dm.md`): protocol `friends.ts` + `dm.ts` (+ tests, `@noble/curves`), copied to the directory repo; client `directoryLink.ts` (second socket, challenge signature), store (friends, presence, conversations, decrypted threads, pair-key cache), `Home.tsx`/`DmView.tsx`, Squorli logo with badge at the top of the rail, member menu with right-click and "Als Freund hinzufügen"/"Nachricht schreiben". Chat server untouched. Directory: migration 0007, `GET /api/ws`, handle search, signed `friends` action for the account page. |
| 2026-09-14 | Server registration at the directory (user's wish: a server reads only its own users' names): `DirectoryClient` with an own Ed25519 key in `server_settings.directory_private_key` (migration 0010), `serverKey` in `/api/health` as host proof, registration after `app.listen` with a 24-h token, re-registration on 401, public fallback without token; `DIRECTORY_PROOF_URL` (config, both env templates, compose). The client-side `server-seen` is gone (the directory records logins from the server's lookups). Every 5 minutes the server syncs all users' handles/names via `POST /api/servers/resolve` (user's requirement: names changed on the account page must reach the servers) and takes pushes from the directory on `POST /api/directory/notify` right away. Smoke +2 (78 with directory). |
| 2026-09-14 | Display names from the directory (user's wish): the directory stores a global name and one per chat server; `refreshFromDirectory()` (formerly `refreshHandle()`) takes `serverDisplayName ?? displayName` at login and on stale `GET /api/me` (5 min), profile dialog shows "Anzeigename auf diesem Server" + "auf allen Servern" for directory accounts and stores both signed at the directory (`profile-update`), client reports `server-seen` after login for the server list on the account page. Protocol: `DisplayName` moved from `index.ts` to `directory.ts` (copied to the directory repo), `ServerHost`, `ProfileUpdateRequest`, `ServerSeenRequest`, `AccountServer`, `DirectoryAccount.displayName/serverDisplayName`. Smoke +3. |
| 2026-09-14 | M6d completed: server directory. Registration carries listed/description/openJoin/memberCount (chat migration 0012, admin panel > Server), the directory stores the server icon itself (its migration 0006) and serves `GET /api/servers/<host>/icon` and the public list `GET /api/servers`; the rail loads icons only from the directory, "Server entdecken" modal (`ServerBrowser.tsx`) with join via link; re-registration after settings/icon changes (debounced). Smoke tests 86 / 65. |
| 2026-09-14 | Server rail (M6d): column at the far left with all servers the own handle has logged in on (signed `account-status` at the directory), links to the servers, icons via their public `/api/server-icon`, current server highlighted. Only with `DIRECTORY_URL`. |
| 2026-09-14 | Login splash with server icon and name, footer "Betrieben mit Squorli · Version" (`version` in `/api/health`), setting "Nur mit Konto" (`requireAccount`, migration 0011, 403 `account_required`, owners exempt), forceable at deployment via `REQUIRE_ACCOUNT` (`requireAccountLocked`, 409 `locked_by_config`). Smoke test +4 checks. |
| 2026-09-14 | `deploy/portainer.yml`: self-contained stack for Portainer (no env_file/build/bind mounts/profiles, LiveKit config via `LIVEKIT_CONFIG`), README section "Portainer" with the variable table. |
| 2026-09-14 | `.gitlab-ci.yml` (test + image build into the GitLab registry, modelled on the directory repo); `deploy/compose.yml` takes a prebuilt image via `APP_IMAGE` (default `squorli/app:local`, build from the repo), variable documented in `.env.example`. |
| 2026-09-14 | Directory service moved out of the monorepo into the unpublished sibling repo `../squorli-directory` (user's decision): `apps/directory`, `deploy/directory/`, `postgres/init-directory.sql`, the `directory` Compose profile, `/id` in Caddy/nginx/overlays, the Dockerfile target and the `smoke:directory`/`db:generate:directory` scripts removed here; `tools/dev.mjs` no longer creates the `directory` database. New section 2a with the sync rules for the protocol copy and the brand package. |
| 2026-09-14 | All documentation, READMEs and AI instructions (AGENTS.md, CLAUDE.md, docs/PLAN.md, docs/brand, deploy READMEs, comments in env templates, compose files, Caddy/nginx/LiveKit configs and the Dockerfile) translated to English (user decision: docs are always English from now on). Code comments and UI texts stay German. |
| 2026-09-14 | Page title = server name (also in the login, from `/api/health`), server icon in the admin panel (upload/remove, favicon, sidebar; migration 0009), multiple owners (`members.is_owner`, `PUT /api/members/:id/owner`, owner group at the very top of the member list, first owner cannot be demoted). Smoke test +6 checks. |
| 2026-09-14 | Rename to **Squorli** and redesign per `docs/brand` (user's decision): packages `@squorli/*`, root package `squorli`, Compose `squorli`/`squorli-dev` (dev volume pinned to the old name), image tags `squorli/*`, containers `squorli-dev-*`, title/503 page/account page. Web client and account page switched completely to the brand tokens (Night/Surface/Raised/Selection, Action blue only for actions, Mint/Amber/Coral, Segoe UI 16 px, radii 12/20 px, focus ring, reduced motion), logo in the login, icon mark in the sidebar and favicon, horizontal logo on the account page. Signature strings and localStorage keys deliberately unchanged. The asset copies in the apps are byte-identical to `docs/brand/` (when changing them there, update both copies). |
| 2026-09-14 | M6c implemented (without e-mail): TOTP authenticator + recovery codes as second factor for key fetch and account changes (`totp.ts`, `secrets.ts`, `DIRECTORY_SECRET_KEY`, migration 0002 with `recovery_codes`/`key_fetches`/`totp_last_step`, signed account actions), account page with sign-in and account area (QR code via `qrcode-generator`), code field in the chat login, device management (sessions with `id`/`label`/`last_used_at`, migration 0008, `GET/DELETE /api/me/sessions`, WS close 4011, `ProfileDialog.tsx` as a categorized modal), `sessionLost()` in the client, CORS fix for `PUT /api/backup`, rate limits count only failed attempts. Smoke tests 38 / 67. |
| 2026-09-13 | M0 scaffold fully checked (dev, bundle, Docker). `.env` loader, smoke fix, `.dockerignore`, `tsup.config.ts`, `deploy --legacy`, `AGENTS.md`/`CLAUDE.md` created. |
| 2026-09-13 | `pnpm dev` runs via `tools/dev.mjs`: start containers and stop them automatically on exit. Raw start as `pnpm dev:apps`. |
| 2026-09-14 | First join without audio (2 users): audio is now unlocked in the click gesture (`prepareAudio()`, shared AudioContext, play silence), gesture fallback, "Mikrofon freigeben" (release microphone) in the dock, audio line in the debug view. Speaker frame without delay: local level measurement for all participants instead of the LiveKit notification; LiveKit `audio.update_interval` 150 ms as fallback. |
| 2026-09-14 | The stage shows display names from the member list instead of from the LiveKit token (a rename takes effect there immediately too). |
| 2026-09-14 | Bootstrap: re-create roles individually when the default role is missing (a fresh DB had only "Mitglied" because of migration 0004). User's dev DB reset, the owner is now the account @datlicht. |
| 2026-09-14 | M6b implemented: key backup (PBKDF2/HKDF/AES-GCM in `packages/protocol/src/backup.ts`), service endpoints `PUT /api/backup`, `GET /api/backup/:handle/params`, `POST /api/backup/fetch` (migration 0001 `auth_hash`, `BACKUP_RATE_LIMIT`), account page of the service (`apps/directory/web/`, tsup bundle to `public/`, Docker target copies it), client login "Mit Konto anmelden" + "Passwort festlegen", `scripts/dev.mjs` for the service, smoke test 20 checks. |
| 2026-09-14 | `tools/dev.mjs`: no longer inherit the server `.env` into the apps' environment (the directory service would otherwise bind :3000 and the chat DB). |
| 2026-09-14 | Directory service for its own host: `deploy/directory/` (Compose with its own DB, Caddy or external, README); the main Compose now sets `DIRECTORY_URL` only from `.env`, embedded variant as profile `directory`. |
| 2026-09-14 | M6a implemented (M6 brought forward): directory service `apps/directory` (register/resolve handles, rate limit, own DB, Docker target, Caddy `/id/*`, proxy overlays), the chat server caches verified handles (`users.handle`, migration 0007) and shows them instead of `anon-…`, the client registers handles in the login, `Me`/`Member` carry `handle`, `/api/health` reports `directoryUrl`. Decisions: monorepo, TOTP as second factor (M6c), SMTP prepared. |
| 2026-09-13 | Voice quality per voice channel: Opus bitrate (24-256 kbit/s, default 64) and stereo in the admin panel (migration 0006), mono with DTX+RED, stereo without audio processing, live switch-over of connected clients, display in the stage header. |
| 2026-09-13 | User-visible texts switched to real umlauts (permission labels, login/connection messages, tooltips, 503 page); convention in section 5 made more precise. |
| 2026-09-13 | Admin panel: categories on the left instead of a tab bar at the top; rule "categorized modals always with categories on the left" in section 5. |
| 2026-09-13 | Move voice participants via drag & drop in the sidebar (moderators; oneself always). |
| 2026-09-13 | Settings with categories on the left (Speaking / Devices / Camera); the camera picker when switching on now includes the background with a real preview of the blur. |
| 2026-09-13 | "Ton aus" (deafen) next to the microphone in dock and stage: also mutes the microphone; when switching it back on, the microphone only returns if it was not muted by hand; shown to others via a LiveKit attribute. |
| 2026-09-13 | All emoji symbols replaced by the Lucide web font (`Icon.tsx`, `tools/icons.mjs` generates the subset), hang-up white on red, voice settings as a modal instead of an inline block. |
| 2026-09-13 | Screen share at 1080p30 without simulcast (was 15 fps with simulcast: blurry and choppy); the screen-audio output is set again after device changes and shown in the debug view. |
| 2026-09-13 | Voice channel moderation (protocol v4): permission `MODERATE_VOICE`, move/remove, end camera/screen, block streaming per member (migration 0005 `stream_blocked`), LiveKit enforcement via RoomServiceClient, menu in the member list, notices for the affected member. Plus background blur (`@livekit/track-processors`) and a separate output device for screen audio. |
| 2026-09-13 | Switching on the camera always asks via its own modal with preview when there are several cameras (`CameraPicker.tsx`); the choice is saved as preselection. |
| 2026-09-13 | Stage: tiles as default and fitted to the window height (no scrolling), click enlarges a tile, click on the large one goes back (user's requirement). |
| 2026-09-13 | Roles: default role "Gast" (view + voice only), new role "Mitglied" with the previous permissions, migration 0004 with backfill; smoke test checks guest limits. |
| 2026-09-13 | M3 implemented: permission `STREAM_VIDEO` (migration 0003), `canPublishSources` in the RTC token, voice core with camera (simulcast, adaptiveStream/dynacast), screen share with its own audio track, stage (`VoiceStage.tsx`: tiles/speaker focus/pinning/volume for screen audio), camera settings, debug view with video bitrates, test page `/test/screenshare.html`, `pnpm bots --video`, bandwidth measurement in PLAN 4.3. |
| 2026-09-13 | Static: `wildcard: true`, so that new assets are served after `pnpm build` without a server restart. |
| 2026-09-13 | Web client: all `confirm`/`prompt` calls replaced by own modals (`dialogs.tsx`), new convention "no browser dialogs". |
| 2026-09-13 | Static serving: 404 for missing assets instead of the SPA fallback, `no-cache` for index.html, `immutable` for assets. |
| 2026-09-13 | M2 implemented: protocol v3 (ServerState, structure/message/typing/removed events, permission bitmask), schema with members/categories/channels/roles/invites/bans/messages/attachments (migration 0002), bootstrap, hub, authz, all admin routes, web client completely new (Sidebar, Chat, Members, Admin, VoiceDock, login with invite), smoke test with 44 checks, Compose volume for attachments. |
| 2026-09-13 | Test aids for restrictive networks: `?ice=relay` (TURN only), ICE path in the debug view, `docs/DEVELOPMENT.md` section "Simulating restrictive networks". |
| 2026-09-13 | First voice chat over the internet confirmed (iPhone Safari external, NPM on another host, dev stack behind router NAT). |
| 2026-09-13 | `tools/dev.mjs` loads `apps/server/.env` for Compose (`LIVEKIT_DEV_NODE_IP`), reports the advertised media address at startup. |
| 2026-09-13 | Explain the disconnect reason (DisconnectReason) in the client, room event log in the debug view, `voice.leave` on unexpected disconnect. |
| 2026-09-13 | Explain LiveKit connection errors: the client names URL and cause (localhost dev value, ws:// under HTTPS, proxy), the debug view shows the URL, the server warns about a localhost URL with a public domain. |
| 2026-09-13 | Explain login errors: `/api/health` returns `domain`; on `signature_invalid` the client reports expected vs. actual domain, on `challenge_invalid` "try again". |
| 2026-09-13 | `pnpm build` copies the web client to `apps/server/public` (`tools/copy-web.mjs`); the server answers `/` with 503 + notice instead of 404 when the client is missing. |
| 2026-09-13 | Proxy docs: section "Proxy on another host" with overlay `remote-proxy.ports.yml` and `PROXY_BIND_IP` in `.env.example`. |
| 2026-09-13 | Proxy docs: Nginx Proxy Manager section with overlay `npm.network.yml`. |
| 2026-09-13 | Prod Compose: `--env-file ../.env` in all commands (placeholders were otherwise empty). |
| 2026-09-13 | Proxy docs: `nginx.ports.yml` overlay added (compose.yml publishes no HTTP ports in external mode), nginx.conf with redirect/IPv6/http2, README with verification steps. |
| 2026-09-13 | M1 implemented: protocol v2 (voice.join/leave/state, /api/me, rtc-token per channelId), VoicePresence, fixed channel "lobby", web client with livekit-client (VoicePanel, DebugPanel, voice/ core: VAD/PTT via GainNode), migration 0001 (display_name), `pnpm bots`, `pnpm smoke`, LiveKit dev with `--node-ip` and matching secret. TURN documented, off. Acceptance across two networks open. |

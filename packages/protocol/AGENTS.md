# packages/protocol – `@squorli/protocol`

Part of the project description. The entry point is the root [AGENTS.md](../../AGENTS.md) (commands, definition of done, cross-cutting conventions, the map of all documentation in its section 0). Whoever adds a schema file, changes the version rule or the permission rules updates this file in the same step.

**Six files here are the source of a byte-identical copy in `../squorli-directory`** (`primitives`, `directory`, `backup`, `useragent`, `friends`, `dm` and their tests): sync in the same work step, root `AGENTS.md` section 2a.

## Structure

```
packages/protocol/     @squorli/protocol: zod schemas for REST and WebSocket, PROTOCOL_VERSION. Single source of truth for the client/server contract. dm.ts also carries link previews of direct messages (`DmPreview`, `DmBlobRef`, `DmControl` = the instruction that removes a preview, `sealDmBlob`/`openDmBlob`; `openDm` checks these fields one by one and drops what does not fit), directory.ts the directory's two services for them (`LinkLookupRequest/Response`, `DmBlobPutRequest/Response`, `DM_BLOB_MAX_BYTES`, `features.dmPreviews`). scan.ts + links.ts (`previewLinks`, `readBareUrl`, `MAX_PREVIEW_LINKS`: which links of a message get a preview, docs/features/link-previews.md; `LinkPreview`, `Message.previews` and `RemovePreviewRequest` are in index.ts). mentions.ts (`mentionToken`, `mentionedUserIds`: who a message text mentions, outside code; shared by the server's counter and the client). primitives.ts (Uuid, Iso, PublicKey, Signature), directory.ts (ChallengeRequest/Response, handle rules, DisplayName + ServerHost, registration/backup/action message, directory contract incl. backup, SecondFactorCode (6 digits TOTP, 8 digits e-mail code, else recovery code), registration with an e-mail address: optional `email`/`emailCode` in DirectoryRegisterRequest, the address as the last line of `directoryRegisterMessage`, 202 `DirectoryRegisterPending`, `DirectoryHealth.features.emailRequired`; e-mail: EmailCode, EmailAddress, EmailUpdateRequest, EmailVerifyRequest, EmailCodeRequest/Response, actions email-set/email-verify/email-code, AccountStatus.email/emailPending, KeyFetch.factor "email"; avatars (19 September 2026): AVATAR_MAX_BYTES, AVATAR_SIZE, AvatarMime, AvatarUpdateRequest/Response, action `avatar-set` with directoryAvatarPayload ("<mime>\n<sha256>"), sniffAvatarMime, avatarDigest (WebCrypto), directoryAvatarUrl, DirectoryAccount.avatarUpdatedAt (public), Friend.avatarUpdatedAt, DirectoryHealth.features.avatars, and in index.ts Member.avatarUrl / Me.avatarUrl (default null, no version bump); TotpSetupResponse, RecoveryCodesResponse, AccountStatus/KeyFetch/AccountServer, ProfileUpdateRequest with its payload helper, SoundSettings + directorySoundSettingsPayload + SoundSettingsUpdateRequest + AccountStatus.soundSettings (action `sound-settings`), sealed settings (21 September 2026: the account's settings as a blob only the user's key opens; `SealedSettings`, `SealedSettingsContent` = `AccountSettings` plus the game display's hide list, `deriveSettingsKey` (HKDF over the identity's seed), `sealSettings`/`openSettings` (AES-256-GCM, padded), `SealedSettingsUpdateRequest`, `parseSealedSettings`, action `settings-sealed`, `AccountStatus.settingsSealed`, `features.settingsSealed`; directory.ts imports the hex helpers of backup.ts for it, which imports only a type back), ServerRegisterRequest/Response + directoryServerRegisterMessage, ServerResolveRequest/Response, DirectoryNotifyRequest, account deletion on a server: action `server-leave`, ServerLeaveRequest/Response, DirectoryLeaveRequest, ServerLeaveConfirmRequest, ServerLeavesResponse, AccountServer.leaveRequestedAt; DirectoryHealth.features.friends; primitives/directory/backup/useragent/friends/dm are COPIED into ../squorli-directory, root AGENTS.md section 2a), friends.ts (game display, 21 September 2026: GameName, GamePresence = a launcher's id (LibraryGameId of directory.ts) and a name, optional `game` on DirectoryClientActivity, Friend.game, FriendPresenceEvent.game, and in index.ts the same on ClientActivity and Member.game; AccountSettings.games in directory.ts, optional without a default; tested in games.test.ts; no version bump, older sides drop the fields; AFK detection: AFK_AFTER_MS, DirectoryClientActivity, Friend.afk, FriendPresenceEvent.afk, tested together with the chat part in afk.test.ts; M7: DIRECTORY_WS_VERSION, directoryWsAuthMessage, Friend/FriendState, FriendSearchResult, DmMessage/DmConversation, DirectoryClientEvent/DirectoryServerEvent unions for the directory socket, DM_DELETE_BOTH_MS, account-page action FriendOp/FriendsActionRequest/directoryFriendsPayload), dm.ts (M7 crypto: dmPublicKeyOf, deriveDmKey, sealDm, openDm; @noble/curves; tested), backup.ts (M6b crypto: PBKDF2 + HKDF + AES-GCM via WebCrypto, isomorphic for browser, Node and account page; tested), useragent.ts (labelFromUserAgent: "Chrome auf Windows", the desktop app as "Squorli Desktop auf Windows" (checked before the browsers, its user agent carries Chrome's token too), for chat server and service; tested), votekick.ts (23 September 2026, docs/features/votekick.md: the vote kick of a voice channel - `VOTEKICK_MS`, `VOTEKICK_MIN_MEMBERS`, `VOTEKICK_BLOCK_MS`, `VOTEKICK_COOLDOWN_MS`, `VOTEKICK_RESULT_MS`, `VoteKick`, `VoteKickResult`, `VoteKickOutcome`, `StartVoteKickRequest`/`CastVoteKickRequest`, and the two rules both sides compute with: `voteKickQuorum` and `voteKickOutcome`; tested. In index.ts `Channel.allowVoteKick` (default true), `ServerVoiceState.voteKick` (default false) and the events `ServerVoteKick`/`ServerVoteKickResult`, all without a version bump), channelBlocks.ts (24 September 2026, docs/features/channel-blocks.md: `CHANNEL_BLOCK_MINUTES`, `ChannelBlockMinutes`, `ChannelBlockSource`, `ChannelBlock`, `ChannelBlocksResponse`, `SetChannelBlockRequest`; in index.ts `ServerVoiceMoved.reason` "blocked" with an optional `until`; no version bump), channels.ts (23 September 2026, docs/features/channel-permissions.md: `PermissionOverwrite`, `SetOverwritesRequest`/`OverwritesResponse` for GET/PUT /api/channels/:id/overwrites and /api/categories/:id/overwrites, `ChannelNotification`, `SlowmodeSeconds`, `UserLimit`, `VoiceLock`; in index.ts `Channel.sticky`/`stickyPersist`/`stickyHideVoice`/`userLimit`/`slowmodeSeconds`/`defaultNotify`/`allowRadio`/`allowVideo`/`private` and `ServerState.myChannelPermissions` (the feature flag) and `myVoiceLock`, the same two on `ServerMe`, all with defaults or optional; in permissions.ts `MOVE_MEMBERS`, `BYPASS_STICKY`, `CHANNEL_OVERRIDABLE`, `CHANNEL_PERMISSION_GROUPS`, `channelOverridableFor`); index.ts incl. SessionInfo (M6c); status API and the server-wide mute display (23 September 2026, docs/features/status-api.md): `StatusApiMode`, `ServerSettings.statusApi` (optional = the feature flag, also for `voice.status`), `ServerSettings.statusApiRoleId` (whose view the answer carries, null = the default role), `StatusApiKeyResponse`, `ServerStatus`/`StatusChannel`/`StatusMember` (what GET /api/status answers; a `StatusMember` always has a `voice` seat, only members in a voice channel are listed), `VoiceStatus` (micMuted, deafened, cameraOn, screenOn; the last two with defaults), `VoiceMember` with the same four fields defaulted, all four optional on `ClientVoiceJoin`, `ClientVoiceStatus`; no version bump. import.ts (22 September 2026, docs/features/import.md: `ImportSource`, `discordTemplateCodeOf` = the code out of discord.new/<code>, discord.com/template/<code> or the bare code, `ImportPlan` with its category/channel/role/dropped parts, `DiscordImportPreviewRequest`, `DiscordImportRequest`, `DiscordImportResult`; `ServerState.importSources` in index.ts is the feature flag, optional, so no version bump; tested in import.test.ts)
```

## Conventions

- **Protocol version:** Bump `PROTOCOL_VERSION` in `@squorli/protocol` on incompatible changes (currently 4; new permission bits are not an incompatible change; a new server event is one when clients must understand it, because the client parses events strictly with zod and drops what it cannot parse. An event that older clients may safely miss, such as `read.update`, needs no bump, and a bump is costly: the multi-server client cannot connect to servers of another version); the server rejects other versions with WS code 4002. The constant is also in `apps/server/scripts/smoke.mjs`, update it there too. Precedent (23 September 2026): the channel permissions needed no bump although every client's channel list changed, because `structure` kept its wire shape (whole lists, filtered per recipient on the server) and every new field carries a default or is optional; new error codes went on REST answers only (`ServerError.code` is a fixed enum that older clients would drop).
- **Feature flags instead of a version bump:** a field the server only sends when it knows a feature (`ServerSettings.radioAutoStop`, `afkChannelId` with null for "none"; the directory's `DirectoryHealth.features.*`) tells the client whether it may use the matching client event or route. A new **client** event needs such a flag, because servers answer unknown client events with `bad_message` (`ClientActivity` is only sent when `afkChannelId` is present, `DirectoryClientActivity` only with `features.afk`).
- **Order and grouping of permissions (user's requirement, 17 September 2026, applies to every permission added from now on):** the role editor shows permissions in groups sorted by meaning (`PERMISSION_GROUPS` in `permissions.ts`: `admin` "Verwaltung" first with `ADMINISTRATOR` at its top (user's decision), then `text` "Kanäle und Nachrichten", `voice` "Sprachkanäle" (until the web radio: "Sprache und Video"), `members` "Mitglieder"; apart from `ADMINISTRATOR` a group runs from everyday to powerful; group titles from the `permGroup.*` keys, rendered as `fieldset.perm-group` in `AdminPanel.tsx`). The bit order is history and never the display order. Whoever adds a permission must sort it into the group and position where it belongs by meaning (next to related ones, e.g. watching before sharing), never simply append it, and must re-check the whole arrangement each time: does a group get too long, does a new group make more sense, are the group titles still accurate. A test in `index.test.ts` fails if a permission is in no group or in two. **Channel permissions (23 September 2026):** a new bit that makes sense per channel goes into `CHANNEL_OVERRIDABLE` and into `CHANNEL_PERMISSION_GROUPS` (the channel dialog's own grouping per channel kind, with `chanPerm.*` labels in both catalogs: "Kanal sehen", not "Kanäle sehen"); a second test pins that the groups cover `CHANNEL_OVERRIDABLE` exactly once per kind and name nothing server-wide. New permissions: append the bit in `permissions.ts`, add the label (`perm.*` in both catalogs), sort it into `PERMISSION_GROUPS`, and for existing servers write it into the appropriate role via a custom migration (`drizzle-kit generate --custom`) (see 0003/0004 as examples); the bit values are pinned in `index.test.ts`.
- **Key backup (M6b):** The private key leaves the client only encrypted. `packages/protocol/src/backup.ts`: PBKDF2-SHA256 (600,000 iterations, 16-byte salt) -> HKDF into two keys: AES-256-GCM (encrypts the 32-byte seed, stays in the client) and auth key (is sent, the service stores only its SHA-256 in `key_backups.auth_hash`). Storing: `PUT /api/backup`, signed over "community-directory-backup\n<host>\n<nonce>\n<ciphertext>", upsert (= change password). Fetching in two steps: `GET /api/backup/:handle/params` (salt, iterations) -> client derives -> `POST /api/backup/fetch` with auth key -> blob with iv + ciphertext. Wrong password = 401 `auth_invalid`; rate limit `BACKUP_RATE_LIMIT` per IP **and** per handle (default 10/min), uploads have their own window. Password at least `BACKUP_MIN_PASSWORD` (8) characters, checked client-side only; no reset possible (the directory does not know it).
- **Scanner for mentions and link previews:** `src/scan.ts` (`scanOutsideCode`) reads a message text the way `apps/web/src/markdown.ts` does as far as code and escapes go; `mentions.ts` (who is mentioned) and `links.ts` (`previewLinks`: which links get a preview; `readBareUrl`, which the Markdown parser itself imports) are built on it. Whoever changes how the parser reads fences, code spans, escapes or bare addresses changes `scan.ts` / `links.ts` too; the web tests `mentions.test.ts` and `linkPreviews.test.ts` compare both with the parser (`docs/features/mentions-unread.md`, `docs/features/link-previews.md`).

## Known pitfalls

- **`@squorli/protocol` has no build:** its entry is `src/index.ts`. The server therefore bundles it into `dist/index.js` via `noExternal`. Anyone who introduces further workspace packages as runtime dependencies of the server must add them to `tsup.config.ts` as well.

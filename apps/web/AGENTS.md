# apps/web – Web client (`@squorli/web`)

Part of the project description. The entry point is the root [AGENTS.md](../../AGENTS.md) (commands, definition of done, cross-cutting conventions, the map of all documentation in its section 0). Whoever adds, removes or repurposes a file of the client, or changes a convention or pitfall, updates this file in the same step.

**After every client change:** `pnpm build` from the repo root and tell the user to reload (root `AGENTS.md` section 4, item 7); after a new `<Icon name>` run `pnpm icons`; after a dependency change `pnpm run licenses`.

## Feature notes: read before touching these files

`src/` is flat, so the notes are found by file, not by folder:

| Files | Notes |
|---|---|
| `voice/`, `VoiceStage.tsx`, `VoiceDock.tsx`, `VideoWindows.tsx`, `StageWindow.tsx` (the whole stage in a window of its own), `usePushToTalk.ts`, `VideoAudioControls.tsx`, `videoDisplay.ts` (`attachVideoView` with `PopoutElementInfo` for views in pop-out windows, `watchDocumentHidden`, fullscreen, window size; tested), `CameraPicker.tsx`, `UserVolumeControl.tsx` | `src/voice/AGENTS.md` (standing rules, pitfalls) and `docs/features/voice-video.md` |
| `activity.ts`, `idleDetection.ts`, the AFK parts of `App.tsx`, `store.ts`, `serverConnection.ts`, `directoryLink.ts`, `VoiceDock.tsx`, `Avatar.tsx`, `ChannelsTab.tsx` | `docs/features/afk.md` |
| `RadioControl.tsx`, `RadioTab.tsx`, `radioLabel.ts`, `EmbedPlayer.tsx`, `embedControl.ts`, `twitch.ts`, `youtube.ts`, `watchSync.ts`, `playerWindow*.ts`, `player-window.html`, `voice/radioPlayer.ts` | `docs/features/radio.md` |
| `mentions.ts`, `MentionSuggest.tsx`, `readState.ts`, unread/mention/mute parts of `serverConnection.ts`, `store.ts`, `Sidebar.tsx`, `ServerRail.tsx` | `docs/features/mentions-unread.md` |
| `markdown.ts`, `MessageText.tsx`, `AutoGrowTextarea.tsx`, `autoGrow.ts`, the composers in `ChatView.tsx` / `DmView.tsx` | `docs/features/markdown-input.md` |
| `emoji/`, `EmojiPicker.tsx` | `docs/features/emoji.md` |
| `SettingsDialog.tsx`, `MiniProfile.tsx`, `accountSettings.ts`, `voice/settings.ts` | `docs/features/settings.md` |
| `AdminPanel.tsx`, `ChannelsTab.tsx`, `roleOrder.ts`, `channelOrder.ts`, `ContextMenu.tsx`, `Avatar.tsx`, general look | `docs/features/ui-admin.md`, brand: `docs/brand/AGENTS.md` |
| `LicensesTab.tsx`, `licenses/` | `docs/features/licenses.md` |
| `LoginScreen.tsx`, directory functions in `api.ts`, `directoryLink.ts`, `Home.tsx`, `DmView.tsx` | `docs/features/directory.md` |
| `platform/` | `docs/features/desktop.md` |

## Structure

```
apps/web/              Web client: Vite 5 + React 18 + livekit-client. The desktop app (apps/desktop) shows this same build.
  src/identity.ts      Key pair in localStorage (unencrypted; backup at the directory from M6b), identityFromPrivateKey()/storeIdentity() for restoring
  src/api.ts           `ServerApi` class = all REST calls of one chat server (auth with the signature domain as a parameter, profile, sessions, messages, upload, admin), one instance per server with its own token and `base` ("" = the server serving the client, otherwise the origin of a foreign server), `abs()` for relative server URLs (attachments, icon), `onUnauthorized` hook on 401 with a token set; module functions for the directory (directoryLookup, directorySetDisplayName(), directorySearchHandles(), directoryHealth(), ...); ApiError with error code; explainLoginError(err, api, domain)
  src/serverConnection.ts Multi-server client (14 September 2026): `ServerConnection` = one chat server (session, WebSocket with reconnect, ServerState, message cache per channel, presence, typing, unread, log, health data); hooks to the store (state, token, session lost, removed, connected, voice.moved/voice.stop). Remembers the channel of its last `voice.join` and sends it again after every reconnect (the server keeps voice presence per socket, LiveKit outlives it); a heartbeat drops a socket on which nothing arrived between two pings, and `connect()` never leaves an earlier socket open (docs/features/afk.md, 18 September 2026). sessionLost (WS close 4011, WS error unauthorized, REST 401) clears the session with a message; logout() also ends the session server-side
  src/store.ts         UI-free client state: identity, `servers` = `ServerConnState` per server (`homeHost` = host of the address bar = the server serving the client, always present; foreign servers keyed by their directory host, base = `directoryServerUrl()`), `activeHost` = the server shown; `openServer(directoryHost)` switches (and on first use logs in at the foreign server with the browser key, signature over its `/api/health` domain, stored session reused), `retryServer()`, `closeServer()`, `hostFor()` maps a directory host to the store key; sessions per server in localStorage `chat.sessions.v2` (v1 migrated); `homeState()`/`activeState()` helpers; setDirectoryName(); logout() = all servers. M7: second socket to the directory (`directoryLink.ts`, opened by `connectDirectory()` after the directory lookup when the key has an account and the directory reports `features.friends`), `friends`, `conversations` (unread per friend), `dms` (decrypted threads, `Dm.text` null = could not decrypt), `homeOpen`/`currentPeer`, pair-key cache (`deriveDmKey`), actions requestFriend/acceptFriend/declineFriend/removeFriend/blockFriend/unblockFriend, sendDm (seal + send, display via the directory's echo), loadDmHistory, markDmRead, deleteDm, clearDm, searchHandles, friendState()
  src/activity.ts      AFK detection: `ActivityTracker` (pure, tested: idle after AFK_AFTER_MS without `touch()`, never while the system-wide detector reports the user active), the page's one instance `activity`, `watchActivity(tracker, window)` = input events of a window (main page in App.tsx, video pop-outs in VideoWindows.tsx) plus the regular check; App.tsx also touches it while the speech gate is open; the store reports every change to all chat servers and the directory
  src/idleDetection.ts Idle Detection API (Chromium); the desktop app runs it always and shows no switch (`platform.systemIdle`). In a browser: switch in Einstellungen > Ansicht, permission prompt inside the click, stored per browser (`chat.idleDetection.v1`), resumed at page load when the permission still stands; feeds `ActivityTracker.setSystemIdle`
  src/directoryLink.ts M7: WebSocket to `<directoryUrl>/api/ws`: answers the `challenge` with a signature (`directoryWsAuthMessage`), reconnect with backoff, ping every 25 s (nothing arrived between two pings = the socket is dropped and replaced; never two sockets, 18 September 2026), stops on `version`/`unauthorized`/`unknown_account`
  src/Home.tsx         M7 home view (Squorli logo in the rail): `HomeSidebar` = search (handles at the directory, debounced; names locally in the member list), incoming requests (accept/decline), friends sorted unread > online > last message, sent requests (withdraw), blocked (unblock); `HomeMain` = `DmView` for the selected friend or a hint
  src/DmView.tsx       M7 conversation: history with older pages on scroll-up, grouping like ChatView, message text as Markdown (`MessageText`), composer without attachments, delete per message (for both within 5 min when mine, else for me), clear the conversation for me
  src/clientHome.ts    Client without a home server (desktop app, docs/features/desktop.md), pure and tested: what the device remembers (`chat.client.v1`: past the login, servers added by address, server viewed last), `chooseInitialServer()`, `parseServerAddress()` (host, address, invite link or `squorli://` link)
  src/DesktopLogin.tsx Login of a client without a home server: directory account first (handle + password, authenticator or e-mail code), below it this device's key; NoServers.tsx = its main area while no server is known (discover, add, sign out)
  src/railServers.ts   `buildRailServers()`: home server (if any), the account's servers, then servers added by address; pure, tested
  src/TitleBar.tsx     Desktop app only (`platform.window.frame`): the own title bar above login and client, drag region plus minimize / maximize-restore / close; nothing in a browser
  src/layout.ts        Widths of the two side columns (channel list, member list) per device (`chat.layout.v1`), limits, drag arithmetic; pure, tested. ColumnHandle.tsx = the drag handle over the gap (pointer capture, arrow keys, Home/double click = default), placed as an absolutely positioned grid child
  src/ServerRail.tsx   Server rail at the far left (M6d, shown when the server names a directory): M7 at the top the Squorli icon mark (`home` prop: open state, badge = incoming requests + unread DMs, toggle), then the own server and the servers on which the own handle has logged in, from the signed `POST /api/account/status` of the directory (`api.directoryAccountStatus()`, `state.accountServers`, refreshed after the directory lookup and after each fresh WS welcome; App.tsx builds the `RailServer[]`). Multi-server client (14 September 2026, user's requirement): every entry is a button that switches the shown server inside the client (`store.openServer()`, also closes the home view); the voice connection stays on its server (green speaker badge on that entry, white bar = unread messages). Icon from the directory (`directoryServerIconUrl()`, never from the foreign server), initials as fallback; right-click on an entry opens a small menu (`.rail-menu`, fixed at the pointer): "Öffnen" and "Konto auf diesem Server löschen" (`askConfirm`, then `store.leaveServer()` = signed `server-leave` at the directory; own server: the server closes the socket with 4012 -> login with a note, foreign server: closed and its session forgotten; not delivered = note that the directory keeps it pending); bottom: "Server entdecken" opens ServerBrowser
  src/ServerBrowser.tsx Server directory modal (M6d): public `GET /api/servers` of the directory (only listed servers), name, host, description, member count, open/invite-only, icon, link "Öffnen" = join via link
  src/App.tsx          Layout: server rail (with directory) | sidebar | chat or stage | members, all for the active server (`activeState`, `store.active.api`); the login screen depends only on the own server (`homeState`). Voice: `voiceHost` = the server the voice connection belongs to; joining a voice channel of another server first leaves the old one (`joinVoice(host, channelId)`), moderation events and kicks only count when they come from `voiceHost`, the stage is shown only while the active server is the voice server (the dock's "Ansicht" switches there). A foreign server without state shows `ServerStatus` (connecting, error with "Erneut versuchen" / "Direkt öffnen" / "Schließen", removed). M7: with `homeOpen` the left column shows `HomeSidebar`, the main area `HomeMain`, the member list is hidden (`.app.home` grid), the voice dock stays; voice client; debug toggle; sets page title and favicon (branding.ts: server name from state or /api/health, server icon or Squorli icon mark)
  src/SettingsDialog.tsx (without a shown server, in a client without a home server: no Profil and Sitzungen, opens on Ansicht; desktop app: Ansicht > Fensterhintergrund from `platform.window.appearance`) All user and profile settings as one categorized modal, opened by the gear in the dock (17 September 2026): Profil (display name on this server; with a directory account also the global name, both stored signed at the directory, the local name = server override or the global one), Ansicht (language, speaker view option), Sprache und Audio (one tab since 19 September 2026: microphone with the microphone test and the level meter, VAD/PTT, threshold, hold time, boost, then voice output, screen audio output, web radio output; the device selection is per browser), Kamera (device per browser, quality, background), Töne (one switch per join/leave cue with preview, common volume), Sitzungen (sessions of this server, log out individually/all others, M6c), Konto (handle, key, link "Konto verwalten" to the account page with ?handle=, log out, discard identity). It only stores (`saveVoiceSettings`); App.tsx hands mode/threshold/hold time/cues to the voice client, so values adopted from the account take effect too. The synced categories say where the settings live (account @handle or this browser) and show `State.settingsSyncError`. While it captures a push-to-talk key, App tells the dock to keep its listener quiet (`pttSuspended`)
  src/MiniProfile.tsx  Mini profile on a click on your own name (ContextMenu opening upwards from the name row, flush with the dock's box, as wide as it and 8 px above it, user's wish; `above`/`width` in `MenuAnchor`, `above` in `menuPosition`, tested): avatar, name, handle, one field "Anzeigename auf <Server>" (`store.setServerDisplayName`), button "Alle Einstellungen"; the field stops the menu's arrow/Home/End handling
  src/accountSettings.ts Pure mapping between the per-device settings and the protocol's `AccountSettings` (`toAccountSettings`, `applyAccountSettings` keeps the device ids, `sameAccountSettings`), tested
  src/VoiceStage.tsx   Stage (M3): tile per participant (camera/avatar) + per screen share. Tiles by default, size computed via ResizeObserver so that everything fits into the area without scrolling; click on a tile = large (focus), click on the large tile = back; "Sprecher" (speaker) follows the speaker/newest watched share without a click. A share nobody turned on shows a tile that offers to watch it; every remote feed has an eye button (and a context menu entry) to turn it off for yourself; in the tile view a head button hides participants without video while any video is sent (component state, never stored). Control bar mic/camera/screen/leave, notice for a share without audio. No close button in the header: selecting a text channel in the sidebar leaves the stage; the right edge of every main header (`.chat-head`, padding-right) is reserved for the debug button (`.debug-toggle`, vertically centered on `--head-h`). Names come from `members` (immediately after renaming), not from the LiveKit token
  src/LoginScreen.tsx  Login splash: server icon (or Squorli icon mark) and server name from /api/health at the top, footer "Betrieben mit Squorli · Version <version>" at the bottom; with `requireAccount` a browser key without handle is not offered at all (no box, no toggle, only "Mit Konto anmelden"); a browser key with a verified handle counts as an account and logs in as before. Login with directory: "Mit Konto anmelden" (sign in with account: handle + password, fetches the key via backup, M6b; code field appears after 401 totp_required, M6c) and "Schluessel dieses Browsers" (this browser's key: register handle, with `state.directoryEmailRequired` in two steps: e-mail address + "Code senden", then the mailed code + "Handle registrieren"; "Passwort festlegen" (set password) = store backup); links to the service's account page (create, manage); invite code (from /invite/<code> or ?invite=), invite preview, error explanation
  src/Sidebar.tsx      Header with server icon (otherwise Squorli icon mark) and server name; categories/channels (exactly one entry is highlighted: the channel shown in the main area, i.e. the text channel or, while the stage is open, the voice channel; the connected voice channel only gets a green icon), voice members per channel (drag & drop into other voice channels: yourself = join, others = POST /move with MODERATE_VOICE), unread, open admin panel
  src/ChatView.tsx     History with loading more when scrolling up, grouping, message text as Markdown (`MessageText`), image attachments, edit/delete, composer with files, typing indicator
  src/readState.ts     Read state per device (channel -> seq of the newest message shown): unread marks and mention counts after a reload or an absence; pure functions plus localStorage, used by serverConnection.ts; tested
  src/mentions.ts      Mentions, pure logic: token `<@userId>`, encode/decode between names and tokens, who a message mentions (Markdown parser and the protocol's `mentionedUserIds` must both say so), typed names only when unique, query at the caret, member suggestions; tested
  src/MentionSuggest.tsx `useMentionSuggest`: member list while typing "@name" in the composer and in the edit field of ChatView (keys, accept, remembers the chosen member per name; `below` = list under the input)
  src/EmojiPicker.tsx  `EmojiButton` (smiley button of a message input) and its picker: search, categories, recently used, skin tone, keyboard, inserts at the caret (docs/features/emoji.md)
  src/emoji/           convert.ts (find real emoji, `:shortcode:` and emoticons -> emoji, for markdown.ts; tested), picker.ts (search, skin tone, recent list, insert at caret, per-device storage; tested), types.ts; GENERATED by tools/emoji.mjs, do not edit by hand: font.css (Noto Color Emoji slices from node_modules), shortcodes.ts (main bundle), data.de.ts / data.en.ts (lazy, picker)
  src/LicensesTab.tsx  Einstellungen > Lizenzen: own license plus the generated third-party list with license texts
  src/licenses/        types.ts; thirdParty.ts GENERATED by tools/licenses.mjs (lazy chunk), do not edit by hand
  src/AutoGrowTextarea.tsx Text input that grows with its content up to a third of the chat area, then scrolls (composer of ChatView and DmView, message editing); rule in `autoGrow.ts` (tested)
  src/MessageText.tsx  Message text as Markdown for ChatView and DmView: renders the tree from `markdown.ts` as React elements (never HTML), memoized, optional "edited" mark; tested with `renderToStaticMarkup`
  src/markdown.ts      Pure Markdown parser for chat messages (own implementation, chat subset, link whitelist http/https/mailto, no embedded images; docs/features/markdown-input.md), tested
  src/MemberList.tsx   Owners at the very top (own group, crown), then members by role/online; context menu (click or right-click): M7 "Als Freund hinzufügen" / "Nachricht schreiben" / request state (only with a directory socket; members without a handle have no directory account), set roles, kick, ban, voice channel moderation (move, end camera/screen, block) depending on permission, for owners additionally "Zum Eigentuemer machen" (make owner) / "Eigentuemerstatus entziehen" (revoke owner status)
  src/EmbedPlayer.tsx  Twitch or YouTube as the radio's source: the one official player iframe that follows the stage's player tile (`EmbedSlot`) or floats in the corner, or lives in the pop-out window (`usePlayerWindow`, page `player-window.html` + `playerWindowMain.ts`, pure part `playerWindow.ts`); what drives it without React is `embedControl.ts` (`TwitchControl`: volume, restart after being hidden; `YoutubeControl`: volume, playing in step, refused sound), pure parts `twitch.ts`, `youtube.ts`, `watchSync.ts` (all tested)
  src/RadioControl.tsx Radio button in the stage head and its menu (own volume/off for everyone, station choice and stop with CONTROL_RADIO); RadioTab.tsx = admin area > Radio (station list, MANAGE_SERVER); the player is voice/radioPlayer.ts, radioLabel.ts = the pure rule whether "now playing" fits in place of the station's name (docs/features/radio.md)
  src/UserVolumeControl.tsx Slider 0-200 % "Lautstärke für mich" for one person (by public key) with reset; used in the context menus of the sidebar's voice members (right-click), the stage tiles (right-click) and the member list
  src/AdminPanel.tsx   Admin panel (modal, categories on the left): Server (name, open, upload/remove server icon, list of owners), Categories/Channels (create, rename, sort, delete), Roles (permissions, color, position), Invites, Bans
  src/ScreenPicker.tsx Desktop app only: which screen or window to share, with thumbnails and (Windows) "Systemton mitteilen", off by default; the shell asks it through `platform.screen.setPicker` (App.tsx), a browser brings its own picker
  src/CameraPicker.tsx Camera selection when turning on: camera + background (blur) with live preview via an unpublished LiveKit track including BackgroundBlur; appears with multiple cameras or when the browser supports background effects. App.tsx: toggleCamera()
  src/VoiceDock.tsx    Bottom left. While in a voice channel a separate area (`.dock-voice`, own background and divider) above the name row: status "Sprache verbunden" + channel (prefixed with the server name when another server is shown), "Ansicht" (open the stage), level meter and the quick controls mute, deafen ("Ton aus"), camera, hang up. Below it always the name row: the name opens the mini profile (`onOpenProfile` with the anchor), the gear opens the settings dialog (`onOpenSettings`); both live in App.tsx. Also holds the push-to-talk key listener
  src/DebugPanel.tsx   Protocol inspector: WS events, LiveKit statistics, ICE path, video bitrates per simulcast layer/received track (measurement for PLAN 4.3); bug button or ?debug
  src/Icon.tsx         <Icon name="…"/>: symbols from the Lucide web font (ISC), self-hosted; no emojis in the user interface
  src/icons/           GENERATED by tools/icons.mjs: icons.css (only used code points) + lucide.woff2; do not edit by hand
  src/styles.css       Styling per docs/brand: maps the --sq-* tokens to app variables (--bg Night, --bg2 Surface, --bg4 Raised, --sel Selection, --accent Action, --brand Squorli blue, --ok/--warn/--danger Mint/Amber/Coral), radii 12/20 px, focus ring in brand blue
  public/brand/        Copy of the brand assets from docs/brand (logo, horizontal logo, icon mark, small icon mark, white icon mark, squorli-tokens.css); index.html loads tokens and favicon, the login shows the logo, the sidebar the small icon mark
  src/dialogs.tsx      askConfirm()/askInput() + DialogHost: own confirmation/input dialogs (replacement for window.confirm/prompt), mounted in main.tsx
  src/i18n/            (app.de.ts, app.en.ts = the desktop app's wording of some keys, see the conventions below) UI texts German/English (15 September 2026): de.ts (reference catalog, flat dotted keys, {name} placeholders), en.ts (typed against de), index.ts (locale detection: localStorage `chat.locale` > navigator.languages > "en"; t(), tOr() for runtime keys, fmtDateTime/fmtTime/fmtDay, setLocalePreference() reloads the page, applyLocaleToDocument() sets <html lang>), index.test.ts (browser-language rule, same keys and placeholders in both catalogs)
  public/worklets/pcm-player.js  AudioWorklet of the desktop app's natively captured share audio (plain JS by URL: the app's CSP allows no blob scripts); used by src/platform/screenAudio.ts
  src/platform/        What differs between the browser and the desktop app (M4, docs/features/desktop.md): types.ts (`Platform`: kind, os, app versions, home server or none, default directory, media, links, screen picker, window, updates), web.ts (browser), desktop.ts (adapter from the shell's bridge `window.squorliDesktop`), index.ts (the singleton `platform`), bridge.ts (contract with the Electron shell: `DesktopBridge`, `DesktopInfo`, IPC channel names) and deepLink.ts (`squorli://` parser, tested); the last two are DOM-free and exported by package.json for the shell's main process
  src/voice/           UI-free core (PLAN 3.4): voiceClient.ts (LiveKit room, camera/screen, background blur, separate screen audio output, tiles, statistics, moderation notices), micPipeline.ts (Web Audio gate; `setMonitor()` = second output for the microphone test; `openMic()` = getUserMedia with a fallback to the default microphone when the chosen device is gone, tested), cameraRetry.ts (one retry when a camera could not be started, Firefox releases it late; tested), gate.ts (VAD logic, tested), micBoost.ts (microphone boost: `AutoGain` learns a gain of 1..12 from the raw speech level, soft clipping curve, settings with their defaults; pure, tested; runs in mono and stereo channels, the gain per microphone and capture mode is remembered in `chat.micBoost.v1`), sounds.ts (synthesized join/leave cues, tested), userVolumes.ts (playback volume per person 0-200 %, per device and keyed by public key; tested), radioPlayer.ts (web radio of the voice channel: plain <audio> element, own volume (5 % until set) and off switch per device in `chat.radio.v2`, reconnect, follows deafen and the output device; settings tested), videoWatch.ts (which feeds of others the user watches: cameras until turned off, screen shares once turned on, in memory only; `videoActive` for the stage's "hide participants without video"; tested), videoAccess.ts (permission VIEW_VIDEO: who may receive camera/screen, as LiveKit track subscription permissions; tested), settings.ts (per device in localStorage; external store with subscribers and change source, tested; everything in it except the four device ids follows the directory account via accountSettings.ts), useVoiceSettings.ts (React hook on it)
  public/test/screenshare.html  Standalone test page for PLAN 3.6 (getDisplayMedia with audio per browser/surface); shipped with the client
```

## Conventions

- **Never reload the page for the language directly (18 September 2026):** go through `store.reloadForLocale()`; it waits while a voice connection runs, because a reload throws the user out of their channel (docs/features/settings.md). The same goes for any other reload added later.
- **The desktop app words some texts differently (18 September 2026):** a text that says "this browser", "this tab", "the page" or names a browser permission gets a second wording in `src/i18n/app.de.ts` AND `app.en.ts` (only the keys that differ, same placeholders; `index.test.ts` checks both). `t()`/`tOr()` take those first while running in the app (`lookupText`, recognised by `window.squorliDesktop`). Components never choose between two keys for this; a separate key is right only when the app says something else altogether (`settings.idleHintApp`, `voice.screenNoAudioApp`). When writing a new text, ask whether it is true in the app.
- **UI texts are bilingual German/English (user decision, 15 September 2026):** every text a user sees in the web client comes from the catalogs `apps/web/src/i18n/de.ts` (reference, all keys) and `en.ts` (same keys, enforced by the `Messages` type and the test) via `t("key", { params })`; never write a literal German or English sentence in a component, an error mapping or the voice core. The locale is fixed at page load: an explicit choice in `localStorage` (`chat.locale`, select in the login footer and in Einstellungen > Ansicht; with a directory account it follows the account, see `docs/features/settings.md`) wins, otherwise the first entry of `navigator.languages` we support, otherwise English. Dates use `fmtDateTime`/`fmtTime`/`fmtDay` from the i18n module (`localeTag`). The 503 page of the server picks German or English from `Accept-Language` (`preferredLanguage()` in `src/index.ts`). **Everything a user sees carries real umlauts** (ä/ö/ü/ß) in the German catalog (user requirement 13 September 2026); the ae/oe/ue transliteration is used only in comments, identifiers and developer log lines. `PERMISSION_LABELS` in the protocol package stays German and is no longer used by the client (labels come from the `perm.*` keys).
- **Symbols only via `<Icon name="…"/>`** (Lucide web font, ISC license, self-hosted under `apps/web/src/icons/`), no emojis and no Unicode arrows in the UI (user requirement: uniform, white, license-free). Emoji as chat content and as the things to choose in the emoji picker (including its skin tone samples) are content, not UI symbols; the picker's own controls use `<Icon>`. Color = `currentColor`, size = `font-size`. Look up new names at https://lucide.dev/icons, then `pnpm icons`. Hang up = `phone` rotated by 135 degrees, white on red.
- **User settings follow the directory account, the device selection does not (17 September 2026):** a new user-level setting goes into `VoiceSettings` (or its own per-device store), into the protocol's `AccountSettings` **with a default** (older stored objects and older clients must stay valid; copy `directory.ts` to `../squorli-directory`), into `accountSettings.ts` (all three functions) and into `SettingsDialog.tsx`. Device ids never go into the account. Never add a second settings dialog: everything a user sets for themselves is reachable through the gear.
- **Categorized modals: categories always on the left** (user requirement 13 September 2026, applies without instruction to the contrary to all future modals with several sections). Building blocks: `.modal` > `.modal-head` (title, `.spacer`, close) + `.settings-layout` (`.settings-nav` on the left with `<Icon>` + label, `.settings-body` on the right). Examples: settings (`SettingsDialog.tsx`) and admin panel (`AdminPanel.tsx`). No tab bar at the top.
- **No browser dialogs in the client:** `alert`, `confirm`, `prompt` are taboo (user requirement). Confirmations via `askConfirm()`, text inputs via `askInput()` from `apps/web/src/dialogs.tsx`; both return promises and render their own modal (z-index above admin panel/profile). Show errors inline as `.error` text, not as a popup.
- **Browser or desktop app: one build, differences only through `platform` (18 September 2026, user's decision):** the desktop app ships exactly this client's `dist`. Never branch on the user agent or on `window.squorliDesktop` in a component; ask `platform` (`src/platform/`) and extend its interface when something new differs. `Store` and `VoiceClient` stay free of platform imports and take what they need as constructor arguments. `window.location` is only right where the page's real origin is meant (embeds, `postMessage`, URL parameters); what means "the server that serves the page" comes from `platform.home`, which the desktop app does not have. `bridge.ts` and `deepLink.ts` must stay DOM-free (the Electron main process bundles them).
- **The client may have no home server (desktop app, 18 September 2026):** `state.homeHost`, `state.activeHost`, `homeState()`, `activeState()`, `store.home` and `store.active` can be null. Code that means "the server on screen" uses `view` in App.tsx (`{ active, conn, server }` or null); code for the home server checks for null and must leave the browser's behaviour alone. New store features that assume a home server need a branch for `homeHost === null`. A server the key has never been on is never signed in to without a click (join view).
- **Multi-server client (14 September 2026, user's requirement):** the web client of one server talks to other Squorli servers directly (`ServerApi` with `base` = their origin, WebSocket `wss://<host>/api/ws`, LiveKit URL from their `/api/rtc-token`). The rail switches the shown server without leaving the page; the voice connection stays on its server until the user joins a voice channel on another server, which disconnects the previous one. Login at a foreign server = the same browser key, signature over that server's `/api/health` domain, no invite (the rail only lists servers the handle is a member of); its session token is kept per host. Consequences: every chat server answers CORS for **all origins** (`@fastify/cors` `origin: true`, auth is Bearer-only, no cookies, so a foreign origin cannot act without the token; the signature stays bound to `PUBLIC_DOMAIN`), relative server URLs (attachments, icon) are resolved through `api.abs()`, older servers without CORS show "nicht erreichbar oder erlaubt keinen Zugriff aus anderen Clients" with a link to open them directly. The admin panel, settings dialog (sessions, name on this server; the mini profile too), member list and chat always act on the active server. Not multi-server: the login screen, the directory account and the friends/DM view (they belong to the own server's directory).

## Known pitfalls

- **`@types/dom-mediacapture-transform`** is a peer dependency of `@livekit/track-processors` (types for MediaStreamTrackProcessor); as a devDependency in the web package, otherwise `tsc` breaks.
- Old client after a change, white screen after a deploy, `Route GET:/ not found`: `apps/server/AGENTS.md` pitfalls (static serving). Browser audio, output devices, screen share without audio: `src/voice/AGENTS.md`.

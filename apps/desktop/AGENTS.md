# apps/desktop – Desktop app (`@squorli/desktop`)

Part of the project description. The entry point is the root [AGENTS.md](../../AGENTS.md) (commands, definition of done, cross-cutting conventions, the map of all documentation in its section 0). Whoever adds, removes or repurposes a file here, or changes a convention or pitfall, updates this file in the same step. Decisions, phases and what was checked: `docs/features/desktop.md`.

**The app has no renderer code.** It is an Electron shell around the web client: the window shows the build of `apps/web` (`apps/web/dist`) from `app://squorli`. Everything a user sees is changed in `apps/web`; what differs between browser and app goes through `apps/web/src/platform/` (`Platform`, the bridge contract `bridge.ts`). After a client change run `pnpm build` (root) before `pnpm --filter @squorli/desktop start`; `pnpm dev:desktop` shows the Vite server instead.

## Structure

```
apps/desktop/
  package.json         version = the app's version (release tag `desktop-v<version>`); every dependency is a devDependency, because the bundles contain everything except `electron`
  tsup.config.ts       two CommonJS bundles: out/main.cjs (main process), out/preload.cjs; `noExternal` must leave `electron` out (it wins over `external`)
  electron-builder.yml packaging: NSIS installer per user (Windows x64), AppImage + deb (Linux), artifact names WITHOUT spaces, the web client's build as `resources/renderer` (no source maps), LICENSE/NOTICE/THIRD-PARTY-NOTICES as resources, protocol `squorli`, generic update feed `https://squorli.com/updates/stable`, fuses (no run-as-node, no NODE_OPTIONS, no --inspect, cookie encryption, app only from the asar)
  build/badge-*.png, build/tray-alert.png  marks for waiting direct messages and mentions (task bar overlay 1–9 and 9+, tray icon with a dot), GENERATED by the same tool: digits centred by their ink box, the 1 by its stem (user's corrections, 20 September 2026); packed through `files` in electron-builder.yml
  build/icon.png       the app icon, GENERATED by `node tools/desktop-icon.mjs` from docs/brand/squorli-icon.svg (brand tile #0C1424); electron-builder derives .ico and the Linux sizes
  release-notes/       `<version>.md` per release: what the GitHub release says (users' words, English; sections New / Improved / Fixed). The release workflow puts the file of the tag's version into the draft release, followed by its standing text about the installers; a version without a file gets that text alone
  release/             output of `pnpm --filter @squorli/desktop dist` (ignored by git)
  native/              the native helpers, Windows only: window-audio/main.cpp (a share's audio, WASAPI process loopback: one process tree, or everything except one; PCM on stdout), system-watch/main.cpp (for the AFK detection: `input` lines when a controller was used, XInput polled and HID joysticks/gamepads through Raw Input with INPUTSINK, only real steps count; `display 0|1` lines for ES_DISPLAY_REQUIRED; for the game detection a watch list on stdin, `watch<TAB>path...`, and `game <path>` / `game` lines for the watched program whose window is the topmost one (the game used last; it counts while it has a visible window of its own, so not a background service), never a program outside the list; for the screen share's picker a question on stdin, `windows<TAB>request<TAB>hwnd...`, answered with `window` lines (tool window or not, window class, the program's path, full screen or not) and a closing `windows` line; for push-to-talk across the system a `keys<TAB>hex...` line names the scan codes to watch and `key <hex> 1|0` lines report only those keys, the keyboard being listened to (Raw Input, INPUTSINK) only while a key is named; reads no mouse or window contents, and of the keyboard only the named keys) and build.mjs (MSVC through vswhere -> native/bin/win32-x64/squorli-window-audio.exe and squorli-system-watch.exe, ignored by git; `pnpm --filter @squorli/desktop native`; a helper held open by a running app is moved aside as `.exe.old` first, electron-builder packs `*.exe` only). Both end when their stdin closes. Packaged as resources/native/
  scripts/run.mjs      starts the unpackaged app with ELECTRON_RUN_AS_NODE removed (VS Code's terminals set it; Electron would run as plain Node). A local `--dev-url` that does not answer: starts the web client's Vite server itself (`node apps/web/node_modules/vite/bin/vite.js --port <port> --strictPort`, stopped with the app, taskkill /T on Windows), so `pnpm dev:desktop` works without `pnpm dev` (26 September 2026)
  src/main/index.ts    entry: arguments (unpackaged only: --dev-url, --directory-url, --no-player-fix, --fake-update, --no-splash, --autostart-test), the start sequence (start window -> update check -> hidden main window -> shown at the client's `clientReady`, at the latest after 12 s), user agent, scheme, single instance, window (sandbox, context isolation, no node integration, backgroundThrottling off so voice survives minimizing), `DesktopInfo` for the preload script, IPC with sender check (`isClientFrame`: the client's own main frame only), the shell's language (`IPC.language`: the client's, for the context menu and the tray; `app.getLocale()` until then) A `--dev-url` that does not answer keeps the main window hidden, shows the start window's `devServer` step and asks the dev server every 2 s until the page loads (docs/features/desktop.md, 26 September 2026); `--user-data-dir` given = `Squorli-dev` is not forced
  src/main/secrets.ts  the client's keys encrypted with `safeStorage` in `<userData>/secrets.json` (IPC `secrets*`, synchronous, only `SECRET_KEYS`, only the client's frame); `available` false without real encryption (docs/features/desktop.md, "Keys in safeStorage")
  src/main/appFiles.ts pure, tested: `APP_ORIGIN` (`app://squorli`), path -> file with traversal guard and single-page fallback, content types, the Content-Security-Policy
  src/main/scheme.ts   registers `app` as a privileged scheme (before ready) and serves the client's files (after ready), CSP header on HTML
  src/main/navigation.ts pure, tested: what `window.open` may open (about:blank for video pop-outs, /player-window.html), what goes to the system (http, https, mailto), what a main frame may navigate to
  src/main/security.ts permission handlers (the client: media, display-capture, fullscreen, speaker-selection, idle-detection, clipboard write, window-management; embedded players: fullscreen, plus the audio permission CHECK while playerAudio.ts is at work), window-open and navigation lock, no webviews, `openExternal` behind the allow list, `letPlayersEmbed` (Twitch/YouTube, see pitfalls)
  src/main/deepLinkArgs.ts pure, tested, no Electron import (tests run without Electron's binary): the first valid `squorli://` argument
  src/main/deepLinks.ts link at start, from a second start (single-instance lock) and `open-url`; delivered once the client subscribed; a packaged app registers the scheme
  src/main/appearance.ts pure, tested: background modes the system offers (mica: Windows 11 22H2+; see-through window "clear": Windows, Linux), normalizing a stored/sent appearance, `appearanceState()` = stored vs. shown vs. restart pending; config.ts = `<userData>/desktop-config.json`
  src/main/updates.ts  electron-updater against the baked-in feed `https://squorli.com/updates/stable` (generic provider; the manifests there carry absolute GitHub addresses, sha512 checked), state to the client over `updateState`; updateMode.ts = pure, tested: self (Windows, AppImage), manual (deb), none (unpackaged); `checkAtStart(splash)` = the check at the start, shown in the start window: 10 s for the check, download with progress and "install later", then `quitAndInstall(true, true)`; never rejects, every failure opens the client; a start in the background only calls `checkSoon()`
  src/main/splash.ts   the start window: small, frameless, sandboxed, no preload, refuses every navigation; the shell writes into it. splashPage.ts = pure, tested: its page (a `data:` address without a script), the texts per step in German and English, the script for the shell's writes, `SPLASH_SKIP_URL` ("install later", recognised in `will-navigate`), `mayInstallAtStart` (a version is handed to the installer at a start only once: no loop when an installation does not take) Step `devServer` (development only): the Vite dev server of `--dev-url` does not answer
  src/main/attention.ts pure, tested: the count the client sends (`IPC.attention`), the overlay image for it, the tool tip's text; index.ts `showAttention()` applies it (Windows `setOverlayIcon`, elsewhere `app.setBadgeCount`, the tray through `setTrayAttention`)
  src/main/contextMenu.ts the window's context menu (user's wish, 23 September 2026): Electron shows none by itself, so the shell builds the ordinary one on every right-click the page did not handle (`context-menu` of the client's main frame only; the client's own menus call `preventDefault`, then no event arrives; the embedded players get none): spelling suggestions and "Zum Wörterbuch hinzufügen", undo/redo, cut/copy/paste/select all as Electron's roles (so "Einfügen" fires the page's `paste` event and a clipboard picture reaches the chat's composer like Ctrl+V), copy/open a link (through `openExternal`), copy a picture (`copyImageAt`), save one a server serves (`downloadURL`; without a `will-download` handler Electron asks where). contextMenuItems.ts = pure, tested: `readContextTarget` (Electron's params made safe), `contextMenuEntries` (the entries of one right-click, German or English), `ShellLanguage`, `readShellLanguage`, `languageOfLocale`
  src/main/autostart.ts pure, tested: `--autostart` (a start by the system stays in the background, tray with `closeToTray`, else minimized, unless the user's `autostartBackground` in desktop-config.json says open the window: `startsInBackground`), the Linux desktop entry and its quoting; autostartSystem.ts = the part that touches the system (Windows login item named "Squorli", read back through `launchItems` because `openAtLogin` does not see a named entry; Linux `~/.config/autostart/squorli.desktop`; only packaged, `--autostart-test` for trying it unpackaged)
  src/main/tray.ts     tray icon (build/tray.png, generated, packed into the asar through electron-builder `files`), menu open/quit in the client's language (`setTrayLanguage` from `IPC.language`; the system's locale until the client says its own), click shows the window; `closeToTray` (desktop-config.json, off by default) makes the close button hide the window, quitting then goes through the tray
  src/main/windowState.ts pure, tested: the stored window bounds made safe for today's displays (null = default place); index.ts stores them in desktop-config.json at close and shortly after a move or resize
  src/main/displayMedia.ts `setDisplayMediaRequestHandler`: lists screens and windows (`desktopCapturer`) and says per source whether audio can go with it, leaves desktop widgets out (`isDesktopWidget`: tool windows, Rainmeter) and marks a detected game's windows (`ScreenSource.gameId`), both from what the system watch helper says about the windows (`SystemWatch.describeWindows`, `GameLookup`; without the helper nothing is left out), asks the client's picker over IPC, answers with the source (the pick's `codec` is the client's business); audio comes from the native helper (then Chromium captures none) or, without the helper, as Chromium's `"loopback"` for screens; no answer within two minutes = refused
  src/main/gameWatch.ts game detection (docs/features/games.md): IPC for the client's switch and added programs, the scan, the file dialog for a program to add, and the running game; hands the helper its watch list. games/launchers.ts = pure, tested: what Steam (Valve's text format), Epic (JSON), GOG (`reg query` output) and the Xbox app (`.GamingRoot`, `MicrosoftGame.config`) say is installed; and where each game's icon lies on this computer (`InstalledGame.icons`, `fileInside`, `steamIconFile`); games/scan.ts reads those files and registry keys (nothing is written, every failure = none from there, 8 s per source); games/icons.ts = `GameIcons`: the icons for the settings' list from those sources (a picture through `nativeImage`, an executable or .ico through `app.getFileIcon`), always re-encoded as a PNG of at most 32 px in a data URL, found ones kept for the session; games/match.ts = pure, tested: the client's settings made safe, the watch line, path -> game, the list for the client without paths
  src/main/systemWatch.ts runs native/squorli-system-watch.exe as long as the app runs (at most five restarts), sends it the watch list and the watched keys (again after a restart), hands `game` lines to gameWatch.ts and `key` lines to hotkeys.ts (`setKeys`, `onKey`; null = the helper is gone) and sends its other events to the client (`IPC.systemActivity`; a client that subscribes, `IPC.systemActivityReady`, gets the current display state); `systemWatchPath()` for `DesktopInfo.systemWatch`, `available()`. `describeWindows(hwnds)` asks it about windows (700 ms, never rejects); systemWatchLines.ts = pure, tested: the helper's lines as `SystemActivityEvent`s, `key` events and the `window`/`windows` answers, cut chunks, unknown lines skipped (docs/features/afk.md, 20/21 September 2026)
  src/main/hotkeys.ts  global shortcuts and control from outside (docs/features/hotkeys.md): IPC `hotkeysSet` registers the client's bindings with `globalShortcut` (accelerator from `@squorli/web/platform/hotkeys`; answers `ok`, `taken` or `invalid` per binding) and hands the push-to-talk key to the system watch helper (scan code from keyCodes.ts, watched only while the client asks and while not suspended; status `ok`, `invalid`, `unsupported`); `hotkeysSuspend` lets everything go while the client captures a key; every command reaches the client as a `ControlEvent` on `IPC.control`; `control(action)` for the links and arguments of a second start (index.ts, carried out without showing the window). keyCodes.ts = pure, tested: `KeyboardEvent.code` -> Windows scan code (Chromium's table, `E0_FLAG`), the helper's `keys` line; controlArgs.ts = pure, tested, no Electron import: `findControl(argv)` = the first `squorli://control/<action>` or `--control=<action>` argument
  src/main/windowAudio.ts `nativeHelperPath(name)` (a helper of native/ by file name, packaged or not), `helperPath()` and `ScreenAudioCapture`: runs native/squorli-window-audio.exe for a share and sends its PCM to the client in 20 ms chunks; captureSource.ts = pure, tested: window handles of source ids, `WindowInfo`, `isDesktopWidget`
  src/main/linkLookup.ts IPC `linkLookup` (bridge `lookUpLink`): looks a link up for the preview of a direct message with `@squorli/link-preview`, from this computer, so no server learns the link; public hosts only, 30 per minute, 4 at a time; `readLinkLookupRequest` pure and tested (docs/features/link-previews.md)
  src/main/playerAudio.ts `PlayerAudioOutput`: the embedded players' sound on an output device of the user's choice, one per kind of player (`playerKindOf` in playerAudioScript.ts): the web radio's players on the radio's device (IPC `playerOutput`), the players of videos linked in the chat, whose address carries the fragment `#squorli-chat`, on the device of screen share audio (IPC `chatPlayerOutput`, 21 September 2026, docs/features/link-previews.md); each a device LABEL: runs playerAudioScript.ts (pure, tested: the player origins, the label check, the script that calls `setSinkId` inside the frame) in every Twitch/YouTube frame every 3 s while a device is chosen; `granting()` opens the microphone permission CHECK (audio only, never a request) for those origins in security.ts while a device is chosen: Chromium asks again at every new video and a "no" silences the player (docs/features/desktop.md, 20 September 2026)
  src/main/userAgent.ts pure, tested: `… Squorli-Desktop/<version> Chrome/<v> Electron/<v> Safari/537.36`
  src/preload/index.ts the bridge `window.squorliDesktop` (contextBridge): plain data both ways, the page never sees ipcRenderer or an event
```

## Packaging and releases

- Local installer: `pnpm build` (root), then `pnpm --filter @squorli/desktop dist` -> `apps/desktop/release/` (`Squorli-Setup-<version>.exe`, its `.blockmap`, `latest.yml`, `win-unpacked/`). Unsigned (`CSC_IDENTITY_AUTO_DISCOVERY=false` avoids a certificate search).
- **Release:** the skill `desktop-release` (`.claude/skills/desktop-release/SKILL.md`, user's wish of 20 September 2026) prepares it: it reads what changed for the app since the last `desktop-v*` tag (`docs/MILESTONE-LOG.md` is the source, the commit subjects are too terse), proposes version numbers with reasons, sets the chosen one and writes `release-notes/<version>.md`; it never tags or pushes. By hand the same: set `version` in `apps/desktop/package.json`, write the notes file, commit, tag `desktop-v<version>` (the workflow refuses a tag that does not match). The GitLab push mirror carries the tag to GitHub, where `.github/workflows/desktop-release.yml` builds on windows-latest and ubuntu-latest and collects exe, blockmap, AppImage, deb, `latest.yml` and `latest-linux.yml` in a **draft** release. A person tests and publishes the draft; the update feed follows from squorli-website (P4). Tags `v*` are the server image's; both pipelines' image jobs ignore `desktop-v*`. GitLab builds no installers.
- The deb package names its maintainer in `electron-builder.yml` (`linux.maintainer`: Daniel Klessa, desktop@squorli.com; the user's decision of 18 September 2026, the address is public with the package). Without it the deb target fails.
- **Trying the release workflow without spending a version:** Actions > "Desktop release" > Run workflow on a branch with the tag field empty = a test build (artifacts only, no tag check, no release). First run of `desktop-v0.1.0` (18 September 2026) failed at `pnpm licenses`: pnpm has a built-in command of that name that hides the script; the workflow uses `pnpm run`.
- Third-party notices: Electron ships its own `LICENSE.electron.txt` and `LICENSES.chromium.html`; the main bundle contains zod and @noble (already in the web client's list) and, since P4, electron-updater with its dependencies (MIT and similar). **Open:** `tools/licenses.mjs` reads `apps/web` only, so those are not yet listed in `THIRD-PARTY-NOTICES.md`; extend the tool to the desktop bundle's production graph.
- **Testing updates without touching an installed app:** build the new version (`dist`), build an old one with `electron-builder --dir -c.extraMetadata.version=<old> -c.directories.output=<scratch>`, write `resources/app-update.yml` there by hand (`provider: generic`, a local `url`, an own `updaterCacheDirName`), serve a manifest locally, watch the states over the DevTools protocol and **kill the test app hard**: a graceful quit runs the downloaded installer.

## Conventions

- **Security baseline, do not weaken:** `contextIsolation: true`, `sandbox: true`, `nodeIntegration: false`, `webSecurity: true`; every `ipcMain` handler checks `isClientFrame(event)`; nothing reaches `shell.openExternal` except through `openExternal()` (http, https, mailto); child windows get no preload; a packaged app ignores every development argument. New IPC channels are added to `IPC` in `apps/web/src/platform/bridge.ts`, to the preload bridge and to `Platform`, in that order.
- **`ipcMain.on` for `sendSync`: assign `event.returnValue` exactly once.** Electron sends it at the first assignment; a default set first (`event.returnValue = null`, then the real value) answers the default for ever, silently. Compute the answer, then assign (`secrets.ts`).
- **`app://squorli` is final once a public version exists.** The client keeps the directory servers' sessions in that origin's localStorage (the keys moved to `safeStorage` on 25 September 2026, `secrets.ts`; where it is not available they stay there too); renaming scheme or host loses them for every user.
- **Pure logic goes into its own module with tests** (`appFiles.ts`, `navigation.ts`, `userAgent.ts`); the Electron-bound files stay thin. `bridge.ts` and `deepLink.ts` of the web package must stay DOM-free: the main process bundles and runs them. (The compiler does not enforce it: the protocol package they pull in needs the DOM library's `CryptoKey` type, so this package is typechecked with the default libraries like the server.)
- **Tests must not import `electron`:** the package's entry throws when its binary is missing, which is the case in CI (`ELECTRON_SKIP_BINARY_DOWNLOAD=1`). Keep what is tested in modules without that import.
- The fixed directory is `https://directory.squorli.com`; `--directory-url` exists for development only.

## Known pitfalls

- **`ELECTRON_RUN_AS_NODE=1`** (set by VS Code in its terminals, also for agents run from it): Electron starts as plain Node, `require("electron").app` is undefined and the app dies with "Cannot read properties of undefined (reading 'isPackaged')". Start through `scripts/run.mjs` (the `dev`/`start` scripts do) or remove the variable.
- **Electron's binary after `pnpm install`:** pnpm 10 only runs install scripts of packages listed in `onlyBuiltDependencies` (pnpm-workspace.yaml lists `electron`). If `node_modules/electron/dist` is missing anyway (it was, right after adding the package on 18 September 2026), run `node node_modules/electron/install.js` in `apps/desktop`. CI test jobs and the Docker build set `ELECTRON_SKIP_BINARY_DOWNLOAD=1`: they never start the app.
- **Twitch and YouTube under `app://`:** without help Twitch refuses to be framed (its `frame-ancestors` names `https://<parent>`, and the parent is the host name `squorli`) and YouTube shows "Fehler 153" (no http(s) referrer). `letPlayersEmbed()` drops `frame-ancestors` for `https://player.twitch.tv/*` and sets `Referer: https://squorli.com/` for `https://www.youtube-nocookie.com/embed/*`, for exactly these hosts. Checked with and without it on 18 September 2026 (`--no-player-fix`).
- **Driving the app from a script:** start `electron.exe <apps/desktop> --remote-debugging-port=<port> --user-data-dir=<dir>` (variable above removed) and attach over the DevTools protocol. `--use-fake-device-for-media-stream` gives a microphone; **never add `--use-fake-ui-for-media-stream`**: it answers `getDisplayMedia` itself and the picker never appears. `Runtime.evaluate` ignores the page's CSP unless `allowUnsafeEvalBlockedByCSP: false` is passed.
- **`pnpm dev:desktop` works on its own:** the app loads the web client from the Vite dev server on 5173; `scripts/run.mjs` starts that server itself when nothing answers there and stops it with the app (since 26 September 2026, after the user's report that a stopped chat server, tested on purpose, left the app with the start window's "dev server does not answer"; before that day an empty window without a title bar). A Vite already running (`pnpm dev`) is left alone. The chat server on :3000 is not needed for the app to come up: a rail server that does not answer gets the notice with the countdown (docs/features/offline.md). A hidden `BrowserWindow` never answers `Page.captureScreenshot` over the DevTools protocol: give such calls a time limit.
- **`app.getLocale()` is only reliable after `ready`** (Windows answers "" before): index.ts reads it inside `whenReady` for the shell's language. **Electron 44's `clipboard` is the promise-based one** (`writeText` returns a promise, `write([new ClipboardItem({...})])`, `read()`); the old `writeImage`/`availableFormats` are gone.
- **Electron's icon in the task bar** belongs to an unpackaged app (`electron.exe`); the window's `icon` and the AppUserModelID make Windows show Squorli's. A packaged app has the icon in its executable.
- **The window is frameless (`frame: false`) in every mode** and the client draws the title bar (`apps/web/src/TitleBar.tsx`). `transparent` cannot change on a living window: a change to or from the see-through mode needs `app.relaunch()`; never try to toggle it at run time. Overlays of the client must start below the title bar (`--titlebar-h`), or a dialog blocks moving and closing the window.
- **A registered global shortcut never reaches any window, ours included** (`globalShortcut` takes the combination from every program): whoever captures a key in the client must have the shell let the shortcuts go first (`IPC.hotkeysSuspend`), and the push-to-talk key must never be a shortcut (the helper watches it instead, docs/features/hotkeys.md). `globalShortcut` on Linux works with X11 only.
- **Without the system watch helper** the app works and the AFK detection does not see controllers (`DesktopInfo.systemWatch` false, `platform.systemActivity` null), and the push-to-talk key works in the window only (`DesktopInfo.hotkeys.globalPtt` false). Chromium's Gamepad API is no replacement: it delivers only while the window has the focus (the user's test, 21 September 2026).
- **Without the native helper** (not built, `pnpm --filter @squorli/desktop native` needs Visual Studio or the Build Tools with the C++ workload) the app works and a share offers the whole system's audio for screens only, the app's own sound included. `DesktopInfo.nativeScreenAudio` tells the client which way it is.
- **Processes named electron.exe may be the user's own running app.** Test instances carry `--remote-debugging-port` and a scratchpad `--user-data-dir` in their command line (`Get-CimInstance Win32_Process`); never kill Electron processes by name. With `closeToTray` on, a closed window leaves the app running by design.
- **Windows Firewall** asks once for "Electron" when the first voice connection opens its UDP port (unpackaged app; a packaged one asks for "Squorli").
- Cancelling the picker reaches the page as `AbortError` ("Invalid capture constraints"), which the voice client already treats as a cancel.

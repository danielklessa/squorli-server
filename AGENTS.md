# AGENTS.md – Working guide for AI agents and new developers

This file is the central project description for all coding agents (Claude Code, Codex, Cursor, etc.).
`CLAUDE.md` includes it and only adds Claude-specific rules.
**Mandatory:** Whoever changes the structure, commands, configuration, protocol or milestone state updates this file in the same step.

Last check of the complete workflow: 14 September 2026 (all green, see section "Verified state").

## 1. What the project is

**Squorli**: self-hosted community chat with voice and video channels (modeled on Discord, but every server belongs to its operator).
Solo development, TypeScript/Node, shipped as Docker containers. The name Squorli applies since 14 September 2026 (user's decision) in the code, in package names (`@squorli/*`), Compose projects, image tags and in the user interface.

**Brand and design: all questions about logo, colors, typography, spacing and tone of voice are governed by [docs/brand/AGENTS.md](docs/brand/AGENTS.md)** (brand package version 2 with assets, `squorli-tokens.css`, `palette.json`, `brand-guide.html`). Read it before any design change; the tokens have been adopted as `--sq-*` in `apps/web/src/styles.css` and `apps/directory/web/static/style.css`, the SVGs exist as copies under `apps/web/public/brand/` and `apps/directory/web/static/brand/` (the source remains `docs/brand/`; on changes, update both copies).
Product plan, architecture decisions and milestones: [docs/PLAN.md](docs/PLAN.md). Read it before building larger features.

**Current state: M3 (video and screen share) implemented in the browser, acceptance (15 real cameras, tab audio from Chrome for everyone, test matrix 3.6) pending. M6a-M6c done (directory: handles, password backup, authenticator, devices), M6d open.** Available: everything from M2 (login with membership, categories/channels, roles, kick/ban, text chat with attachments, admin panel) plus camera with simulcast, stage with tile and speaker view, screen share with audio as a separate audio track (only Chromium delivers audio, other browsers get a notice), video bitrates in the debug view, test page `/test/screenshare.html` for the browser matrix, video bots. Voice as in M1 (LiveKit, VAD/PTT, device selection). Remaining M1 acceptance (restrictive network, TURN) deferred, see section 8.
**M6 brought forward (user's decision, 14 September 2026):** directory service so that registration works properly. **Since 14 September 2026 it lives in its own, unpublished repo `../squorli-directory`** (user's decision: the directory is not published with the chat; until then `apps/directory` in this monorepo). Operated on a **dedicated host with its own subdomain**; this repo only knows `DIRECTORY_URL`. Sync rules for the shared protocol part and the brand package: section 2a. The second factor is a TOTP authenticator; SMTP is prepared in configuration and schema but will only be implemented later. Phases: **M6a (done)** register and resolve handles, chat server shows verified handles; **M6b (done 14 September 2026)** password-encrypted key backup: create an account (handle + password) directly on the service's account page (user's wish), log in on any chat server with handle + password, change the password on the account page, backup for existing browser keys in the login; **M6c (done 14 September 2026, without e-mail)** TOTP authenticator and recovery codes as second factor for key retrieval and account changes (user's decisions of 14 September 2026: factor on retrieval + password change + disabling + regenerating codes, never on chat login by key; setup only on the account page; dependency `qrcode-generator` allowed), device management = sessions per chat server with remote logout (Profile > Geraete) plus a read-only display of key retrievals on the account page (the service has no sessions; whoever has the key has it); **M6d** server directory.
**Voice channel moderation (13 September 2026, user's requirement):** permission `MODERATE_VOICE`: move members to other voice channels or remove them, end their camera/screen share, block camera/screen per member (`members.stream_blocked`, overrides roles). LiveKit enforces muting and publish permissions server-side (`livekit/admin.ts`), the client follows via WS event. Also: camera selection with multiple cameras, background blur (`@livekit/track-processors`), separate output device for screen audio.
**Roles (user's decision, 13 September 2026):** the default role is "Gast" (guest) and may only see channels and join voice channels. "Mitglied" (member: write, attach, invite, camera/screen) is assigned manually by admins; migration 0004 automatically turned existing members into "Mitglied".
Decisions from PLAN 9 for M2 (preliminary, made by Claude on 13 September 2026, not confirmed by the user): text chat = channel messages with edit/delete, no direct messages/reactions/threads; moderation = role permissions + kick/ban, no audit log, no report function; permissions apply server-wide, no channel overrides. Not available: desktop client (M4), TURN acceptance.

## 2. Repository structure

```
apps/server/           App server: Fastify 5, Drizzle ORM + postgres-js, LiveKit server SDK. Serves the built web client.
  src/index.ts         Entry point: load .env, config, migrations, plugins, routes, static files
  src/config.ts        zod schema for all environment variables (the single place for configuration); empty values count as unset; DIRECTORY_URL = directory service (optional)
  src/directory.ts     refreshHandle(): on login, look up key -> handle at the directory (2.5 s timeout, outage tolerated) and cache it in users.handle
  src/bootstrap.ts     On startup: settings, roles "Gast" (default: view + voice), "Mitglied", "Admin", category "Allgemein" with #allgemein and lobby, one-time adoption of users without membership
  src/authz.ts         Pure permission logic (outranks, canTouchRole, canGrant, effectivePermissions), tested
  src/state.ts         Load server state (settings/categories/channels/roles/members), actorOf() = permission context, broadcastStructure()
  src/hub.ts           WebSocket connections per user: online status, broadcast, sendToUser, disconnectUser (kick/ban), disconnectSession (remote logout, close 4011)
  src/auth/            Challenge store, /api/auth/challenge + /verify (ban check, consume invite, set owner, session with device label from the user agent); session.ts: requireSession() (returns sessionId, updates last_used_at throttled), requireMember()
  src/users/           GET/PATCH /api/me (display name); M6c: GET /api/me/sessions, DELETE /api/me/sessions/:id | /others | /current (device management, remote logout closes the WS with 4011)
  src/routes/          settings.ts (/api/state, /api/settings; server icon: PUT/DELETE /api/settings/icon with MANAGE_SERVER, GET /api/server-icon public, file DATA_DIR/server-icon), channels.ts (categories + channels), roles.ts, members.ts (set roles, kick, bans; PUT /api/members/:id/owner = appoint/revoke owner, only by an owner; moderation: POST /api/members/:id/move, POST /api/members/:id/stream/stop, PUT /api/members/:id/stream), invites.ts, messages.ts (history, send, edit, delete), attachments.ts (upload/download under DATA_DIR, cleanup; @fastify/multipart is registered in index.ts)
  src/livekit/         routes.ts: POST /api/rtc-token (voice channels only, CONNECT_VOICE; URL without /rtc path; canPublishSources: camera/screen only with STREAM_VIDEO). admin.ts: RoomServiceClient for moderation (mute tracks, publish permissions at runtime)
  src/voice/           VoicePresence: who is in which voice channel (per WS connection, in-memory)
  src/ws/              GET /api/ws (hello -> welcome with ServerState + voice.state per channel; ping, voice.join/leave, typing). Server -> client additionally voice.moved / voice.stop (moderation)
  src/db/schema.ts     Drizzle schema: users (handle, handle_checked_at), sessions (id, label, last_used_at), server_settings (owner_id = first owner, icon_mime, icon_updated_at), members (stream_blocked, is_owner), categories, channels (audio_bitrate, audio_stereo), channels, roles, member_roles, invites, bans, messages (bigserial seq), attachments
  drizzle/             SQL migrations; 0000-0002, 0005-0009 generated, 0003/0004 by hand (`drizzle-kit generate --custom`: permission backfill, roles Gast/Mitglied)
  tsup.config.ts       Server bundle; bundles @squorli/protocol in (noExternal) so that dist runs without a workspace link
  scripts/smoke.mjs    End-to-end smoke test against a running server
  public/              (gitignored) built web client, served if present
apps/web/              Web client: Vite 5 + React 18 + livekit-client. Later the basis for the Electron shell.
  src/identity.ts      Key pair in localStorage (unencrypted; backup at the directory from M6b), identityFromPrivateKey()/storeIdentity() for restoring
  src/api.ts           All REST calls (auth, profile, sessions, messages, upload, admin, directory); ApiError with error code; explainLoginError(); onUnauthorized() hook on 401 with a token set
  src/store.ts         UI-free client state: session (localStorage), WebSocket with reconnect, ServerState, message cache per channel, presence, typing, unread; sessionLost() (WS close 4011, WS error unauthorized, REST 401) = back to the login with a message; logout() also ends the session server-side
  src/App.tsx          Layout: sidebar | chat or stage | members; voice client; debug toggle; sets page title and favicon (branding.ts: server name from state or /api/health, server icon or Squorli icon mark)
  src/ProfileDialog.tsx Profile as a categorized modal (M6c): Profile (display name), Devices (sessions of this server, log out individually/all others), Account (handle, key, link "Konto verwalten" (manage account) to the account page with ?handle=, log out, discard identity)
  src/VoiceStage.tsx   Stage (M3): tile per participant (camera/avatar) + per screen share. Tiles by default, size computed via ResizeObserver so that everything fits into the area without scrolling; click on a tile = large (focus), click on the large tile = back; "Sprecher" (speaker) follows the speaker/newest share without a click. Control bar mic/camera/screen/leave, notice for a share without audio. Names come from `members` (immediately after renaming), not from the LiveKit token
  src/LoginScreen.tsx  Login with directory: "Mit Konto anmelden" (sign in with account: handle + password, fetches the key via backup, M6b; code field appears after 401 totp_required, M6c) and "Schluessel dieses Browsers" (this browser's key: register handle, "Passwort festlegen" (set password) = store backup); links to the service's account page (create, manage); invite code (from /invite/<code> or ?invite=), invite preview, error explanation
  src/Sidebar.tsx      Header with server icon (otherwise Squorli icon mark) and server name; categories/channels, voice members per channel (drag & drop into other voice channels: yourself = join, others = POST /move with MODERATE_VOICE), unread, open admin panel
  src/ChatView.tsx     History with loading more when scrolling up, grouping, links, image attachments, edit/delete, composer with files, typing indicator
  src/MemberList.tsx   Owners at the very top (own group, crown), then members by role/online; context menu: set roles, kick, ban, voice channel moderation (move, end camera/screen, block) depending on permission, for owners additionally "Zum Eigentuemer machen" (make owner) / "Eigentuemerstatus entziehen" (revoke owner status)
  src/AdminPanel.tsx   Admin panel (modal, categories on the left): Server (name, open, upload/remove server icon, list of owners), Categories/Channels (create, rename, sort, delete), Roles (permissions, color, position), Invites, Bans
  src/CameraPicker.tsx Camera selection when turning on: camera + background (blur) with live preview via an unpublished LiveKit track including BackgroundBlur; appears with multiple cameras or when the browser supports background effects. App.tsx: toggleCamera()
  src/VoiceDock.tsx    Bottom left: name, voice status, mute, deafen ("Ton aus"), camera, hang up, "Ansicht" (view: open the stage). Settings as a modal with categories on the left: Speaking (VAD/PTT, threshold, hold time, level), Devices (microphone, voice output, screen audio output), Camera (device, quality, background)
  src/DebugPanel.tsx   Protocol inspector: WS events, LiveKit statistics, ICE path, video bitrates per simulcast layer/received track (measurement for PLAN 4.3); bug button or ?debug
  src/Icon.tsx         <Icon name="…"/>: symbols from the Lucide web font (ISC), self-hosted; no emojis in the user interface
  src/icons/           GENERATED by tools/icons.mjs: icons.css (only used code points) + lucide.woff2; do not edit by hand
  src/styles.css       Styling per docs/brand: maps the --sq-* tokens to app variables (--bg Night, --bg2 Surface, --bg4 Raised, --sel Selection, --accent Action, --brand Squorli blue, --ok/--warn/--danger Mint/Amber/Coral), radii 12/20 px, focus ring in brand blue
  public/brand/        Copy of the brand assets from docs/brand (logo, horizontal logo, icon mark, small icon mark, white icon mark, squorli-tokens.css); index.html loads tokens and favicon, the login shows the logo, the sidebar the small icon mark
  src/dialogs.tsx      askConfirm()/askInput() + DialogHost: own confirmation/input dialogs (replacement for window.confirm/prompt), mounted in main.tsx
  src/voice/           UI-free core (PLAN 3.4): voiceClient.ts (LiveKit room, camera/screen, background blur, separate screen audio output, tiles, statistics, moderation notices), micPipeline.ts (Web Audio gate), gate.ts (VAD logic, tested), settings.ts (per device in localStorage)
  public/test/screenshare.html  Standalone test page for PLAN 3.6 (getDisplayMedia with audio per browser/surface); shipped with the client
packages/protocol/     @squorli/protocol: zod schemas for REST and WebSocket, PROTOCOL_VERSION. Single source of truth for the client/server contract. primitives.ts (Uuid, Iso, PublicKey, Signature), directory.ts (ChallengeRequest/Response, handle rules, registration/backup/action message, directory contract incl. backup, SecondFactorCode, TotpSetupResponse, RecoveryCodesResponse, AccountStatus/KeyFetch; primitives/directory/backup/useragent are COPIED into ../squorli-directory, section 2a), backup.ts (M6b crypto: PBKDF2 + HKDF + AES-GCM via WebCrypto, isomorphic for browser, Node and account page; tested), useragent.ts (labelFromUserAgent: "Chrome auf Windows", for chat server and service; tested), index.ts incl. SessionInfo (M6c)
deploy/                compose.yml (prod, profiles bundled/external; services app, postgres, livekit, caddy), compose.dev.yml (Postgres + LiveKit), caddy/Caddyfile (/rtc* -> livekit, rest -> app), livekit/livekit.yaml
deploy/proxies/        external mode: README (instructions for nginx, Nginx Proxy Manager, Traefik), nginx.conf, nginx.ports.yml (overlay: 3000/7880 only on 127.0.0.1), npm.network.yml (overlay: app/livekit into the proxy network), remote-proxy.ports.yml (overlay: 3000/7880 on PROXY_BIND_IP for a proxy on another host), traefik.labels.yml
docs/PLAN.md           Project plan (architecture, network, milestones, risks, open decisions)
docs/brand/            Squorli brand package (version 2): AGENTS.md = binding design rules, README, brand-guide.html, palette.json, squorli-tokens.css, SVG logos/icon marks, previews. Single source for brand and design; copies of the assets live in both apps
tools/dev.mjs          Development start with cleanup (containers up, start apps, stop containers on exit); Node-only, no dependencies
tools/copy-web.mjs     copies apps/web/dist to apps/server/public (part of `pnpm build`)
tools/icons.mjs        collects all `<Icon name>` names in the web client, generates apps/web/src/icons/ from lucide-static (part of `pnpm build`, also `pnpm icons`); unknown name = build abort
tools/bots.mjs         Load test bots: `lk load-test` from the image livekit/livekit-cli, attaches to the network of the LiveKit dev container; `--video N` for camera bots (M3 bandwidth measurement)
Dockerfile             Multi-stage build, target `app` (web + server); `pnpm deploy --legacy` (pnpm 10 requires the flag), runtime image only dist + drizzle + node_modules + public
.dockerignore          keeps node_modules, dist, .env, public out of the build context
.gitlab-ci.yml         GitLab CI: `test` (typecheck, vitest, build; every push/MR/tag), `build` (image target `app` into the project registry on the default branch: `<branch-slug>`, `<short-sha>`, `latest`), `build-tag` (on git tags: `<tag>`, `latest`). Modelled on `../squorli-directory/.gitlab-ci.yml`, keep both in step. Servers use the image via `APP_IMAGE` in `.env` (compose.yml: `${APP_IMAGE:-squorli/app:local}`)
.env.example           Template for production (.env in the repo root, read by compose.yml)
.env.development       Template for local development (copy to apps/server/.env)
```

Workspace: pnpm 10 (`packageManager` in package.json, `corepack enable`), Node >= 22 (tested with Node 25).
Package names: `@squorli/server`, `@squorli/web`, `@squorli/protocol` (the directory service `@squorli/directory` lives in `../squorli-directory`). The protocol package is consumed as TypeScript source (no build step). `packages/protocol/src/permissions.ts` = permission bitmask (append new permissions at the end).

## 2a. Sibling repo `../squorli-directory` (not published) and synchronized copies

The directory service (handles, key backup, authenticator) is the separate, **unpublished** repo `squorli-directory` next to this one (same parent folder). It has its own `AGENTS.md`, dev stack (Postgres on :5433, service on :3100), Dockerfile and `deploy/`. This repo never imports from it; the chat server talks to it only over HTTP (`DIRECTORY_URL`), the web client directly (URL from `/api/health`).

Two things exist in both repos and **must stay byte-identical**. Whoever changes one side syncs the other in the same work step (this is mandatory for every agent) and mentions it in the final report. Check with `diff -r` before finishing.

| What | Source (edit here) | Copy in `../squorli-directory` |
|---|---|---|
| Directory contract of the protocol: `packages/protocol/src/{primitives,directory,backup,useragent}.ts` and their `*.test.ts` | this repo | `packages/protocol/src/` (its `index.ts` only re-exports these four; the chat part of the protocol is not copied) |
| Brand package `docs/brand/` (assets, tokens, `AGENTS.md` with the design rules) | this repo | `docs/brand/` and the asset copy `apps/directory/web/static/brand/` (same files as `apps/web/public/brand/` here) |

Rules: new or changed directory schemas are defined here first (the web client parses the responses), then copied over and `pnpm typecheck && pnpm test` run in both repos; a brand change here is copied to `docs/brand/` there and to both asset folders (`apps/web/public/brand/` here, `apps/directory/web/static/brand/` there). If `../squorli-directory` is not checked out, say so in the report instead of guessing.

## 3. Commands

All from the repo root:

| Command | Purpose |
|---|---|
| `pnpm install` | Dependencies (the lockfile is binding, `--frozen-lockfile` in the Docker build) |
| `cp .env.development apps/server/.env` | Provide the dev configuration (once) |
| `pnpm dev` | **Normal development start** (`tools/dev.mjs`): starts Postgres + LiveKit via Compose, then the server (`tsx watch`, :3000) and the web client (Vite, :5173, proxies `/api` including WS to :3000). The directory service is started in `../squorli-directory` (`pnpm dev` there, :3100). On exit (Ctrl+C, crash of an app) the containers are stopped automatically (`compose stop`, data is kept). Options: `--no-docker` (leave the containers alone), `--down` (`compose down` on exit). |
| `pnpm dev:apps` | Only server + web client in parallel, without container management |
| `pnpm docker:dev` / `pnpm docker:dev:down` | Start / remove Postgres (5432) + LiveKit in dev mode (7880/7881/7882) manually |
| `pnpm typecheck` | `tsc --noEmit` in all packages |
| `pnpm test` | Vitest in all packages (`--passWithNoTests`) |
| `pnpm icons` | Regenerate the icon subset from Lucide (also runs in `pnpm build`); run once after every new `<Icon name>`, otherwise the glyph is missing in the dev server |
| `pnpm build` | Generate icons, web client (`tsc` + `vite build` to `apps/web/dist`), server (`tsup` to `apps/server/dist/index.js`), then `tools/copy-web.mjs` copies the client to `apps/server/public` |
| `pnpm smoke` | Smoke test (auth, owner, roles "Gast"/"Mitglied", invites, sessions/devices with remote logout, structure, permissions, messages, attachments, WS, RTC token grants, voice channel moderation, kick/ban, voice profile per channel, appoint/revoke owner, server icon, directory handle if `DIRECTORY_URL` is set; 71-73 checks), **needs a running server on :3000** (`SMOKE_URL` for others). Remembers the owner key in `apps/server/scripts/.smoke-owner.json`. **Best against a separate test DB** (see pitfalls), because the first login becomes the owner. |
| `pnpm bots [--audio 15] [--video 0] [--resolution high] [--duration 60s] [--subscribers 0] [--room lobby]` | Put load-test bots into the voice channel; needs the running dev LiveKit. Visible and audible in the web client as "(extern)" (external). `--video 15 --audio 0 --subscribers 1` = bandwidth measurement as in PLAN 4.3 (room = channel UUID so they appear in the client) |
| `pnpm db:generate` | Generate a Drizzle migration from a schema change |
| Client URL `?debug` / `?ice=relay` | Show the debug view / force the browser onto TURN (README "Simulating restrictive networks") |
| Client URL `/test/screenshare.html` | Test matrix PLAN 3.6: does this browser deliver an audio track for tab/window/screen? Enter the result in the table |
| `docker build --target app -t squorli/app:local .` | Build the production image |

Individual packages: `pnpm --filter @squorli/server <script>`; inside the package folder `pnpm dev`, `pnpm build`, `pnpm start` also work directly.

Start the built server locally: `node apps/server/dist/index.js` (reads `apps/server/.env`, serves `apps/server/public`); `STATIC_DIR` overrides the path. If `index.html` is missing there, `/` responds with 503 and an explanation and the startup log warns.

## 4. Definition of Done for every change

1. `pnpm typecheck` and `pnpm test` green.
2. For changes to the server, protocol or auth: start the server and `pnpm --filter @squorli/server smoke` green.
3. For changes to the Dockerfile, compose, configuration: `docker build` runs through.
4. Affected documentation updated: `AGENTS.md` (this file), `README.md`, if applicable `docs/PLAN.md` and `.env.example`.
5. New environment variables: in `apps/server/src/config.ts` **and** `.env.example` **and** `.env.development` **and** if applicable `deploy/compose.yml`.
6. For changes to the directory contract or the brand package: copies in `../squorli-directory` synchronized (section 2a), typecheck/test there green.

## 5. Conventions

- **Language:** Documentation, READMEs and AI instructions (AGENTS.md, CLAUDE.md, docs/, deploy READMEs, comments in env templates and deploy configs) are always in English (decision of the user, 14 September 2026). Code comments and UI texts stay German. **Everything a user sees carries real umlauts** (ä/ö/ü/ß): JSX text, message strings, tooltips, placeholders, `PERMISSION_LABELS` in the protocol package, server response texts such as the 503 page (user requirement 13 September 2026). The ae/oe/ue transliteration is used only in comments, identifiers and developer log lines.
- **Formatting:** `.editorconfig` (2 spaces, LF, UTF-8, newline at end of file). No linter/formatter configured; keep the existing style (compact one-liners are common).
- **TypeScript:** `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`. ESM everywhere (`"type": "module"`). Imports without file extension.
- **Validation:** All inputs (HTTP bodies, WS messages, env) go through zod schemas. Define new REST/WS types first in `packages/protocol/src/index.ts`, derive the types from there, never declare them twice.
- **Protocol version:** Bump `PROTOCOL_VERSION` in `@squorli/protocol` on incompatible changes (currently 4; new permission bits are not an incompatible change, new server events are, because the client parses them strictly with zod); the server rejects other versions with WS code 4002. The constant is also in `apps/server/scripts/smoke.mjs`, update it there too.
- **Voice channel presence vs. media:** `voice.join`/`voice.leave` over the WebSocket are the intent ("I am in the channel", for everyone's sidebar). Whether someone actually sends, hears or speaks is known only to LiveKit; the client shows both together. Participants known only to LiveKit (bots) appear as "(extern)" (external).
- **Mute and deafen (Discord logic, user requirement 13 September 2026):** `setMuted()` = microphone muted by hand; `setDeafened()` = incoming audio muted (`<audio>.muted`) **and** microphone force-muted. Undeafening restores the microphone only if it was not muted by hand (`micMutedByUser` in the voice core). Clicking the microphone while deafened ("Ton aus") lifts both. Deafen ("Ton aus") is set as the LiveKit attribute `deafened` (token grant `canUpdateOwnMetadata`) so others see it in the sidebar and tiles.
- **Voice codec:** always Opus in the browser (mandatory WebRTC codec, nothing better available). Quality per voice channel in the admin panel: `audioBitrate` (kbit/s, choice from `AUDIO_BITRATES`, default 64) and `audioStereo`. Mono = DTX + RED (silence costs nothing, redundancy against packet loss) with the browser's echo/noise suppression; stereo = `forceStereo`, capture with `channelCount: 2` and **without** audio processing (which would mix down to mono), for music. Changing the channel switches connected clients live (`setAudioProfile()`: republish the microphone).
- **Unlock audio inside the user gesture:** `client.prepareAudio()` sits in `App.joinVoice()` before the first `await`. It creates the single shared `AudioContext` (microphone gate and level meter hang off it), calls `resume()` and plays 0.1 s of silence once (`SILENT_WAV`) so that Safari/Firefox unlock `<audio>` playback for the document. Without this the context was only created after the token and `room.connect()`: level 0 (nobody heard the user) and silent playback on the first join (reported by two users on 14 September 2026). In addition, every gesture in the document (pointerdown/keydown, capture) releases blocked audio; the dock shows "Ton freigeben" (release audio, playback) or "Mikrofon freigeben" (release microphone, context not `running`), the debug view shows the "Audio" line. `MicPipeline.start()` waits for `resume()` at most 300 ms.
- **Speaker outline:** own participant = `gateOpen && !micMuted` (immediate). Others = local RMS measurement per subscribed microphone track (AnalyserNode, 50 ms, `VoiceGate` with threshold 0.015 and 250 ms hold, `addMeter()`); LiveKit's `isSpeaking` (server notification, ~0.5-1 s later) only as a fallback when the audio context is not running. `deploy/livekit/livekit.yaml` sets `audio.update_interval: 150` for this.
- **Speech gate is local:** voice activation and PTT switch a GainNode in `micPipeline.ts`, not the LiveKit track. The track stays published and "unmuted"; only the mute button uses LiveKit mute (visible to others). This avoids signaling on every word (PLAN 3.5).
- **Owners (several, 14 September 2026):** `server_settings.owner_id` = first owner (at first login, irrevocable), further ones via `members.is_owner` (`PUT /api/members/:id/owner`, owners only, not oneself). `actorOf()` sets `isOwner` from both, the bootstrap marks the first owner in `members` retroactively. Owners always have `ADMINISTRATOR`, rank above every role (`outranks`), cannot be kicked/banned/moderated and are at the very top of the member list. `Member.isOwner` in the protocol; `ServerSettings.ownerId` remains the first one.
- **Server name and icon:** `/api/health` returns `serverName` and `iconUrl` so that title and favicon are already correct in the login; when logged in, `settings.name`/`settings.iconUrl` apply (live update via `structure`). Icon only as PNG/JPEG/WebP/GIF up to 2 MB (no SVG: could contain scripts and is served same-origin), file `DATA_DIR/server-icon`, URL with `?v=<timestamp>` and `immutable` cache.
- **Checking permissions:** Every protected route fetches `requireMember()` and checks `can(actor, Permission.X)`; effects on other members/roles additionally `outranks()`/`canTouchRole()`/`canGrant()` from `authz.ts`. The owner is always administrator. Structure changes end with `broadcastStructure()`. New permissions: append the bit in `permissions.ts`, add the label, and for existing servers write it into the appropriate role via a custom migration (`drizzle-kit generate --custom`) (see 0003/0004 as examples); the bit values are pinned in `index.test.ts`.
- **Media permissions twice:** camera/screen only with `STREAM_VIDEO` and without a block (`members.stream_blocked`, subtracted in `actorOf()`). The client hides the buttons, the app server writes `canPublishSources` into the LiveKit token (microphone always; camera, screen, screen audio only with the permission). LiveKit enforces it; on changes at runtime via `RoomServiceClient.updateParticipant` (`livekit/admin.ts`).
- **Moderation takes effect server-side:** stopping camera/screen = LiveKit `mutePublishedTrack` + WS `voice.stop`; moving = WS `voice.moved` (the client fetches a token for the target channel itself and joins, the server checks that like any join). Only with `MODERATE_VOICE`, never against the owner or oneself, rank must be higher.
- **Background blur:** `@livekit/track-processors` (BackgroundBlur, MediaPipe segmentation in the browser). Loads WASM from jsdelivr and the model from storage.googleapis.com; without internet on the client the effect does not work (self-host later via `assetPaths`). Check support via `supportsBackgroundProcessors()`; Firefox/Safari show the option disabled.
- **Separate output for screen audio:** `setSinkId` per `ScreenShareAudio` track (Chromium only). LiveKit's `switchActiveDevice("audiooutput")` re-routes all tracks, as does `MediaDevicesChanged`; `applyScreenSink()` therefore runs again after subscription, device change and settings change. The debug view shows device, track count and errors ("Bildschirm-Ton Ausgabe"); without a track (0) there is simply no remote share with audio yet.
- **Video (M3):** camera 720p with simulcast (180p/360p/720p, VP8), `adaptiveStream` + `dynacast` in the room: receivers get the layer that matches the size of the `<video>`. The stage only has to render the `<video>` at a suitable size. Screen share: 1080p30 (`ScreenSharePresets.h1080fps30`, up to 5 Mbit/s), **without simulcast** (otherwise adaptiveStream fetches the coarse layer for small tiles, text becomes unreadable), `contentHint: "detail"`. It always requests `audio: true` and publishes the audio as its own track (`ScreenShareAudio`), never mixed with the microphone; whether audio arrives is reported by `screenAudio` in the state, `explainScreenAudio()` provides the hint text. Cancelling in the browser dialog (NotAllowedError/AbortError) is not an error.
- **Database:** Change the schema only in `apps/server/src/db/schema.ts`, then `pnpm db:generate`. Migrations are applied automatically at server start; do not edit generated SQL files by hand.
- **Configuration:** Exclusively via environment variables, validated in `config.ts`. The server loads `apps/server/.env` via `process.loadEnvFile` (built into Node, no dotenv dependency); variables already set take precedence. In the container there is no .env file, everything comes via compose.
- **No new dependencies** without need; if necessary, pin the version as before (caret ranges) and commit the lockfile.
- **Directory service (M6):** The chat server never depends on the service: handle resolution at login is best effort (timeout 2.5 s, errors only logged), display falls back to the display name or `anon-<key>`. The web client calls the service directly (URL from `/api/health`, CORS open for GET/POST). Registration = signature over the service's host + handle + nonce; one key has exactly one handle, handles are immutable (M6a). The service itself (routes, schema, account page, smoke test) lives in `../squorli-directory` (section 2a).
- **Key backup (M6b):** The private key leaves the client only encrypted. `packages/protocol/src/backup.ts`: PBKDF2-SHA256 (600,000 iterations, 16-byte salt) -> HKDF into two keys: AES-256-GCM (encrypts the 32-byte seed, stays in the client) and auth key (is sent, the service stores only its SHA-256 in `key_backups.auth_hash`). Storing: `PUT /api/backup`, signed over "community-directory-backup\n<host>\n<nonce>\n<ciphertext>", upsert (= change password). Fetching in two steps: `GET /api/backup/:handle/params` (salt, iterations) -> client derives -> `POST /api/backup/fetch` with auth key -> blob with iv + ciphertext. Wrong password = 401 `auth_invalid`; rate limit `BACKUP_RATE_LIMIT` per IP **and** per handle (default 10/min), uploads have their own window. Password at least `BACKUP_MIN_PASSWORD` (8) characters, checked client-side only; no reset possible (the directory does not know it).
- **Account page of the service (`../squorli-directory/apps/directory/web/`):** deliberately without a framework (one script file, tsup), same colors as the client, German texts with umlauts, no browser dialogs. "Konto anlegen" (create account) first registers the handle and then stores the backup; if only the backup fails, the key stays in page memory and the next click repeats only the backup step; afterwards you are directly in the account area. "Anmelden" (sign in; handle + password, code if needed) fetches the seed into page memory only (never localStorage) and signs the account actions with it; "Abmelden" (sign out) nulls it. The chat client links the page in the login ("Noch kein Konto?", "Konto verwalten" with `?handle=`) and in Profile > Konto and signs in with "Mit Konto anmelden": `api.directoryRestore()` fetches and decrypts, `store.loginWithHandle()` replaces the device key (with a confirmation via `askConfirm` if the previous one has a handle) and then logs in normally.
- **Second factor (M6c):** TOTP per RFC 6238 (own implementation `../squorli-directory/apps/directory/src/totp.ts`, no library) or recovery code; the service recognizes the form (6 digits = TOTP). Required, if `totp_enabled`, for `POST /api/backup/fetch`, `PUT /api/backup`, `totp-disable` and `recovery-regenerate`; **never at chat login by key** (the key is the factor). `totp_required` (401) comes only after a correct password or valid signature, so that nobody learns without the password whether an account has an authenticator; public responses (`/api/handles`, `/api/keys`) reveal nothing. Each TOTP step is valid once (`totp_last_step`, otherwise 401 `totp_reused`), window +-1 step; recovery codes are consumed atomically (`used_at`). Only failed attempts count against `BACKUP_RATE_LIMIT` per account (`RateLimiter.blocked()/hit()`), likewise only wrong passwords per IP/handle. The secret is stored AES-256-GCM-encrypted in the DB (`secrets.ts`, key `DIRECTORY_SECRET_KEY`, AAD = account ID); without the key `features.totp=false` and 503 `totp_unavailable`, recovery codes (SHA-256 only) keep working. Setup: `totp-setup` generates the secret (only with a backup, otherwise 409 `no_backup`), it becomes active only with `totp-enable` + code, which at the same time returns the 10 recovery codes (shown once). Signed account actions: `directoryActionMessage(host, action, nonce, code)`, body `SignedActionRequest`/`CodeActionRequest`; the code is part of the message so that it cannot be swapped.
- **Device management (M6c):** `sessions.id` is the public identifier (the token stays secret), `label` comes from the User-Agent at login (`labelFromUserAgent`, the client cannot set it), `last_used_at` is written in `resolveSession()` at most every 5 min. Remote logout deletes the row and closes exactly its WebSocket with **Close 4011 `session_revoked`** (`hub.disconnectSession`; kick remains 4010 + `removed`). The client reacts in `store.sessionLost()`: WS close 4011, WS error `unauthorized` and REST 401 with a token set (`api.onUnauthorized`) lead to the login with a message, without reconnect. `store.logout()` calls `DELETE /api/me/sessions/current` (best effort). No `PROTOCOL_VERSION` bump: close codes are not events. The directory knows no devices, only the list of key fetches (`key_fetches`, 90 days, display only): whoever has the key has it.
- **Security:** Do not weaken the auth path (single-use challenge, domain binding of the signature, session TTL). LiveKit secret at least 16 characters (schema), 32+ in production. `TRUSTED_PROXIES` only relevant in external mode.
- **Symbols only via `<Icon name="…"/>`** (Lucide web font, ISC license, self-hosted under `apps/web/src/icons/`), no emojis and no Unicode arrows in the UI (user requirement: uniform, white, license-free). Color = `currentColor`, size = `font-size`. Look up new names at https://lucide.dev/icons, then `pnpm icons`. Hang up = `phone` rotated by 135 degrees, white on red.
- **Categorized modals: categories always on the left** (user requirement 13 September 2026, applies without instruction to the contrary to all future modals with several sections). Building blocks: `.modal` > `.modal-head` (title, `.spacer`, close) + `.settings-layout` (`.settings-nav` on the left with `<Icon>` + label, `.settings-body` on the right). Examples: settings (`VoiceDock.tsx`) and admin panel (`AdminPanel.tsx`). No tab bar at the top.
- **No browser dialogs in the client:** `alert`, `confirm`, `prompt` are taboo (user requirement). Confirmations via `askConfirm()`, text inputs via `askInput()` from `apps/web/src/dialogs.tsx`; both return promises and render their own modal (z-index above admin panel/profile). Show errors inline as `.error` text, not as a popup.
- **Tests:** Vitest, files `*.test.ts` next to the code. Pure logic (stores, schemas) via unit test; flows via `scripts/smoke.mjs`.

## 6. Architecture in brief

- One process: Fastify serves the API, the WebSocket (`/api/ws`) and the static web client (SPA fallback to `index.html`, unknown `/api/*` -> 404 JSON).
- Identity = Ed25519 public key; the first login creates the user (`onConflictDoUpdate`). What is signed is `community-chat-login\n<domain>\n<nonce>`, so `PUBLIC_DOMAIN` must match the hostname the client sees (dev: `localhost`).
- Media never passes through the app server: the client fetches `/api/rtc-token` and connects directly to LiveKit (`wss://<PUBLIC_DOMAIN>/rtc`, the proxy forwards `/rtc*` to `livekit:7880`). UDP 7882 / TCP 7881 must be reachable directly.
- Operating modes: `PROXY_MODE=bundled` (bundled Caddy with Let's Encrypt on 443) or `external` (own proxy, see `deploy/proxies/README.md`, status untested).
- The challenge store is in-memory (single node). Multi-node later via Redis.

## 7. Known pitfalls

- **Smoke test without a running server** fails with `ECONNREFUSED`; run `pnpm dev` or `node dist/index.js` first.
- **`PUBLIC_DOMAIN` must match the hostname in the browser address bar exactly** (dev: `localhost`; behind the proxy: `chat.example.org`; access via LAN IP: the IP). Otherwise `POST /api/auth/verify -> 401 (signature_invalid)`. `/api/health` returns `domain`; since the fix, the client shows both values in the error message. The smoke test always signs with `localhost`.
- **Windows:** `process.exit()` while WebSockets are still closing triggers a libuv assertion; the smoke test therefore waits for `close`. Follow the same pattern in future scripts.
- **Docker build from Windows:** without `.dockerignore`, the host `node_modules` end up in the build context; do not delete the file. `tools/` is deliberately excluded: the Dockerfile must not call `tools/icons.mjs`; the generated icon files under `apps/web/src/icons/` are in the repo and are sufficient.
- **`pnpm deploy` under pnpm 10** aborts without `--legacy` with `ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE` (the flag is in the Dockerfile).
- **`@squorli/protocol` has no build:** its entry is `src/index.ts`. The server therefore bundles it into `dist/index.js` via `noExternal`. Anyone who introduces further workspace packages as runtime dependencies of the server must add them to `tsup.config.ts` as well.
- **Testing the image locally:** `docker run --rm -p 3000:3000 --network squorli-dev_default -e PUBLIC_DOMAIN=localhost -e DATABASE_URL=postgres://chat:chat@postgres:5432/chat -e LIVEKIT_URL=http://livekit:7880 -e LIVEKIT_API_KEY=devkey -e LIVEKIT_API_SECRET=<32 characters> squorli/app:local`, then run the smoke test.
- **Background processes on Windows:** `tsx watch` and Vite survive a hard kill of the parent shell. `pnpm dev` (wrapper) cleans up via `taskkill /T`; anyone using `pnpm dev:apps` directly checks `netstat -ano | grep ":3000 "` and `taskkill //F //T //PID <pid>` before restarting. **Beware German Windows:** `netstat` prints `ABHÖREN` instead of `LISTEN`; filter with `grep -E "ABH|LISTEN"`, otherwise the cleanup finds nothing and an old test server (with an old protocol) stays on the port while the new one cannot bind.
- **"Batchvorgang abbrechen (J/N)?" on Ctrl+C** ("Terminate batch job (Y/N)?") comes from the cmd.exe shells that pnpm opens for `tsx watch` and `vite`. Cosmetic; the wrapper terminates them immediately. Do not answer with J/N.
- **Wrapper test:** Ctrl+C cannot be triggered from scripts via `kill -INT` (MSYS does not reach native processes). Tested with `GenerateConsoleCtrlEvent` from PowerShell; alternatively terminate the apps via `taskkill`, the wrapper then stops the containers through the exit path.
- **Port 3000/5173/5432/7880-7882 in use:** dev compose and the Vite proxy are hard-wired to these ports.
- **Compose project is called `squorli-dev` since 14 September 2026 (previously `community-chat-dev`):** if old containers are still running, they occupy 5432/7880-7882 and the new stack does not start. Run `docker compose -p community-chat-dev down` (without `-v`) once, then `pnpm dev`. The data volume is pinned in `compose.dev.yml` to the old name `community-chat-dev_pgdata-dev` and declared as `external: true` (Compose requires this for volumes from another project); `tools/dev.mjs` creates it via `docker volume create` before `compose up`, so that a fresh machine starts as well. Anyone using `pnpm docker:dev` directly must create the volume once themselves. The dev DB is preserved this way (verified on 14 September 2026: users and handles present after the switch). Containers are now called `squorli-dev-postgres-1` / `squorli-dev-livekit-1` (in all `docker exec` commands). For an already running prod stack (`community-chat`), the same applies to `deploy/compose.yml` (network `squorli_internal`, volumes not pinned: back up data first).
- **LiveKit dev container** runs with `--dev` (keys `devkey`/`secret`); `.env.development` deliberately uses a longer secret only for token signing; a real media connection is not yet part of the client in M0.
- **Directory service in dev:** `pnpm dev` in `../squorli-directory` (own Postgres on :5433, service on :3100); the chat server needs `DIRECTORY_URL=http://localhost:3100` in `apps/server/.env`. Chat smoke test with directory: start the chat server with `DIRECTORY_URL=http://localhost:3101` against a smoke instance there (see its AGENTS.md). Until 14 September 2026 the dev database `directory` lived in `squorli-dev-postgres-1`; the README there has the `pg_dump | psql` line for taking the data over.
- **`EADDRINUSE :3000` right after `pnpm dev` (fixed 14 September 2026):** `tools/dev.mjs` used to load the entire `apps/server/.env` via `loadEnvFile` into its environment, which all apps inherit. The directory service thereby got `PORT=3000` and the chat `DATABASE_URL`. Now the wrapper reads only `LIVEKIT_DEV_NODE_IP` from the file and passes it exclusively to Compose. Rule: the wrapper never sets app variables; each app loads its own `.env`.
- **`DIRECTORY_URL` missing in `apps/server/.env` after the update:** `.env.development` has it, an existing `.env` gets it by hand (`DIRECTORY_URL=http://localhost:3100`); without it the login shows "Dieser Server nutzt kein Verzeichnis" (this server does not use a directory).
- **Session "logged out from another device" in your own browser:** after "Alle anderen Geraete abmelden" (log out all other devices) or a `DELETE /api/me/sessions/:id`, the session currently in use is kept (`current`); anyone who gets thrown out anyway has two tabs with two sessions of the same key (every login creates a new session; a second tab only shares it via `localStorage`).
- **Smoke test against a test DB:** `docker exec squorli-dev-postgres-1 psql -U chat -d chat -c "CREATE DATABASE chat_smoke"`, then `PORT=3001 PUBLIC_DOMAIN=localhost DATABASE_URL=postgres://chat:chat@localhost:5432/chat_smoke npx tsx src/index.ts` in `apps/server` and `SMOKE_URL=http://localhost:3001 pnpm smoke`. This way the owner of the dev DB remains your own browser key.
- **Schema change while `tsx watch` is running:** the dev server restarts immediately after `schema.ts` is saved, i.e. before `pnpm db:generate` has generated the migration; it then runs against the old schema and queries on new columns fail (server stops responding). After `db:generate`, trigger a second restart (`touch apps/server/src/index.ts`), then the migrations are applied. Check: `SELECT count(*) FROM drizzle.__drizzle_migrations`.
- **Raw SQL with Date parameters** (`sql` + `${new Date()}`) fails in postgres-js; use `now()` in SQL.
- **Attachments are publicly retrievable by ID** (`/api/attachments/<uuid>/<name>`, no token), so that `<img src>` works. Like CDN links on Discord; the UUID is the secret.
- **White screen after deploy, "Expected a JavaScript module ... MIME type text/html":** the browser had the old `index.html` with the old asset hash in its cache. Since the fix: missing files with an extension get a 404 instead of the app shell, `index.html` is served with `cache-control: no-cache`, hashed assets with `immutable`. The user must hard-reload once after the fix. Run `pnpm build` (root) after every client change, otherwise an old client sits in `apps/server/public`. Second cause (fixed): `@fastify/static` ran with `wildcard: false` and registered one route per file at startup; new assets after a build without a restart got a 404. Now `wildcard: true`, files are resolved per request.
- **`Route GET:/ not found` (Fastify 404 on `/`):** the server is running without a built web client. Since the fix, `/` answers with 503 and an explanation; the cause is a start without `pnpm build` (missing `apps/server/public`) or an old Docker image. `/api/health` works regardless.
- **`apps/server/public/` and `apps/server/.env` are gitignored**; never commit them. New variables in `.env.development` must be copied by hand into the existing `apps/server/.env` (e.g. `LIVEKIT_PUBLIC_URL`).
- **"could not establish signal connection" / Safari "Load failed":** the client cannot reach the LiveKit URL issued by the server. Usually `apps/server/.env` still contains `LIVEKIT_PUBLIC_URL=ws://localhost:7880` from `.env.development` while the server runs publicly. Remove the variable (default `wss://PUBLIC_DOMAIN`, the proxy forwards `/rtc`). The server warns about this combination at startup; the client names the URL in the error message and in the debug view. Second cause: `LIVEKIT_PUBLIC_URL=wss://domain:7880` (port 7880 does not speak TLS and is not exposed; correct is without a port via the proxy). Third cause: the proxy does not forward `/rtc` (`/rtc/validate` must return 401).
- **Participant drops out of the channel after ~15 s:** the signal connection is up, media (ICE) never gets established, LiveKit disconnects with CONNECTION_TIMEOUT/UNKNOWN. Causes: 7882/udp + 7881/tcp not forwarded to the chat host, LiveKit does not know its public IP (`LIVEKIT_NODE_IP`), or the dev stack is running with `--node-ip 127.0.0.1`. The client shows the disconnect reason as an error and logs room events in the debug view; on the server, search `docker logs <livekit>` for "ice" / "connection timeout". SIGNAL_CLOSE, by contrast, = WebSocket closed (proxy timeout, network switch, phone in the background).
- **LiveKit URL for clients without `/rtc`:** livekit-client appends `/rtc` itself. `wss://domain/rtc` would result in `/rtc/rtc` and fails. The server delivers `LIVEKIT_PUBLIC_URL` or `wss://PUBLIC_DOMAIN`.
- **LiveKit in Docker on Windows/macOS:** without `--node-ip`, LiveKit offers the internal container IP as ICE candidate, and browsers on the host then get no media. `compose.dev.yml` sets `--node-ip ${LIVEKIT_DEV_NODE_IP:-127.0.0.1}`; `tools/dev.mjs` loads `apps/server/.env` for this. For a second device in the LAN set `LIVEKIT_DEV_NODE_IP=<host LAN IP>`, for tests from outside the public IP, and open the client from that device via the LAN IP (then `localhost` is no longer a secure context: microphone only with HTTPS, see PLAN 5 mkcert).
- **LiveKit dev secret:** `--dev` sets `devkey`/`secret`; the container receives the longer secret from `.env.development` via `LIVEKIT_KEYS`, because `config.ts` requires at least 16 characters. Token check without a browser: `GET http://localhost:7880/rtc/validate?access_token=<jwt>` must return 200.
- **Browser autoplay:** audio from other participants may be blocked until the user clicks once; the client then shows "Ton freigeben" (unblock audio) (`room.startAudio()`).
- **Choosing an output device** (setSinkId) exists only in Chromium; Firefox/Safari show an empty list.
- **Screen share without audio:** only Chromium delivers audio, and there only with "Tab" (tab audio) or on Windows with "entire screen" with system audio enabled; individual windows never. Firefox/Safari never. The stage shows a yellow notice in both cases. The matrix in PLAN 3.6 is checked with `/test/screenshare.html` per browser/operating system and recorded there.
- **`docker stats` does not measure the load test:** `pnpm bots` attaches itself to the network namespace of the LiveKit container; the media traffic runs over loopback and does not show up in NET I/O. Read the bandwidth from the `lk` table (subscriber) or from the debug view in the client.
- **`@types/dom-mediacapture-transform`** is a peer dependency of `@livekit/track-processors` (types for MediaStreamTrackProcessor); as a devDependency in the web package, otherwise `tsc` breaks.
- **Fresh database had only the role "Mitglied" (fixed 14 September 2026):** migration 0004 creates "Mitglied" in empty databases as well; since then the bootstrap no longer checks for "no roles" but for "no default role" and creates Gast/Admin/Mitglied individually as needed. Check the fresh-install path: recreate `chat_smoke` from scratch via `DROP DATABASE ... WITH (FORCE)` + `CREATE DATABASE` and run `pnpm smoke` (not only against an incrementally migrated DB).
- **Resetting the dev database** (new owner at the next login): `docker exec squorli-dev-postgres-1 psql -U chat -d postgres -c "DROP DATABASE chat WITH (FORCE)" -c "CREATE DATABASE chat OWNER chat"`, empty `apps/server/data/attachments`, then `touch apps/server/src/index.ts` (migrations + bootstrap run only at startup). The database `directory` in the same container is the old dev database of the directory service (before the move); leave it until the data is taken over into `../squorli-directory`.
- **Only guest after joining:** anyone who cannot write does not yet have the "Mitglied" (member) role. An admin assigns it in the member list (click on the name). This is intended (decision of 13 September 2026), not a bug.
- **Production compose always with `--env-file ../.env`:** `env_file:` in the service only covers the container environment; the `${POSTGRES_PASSWORD}`/`${LIVEKIT_API_KEY}` placeholders in `compose.yml` are filled by Compose only from `deploy/.env` or the `--env-file`. Without the flag: empty DB password, `LIVEKIT_KEYS: ": "`. Check with `docker compose --env-file ../.env ... config`.
- **external mode publishes no HTTP ports.** A proxy on the host needs the overlay `proxies/nginx.ports.yml`; a proxy in a container attaches to the network `squorli_internal`.

## 8. Verified state (14 September 2026, Windows 11, Node 25.2.1, pnpm 10.15.0, Docker 29, LiveKit 1.13.6)

- **Directory moved out (14 September 2026):** after removing `apps/directory` and the Compose/Caddy/proxy/Dockerfile parts: `pnpm typecheck` 3/3, `pnpm test` protocol 24 / server 13 / web 4, lockfile without the directory importer, `docker build --target app` green. In `../squorli-directory`: typecheck 2/2, tests 13 + 14, build, `docker build`, `pnpm smoke` 38 green against its own Postgres (:5433). `ChallengeRequest`/`ChallengeResponse` moved into `directory.ts`, no protocol version bump (same shapes). Not run again: the chat smoke test with `DIRECTORY_URL` (unchanged code path).
- **Owner/icon/title (14 September 2026):** `pnpm smoke` against `chat_smoke` without a directory: 71 green, twice (appointing an owner only by an owner, permissions immediately ADMINISTRATOR, first owner and oneself cannot be demoted, demotion restores role permissions; icon upload as PNG, served with type, `iconUrl`/`serverName` in `/api/health`, SVG and non-admin rejected, removal). Typecheck 4/4, 55 tests, build green. Still open by hand: favicon change in the browser, icon upload in the admin panel ("Verwaltung"), owner group in the member list.
- **M6c (14 September 2026):** `pnpm typecheck` 4/4 packages, `pnpm test` protocol 24 (new: useragent 9), directory 14 (new: totp 11 with RFC 6238/4226 vectors, secrets 3), server 13, web 4. `pnpm smoke:directory` against its own instance (:3101, `directory_smoke`, with `DIRECTORY_SECRET_KEY`): 38 green (status, setup without backup 409, without account 404, enable with wrong/correct code, `totp_required` only after the correct password, TOTP fetch, `totp_reused`, recovery code single-use and normalized, change password without/with code, regenerating codes invalidates the old ones, disabling, then fetch without code); without the key 22 green (503 `totp_unavailable`, block skipped). `pnpm smoke` against `chat_smoke` with `DIRECTORY_URL`: 67 green (twice in a row; migration 0008 on existing sessions), against a completely fresh `chat_smoke` without a directory 65 green (session list with exactly one `current`, label "Firefox auf Windows" (Firefox on Windows) from the user agent, remote logout closes the WS with 4011 and the token returns 401, foreign/unknown id 404, "others" leaves one's own session in place, "current" ends one's own). `pnpm build` (web 1.04 MB, server 85 kB, service 45 kB + account page 105 kB incl. QR code), `docker build --target directory` and `--target app` run through.
- **Not yet checked by hand (M6c):** account page in the browser (scan the authenticator with a real app, codes, fetch list), code field in "Mit Konto anmelden" (sign in with account), Profile > Devices in two browsers, "Passwort festlegen" (set password) from the chat client (CORS for PUT now allowed).
- `pnpm typecheck`: 3/3 packages without errors.
- `pnpm test`: protocol 11, server 13, web 4 tests. All green (permission bits 1152/7616 pinned).
- `pnpm smoke` (protocol v4, against test DB `chat_smoke`, twice in a row): 55 checks green, including moderation (move without permission 403, `voice.moved`/`voice.stop` at the target, block removes STREAM_VIDEO from `me` and token, unblocking restores it, removing = `voice.moved null`). Before that (v3): 49 checks green (owner, roles "Gast"/"Mitglied" (guest/member), guest may not write but may use voice, invites incl. limit, permission hierarchy, messages with history/cursor/edit/delete, attachment upload and download, cleanup on delete, typing, voice presence, RTC token with/without camera grant, kick with `removed` event, ban/unban).
- Migrations 0003/0004 on the existing dev DB: the default role is now called "Gast" (1152), new role "Mitglied" (7616) assigned to all existing members.
- **M3 load test (13 September 2026, local, Docker Desktop):** `pnpm bots --video 15 --audio 0 --subscribers 1` (lk preset "high", simulcast, 4x4 layout): the listener receives 15/15 tracks at a combined 4.1 Mbit/s (236-309 kbit/s per track), packet loss 0.03 %; LiveKit ~9 % CPU, ~135 MB RAM. Matches the estimate in PLAN 4.3 (client download ~4 Mbit/s); values entered there.
- **M6b (14 September 2026):** `pnpm smoke:directory` 20 checks green (additionally: backup without account 404, wrong signature 401, storing, `hasBackup`, params without iv, wrong password 401, fetch + decryption yields the seed, unknown handle 404, replacing = old password rejected). Chat smoke test with directory still 61 green; against a completely fresh `chat_smoke` (new installation) 59 green with all three roles. `pnpm build` produces `apps/directory/public/` (main.js 77 kB), account page served via https://community-id.datlicht.eu/ from the user's dev stack (200, `features.backup: true`). Protocol tests 15 (4 new for backup.ts). **Confirmed by hand (14 September 2026, Daniel):** account @datlicht created on the account page, signed in on the PC (owner), then "Mit Konto anmelden" (sign in with account) on the phone: same key, second session of the same user. Not yet checked: "Passwort festlegen" (set password) for an existing browser key, password change on the account page.
- **M6a (14 September 2026):** `pnpm smoke:directory` 11 checks green (registration, double registration, taken handle, wrong signature, handle rules, resolution in both directions, replay). Chat smoke test with the directory attached: the owner gets their handle at login (61 checks), without a directory 59. Docker image `--target directory` builds.
- **Not checked automatically:** the UI in the browser (login with invite, chat, admin panel, member list, stage with camera and screen share, notice for a share without audio). Must be done by hand in two browser profiles; the screen-share test matrix (PLAN 3.6) with `/test/screenshare.html` per browser/operating system.
- **Open for the M3 acceptance:** 15 real cameras (or video bots against a target server on the internet instead of locally), tab audio from Chrome at all receivers, matrix 3.6 filled in.
- LiveKit accepts the app server's tokens (`/rtc/validate` -> 200).
- Load test `pnpm bots --subscribers 1`: 15 audio bots + 1 listener, all 15 tracks subscribed, 0 % packet loss, ~284 kbit/s at the listener, LiveKit 10-15 % CPU / ~90 MB RAM, app server and Postgres unimpressed. The LiveKit log shows "error reading data channel ... User Initiated Abort" during teardown: harmless.
- **Confirmed by hand (13 September 2026, Daniel):** voice chat from an iPhone (Safari, external) to the dev stack behind router NAT, access via Nginx Proxy Manager on another host, domain community-test.datlicht.eu. Required: `PUBLIC_DOMAIN` = domain, `LIVEKIT_PUBLIC_URL` without port (or omitted), `LIVEKIT_DEV_NODE_IP` = public IP, 7882/udp + 7881/tcp forwarded at the router to the chat host.
- **Testing restrictive networks:** README section "Simulating restrictive networks" (remove the UDP forwarding -> TCP fallback; `?ice=relay` -> TURN only). The debug view shows the active ICE path from `candidate-pair` statistics (`room.engine.pcManager`, internal API, check after SDK updates).
- **Deferred (decision of 13 September 2026): remaining M1 acceptance later, M2 begins.** Open points see the next line; procedure in the README section "Simulating restrictive networks".
- **Still open for the M1 acceptance:** a peer in a restrictive network (corporate Wi-Fi, UDP blocked) and thus TURN; device switching and PTT key only checked superficially; the 15-bot test only ran locally.
- `pnpm docker:dev`: Postgres and LiveKit start, migrations create `users` and `sessions`.
- `pnpm dev` (wrapper): containers up, server on :3000, Vite on :5173, proxy `/api/health` works. Ctrl+C (a real console event) and a crash of the apps both stop the containers and leave no processes behind on :3000/:5173.
- `pnpm build`: web bundle ~209 kB, server bundle ~10 kB. `node dist/index.js` with `STATIC_DIR` serves the SPA incl. fallback.
- `pnpm --filter @squorli/server smoke`: challenge/verify 200, replay 401, wrong signature 401, RTC token 200, WS welcome/pong, version 99 rejected. Exit 0. Green alike against `tsx watch`, against `node dist/index.js` and against the container.
- `docker build`: image ~291 MB, starts against the dev database, serves the web client and passes the smoke test.

Bugs fixed along the way (M0 check): the server did not read `apps/server/.env` (loader added); the smoke test aborted on exit under Windows (wait for close); `.dockerignore` was missing; `pnpm deploy` needed `--legacy`; the server bundle imported `@squorli/protocol` as TypeScript source (now bundled in).
Fixed in M1: the RTC URL ended in `/rtc` (the SDK appends it itself); the LiveKit dev secret did not match the app server; LiveKit in the container offered an unreachable ICE address (`--node-ip`).

## 9. Milestone log

| Date | Change |
|---|---|
| 2026-09-14 | `.gitlab-ci.yml` (test + image build into the GitLab registry, modelled on the directory repo); `deploy/compose.yml` takes a prebuilt image via `APP_IMAGE` (default `squorli/app:local`, build from the repo), variable documented in `.env.example`. |
| 2026-09-14 | Directory service moved out of the monorepo into the unpublished sibling repo `../squorli-directory` (user's decision): `apps/directory`, `deploy/directory/`, `postgres/init-directory.sql`, the `directory` Compose profile, `/id` in Caddy/nginx/overlays, the Dockerfile target and the `smoke:directory`/`db:generate:directory` scripts removed here; `tools/dev.mjs` no longer creates the `directory` database. New section 2a with the sync rules for the protocol copy and the brand package. |
| 2026-09-14 | All documentation, READMEs and AI instructions (AGENTS.md, CLAUDE.md, docs/PLAN.md, docs/brand, deploy READMEs, comments in env templates, compose files, Caddy/nginx/LiveKit configs and the Dockerfile) translated to English (user decision: docs are always English from now on). Code comments and UI texts stay German. |
| 2026-09-14 | Page title = server name (also in the login, from `/api/health`), server icon in the admin panel (upload/remove, favicon, sidebar; migration 0009), multiple owners (`members.is_owner`, `PUT /api/members/:id/owner`, owner group at the very top of the member list, first owner cannot be demoted). Smoke test +6 checks. |
| 2026-09-14 | Rename to **Squorli** and redesign per `docs/brand` (user's decision): packages `@squorli/*`, root package `squorli`, Compose `squorli`/`squorli-dev` (dev volume pinned to the old name), image tags `squorli/*`, containers `squorli-dev-*`, title/503 page/account page. Web client and account page switched completely to the brand tokens (Night/Surface/Raised/Selection, Action blue only for actions, Mint/Amber/Coral, Segoe UI 16 px, radii 12/20 px, focus ring, reduced motion), logo in the login, icon mark in the sidebar and favicon, horizontal logo on the account page. Signature strings and localStorage keys deliberately unchanged. The asset copies in the apps are byte-identical to `docs/brand/` (when changing them there, update both copies). |
| 2026-09-14 | M6c implemented (without e-mail): TOTP authenticator + recovery codes as second factor for key fetch and account changes (`totp.ts`, `secrets.ts`, `DIRECTORY_SECRET_KEY`, migration 0002 with `recovery_codes`/`key_fetches`/`totp_last_step`, signed account actions), account page with sign-in and account area (QR code via `qrcode-generator`), code field in the chat login, device management (sessions with `id`/`label`/`last_used_at`, migration 0008, `GET/DELETE /api/me/sessions`, WS close 4011, `ProfileDialog.tsx` as a categorized modal), `sessionLost()` in the client, CORS fix for `PUT /api/backup`, rate limits count only failed attempts. Smoke tests 38 / 67. |
| 2026-09-13 | M0 scaffold fully checked (dev, bundle, Docker). `.env` loader, smoke fix, `.dockerignore`, `tsup.config.ts`, `deploy --legacy`, `AGENTS.md`/`CLAUDE.md` created. |
| 2026-09-13 | `pnpm dev` runs via `tools/dev.mjs`: start containers and stop them automatically on exit. Raw start as `pnpm dev:apps`. |
| 2026-09-14 | First join without audio (2 users): audio is now unlocked in the click gesture (`prepareAudio()`, shared AudioContext, play silence), gesture fallback, "Mikrofon freigeben" (release microphone) in the dock, audio line in the debug view. Speaker frame without delay: local level measurement for all participants instead of the LiveKit notification; LiveKit `audio.update_interval` 150 ms as fallback. |
| 2026-09-14 | The stage shows display names from the member list instead of from the LiveKit token (a rename takes effect there immediately too). |
| 2026-09-14 | Bootstrap: re-create roles individually when the default role is missing (a fresh DB had only "Mitglied" because of migration 0004). User's dev DB reset, the owner is now the account @datlicht. |
| 2026-09-14 | M6b implemented: key backup (PBKDF2/HKDF/AES-GCM in `packages/protocol/src/backup.ts`), service endpoints `PUT /api/backup`, `GET /api/backup/:handle/params`, `POST /api/backup/fetch` (migration 0001 `auth_hash`, `BACKUP_RATE_LIMIT`), account page of the service (`apps/directory/web/`, tsup bundle to `public/`, Docker target copies it), client login "Mit Konto anmelden" + "Passwort festlegen", `scripts/dev.mjs` for the service, smoke test 20 checks. |
| 2026-09-14 | `tools/dev.mjs`: no longer inherit the server `.env` into the apps' environment (the directory service would otherwise bind :3000 and the chat DB). |
| 2026-09-14 | Directory service for its own host: `deploy/directory/` (Compose with its own DB, Caddy or external, README); the main Compose now sets `DIRECTORY_URL` only from `.env`, embedded variant as profile `directory`. |
| 2026-09-14 | M6a implemented (M6 brought forward): directory service `apps/directory` (register/resolve handles, rate limit, own DB, Docker target, Caddy `/id/*`, proxy overlays), the chat server caches verified handles (`users.handle`, migration 0007) and shows them instead of `anon-…`, the client registers handles in the login, `Me`/`Member` carry `handle`, `/api/health` reports `directoryUrl`. Decisions: monorepo, TOTP as second factor (M6c), SMTP prepared. |
| 2026-09-13 | Voice quality per voice channel: Opus bitrate (24-256 kbit/s, default 64) and stereo in the admin panel (migration 0006), mono with DTX+RED, stereo without audio processing, live switch-over of connected clients, display in the stage header. |
| 2026-09-13 | User-visible texts switched to real umlauts (permission labels, login/connection messages, tooltips, 503 page); convention in section 5 made more precise. |
| 2026-09-13 | Admin panel: categories on the left instead of a tab bar at the top; rule "categorized modals always with categories on the left" in section 5. |
| 2026-09-13 | Move voice participants via drag & drop in the sidebar (moderators; oneself always). |
| 2026-09-13 | Settings with categories on the left (Speaking / Devices / Camera); the camera picker when switching on now includes the background with a real preview of the blur. |
| 2026-09-13 | "Ton aus" (deafen) next to the microphone in dock and stage: also mutes the microphone; when switching it back on, the microphone only returns if it was not muted by hand; shown to others via a LiveKit attribute. |
| 2026-09-13 | All emoji symbols replaced by the Lucide web font (`Icon.tsx`, `tools/icons.mjs` generates the subset), hang-up white on red, voice settings as a modal instead of an inline block. |
| 2026-09-13 | Screen share at 1080p30 without simulcast (was 15 fps with simulcast: blurry and choppy); the screen-audio output is set again after device changes and shown in the debug view. |
| 2026-09-13 | Voice channel moderation (protocol v4): permission `MODERATE_VOICE`, move/remove, end camera/screen, block streaming per member (migration 0005 `stream_blocked`), LiveKit enforcement via RoomServiceClient, menu in the member list, notices for the affected member. Plus background blur (`@livekit/track-processors`) and a separate output device for screen audio. |
| 2026-09-13 | Switching on the camera always asks via its own modal with preview when there are several cameras (`CameraPicker.tsx`); the choice is saved as preselection. |
| 2026-09-13 | Stage: tiles as default and fitted to the window height (no scrolling), click enlarges a tile, click on the large one goes back (user's requirement). |
| 2026-09-13 | Roles: default role "Gast" (view + voice only), new role "Mitglied" with the previous permissions, migration 0004 with backfill; smoke test checks guest limits. |
| 2026-09-13 | M3 implemented: permission `STREAM_VIDEO` (migration 0003), `canPublishSources` in the RTC token, voice core with camera (simulcast, adaptiveStream/dynacast), screen share with its own audio track, stage (`VoiceStage.tsx`: tiles/speaker focus/pinning/volume for screen audio), camera settings, debug view with video bitrates, test page `/test/screenshare.html`, `pnpm bots --video`, bandwidth measurement in PLAN 4.3. |
| 2026-09-13 | Static: `wildcard: true`, so that new assets are served after `pnpm build` without a server restart. |
| 2026-09-13 | Web client: all `confirm`/`prompt` calls replaced by own modals (`dialogs.tsx`), new convention "no browser dialogs". |
| 2026-09-13 | Static serving: 404 for missing assets instead of the SPA fallback, `no-cache` for index.html, `immutable` for assets. |
| 2026-09-13 | M2 implemented: protocol v3 (ServerState, structure/message/typing/removed events, permission bitmask), schema with members/categories/channels/roles/invites/bans/messages/attachments (migration 0002), bootstrap, hub, authz, all admin routes, web client completely new (Sidebar, Chat, Members, Admin, VoiceDock, login with invite), smoke test with 44 checks, Compose volume for attachments. |
| 2026-09-13 | Test aids for restrictive networks: `?ice=relay` (TURN only), ICE path in the debug view, README section "Simulating restrictive networks". |
| 2026-09-13 | First voice chat over the internet confirmed (iPhone Safari external, NPM on another host, dev stack behind router NAT). |
| 2026-09-13 | `tools/dev.mjs` loads `apps/server/.env` for Compose (`LIVEKIT_DEV_NODE_IP`), reports the advertised media address at startup. |
| 2026-09-13 | Explain the disconnect reason (DisconnectReason) in the client, room event log in the debug view, `voice.leave` on unexpected disconnect. |
| 2026-09-13 | Explain LiveKit connection errors: the client names URL and cause (localhost dev value, ws:// under HTTPS, proxy), the debug view shows the URL, the server warns about a localhost URL with a public domain. |
| 2026-09-13 | Explain login errors: `/api/health` returns `domain`; on `signature_invalid` the client reports expected vs. actual domain, on `challenge_invalid` "try again". |
| 2026-09-13 | `pnpm build` copies the web client to `apps/server/public` (`tools/copy-web.mjs`); the server answers `/` with 503 + notice instead of 404 when the client is missing. |
| 2026-09-13 | Proxy docs: section "Proxy on another host" with overlay `remote-proxy.ports.yml` and `PROXY_BIND_IP` in `.env.example`. |
| 2026-09-13 | Proxy docs: Nginx Proxy Manager section with overlay `npm.network.yml`. |
| 2026-09-13 | Prod Compose: `--env-file ../.env` in all commands (placeholders were otherwise empty). |
| 2026-09-13 | Proxy docs: `nginx.ports.yml` overlay added (compose.yml publishes no HTTP ports in external mode), nginx.conf with redirect/IPv6/http2, README with verification steps. |
| 2026-09-13 | M1 implemented: protocol v2 (voice.join/leave/state, /api/me, rtc-token per channelId), VoicePresence, fixed channel "lobby", web client with livekit-client (VoicePanel, DebugPanel, voice/ core: VAD/PTT via GainNode), migration 0001 (display_name), `pnpm bots`, `pnpm smoke`, LiveKit dev with `--node-ip` and matching secret. TURN documented, off. Acceptance across two networks open. |
